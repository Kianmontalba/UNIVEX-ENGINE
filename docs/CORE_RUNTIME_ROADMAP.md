<div align="center">

<h1><strong>UNIVEX ENGINE — CORE & RUNTIME ROADMAP</strong></h1>

<strong>Native runtime contracts, scene ownership, assets, simulation, and platform foundations</strong>

</div>

> This roadmap protects the engine-owned C++ runtime. Editor features consume these contracts through `EngineServicesUVE`; they do not create a second scene, asset, or runtime lifecycle.

| Status | Completed foundation | Delivered capability | Ongoing boundary |
|---|---|---|---|
| **COMPLETED** | **Increment 1** | Engine loop, logging, timing, events, and `EngineCoreUVE`. | The documented `Init → Load → BeginFrame → Update → LateUpdate → Render → EndFrame → Shutdown` lifecycle remains the single runtime path. |
| **COMPLETED** | **Increment 2** | Memory manager with pool, stack, and heap allocators. | RAII and measured allocation behavior remain required for future low-level work; Increment 466 makes PoolAllocatorUVE reject alignment-rounding and total-footprint `std::size_t` overflow with `std::bad_alloc` before allocation while preserving valid pool behavior. |
| **COMPLETED** | **Increment 3** | Work-stealing thread-pool job system. | Thread affinity and main-thread rendering constraints remain explicit. |
| **COMPLETED** | **Increment 4** | Configuration and command-line foundations. | Settings must remain versioned and safely recover from malformed/missing files. |
| **COMPLETED** | **Increments 5–6** | ECS, scene graph, prefabs, and scene serialization. | The runtime scene is authoritative; the editor only authors it through commands. |
| **COMPLETED** | **Increment 7** | Asset manager, generic importer, hot reload, and asset bundle foundations. | Asset identity remains GUID-based; future importers must preserve deterministic cache/invalidation rules. |
| **COMPLETED** | **Increment 8** | Virtual file system. | New content systems must use the existing virtual-path and mount contracts where appropriate. |
| **COMPLETED** | **Increments 15–16** | Physics/collision, raycasts, and physics materials. | Current collision/raycast implementation is the safe interaction foundation; advanced body/shape work remains partial. |
| **COMPLETED** | **Increment 17** | Input action and binding foundations. | Expanded gamepad/mobile input stays separate from editor-camera behavior. |
| **COMPLETED** | **Increment 18** | Audio system foundations. | Effects, streaming, side-chain policy, concrete platform backend selection, and editor mixer authoring remain partial; the existing listener/source runtime lifecycle and bounded clip identity resolution are documented below as completed slices. |
| **COMPLETED** | **Increment 19** | Save-game and checkpoint foundations. | Compression, encryption, cloud sync, migration, screenshots, and gameplay-state sections remain partial. |
| **COMPLETED** | **Increment 20** | GLFW window manager and desktop OpenGL backend. | Window/context ownership remains isolated from the renderer. |
| **COMPLETED** | **Increments 59–61** | Project index, import queue/cache, portable change watch, and targeted stale-cache marking. | No automatic reimport, source mutation, or native directory-watch backend is claimed. |
| **COMPLETED** | **Legacy Partial Slice — Typed UVE Envelope Importer Contracts** | The existing generic `AssetImporterUVE` now composes deterministic copy-and-register support for `.uvemodel`, `.uvetex`, `.uveshader`, and `.uvemat`, and EngineCore exposes the same registrations through `EngineServicesUVE`. | Typed envelope reimport is proven; raw FBX/OBJ/glTF/PNG/material-source conversion, parser licensing, derived-data generation, and format-specific diagnostics remain PARTIAL. |
| **COMPLETED** | **Legacy Partial Slice — Asset Load Failure Diagnostics v1** | `IAssetManagerUVE` and `AssetHandleUVE` expose a thread-safe copied reason for the most recent failed load/reload, with deterministic categories for unresolved paths, missing files, missing loaders, and loader rejection. | Diagnostics are observability only; AssetManager remains the sole owner of records/load state, and renderer fallback resources, source conversion, and automatic recovery remain separate PARTIAL work. |
| **COMPLETED** | **Legacy Partial Slice — Raw Asset Source Classification v1** | `IAssetImporterUVE` classifies normalized model, texture, material, audio, and existing UVE envelope extensions through a copied DTO, explicitly reports whether a format-specific parser is required, distinguishes unsupported extensions from custom importer registration, and exposes `rawAudio` through the editor bridge label seam. | This is a policy/diagnostics boundary only; it performs no conversion, decoder registration, metadata parsing, or filesystem read. Raw parser licensing, source-to-derived-data transforms, audio decoder backends, streaming preparation, and representative format fixtures remain PARTIAL; bounded PCM WAV metadata parsing now validates RIFF/WAVE/fmt/data coherence and returns copied duration/channel/rate facts without decoding samples. |
| **COMPLETED** | **Legacy Partial Slice — Text Import Parser v1** | `.txt` import now rejects NUL bytes, deterministically normalizes CR/CRLF to preserve/LF/CRLF policy, optionally ensures one trailing line ending, writes through a temporary file, and exposes a stable cache-version fingerprint through `TextImportSettingsUVE`. | The parser is intentionally byte-oriented and bounded: it does not infer encodings, rewrite Unicode, or claim raw model/texture/material conversion. Those external-library and derived-data decisions remain PARTIAL. |
| **COMPLETED** | **Phase 4 Slice — Resource Dependency Invalidation Plan v1** | `ResourceDependencyGraphUVE` now produces a copied, generation-stamped, deterministic breadth-first reverse-dependent closure with explicit maximum-result truncation and invalid/stale/unknown root diagnostics. | The plan is an invalidation input only; it does not own asset lifetime, perform reloads, or select raw parser libraries. Reload execution, derived-data rebuild policy, and external raw conversion remain separate boundaries. |
| **COMPLETED** | **Scene Component Slice — Camera Validation v1** | `CameraComponentUVE` exposes a shared projection-safe validity predicate for finite open-range FOV and positive ordered clip planes; scene restore rejects invalid payloads through existing rollback semantics. | Validation is a rejection boundary only. Camera projection matrices, render targets, advanced camera variants, and authoring repair UX remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — Light Validation v1** | `LightComponentUVE` exposes a shared renderer-safe validity predicate for finite non-negative HDR color/intensity, known light type, positive range, and open spot angle; scene restore rejects invalid payloads and LightSystemUVE asserts the runtime precondition before extraction. | Validation does not select lights, alter lighting equations, or own GPU resources. Light ranking, shadows, attenuation, authoring repair UX, and advanced lighting remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — Collider Validation v1** | `ColliderComponentUVE` exposes a shared physics-safe validity predicate for finite positive half-extents, nonzero collision layer, bounded friction/restitution, and positive finite density; scene restore rejects invalid payloads through existing rollback semantics. | Validation does not add shape types, change collision filtering, wire density to mass, or repair malformed data. Runtime material extraction retains its established friction/restitution clamp for legacy hand-authored values; collision masks, shape breadth, rigid-body depth, and editor repair UX remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — RigidBody Validation v1** | `RigidBodyComponentUVE` exposes a shared physics-safe validity predicate for finite non-negative mass, finite velocity, non-negative drag, and finite non-negative gravity scale; scene restore rejects invalid payloads and PhysicsSystemUVE asserts the integration precondition. | Zero mass remains the established immovable-body state. Validation does not add body types, mass/inertia solving, constraints, character controllers, or authoring repair UX. |
| **COMPLETED** | **Scene Component Slice — AudioSource Validation v1** | `AudioSourceComponentUVE` exposes a shared audio-safe validity predicate for finite non-negative volume, positive finite pitch, known attenuation curve, and spatial-only ordered distance bounds; scene restore rejects invalid payloads and AudioSourceSystemUVE asserts the synchronization precondition. | Empty asset paths remain the existing no-clip default. Validation remains pure and does not resolve clips, own voices, add mixer routing/effects, or choose a platform backend; optional clip identity resolution is owned by the separate AudioSystemUVE resolver seam. |
| **COMPLETED** | **Legacy Partial Closure — AudioSource Listener Runtime Binding v1** | Existing Increment 18 runtime proof binds validated AudioSourceComponentUVE entities to stable AudioSystemUVE voices, honors play-on-awake, synchronizes world positions and volume/pitch, computes listener-relative attenuation, updates the active camera as listener in EngineCoreUVE::LateUpdate(), and destroys voices when components/entities disappear; Increment 341 makes AudioSystemUVE reject non-finite or negative source position/volume/pitch updates while preserving the last valid device-facing state. | AudioSourceSystemUVE owns only the entity-to-voice map; AudioSystemUVE owns listener/source state and attenuation. Effects, streaming, side-chain policy, concrete platform backend selection, and editor mixer authoring remain partial; clip identity resolution and mixer routing are closed bounded slices. |

<div align="center">

<h2><strong>PARTIAL — CORE & RUNTIME EXPANSION</strong></h2>

</div>

| Status | Area | Intended next capability | Completion proof / boundary |
|---|---|---|---|
| **PARTIAL** | Scene components and user-facing nodes | Introduce only supported components/nodes such as richer camera, mesh, light, collider, rigid-body, audio-source, animation, particle, and script attachments. | Camera, light, collider, rigid-body, and audio-source value validation plus invalid-scene rollback are complete; MeshComponent asset-reference coherence and Script/Particle authored-parameter validation plus rollback are now also complete; ScriptAssetLoaderUVE now reads bounded validated ScriptComponentUVE virtual paths through IFileSystemUVE and decodes copied ScriptGraphSchemaUVE data with deterministic failure diagnostics; validated ScriptComponent ownership still reconciles empty-path detach and graph-to-runtime attach without owning filesystem/asset state; ParticleRuntimeUVE now owns bounded CPU particle emission/update state with finite preflight, stable sequence IDs, semi-implicit integration, and lifetime culling; Increment 389 hardens `ParticleRuntimeUVE::EmitDetailedUVE` against deterministic sequence-range exhaustion, rejecting an emission before reserve or particle mutation while preserving nonzero monotonic sequence IDs; Increment 212 adds `IsValidParticleDrawCommandUVE`, a bounded finite copied particle draw-command gate for render-bridge handoff; shared SkeletonDefinitionUVE/PoseBufferUVE/AnimationEvaluationContextUVE contracts now converge Motion Query and Control Rig validation/evaluation; Increment 211 adds `ValidateSkeletonRetargetMapUVE`, a bounded copied one-to-one source/target joint-map validator against the shared skeleton topology; Increment 381 hardens the shared SkeletonDefinitionUVE, PoseBufferUVE, and SkeletonRetargetMapEntryUVE validators to reject embedded-NUL identifiers before duplicate/parent/map lookup or pose publication, preserving empty-contract compatibility with focused animation-pose regressions; Increment 392 makes `SceneGraphUVE::UpdateUVE` stage derived world position/rotation/scale candidates and retain the prior finite cache with `dirty` evidence when finite hierarchy composition overflows, while propagating invalid parent state to descendants without publishing non-finite world transforms; runtime asset handles now preserve last-known-good values across failed asynchronous replacement reloads with copied diagnostics and successful replacement invalidation semantics; Increment 228 adds `ValidateParticleRenderSnapshotUVE`, a bounded copied particle render-snapshot gate for source-count/truncation consistency, valid entities/sequences, finite positions/depth/lifetimes, and caller caps without GPU work; Increment 397 makes `RenderQueueUVE::AppendParticleSnapshotUVE` reuse that gate and reject malformed copied snapshots before queue mutation, preserving existing particle items while recording truncation evidence; Increment 251 adds `PlanParticleGpuUploadUVE` for fixed-stride overflow-safe byte planning over validated copied draw commands with truncation consistency and failure-atomic rejection, without GPU allocation or submission; Increment 279 adds `EvaluateMeshRenderEligibilityUVE`, a bounded value-only scene-to-render gate for mesh/material GUID coherence, finite world-transform normalization, ordered finite mesh bounds, frustum visibility, finite near-plane sort depth, and copied `invalidRenderEligibility` queue diagnostics without asset loading, GPU allocation, or command submission. GPU simulation/backend/renderer integration beyond the validated snapshot/draw-command handoff, pose conversion/reference-pose adaptation, full animation playback/retargeting, editor command paths, and each remaining component still require real behavior and tests; Increment 291 adds the first typed Visual Scripting Audio node, `audio.set_volume`, through caller-owned volume control and bounded VM/editor proof; Increment 292 adds the typed `audio.set_pitch` node through caller-owned positive-finite pitch control and bounded VM/editor proof; Increment 294 adds the typed `audio.set_3d_position` node through caller-owned finite Vector3 position control and bounded VM/editor proof; Increment 295 adds the typed `audio.play_sound` node through caller-owned trigger control and bounded VM/editor proof; Increment 296 adds the typed `audio.stop_sound` node through a separate caller-owned stop trigger and bounded VM/editor proof; Increment 297 adds the typed `audio.is_playing` node through caller-owned playback-state query control and bounded VM/editor proof; Increment 298 adds the typed `audio.set_attenuation` node through caller-owned finite ordered distance/model control and bounded VM/editor proof; Increment 299 adds the typed `debug.print` node through the existing caller-owned Number log callback and bounded VM/editor proof; Increment 300 adds the typed `debug.warning` node through a distinct caller-owned warning callback, accepted Boolean Result publication, and bounded VM/editor proof; Increment 301 adds the typed `debug.error` node through a distinct caller-owned error callback, accepted Boolean Result publication, and bounded VM/editor proof; Increment 311 adds `IsTransformComponentValidUVE`, requiring finite local position/scale and a near-unit finite local rotation before scene restore or SceneGraphUVE attach/set preconditions; invalid transform payloads roll back atomically and valid transform propagation remains unchanged; Increment 321 adds `IsNameComponentValidUVE` with a 128-byte NUL-free authored-name cap at scene restore, preserving optional empty names and legacy documents that omit the component while rolling back oversized payloads atomically; Increment 329 adds a 1024-byte NUL-free optional `AudioSourceComponentUVE::audioAssetPath` boundary, preserving empty no-clip state and leaving asset resolution/traversal policy caller-owned; Increment 332 tightens `IsCameraComponentValidUVE` with a 0.1-degree minimum FOV so `tan(FOV/2)` remains projection-safe for CameraSystemUVE, preserving the open upper bound and ordered clip-plane rules; Increment 364 hardens CameraSystemUVE projection and frustum-corner entry points with finite-positive aspect-ratio assertions before PerspectiveUVE division and frustum-size arithmetic, with focused debug regressions; Increment 339 hardens `IsScriptAssetPathValidUVE` to reject colon-containing URI and Windows-drive forms in addition to absolute, traversal, separator, NUL, and size violations, with focused validator and serializer rollback regressions; Increment 352 makes `SceneSerializerUVE::RestoreUVE` reject unregistered component names before callback dispatch, routing malformed payloads through entity rollback while preserving pre-existing entities; Increment 371 hardens `ScriptVmExecutionContextUVE` binding/component-fact publication to reject zero node IDs and oversized or embedded-NUL pin/component identifiers before lookup or vector mutation, preserving valid context state with focused regression coverage; Increment 424 makes `SceneSerializerUVE::EncodeScenePayloadUVE` validate every registered authored component before CaptureUVE/SaveUVE JSON publication, rejecting invalid transform, mesh, primitive-mesh, light, camera, name, collider, area, rigid-body, audio-source, script, particle, and prefab-instance state while preserving source ECS state; Increment 421 makes `IEntityManagerUVE::GetComponentPointerUVE` preflight entity liveness and component presence, retaining debug assertions and returning nullptr for stale or absent type-erased pointer requests in release builds without changing reference-returning GetComponentUVE semantics; Increment 420 makes `EntityManagerUVE::RemoveComponentUVE` preflight entity liveness and component presence before archetype migration, retaining debug assertions and returning without mutation for stale or absent-component requests in release builds; Increment 419 makes `EntityManagerUVE::DestroyEntityUVE` return before ECS mutation for stale or invalid handles in release builds while retaining debug assertions, and makes `GetComponentTypesUVE`/`HasComponentUVE` return empty/false for stale queries; Increment 416 makes `SceneGraphUVE::AttachTransformUVE`, `SetLocalTransformUVE`, and `SetParentUVE` reject invalid release-build preconditions before ECS mutation while retaining debug assertions, including cycle-safe traversal over malformed hierarchy state; Increment 409 makes `SceneSerializerUVE::RestoreUVE` validate hierarchy `parentLocalId` values as integers within the `-1` root-sentinel through uint32 local-ID range before creating any entities, preserving valid root/child and legacy restores while rejecting malformed hierarchy payloads failure-atomically with focused rollback coverage; Increment 541 makes `SceneSerializerUVE::RestoreUVE` reject self-parent and cyclic local-ID hierarchy chains before ECS entity creation, preserving valid roots and external-parent-to-root behavior with failure-atomic cycle regression coverage; Increment 374 makes `SceneSerializerUVE::SaveUVE` publish through a temporary sibling and final rename, preserving an existing scene destination when temporary write or publication fails while cleaning failed temporary state; Increment 465 makes `SolveTwoBoneIKUVE` reject finite control positions whose pole-minus-root vector overflows before projected-pole fallback or pose publication, preserving the copied failure result and valid pole-vector solves; Increment 469 makes sphere-vs-AABB narrow-phase distance and radius-square comparison use finite double-precision intermediates, preserving real overlaps for constructible finite radii near `float::max()` instead of rejecting them after float-square overflow; Increment 475 makes `FindBestMotionMatchUVE` reject finite non-negative weights whose aggregate overflows the public float scoring domain before candidate evaluation, preserving the existing `InvalidWeights` result; Increment 478 makes Motion Query continuity blend finite extreme position and scale values with double intermediates and rejects any non-finite blended pose before publication; Increment 479 makes animation-node settings validation accumulate finite matching weights in double before accepting the aggregate, aligning the caller boundary with the core matcher’s InvalidWeights contract; Increment 480 accumulates finite per-sample trajectory costs in double and narrows only after checking the public float cost domain, preserving finite trajectory means across the bounded 32-sample horizon; Increment 481 derives TryBuildMotionQueryUVE root velocity components in double and rejects only non-finite or float-unrepresentable results before query publication, preserving valid opposite-extreme position deltas; Increment 482 makes Motion Query facing normalization use scaled double magnitude preflight before the shared safe normalizer, preserving finite near-maximum directions during matching; Increment 483 makes the Control Rig local direction normalizer use scaled double magnitude preflight before the shared safe normalizer, preserving finite extreme aim and bone directions while retaining zero/invalid fallbacks; Increment 484 widens Control Rig aim and optional-pole position differences before normalization, preserving valid opposite-extreme directions without float subtraction overflow; Increment 485 widens Control Rig two-bone IK distance and solve intermediates to double with explicit finite float-domain narrowing, and publishes reachability only after all preflights and solved poses succeed, preserving finite extreme geometry and failure-atomic rejection; Increment 486 projects finite extreme two-bone pole offsets with scaled double intermediates before normalization, preserving valid bend directions when the float dot product would overflow; Increment 489 computes sphere-vs-sphere center distance and penetration depth in scaled double before explicit finite float-domain narrowing, preserving valid extreme-radius overlaps while retaining radius-sum rejection; Increment 490 computes capsule-vs-sphere segment projection, closest-point distance, and penetration depth in scaled double before explicit finite float-domain narrowing, preserving valid extreme-radius contacts while retaining combined-radius rejection; Increment 491 computes capsule-vs-capsule segment candidates, closest-point distances, and penetration depth in double before explicit finite float-domain narrowing, preserving valid extreme-radius contacts while retaining combined-radius rejection and candidate ordering; Increment 492 computes capsule-vs-AABB segment candidates, closest-point distances, and penetration depth in double before explicit finite float-domain narrowing, preserving valid extreme-radius contacts while retaining strict-touching and inside-box behavior; Increment 493 computes oriented-box SAT center projections, support radii, overlap depths, and final float-domain publication in double, preserving valid extreme-center overlaps while retaining 15-axis ordering, strict-positive overlap, and bounded output behavior; Increment 494 computes sphere-vs-oriented-box center offsets through scaled local-frame arithmetic and rescales only validated finite penetration depth, preserving valid extreme-center overlaps while retaining local-frame, strict-touching, and finite-output behavior; Increment 495 computes capsule-vs-oriented-box endpoint offsets, extents, and radius through one scaled local-frame domain before helper reuse and rescales only validated finite penetration depth, preserving valid extreme-center overlaps while retaining local-frame, strict-touching, and finite-output behavior; Increment 538 makes Renderer3DUVE reject finite primitive transforms whose composed matrix or transformed built-in bounds become non-finite or unordered, and rejects non-finite primitive sort depth before render-item publication while preserving valid primitive extraction and diagnostics; no placeholder node catalog. |
| **PARTIAL** | Prefab maturity | Nested prefabs, override tracking, safe apply/revert, and deterministic conflict handling. | PrefabInstance source-reference validation and invalid-scene rollback are proven; bounded deterministic override records now persist with sorted property paths, explicit size/count caps, and backward-compatible empty defaults; caller-owned live-target Apply/Revert is failure-atomic and baseline-explicit; DetectPrefabOverrideConflictsUVE now performs a bounded read-only baseline preflight with copied expected/actual values and truncation evidence; baseline-checked source-prefab mutation through caller-owned targets is now proven with failure-atomic writes and post-success override clearing; Increment 228 adds `EvaluatePrefabRevisionUVE`, a bounded value-only Current/Stale/Invalid comparison for nonzero source and instance revisions; Increment 280 adds backward-compatible persisted `sourceRevision`/`instanceRevision` fields with legacy default 1, a revision-aware `InstantiateWithRevisionUVE` path that stamps both fields atomically, and strict nonzero validation without automatic merge or asset lookup; Increment 312 extends `CommitPrefabOverridesToSourceUVE` with an optional caller-supplied nonzero committed revision, publishing it to both persisted revision fields only after all source writes succeed while preserving the legacy no-revision path and failure atomicity; Increment 322 makes `ApplyPrefabOverridesUVE` complete a read/staging phase for every override before the first target write, keeping allocation/read failures fully read-only while preserving reverse rollback for write failures and adding a regression for zero writes after a later staging-read failure; Increment 340 clamps `DetectPrefabOverrideConflictsUVE` output to the shared 64-conflict cap even when callers request a larger limit, preserving read-only inspection and truncation evidence with over-cap regression coverage; Increment 353 makes `PrefabSystemUVE::InstantiateWithRevisionUVE` reject stale non-sentinel parent handles before asset resolution or prefab entity loading, preventing SceneGraphUVE assertion paths and preserving zero-mutation failure behavior; Increment 375 makes SceneSerializerUVE enforce exactly one root for Prefab capture/save/load before entity publication, preventing malformed multi-root Prefab payloads from leaving extra instantiated entities while preserving multi-root Scene behavior; Increment 432 makes `PrefabSystemUVE::SavePrefabUVE` catch standard and unknown asset-database exceptions during registration/persistence and makes `InstantiateWithRevisionUVE` catch resolver exceptions before prefab loading or entity publication; Increment 413 makes `PrefabSystemUVE::SavePrefabUVE` reject an invalid asset GUID returned by registration before registry persistence or identity publication, with a rejecting-database regression; Increment 403 makes `CommitPrefabOverridesToSourceUVE` reject a supplied nonzero committed revision below the instance’s current source revision before baseline reads or source writes, preserving revision metadata, overrides, and target state atomically. Increment 526 adds `MergePrefabOverridesUVE`, a deterministic value-only three-way merge that stages output atomically, preserves non-conflicting local/source edits, reports divergent-path conflicts, and leaves source targets, asset databases, instance state, and live entities untouched; conflict resolution UX and asset-revision tracking remain partial. |
| **PARTIAL** | Asset-type importers | Extend the typed-envelope foundation with deliberately selected model, texture, audio, material, animation, and shader source-import contracts. | `.txt` bounded parsing, schema-driven CSV/TSV/JSON DataTable import with bounded typed destinations, generation-aware dependency invalidation planning, and `.uvemodel`/`.uvetex`/`.uvemat`/`.uveshader` copy-and-register are complete; raw model/texture/material/animation source-parser licensing, source/derived-data transforms, and representative fixtures are still required; bounded BMP BI_RLE4/BI_RLE8 indexed packet decoding now validates encoded/absolute/delta/EOL/EOB streams with palette and coordinate bounds, while WAV PCM metadata, PNG IHDR metadata, JPEG frame metadata, bounded OBJ source statistics, glTF/GLB structural metadata, and MTL source statistics are now validated handoff slices, while Increment 216 adds a bounded RGBA8 decoded-pixel budget predicate with overflow-safe width×height×4 rejection and a 64 MiB default cap, Increment 217 adds `ResolveObjIndexUVE` for bounded positive 1-based and negative-relative face-index resolution with failure-atomic output, and Increment 218 adds `ValidateGltfAccessorSpanUVE` for overflow-safe bounded accessor byte-span validation with stride and element-count caps; Increment 219 adds `ValidateMtlTextureReferenceUVE` for bounded relative, NUL-free, traversal-safe texture-reference preflight without filesystem access, Increment 220 adds `ValidateJpegRgba8PixelBudgetUVE` for overflow-safe normalized RGBA8 decoded-pixel budgeting with a 64 MiB default cap, and Increment 230 adds `ClassifyGltfResourceUriUVE` for bounded traversal-safe relative-path or `data:` URI classification with percent-encoded traversal rejection and a 4096-byte cap, while Increment 231 adds `ResolveGltfResourceVirtualPathUVE` for failure-atomic relative-resource composition against an asset directory with VFS separator normalization and a combined 4096-byte bound, and Increment 232 adds `DecodeGltfDataUriUVE` for bounded percent-encoded or strict base64 byte publication with a 64 MiB cap and failure-atomic output, and Increment 233 adds `ResolveObjFaceVertexUVE` for bounded `v`, `v/vt`, `v//vn`, and `v/vt/vn` token resolution with positive/negative index support and failure-atomic copied output, and Increment 234 adds `ParseMtlMaterialPropertyUVE` for bounded finite scalar/vector3 facts and validated single-token texture references with exact arity and failure-atomic output, and Increment 235 adds `ParseMtlTextureMapUVE` for bounded `-s`/`-o` three-component transforms, `-clamp on|off`, validated path publication, and failure-atomic output, and Increment 236 adds `UnfilterPngRgba8ScanlineUVE` for bounded RGBA8 None/Sub/Up/Average/Paeth scanline reconstruction with previous-row shape validation and failure-atomic output; Increment 252 adds `DecodePngRgba8ImageUVE` for bounded 8-bit non-interlaced RGBA PNG chunk/CRC validation, zlib IDAT inflation, scanline reconstruction, and failure-atomic copied pixel publication; Increment 253 adds `DecodeJpegRgba8ImageUVE` for bounded baseline JPEG RGBA8 decoding via libjpeg with explicit error recovery, RGB-to-RGBA expansion, and failure-atomic copied pixel publication; Increment 254 extends `DecodeJpegRgba8ImageUVE` to bounded progressive JPEG RGBA8 decoding through the same recovery, budget, and copied-pixel path; Increment 255 extends `DecodePngRgba8ImageUVE` to bounded non-interlaced 8-bit RGB PNG decoding with RGB-to-opaque-RGBA8 expansion through the existing zlib and scanline path; Increment 256 extends it to bounded non-interlaced 8-bit grayscale PNG decoding with grayscale-to-opaque-RGBA8 expansion through the same path; Increment 257 extends it to bounded non-interlaced 8-bit indexed PNG decoding with strict PLTE/tRNS palette validation, palette-index bounds checks, and copied RGBA8 publication; Increment 258 extends it to bounded non-interlaced 16-bit RGBA PNG decoding with big-endian high-byte down-conversion to RGBA8; Increment 259 extends it to bounded non-interlaced 16-bit grayscale PNG decoding with high-byte grayscale replication to opaque RGBA8; Increment 260 extends it to bounded non-interlaced 16-bit RGB PNG decoding with high-byte RGB conversion to opaque RGBA8; Increment 261 extends it to bounded Adam7-interlaced 8-bit RGBA PNG decoding with overflow-safe pass planning, per-pass unfilter state, and final-coordinate RGBA8 scatter; Increment 262 extends the same bounded Adam7 path to 8-bit RGB PNG input with opaque-alpha normalization; Increment 263 extends it to 8-bit grayscale PNG input with replicated opaque RGBA8 output; Increment 264 extends it to 8-bit indexed PNG input with validated PLTE/tRNS palette mapping and palette-index bounds checks; Increment 265 extends the Adam7 path to 16-bit RGBA PNG input with big-endian high-byte RGBA8 conversion; Increment 266 extends the Adam7 path to 16-bit grayscale PNG input with high-byte grayscale replication to opaque RGBA8; Increment 267 extends the Adam7 path to 16-bit RGB PNG input with high-byte RGB conversion to opaque RGBA8; Increment 268 extends the Adam7 path to 16-bit grayscale-alpha PNG input with high-byte grayscale and alpha conversion to RGBA8; Increment 582 extends the non-interlaced PNG path to 16-bit grayscale-alpha input with high-byte grayscale replication and alpha conversion to RGBA8; Increment 269 registers `RegisterPngImporterUVE`, which reads bounded raw PNG files, decodes through `DecodePngRgba8ImageUVE`, and persists validated RGBA8 `TextureAssetUVE` data to `.uvetex` with failure-atomic destination publication; Increment 270 adds `ValidateTextureUploadUVE`, rejects invalid texture descriptors and partial initial uploads before Null/OpenGL resource allocation, and makes Renderer3DUVE avoid caching failed texture handles; Increment 271 adds `ConvertObjMeshUVE` for bounded finite position/UV/normal parsing, supported OBJ face-token resolution, fan triangulation, copied `MeshAssetUVE` publication, computed fallback normals/bounds, and failure-atomic output without MTL, importer-registration, or GPU ownership; Increment 272 registers the bounded raw-filesystem OBJ importer, which converts through `ConvertObjMeshUVE`, publishes `.uvemodel` envelopes by temporary sibling rename, and leaves generic database registration with `AssetImporterUVE`; Increment 273 adds `ConvertMtlMaterialUVE` for one bounded `newmtl` block, validated Kd/Ke/Ks/Ns/d/Tr scalar/vector mapping, strict map syntax validation, and failure-atomic `MaterialAssetUVE` publication without fabricating texture/shader GUIDs; Increment 274 registers the bounded raw-filesystem MTL importer, converts through `ConvertMtlMaterialUVE`, publishes `.uvemat` envelopes by temporary sibling rename, and leaves generic database registration with `AssetImporterUVE`; Increment 275 adds `ConvertGltfPrimitiveUVE`, a bounded caller-owned glTF TRIANGLES primitive handoff that validates float32 position/normal/UV and U8/U16/U32 index accessor spans, reads little-endian data, computes fallback face normals and local bounds, generates tangents, and publishes `MeshAssetUVE` failure-atomically without JSON, URI, filesystem, material, image, buffer, or GPU ownership; Increment 276 adds the bounded raw `.gltf`/`.glb` filesystem bridge for one buffer, one mesh, one TRIANGLES primitive, POSITION plus optional NORMAL/TEXCOORD_0 and indices, safe relative/data-URI/BIN bytes, delegated `ConvertGltfPrimitiveUVE` conversion, and temporary-sibling `.uvemodel` publication; Increment 277 registers bounded `.jpg` and `.jpeg` filesystem bridges, decodes baseline/progressive JPEG through `DecodeJpegRgba8ImageUVE`, validates RGBA8 byte counts, and publishes `.uvetex` textures through temporary-sibling rename; Increment 278 adds `AudioAssetUVE` and `AssetKindUVE::Audio` for bounded interleaved normalized PCM16-derived samples, implements the cycle-safe audio-side `RegisterWavImporterUVE` bridge for `.wav` to `.uveaudio` publication through EngineCore registration, and preserves runtime stream/mixer/device ownership as deferred; Increment 309 hardens `ParseWavMetadataUVE` to stop at the declared RIFF payload boundary, reject truncated in-bound chunk tails, and ignore unrelated trailing bytes outside the container; Increment 306 hardens the generic UVE-envelope importer to publish through a temporary sibling and final rename, preserving the prior destination on copy or publication failure without adding raw-format parser ownership; Increment 319 adds bounded AssetBundle framing with a 4096-entry cap, a 256-byte non-empty NUL-free virtual-name cap, and symmetric Pack/Scan rejection before untrusted entry ranges are visited or copied; Increment 549 makes AssetBundleUVE and FileSystemUVE reject backslash separator forms independently of host-native path semantics, preserving the forward-slash-only virtual-path contract across bundle packing, VFS matching, and writes; Increment 550 makes the shared AssetBundleUVE scanner reject unsafe stored entry names before HasEntryUVE, ReadEntryUVE, or UnpackUVE visitor dispatch, preventing malformed bundles from exposing traversal-shaped names through direct lookup; Increment 328 adds a 16 MiB built-in text-source cap in `TextFileImportUVE`, enforced before reading and after the read to reject unsized, oversized, or source-growth cases before normalization and destination publication while preserving NUL rejection and line-ending settings; Increment 422 makes schema-driven CSV/TSV DataTable header-mismatch failures publish diagnostics and advance the public DataTable generation counter consistently, including per-column schema mismatches, while preserving failure-atomic rows and typed parsing; Increment 331 hardens schema-driven CSV/TSV/JSON DataTable import with a pre-read check against the existing 4 MiB document cap, retaining the post-read check so source-growth documents fail before typed parsing or `.uvetable` publication; Increment 343 hardens `AssetImporterUVE::RegisterImporterUVE` so empty normalized extensions and empty callbacks are rejected before registration-map mutation, preserving prior valid importers and preventing empty-function invocation; Increment 429 makes `AssetImporterUVE::ImportUVE` catch standard and unknown importer callback exceptions before database registration, preserve the established `import failed` diagnostic with exception detail, and return an invalid GUID without invoking the database; Increment 351 makes `AssetImporterUVE::ImportUVE` reject an invalid GUID returned by the asset-database registration boundary after conversion, preserving fail-closed import results with a rejecting-database regression; Increment 546 makes `AssetImporterUVE::ImportUVE` catch standard and unknown asset-database registration exceptions after successful conversion, preserving the published destination while returning the invalid-GUID sentinel without escaping; Increment 439 makes `ConvertGltfPrimitiveUVE` reject finite-but-overflowed generated normal accumulations immediately after addition, preserving failure-atomic output instead of silently publishing the fallback normal; Increment 360 hardens `ConvertGltfPrimitiveUVE` against finite explicit or generated normals whose squared length overflows, rejecting explicit malformed normals before normalization and using a safe fallback for accumulated normals while preserving failure-atomic output; Increment 368 hardens `DecodeUveFileEnvelopeUVE` to reject raw asset-kind values outside the complete `AssetKindUVE` enum before copied header publication, preserving caller-owned expected-kind checks for valid kinds; Increment 372 makes `WriteUveFileUVE` reject invalid asset-kind values before opening the destination, preserving an existing valid envelope on malformed write attempts; Increment 399 adds `IsUveFilePayloadSizeValidUVE`, a shared 512 MiB payload bound reused by `EncodeUveFileEnvelopeUVE` and `WriteUveFileUVE` so oversized in-memory or filesystem envelope payloads fail closed before publication while valid empty payloads remain supported; Increment 428 makes `AssetManagerUVE::ExecuteLoadUVE` catch standard and unknown registered-loader exceptions, publish a terminal failure diagnostic for initial loads, and preserve last-known-good data during exception reloads while retaining one completion event; Increment 542 makes `AssetManagerUVE::LoadErased` and `ReloadUVE` catch standard and unknown `IAssetDatabaseUVE::ResolveUVE` exceptions, transition initial loads to Failed or preserve loaded reload data with a copied diagnostic, and queue one failed completion event before returning; Increment 548 makes `AssetDatabaseUVE::LoadUVE` publish its default save path only after a successful parse or missing-file initialization, preserving the prior registry and no-argument save target when malformed JSON is rejected; Increment 418 makes `AssetManagerUVE::AddRefErased` and `ReleaseErased` preserve debug lifecycle assertions while returning without refcount mutation for missing records in release builds, protecting stale-handle plumbing from release-only dereference and underflow paths; Increment 405 makes `AssetManagerUVE::LoadErased` create an immediate Failed record with a copied diagnostic and completion event when the requested C++ asset type has no registered loader, avoiding release-build dereference of an absent loader and avoiding unnecessary asset-database resolution or worker submission; Increment 380 makes `SaveTextureAssetUVE` reject unsupported formats, overflowing dimensions, and mismatched pixel byte counts before payload construction or destination opening, preserving an existing valid texture envelope with focused descriptor regressions; Increment 384 hardens `LoadTextureAssetUVE` with subtraction-based payload bounds checks and checked width×height×bytes-per-pixel arithmetic before pixel iterator construction or output publication; Increment 393 hardens `ConvertObjMeshUVE` against finite OBJ normal and triangle-edge arithmetic overflow before normalization or mesh publication, and validates generated vertex/tangent/bounds fields before failure-atomic output assignment; Increment 440 makes shared `TryGenerateMeshTangentsUVE` reject finite triangle tangent/bitangent terms, per-vertex accumulator overflow, and non-finite tangent-basis intermediates before copying generated fields back, with GLTF/OBJ/legacy mesh conversion rejecting the failure before candidate publication while the runtime wrapper retains deterministic fallback behavior; Increment 441 makes `LoadMeshAssetUVE` validate serialized vertex and index counts against the remaining bounded payload before either vector reserve, rejecting impossible counts without allocation and retaining the existing output until all mesh and tangent validation succeeds; Increment 442 makes `LoadShaderUVE` and `SaveShaderAssetUVE` reject unknown `ShaderStageKindUVE` values before typed payload publication while preserving the documented reserved `Compute` value; Increment 443 makes `LoadDataTableAssetUVE` reject empty or over-cap typed-envelope payloads before constructing the document string while preserving the existing destination table until deserialization succeeds; Increment 547 makes `DataTableUVE::DefineColumnUVE` reject out-of-domain `DataTableColumnTypeUVE` values before schema publication or generation changes, preserving failure-atomic typed schema construction; Increment 383 adds `IsMaterialAssetValidUVE` and enforces finite/range-safe albedo, metallic, roughness, and emissive values before MaterialAssetUVE save publication and after decoded load, preserving valid texture/shader GUID sentinel semantics with failure-atomic regressions; Increment 559 adds `DecodeBmpRgba8ImageUVE` for bounded 24/32-bit uncompressed BI_RGB BMP sources with strict header, offset, row-stride, dimension, decoded-pixel, and allocation checks, canonical top-down opaque RGBA8 output, and failure-atomic publication; `RegisterBmpImporterUVE` wires `.bmp` to atomic typed `.uvetex` publication and `AssetImporterUVE` classification/registration; Increment 560 adds `DecodeTgaRgba8ImageUVE` for bounded uncompressed true-color TGA type 2 sources with 24-bit BGR or 32-bit BGRA pixels, origin-bit normalization, strict header/offset/size/allocation checks, canonical top-down opaque RGBA8 output, and failure-atomic publication; `RegisterTgaImporterUVE` wires `.tga` to atomic typed `.uvetex` publication and `AssetImporterUVE` classification/registration; Increment 561 extends `DecodeTgaRgba8ImageUVE` to bounded type 10 true-color RLE packets with exact decoded-pixel accounting, raw/run packet bounds checks, the same origin normalization and opaque RGBA8 output, and failure-atomic malformed-packet rejection; Increment 562 extends the decoder to bounded 8-bit grayscale image types 3 and 11, including origin normalization, grayscale-to-opaque RGBA8 conversion, RLE packet bounds checks, and failure-atomic malformed-depth rejection; Increment 563 extends the decoder to bounded 8-bit indexed image types 1 and 9 with validated 24/32-bit BGR/BGRA palette entries, first-index/length and pixel-index bounds checks, origin normalization, shared raw/RLE packet handling, and failure-atomic malformed palette/header rejection; Increment 564 extends the decoder to bounded 16-bit grayscale+alpha image types 3 and 11, preserving grayscale replication, source alpha, raw/RLE packet bounds, origin normalization, and failure-atomic rejection; Increment 565 extends the decoder to bounded 16-bit BGR5551 true-color type 2/type 10 images with checked attribute-bit semantics, 5-bit channel expansion, 1-bit alpha conversion, raw/RLE packet bounds, and failure-atomic rejection; Increment 566 extends indexed type 1/type 9 images to bounded 16-bit BGR5551 palette entries with the same checked attribute-bit semantics and binary alpha conversion while retaining palette-index and packet bounds; TGA indexed 24/32-bit palette forms retain their prior opaque conversion behavior; Increment 567 adds bounded 32-bit BGRA palette alpha conversion for indexed type 1/type 9 images when the descriptor declares eight attribute bits, preserving raw/RLE packet bounds and existing opaque descriptor-zero behavior; Increment 568 extends packed 16-bit type 2/type 10 true-color and indexed palette images to BGR565 when the descriptor declares zero attribute bits, with six-bit green expansion and opaque alpha while retaining BGR5551 one-bit alpha semantics; Increment 569 preserves source alpha for 32-bit BGRA true-color type 2/type 10 images when the descriptor declares eight attribute bits, sharing the bounded raw/RLE BGRA path while retaining descriptor-zero opaque behavior; Increment 570 extends indexed TGA type 1/type 9 images to bounded 16-bit little-endian pixel indices, preserving nonzero first-index mapping, raw/RLE packet bounds, and failure-atomic rejection while retaining the existing 8-bit indexed and palette-entry conversions; Increment 571 extends true-color TGA type 2/type 10 images to bounded 15-bit BGR555 decoding with two-byte storage stride, descriptor-zero enforcement, raw/RLE packet bounds, opaque RGBA8 conversion, and failure-atomic rejection while retaining 16-bit BGR5551/BGR565 semantics; Increment 572 extends indexed TGA type 1/type 9 palette entries to bounded 15-bit BGR555 conversion with two-byte palette-entry stride, descriptor-zero enforcement, raw/RLE packet bounds, opaque RGBA8 conversion, and failure-atomic rejection while retaining existing 8/16-bit index and palette formats; Increment 573 extends the bounded BMP BITMAPINFOHEADER decoder to 8-bit BI_RGB indexed BGRA palette pixels with a capped 256-entry table, palette-before-pixel bounds, row padding and origin normalization, per-index validation, opaque RGBA8 conversion, and failure-atomic rejection while retaining 24/32-bit paths; Increment 574 extends the same bounded BI_RGB decoder to 16-bit BGR555 pixels with little-endian packed channel expansion and opaque RGBA8 output while retaining existing indexed, 24-bit, 32-bit, row/origin, and failure-atomic behavior; Increment 575 extends the bounded BMP decoder to 16-bit BI_BITFIELDS BGR565 when the three masks are exactly 0xF800/0x07E0/0x001F immediately after a 40-byte info header, preserving mask/pixel bounds, explicit 5/6-bit conversion, opaque RGBA8 output, and failure-atomic rejection while retaining existing BI_RGB/indexed paths; Increment 576 extends the bounded BMP decoder to 4-bit BI_RGB indexed pixels with high/low nibble extraction, a capped 16-entry BGRA palette, packed row-stride and padding checks, bottom-up/top-down normalization, opaque RGBA8 conversion, and failure-atomic rejection while retaining 8-bit indexed and true-color paths; Increment 577 extends the bounded BMP decoder to 1-bit BI_RGB indexed pixels with MSB-first bit extraction, a capped two-entry BGRA palette, ceil(width/8) row sizing and padding checks, bottom-up/top-down normalization, opaque RGBA8 conversion, and failure-atomic rejection while retaining 4/8-bit indexed and true-color paths; Increment 578 extends the bounded BMP decoder to exact-mask 16-bit BI_BITFIELDS BGR555 alongside BGR565, validating the supported mask triplets after safe mask/pixel bounds and preserving explicit 5-bit or 5/6-bit conversion, opaque RGBA8 output, and failure-atomic rejection; Increment 579 extends the bounded BMP decoder to exact-mask 32-bit BI_BITFIELDS BGRX with 0x00FF0000/0x0000FF00/0x000000FF color masks, safe mask/pixel bounds, ignored fourth byte, opaque RGBA8 conversion, and failure-atomic rejection while retaining existing BI_RGB and 16-bit bitfield paths; Increment 580 extends the bounded BMP decoder to 8-bit BI_RLE8 indexed packets with encoded runs, absolute packets, EOL/EOB and delta bounds, palette-index checks, bottom-up/top-down coordinate normalization, and candidate-only opaque RGBA8 publication while retaining existing uncompressed and bitfield paths; TGA color maps outside the bounded indexed forms, unsupported depth/attribute variants, and backend-specific compression, mip generation, streaming, and broader resource lifetime policy remain partial; MTL shader compilation, texture loading, unmodeled map options, material conversion beyond bounded MTL property mapping, mesh/material/image/buffer parsing beyond the bounded OBJ converter, PNG zlib/IDAT/chunk/interlace decoding beyond the bounded non-interlaced 8-bit grayscale/RGB/indexed/RGBA and 16-bit grayscale/RGB/RGBA paths plus Adam7-interlaced 8-bit grayscale/RGB/indexed/RGBA and 16-bit grayscale/grayscale-alpha/RGB/RGBA; JPEG/other raw format conversion and GPU texture ownership, JPEG entropy/pixel decoding beyond the bounded baseline/progressive RGBA8 path, audio codecs beyond the bounded PCM16 WAV decoder, derived `.uveaudio`/`.uvetex` conversion, compression, deterministic full metadata import, filesystem reads, media-type interpretation, and streaming preparation remain partial. |
| **PARTIAL** | Physics depth | Rigid bodies, expanded shapes, character controller, trigger areas, broad-phase acceleration, and measured simulation hardening. | Existing deterministic rigid-body integration, AABB collision resolution, raycast layer queries, symmetric collider layer/mask filtering, AreaComponentUVE scene persistence, bounded read-only area-to-collider overlap queries, a generation-safe copied enter/exit lifecycle tracker with truncation-safe baseline retention, EngineCore typed queued Entered/Exited event-bus delivery, CharacterControllerUVE bounded kinematic substep movement with contact correction/sliding and caller-selected grounded/support-normal classification, validated Box/Sphere/Capsule collider descriptors mapped into a shared conservative AABB cache, exact value-only Sphere↔Sphere, Capsule↔Sphere, Capsule↔Capsule, Sphere↔Box, rotated local-Y Capsule↔Box, Sphere↔rotated Box, Capsule↔rotated Box, and oriented Box↔Box SAT penetration integrated after broad-phase candidate filtering with deterministic axis/depth, strict touching rejection, coincident-center +X fallback, normalized world-rotation centerline construction, tight rotated-capsule and rotated-box AABB extents, box-local exact sphere/capsule evaluation with world-axis restoration, deterministic 15-axis OBB SAT with strict touching rejection, capsule-centerline closest-point and closest-segment-pair evaluation, segment-to-AABB closest-distance evaluation, and diagonal false-positive rejection, a stateless per-query median-split BVH broad phase that preserves legacy pair ordering, an optional bounded generation-safe distance/hinge positional solver integrated after collision resolution, bounded read-only conservative SphereCastSystemUVE, axis-aligned BoxCastUVE, and local-Y CapsuleCastUVE over expanded collider world-AABBs with finite shape validation, layer/ignore filters, deterministic closest hits, moving-center/normal/material facts, and focused native regressions, opt-in CharacterControllerUVE `maximumStepHeight` lift/clearance/drop traversal with failure-atomic rollback and copied `stepUpUsed` evidence, a bounded value-only hinge motor/limit policy evaluator with capped target-speed correction, deterministic lower/upper limit correction, disabled no-op behavior, and failure-atomic finite-input validation (five focused native tests), an opt-in CharacterControllerUVE dynamic-body push path that mutates only validated non-kinematic positive-mass target velocity along the mover-to-target normal with bounded speed and clamp/count diagnostics (three focused controller regressions), and the persisted RigidBody angular state/PhysicsSystem semi-implicit torque plus normalized local-quaternion advancement are proven; Increment 208 adds `EvaluateGyroscopicTorqueUVE`, a bounded value-only diagonal-inertia gyroscopic torque evaluator with finite validation and caller-applied ownership; Increment 225 adds `EvaluateCcdEligibilityUVE`, a bounded finite displacement-threshold classifier returning Invalid, Disabled, or Enabled before caller-owned continuous sweeps; Increment 249 integrates the validated `EvaluateGyroscopicTorqueUVE` term into PhysicsSystemUVE angular-velocity advancement with deterministic nonzero-spin regression coverage; Increment 250 adds `BuildConstraintIslandPlanUVE`, a bounded failure-atomic deterministic connected-component planner over copied constraint entity edges with fixed-cap output. Increment 281 integrates the existing bounded plan into `PhysicsConstraintSystemUVE::SolveUVE`, orders occupied constraint slots by ascending island while preserving slot order within each island, and publishes copied `islandCount`/`islandPlanValid` diagnostics without changing positional equations or claiming parallel/backend execution; Increment 326 makes overflowed finite constraint geometry fail closed as a skipped constraint and preflights both proposed local transform deltas through `IsTransformComponentValidUVE` before either body write, preventing non-finite transform publication or one-sided pair mutation; Increment 305 hardens the shared `MaterialOfUVE` read boundary to fail closed on non-finite friction/restitution and non-positive or non-finite density, with focused physics material regressions; Increment 313 adds a shared finite-and-ordered expanded-AABB guard to SphereCastUVE, CapsuleCastUVE, and BoxCastUVE, skipping overflowed conservative bounds before ray intersection while preserving existing hit ordering and material publication; Increment 396 hardens `IntersectRayUVE` with finite ray/AABB/max-distance preflight and finite slab numerator/parameter validation, rejecting inverted inputs and overflowed hit-distance candidates before RaycastSystemUVE can publish malformed hits; Increment 320 hardens `PhysicsSystemUVE::StepUVE` to fail closed on non-finite injected gravity or non-finite/negative fixed delta before mutating rigid-body velocity, angular state, or transforms; Increment 333 hardens CharacterControllerUVE and `EvaluateCcdEligibilityUVE` to reject finite displacement vectors whose squared length overflows, preventing silent zero-length classification and preserving transform state on invalid controller input; Increment 338 makes CharacterControllerUVE reject non-finite or non-unit collision separation axes and non-finite correction deltas before controller or dynamic-target mutation, with malformed-pair regression coverage; Increment 349 makes `IsValidHingeUVE` reject finite world axes whose squared length overflows before constraint-slot publication, preserving bounded hinge descriptor validity; Increment 545 makes `EvaluateHingeMotorLimitUVE` compute finite motor speed caps and requested corrections in double before explicit float-domain narrowing, preserving valid finite corrections when torque×inverse-inertia×timestep exceeds float range while retaining failure-atomic invalid-input rejection; Increment 358 makes `PhysicsConstraintSystemUVE::SolveUVE` classify non-finite inverse-mass calculations as a one-time skipped-constraint diagnostic before delta arithmetic or transform mutation, covering valid subnormal positive masses with an atomic regression; Increment 362 makes `ComputeBoxInverseInertiaUVE` reject finite box half-extents whose squared values overflow before inertia construction, with focused angular-dynamics regression coverage; Increment 367 makes `BuildColliderWorldAabbCacheUVE` skip derived world AABBs that are non-finite or inverted after center/extents arithmetic, protecting collision, raycast, shape-cast, area-overlap, and controller TOI consumers while preserving valid cache order; Increment 369 makes `AreaOverlapSystemUVE` skip finite authored areas whose world center/extents arithmetic produces non-finite or inverted AABBs before penetration evaluation, preserving valid area/collider results; Increment 395 makes `AreaOverlapSystemUVE::QueryUVE` reject non-finite or non-positive penetration depths derived from finite overlapping AABBs before copied overlap publication, preserving inspected counts and truncation semantics; Increment 398 hardens `ComputePenetrationUVE` with finite/ordered AABB preflight, finite overlap-depth validation, and finite derived-center validation before returning copied penetration facts to collision and area-overlap consumers; Increment 378 makes `PhysicsSystemUVE::StepUVE` stage gravity, drag, angular integration, and position candidates and fail closed before rigid-body or transform publication when finite inputs produce non-finite derived state, preserving atomic pre-step state with focused overflow regressions; Increment 390 makes `CharacterControllerUVE` preflight finite local deltas and reject non-finite derived local positions before SceneGraph mutation, propagating failure through discrete movement, collision correction, and TOI step-up paths while preserving finite controller state with focused overflow regression coverage; Increment 417 makes `PhysicsSystemUVE::StepUVE` reject invalid authored RigidBodyComponentUVE values before integration, collision resolution, or constraint-side mutation, retaining debug assertions and skipping malformed bodies failure-atomically in release builds; Increment 412 makes `AreaOverlapSystemUVE` cap its copied valid-area snapshot at 4096 before overlap evaluation and propagate the cap through existing `truncated` evidence, preserving deterministic encounter order; Increment 423 makes exact sphere-sphere, capsule-sphere, and capsule-capsule narrow-phase helpers reject finite authored radii whose combined-radius sum overflows to a non-finite value before copied penetration publication, preserving valid contacts and deterministic fallback axes; Increment 470 hardens shared `NormalizeUVE` to scale finite vectors before measuring length, preserving unit-vector publication for constructible inputs near `float::max()` without changing the documented zero-vector contract; Increment 471 hardens checked quaternion normalization and inversion with scaled finite-magnitude fallbacks, preserving valid large finite rotations and failure-atomic output for invalid inputs; Increment 472 hardens `TryMakeAxisAngleUVE` to normalize finite axes through the scaled Vector3 path before quaternion construction, preserving valid axis-angle rotations for constructible axes near `float::max()` while retaining zero/non-finite rejection; Increment 473 hardens `TryMakeLookAtUVE` to normalize finite direction and up vectors before basis construction, preserving valid look-at rotations for constructible vectors near `float::max()` while retaining zero, non-finite, and collinear rejection; Increment 407 makes `BuildColliderWorldAabbCacheUVE` cap its copied proxy snapshot at the existing 4096 `DynamicAabbBvhUVE` limit before linear collision, raycast, shape-cast, area-overlap, or controller consumers receive it, preserving deterministic encounter order and valid BVH construction while leaving overflow reporting and broad-phase reuse policy to future work; exact oriented/rotated-box/rotated-capsule casts, sphere/capsule mixed pairs beyond current narrow phases, exact step geometry, gyroscopic coupling, full angular hinge motor/limit application, broader gyroscopic coupling beyond the diagonal-inertia integration, sweep execution beyond the existing controller TOI path, backend-wide continuous response, full locomotion integration, backend-wide CCD, BVH reuse/update policy, persistent/editor-authored constraints, constraint islands, and measured backend hardening remain partial. Any backend replacement still requires a migration/test plan. |
| **PARTIAL** | Audio depth | Source/listener components, mixer groups, effects, streaming, and platform backend selection. | AudioSource runtime binding and listener-relative attenuation are proven; AudioSystemUVE now supports bounded named mixer groups with Master default, validated volume/pitch multipliers, explicit source routing, final device-param scaling, copied deterministic diagnostics, and an optional borrowed clip resolver with failure-atomic rejection and accepted-path copying; decoder-independent PCM WAV metadata parsing now provides copied validated format/rate/channel/data-size/duration facts; Increment 210 adds bounded PCM16 WAV sample decoding into copied normalized float samples with malformed-input rejection and a 1,048,576-sample cap; Increment 206 adds a bounded backend-neutral `ApplyPcmGainEffectUVE` over copied normalized float samples with finite gain validation, hard output clamping, a 1,048,576-sample cap, and failure-atomic publication; Increment 221 adds `ValidateWavPcm16SampleWindowUVE` for bounded caller-owned PCM16 streaming/chunk windows with positive-size, end-bound, and cap validation; Increment 226 adds `ApplyPcmGainEffectChainUVE` for an ordered finite nonnegative eight-stage gain chain with per-stage hard clamping, sample-count cap enforcement, and failure-atomic output publication; Increment 327 wraps all gain-chain allocations, including empty-chain copying, so allocation failure returns false without terminating or publishing partial output while valid stage ordering remains unchanged; Increment 237 adds `DecodeWavPcm16SampleWindowUVE` for direct bounded PCM16 byte-range decoding into copied normalized samples with failure-atomic output; Increment 238 adds `PlanPcm16StreamWindowUVE` for non-mutating contiguous refill-window planning with explicit end and loop-wrap facts under a bounded sample cap; Increment 288 adds `AdvancePcm16StreamCursorUVE` for bounded caller-owned cursor advancement with non-loop end clamping, loop wrapping, cursor-at-end normalization, and failure-atomic invalid-input handling without decoding, refill scheduling, voice/device ownership, mixer routing, or platform backend selection; Increment 334 aligns `PlanPcm16StreamWindowUVE` with the same total-sample cap, rejecting over-budget streams before refill-plan publication while preserving atomic invalid-input handling and loop-wrap facts; Increment 239 adds `ApplyScheduledPcmGainEffectsUVE` for ordered bounded gain windows with overlap-by-caller-order, per-window hard clamping, finite validation, and failure-atomic copied output; Increment 240 adds `EvaluateAudioMixParametersUVE` for finite source/group/attenuation composition into clamped final gain and pitch, and routes AudioSystemUVE through its fail-closed policy; Increment 241 adds `ValidateAudioVoiceParamsUVE` for finite device-facing position, clamped gain, and nonnegative pitch validation, with NullAudioDeviceUVE rejecting invalid parameters before recording calls; Increment 415 makes `AudioSourceSystemUVE::SyncUVE` validate authored AudioSourceComponentUVE values before descriptor creation or per-frame voice setters, ignoring invalid components in release builds while preserving any existing last-known-good voice; Increment 282 adds failure-safe live authored-field refresh in `AudioSourceSystemUVE::SyncUVE`, replacing voices for audio path, looping, spatial, distance, or attenuation edits only after replacement creation and playback restart succeed, preserving the prior Stopped/Playing state and retaining the old voice on failure while volume, pitch, and position continue updating in place; Increment 310 hardens `DecodeWavPcm16SamplesUVE` to catch bounded sample-vector allocation failure and return false without publishing partial output; Increment 318 applies the existing 64-byte non-empty NUL-free mixer-group name contract to all lookup, mutation, source-routing, and default-resolution APIs, rejecting malformed names before temporary map-key copies while preserving Master/default behavior; Increment 350 makes `AudioSystemUVE::CreateSourceUVE` reject an invalid zero or already-published device handle before mixer attachment or source-map publication, preserving failure-atomic routed-source state with focused backend-failure regressions; Increment 359 makes `AudioSystemUVE::SetListenerPositionUVE` reject non-finite coordinates before listener-state publication, preserving the last valid position and preventing non-finite spatial distance derivation with focused regression coverage; Increment 373 adds `IsAudioSourceDescValidUVE` and makes `AudioSystemUVE::CreateSourceUVE` reject non-finite gain/pitch, invalid attenuation models, and invalid spatial distance ordering before clip resolution, device creation, or source-map publication with focused regression coverage; Increment 391 makes `AudioSystemUVE::UpdateUVE` fail closed to finite zero attenuation when finite source/listener positions produce a non-finite squared distance, preserving valid device-facing voice parameters with focused overflow regression coverage; Increment 433 makes `AudioMixerGroupUVE::GetDiagnosticsUVE` accumulate routed source counts through a size_t-saturating helper, preventing multi-group diagnostic overflow while preserving copied snapshots and truncation; Increment 430 makes `AudioSystemUVE::CreateSourceUVE` catch standard and unknown `IAudioClipResolverUVE` exceptions before backend voice creation, returning an invalid handle without source or mixer publication; Increment 402 adds `IsAudioListenerOrientationValidUVE` and makes `AudioSystemUVE::SetListenerOrientationUVE` reject zero, non-finite, or squared-length-overflowed listener basis vectors before internal orientation publication, preserving the last valid orientation with focused regression coverage; Increment 539 makes `AudioSystemUVE` preflight source-map membership before forwarding Play/Stop/GetState/Destroy lifecycle calls, returning false or Stopped and logging a safe no-op for unknown or invalid handles while preserving valid backend forwarding; Increment 551 adds `Pcm16StreamCursorUVE`, a bounded caller-owned persistent cursor over the existing window planner and advance helper, with atomic reset, contiguous window consumption, non-loop end clamping, loop wrapping, and no decoder, refill, voice, device, or backend ownership; Increment 552 adds `Pcm16StreamRefillSchedulerUVE`, a fixed-capacity caller-owned FIFO of planned PCM16 windows with atomic reset/scheduling, deterministic pop order, queue-overflow rejection, empty end-marker rejection, and no sample, decoder, thread, voice, device, or backend ownership; Increment 553 adds `PcmGainEffectScheduleUVE`, a fixed-capacity caller-owned FIFO of validated gain windows that applies them in order through the existing copied-sample helper, retains queued work on malformed application, and consumes the schedule only after successful output publication; Increment 554 adds an optional bounded NUL-free authored `mixerGroup` to `AudioSourceComponentUVE`, persists it with legacy-empty compatibility, canonicalizes empty names to runtime Master, and reroutes existing voices in place with failure-safe descriptor state; side-chain scheduling beyond copied gain windows, concrete platform backends, and editor mixer authoring remain partial. |
| **PARTIAL** | Input breadth | Gamepad, touch, gyro, mobile gesture, remapping, and platform-specific input policy. | GamepadInputSystemUVE now provides a bounded injectable four-slot snapshot with six finite/clamped dead-zone-normalized axes, sixteen buttons, copied current/previous frames, deterministic edge queries, disconnect clearing, and invalid-index safety; InputSystemUVE now provides optional borrowed gamepad injection with per-input-frame copied current/previous snapshot history, deterministic button action trigger/held/release edges, scaled/clamped axis binding composition, null-safe fallback, and failure-atomic bounded action remapping with finite/index validation; MobileInputSystemUVE now provides a bounded backend-neutral ten-slot touch snapshot with stable-identifier deltas, finite positions, clamped pressure, and finite gyroscope rotation-rate values; MobileGestureRecognizerUVE now provides deterministic frame-driven tap/swipe classification with stable touch tracking, finite thresholds, dominant-axis direction, duplicate-frame suppression, and bounded reports; Increment 207 adds `NormalizeTouchCoordinateUVE`, a bounded value-only safe-area-relative normalized coordinate policy with finite validation, clamping, and failure-atomic invalid-input handling; Increment 222 adds `ApplyTouchOrientationUVE` with finite cardinal 0/90/180/270-degree normalized-coordinate mapping and failure-atomic invalid-input handling; Increment 227 adds `EvaluateTouchChordUVE` for exact-count, unique-identifier, finite-position multi-touch chord centroid evaluation with failure-atomic output; Increment 242 adds `EvaluateTouchLifecycleTransitionUVE` for copied per-slot None/Began/Moved/Ended/Replaced classification with stable-identifier and finite-field validation, and registers its native regression source in CMake; Increment 243 adds `EvaluateTouchChordLifecycleTransitionUVE` for exact-count copied previous/current multi-touch Began/Moved/Ended/Replaced/None classification with unique identifiers, finite-field validation, and failure-atomic output; Increment 244 adds `EvaluateGamepadConnectionTransitionUVE` for copied Connected/Disconnected/None edge classification with finite axis validation and failure-atomic output, providing a backend-neutral handoff seam without polling hardware; Increment 245 adds the borrowed `IWindowManagerUVE::AttachInputSystemUVE` desktop bridge, real GLFW keyboard/mouse/scroll polling into the existing InputSystemUVE injection seam, and poll-before-input-commit frame ordering, with NullWindowManagerUVE remaining inert; Increment 246 adds `EvaluateMobileLifecycleTransitionUVE` for copied Inactive/Active/Suspended/Terminated state classification into Activated/Resumed/Suspended/Deactivated/Terminated/Reinitialized/None transitions with invalid-state rejection and failure-atomic output; Increment 247 adds `ValidateTouchCoordinateViewportUVE` for copied finite positive viewport and ordered bounded safe-area validation without native metric acquisition; Increment 248 adds `EvaluateMobileInputPolicyUVE` for copied lifecycle/focus/background-allowance facts into Disabled/TouchOnly/TouchAndGyroscope availability with invalid-state rejection and failure-atomic output; Increment 287 hardens `InputSystemUVE` against keyboard/mouse sentinel bindings and inputs, making `KeyCodeUVE::Count` and `MouseButtonUVE::Count` inert in injection, raw edge queries, binding evaluation, and remapping validation with focused failure-safe regressions. Increment 293 adds `kInputActionAxisTriggerThresholdUVE`, queues copied Axis1D trigger events on absolute crossings above 0.5, and samples previous gamepad axes correctly without platform callbacks; Increment 303 hardens GamepadInputSystemUVE snapshot and query getters with the existing state mutex, preventing concurrent live-state mutation/update races without changing deadzone or frame semantics; Increment 314 adds a 128-byte non-empty NUL-free action-name cap to `InputSystemUVE::RegisterActionUVE`, rejecting malformed names before copied action-map mutation while preserving valid registration, remapping, and query behavior; Increment 324 hardens `SetMouseScrollDeltaUVE` to ignore non-finite deltas and finite additions that would overflow the cross-thread accumulator, preserving the last valid value and existing multi-report accumulation semantics through `UpdateUVE`, with focused regression coverage; Increment 337 makes `MobileInputSystemUVE::SetTouchStateUVE` reject active zero identifiers before snapshot publication or replacement, keeping injected snapshots aligned with the stable nonzero identifier requirement of `EvaluateTouchLifecycleTransitionUVE`; Increment 342 makes `MobileGestureRecognizerUVE` reject non-finite release deltas and compare finite squared distances in double precision, preventing overflowed endpoint deltas from publishing false gestures; Increment 348 makes the same setter reject an active identifier already used by another slot before live-state mutation, preserving unique touch identity for downstream chord and gesture consumers; Increment 357 makes `GamepadInputSystemUVE` ignore axis/button reports while a device is disconnected, preventing staged phantom input from becoming active on reconnect, with focused regression coverage; Increment 376 makes `InputSystemUVE::SetMousePositionUVE` reject non-finite coordinates before cross-thread live-state publication, preserving the last valid position and frame delta with focused regression coverage; Increment 377 makes `InputSystemUVE::UpdateUVE` fail closed when finite endpoint subtraction overflows into a non-finite mouse delta, preserving a finite zero delta with focused regression coverage; Increment 394 makes `MobileInputSystemUVE::UpdateUVE` fail closed when finite touch endpoint subtraction overflows into a non-finite delta, preserving a finite zero touch delta with focused regression coverage; Increment 425 routes `GamepadInputSystemUVE` and `MobileInputSystemUVE` frame publication through a shared saturating uint64 frame helper, preserving monotonic current/previous snapshot numbers at max instead of wrapping; Increment 435 makes `EvaluateTouchChordUVE` reject non-finite running centroid sums before output publication, preserving the prior centroid when individually finite touch positions overflow their aggregate; Increment 414 makes `MobileGestureRecognizerUVE::ConsumeSnapshotUVE` preflight direct snapshots for nonzero unique active identifiers, finite position/delta/pressure, and bounded pressure before frame or track mutation, preserving failure-atomic gesture state with focused invalid-snapshot coverage; Increment 406 caps `InputSystemUVE` at 256 registered action names, rejecting new entries at capacity while preserving atomic replacement of existing actions and bounded per-action bindings; Increment 434 reuses the bounded finite/index validator for initial positive and negative binding vectors, rejecting sentinel, malformed, or over-cap registrations before action-map publication while preserving empty bindings and valid replacements; native Android/iOS callbacks, application/process ownership, background execution, mobile coordinate acquisition, platform gesture frameworks, configurable hysteresis, and responsiveness measurements remain partial. |
| **PARTIAL** | Save-game depth | Version migration, compression, encryption, cloud hooks, screenshot thumbnails, and gameplay-domain state. | SaveGameSystemUVE now composes a bounded caller-owned SavePayloadMigrationRegistryUVE with exact source/target transform registration, deterministic shortest-path multi-hop planning with registration-order tie breaks, failure-atomic working-payload staging, copied applied-step diagnostics, and proven registered v0-to-v1 plus multi-hop load paths; Increment 205 adds save-only bounded RLE compression with magic/size validation, raw backward-compatible fallback, failure-atomic decompression, and a 64 MiB expanded-payload cap used by SaveUVE, LoadUVE, and metadata reads; Increment 284 extends compressed writes with a versioned non-cryptographic payload fingerprint, verifies decompressed bytes failure-atomically, preserves legacy `UVSC` compressed and raw payload reads, and keeps encryption/cloud ownership deferred; Increment 289 separates automatic and manual checkpoint persistence into distinct reserved slots (`kAutoSaveSlotIndexUVE` and `kManualCheckpointSlotIndexUVE`), keeps both hidden from numbered-slot enumeration, preserves metadata slot identity, and retains the shared timer-reset/manual-defers-next-auto-save policy without adding cloud, encryption, or gameplay-domain ownership; Increment 214 adds `EvaluateSaveRevisionUVE`, a bounded base/local/remote Current-style revision classifier returning Unchanged, LocalAhead, RemoteAhead, Conflict, or Invalid; Increment 224 adds `EvaluateSaveSyncActionUVE` to map those statuses to NoOp, Upload, Download, Conflict, or Invalid without performing cloud I/O. Increment 302 adds bounded SaveGameSystemUVE metadata preflight that rejects non-finite or negative playtime before scene serialization or save-directory mutation; Increment 315 hardens `GetSaveMetadataUVE` framing validation to require a readable uint64 world-section length whose bytes consume the decompressed fixed-layout payload exactly, rejecting truncated and trailing world sections without copying world JSON; Increment 323 makes SaveGameSystemUVE select the schema from a bounded metadata discriminator before full metadata decode, migrate the actual framed payload before scene creation or final metadata parsing, and have `GetSaveMetadataUVE` migrate that same real payload rather than a one-byte probe, proven with a legacy field that is undecodable until migration; Increment 336 hardens fixed-frame byte slicing against uint64 length addition/truncation overflow, rejecting an absurd world-section length before copied bytes or iterator arithmetic; Increment 345 rejects embedded metadata whose slot identity differs from the requested slot before `GetSaveMetadataUVE` publication or `LoadUVE` scratch-scene creation, preserving failure-atomic metadata/world loading; Increment 346 revalidates decoded playtime as finite and nonnegative before those same metadata/world publication boundaries, rejecting tampered negative values without entity creation; Increment 355 makes `HasSaveUVE` reuse canonical metadata/envelope validation so corrupt, unsupported, or slot-mismatched files are not reported as usable, and `ListUsedSlotsUVE` consequently hides invalid numbered slots without changing reserved-slot enumeration; Increment 363 adds a shared 64 MiB migration payload cap, rejecting oversized input and transform output before original-payload publication with focused failure-atomic coverage; Increment 527 extends the same cap to current-version pass-through in `MigrateSavePayloadUVE`, rejecting oversized payloads before the `NotRequired` result while preserving caller payload bytes; Increment 536 makes `SaveGameSystemUVE::SaveUVE` reject the empty compression-rejection sentinel before temporary envelope publication, preventing oversized framed payloads from replacing an existing valid slot while cleaning scratch state; Increment 537 makes `SaveGameSystemUVE::SaveUVE` reject a caller-owned scratch envelope whose asset type is not `Scene` before embedding its payload, preserving the existing slot and scratch cleanup contract; Increment 370 adds a 128-byte NUL-free optional save-name contract enforced before SaveUVE filesystem/scene work and again before decoded metadata publication, preserving empty labels and failure-closed tampered metadata reads; Increment 379 makes decoded save metadata reject negative Unix timestamps before either GetSaveMetadataUVE publication or LoadUVE scratch-scene creation, preserving zero engine-version compatibility and legacy schema migration with focused tampered-timestamp coverage; Increment 431 makes `SaveGameSystemUVE::SaveUVE` catch borrowed scene-serializer exceptions, remove partial scratch scenes through a noexcept RAII guard, and preserve an existing prior save before temporary/final publication; Increment 426 makes `SaveGameSystemUVE::LoadUVE` catch caller-owned scene-serializer exceptions into its documented empty-result failure path and remove the scratch scene through a noexcept RAII guard, preserving cleanup on normal return, write failure, and thrown serializer exits; Increment 410 makes `SaveGameSystemUVE::SaveUVE` fail immediately when save-directory preparation reports an error, before scratch-scene serialization or temporary/final save publication, with a counting-serializer regression proving no scene side effect on structural directory failure; Increment 437 makes checkpoint autosave intervals finite and positive, falling back to 300 seconds for invalid construction input and retaining the last valid interval for invalid setter values; Increment 436 makes `CheckpointManagerUVE::UpdateUVE` ignore negative or non-finite delta times before playtime/timer mutation or autosave decisions, preserving both counters and pending checkpoint state; Increment 404 makes `CheckpointManagerUVE` reset its elapsed-since-save counter only after `SaveUVE` succeeds, preserving accumulated time and retry behavior when automatic or manual checkpoint publication fails. Current v1 passes through and unsupported paths fail before scene creation. Encryption, cloud transport, conflict-resolution application, screenshot thumbnails, gameplay-domain migration, rewrite-back policy, and backend ownership remain partial. |
| **PARTIAL** | Platform abstraction | Desktop display modes, Android/iOS lifecycle, mobile safe areas, power management, and other native platform contracts. | Increment 203 adds `ValidateWindowDescUVE`, a bounded value-only desktop descriptor gate for positive dimensions, the shared 16K `kMaximumDisplayModeAxisUVE` cap on both axes, non-empty title, and a valid OpenGL major version; the cap prevents uint32-to-GLFW signed-int narrowing overflow and the unsigned minor-version field is inherently nonnegative. Increment 215 adds `ValidateMonitorSnapshotUVE`, a bounded value-only monitor snapshot gate for bounded names, positive dimensions, and at most one primary monitor; Increment 286 adds `ValidateDisplayModeDescUVE`, a bounded caller-owned desktop display-mode descriptor gate with positive dimensions, a 16K axis cap, and a zero-to-1000 Hz refresh policy, without native enumeration, display-mode application, monitor handles, or hot-plug ownership; Increment 307 adds a 32-entry cap to `ValidateMonitorSnapshotUVE`, rejecting runaway copied monitor enumeration while preserving duplicate-primary and per-entry validation; Increment 317 routes native `WindowManagerUVE::EnumerateMonitorsUVE` through that shared validator and rejects null/negative GLFW enumeration results before returning copied monitor state; Increment 330 makes `WindowManagerUVE::SetFullscreenUVE` commit its cached fullscreen/windowed state only after GLFW confirms the requested monitor transition, preserving the previous state when confirmation fails; Increment 354 makes `WindowManagerUVE::EnumerateMonitorsUVE` reject a GLFW-reported monitor count above the shared 32-entry snapshot cap before vector reservation, preventing oversized backend counts from causing unbounded copied-snapshot allocation; Increment 361 extends `ValidateWindowDescUVE` with a 256-byte title cap and embedded-NUL rejection before GLFW publication, with exact-boundary and malformed-title regressions. Increment 366 makes `WindowManagerUVE` invoke the validator before GLFW initialization and leave invalid descriptors without a native handle, with a failure-closed construction regression; Increment 411 adds `ValidateMonitorDimensionsUVE` and routes signed GLFW monitor-mode dimensions through it before uint32 conversion or copied monitor-snapshot publication, rejecting non-positive reports with failure-atomic outputs while preserving valid maximum signed dimensions; Increment 401 adds `ValidateFramebufferSizeUVE` and routes signed GLFW framebuffer dimensions through it before resize-event publication or uint32 getter conversion, rejecting non-positive reports with failure-atomic outputs and preserving valid framebuffer sizes. The contracts are covered by focused native regressions and do not own GLFW creation, monitor handles, hot-plug state, display-mode application, backend choice, or mobile lifecycle; Increment 540 makes `WindowManagerUVE::EnumerateMonitorsUVE` return an empty inert snapshot before any GLFW query when window construction is invalid, preserving the public degraded-state contract while retaining validated enumeration for live windows. Android/iOS lifecycle, mobile safe areas, power management, hot-plug refresh policy, and real platform display-mode enumeration/application remain partial. |
| **PARTIAL** | Networking | Reliable UDP channels, replication, prediction, and reconciliation. | Increment 204 adds a bounded value-only reliable sequence window with wrap-safe uint32 ordering, deterministic accepted/duplicate/too-old/invalid classification, 32-entry receive history, and cumulative plus forward selective ACK application; Increment 213 adds `EvaluateReliableRetransmitPolicyUVE`, a bounded caller-owned Waiting/Due/Exhausted/Invalid retry decision; Increment 285 adds `ComputeReliableRetryTimeoutUVE`, a finite caller-owned exponential timeout calculator with retry-zero base semantics, overflow-safe doubling, maximum-timeout clamping, and failure-atomic output without clock, timer, socket, queue, or transport ownership; Increment 290 adds exact 12-byte little-endian `SerializeReliablePacketHeaderUVE`/`DeserializeReliablePacketHeaderUVE` with nonzero sequence/ack validation, exact-size decode, and failure-atomic copied output without payload serialization, sockets, timers, peers, or transport ownership; Increment 304 hardens `ApplyReliableAcknowledgementsUVE` to reject reserved zero packet sequences before mutating pending acknowledgement masks, with focused atomic regression coverage; Increment 308 hardens the same bounded ACK mask at the 32-entry cumulative boundary by using an explicit full-mask path for distance 31, avoiding a 32-bit shift and proving complete pending-window clearing; Increment 325 hardens selective ACK arithmetic at the uint32 wrap boundary by ignoring a selective sequence that wraps to reserved zero, preventing an unrelated pending bit from being cleared while preserving valid nearby ACKs; Increment 400 additionally requires every computed selective ACK sequence to be genuinely newer than the acknowledged sequence under the canonical uint32 half-space rule before pending-mask mutation, with focused near-boundary regression coverage; Increment 408 makes `AcceptReliableSequenceUVE` handle an exact 32-sequence forward advance explicitly by placing the previous latest packet in history bit 31, avoiding a uint32 shift-by-32 while preserving duplicate detection at the oldest retained history boundary; Increment 316 adds the derived 1,228,800-byte aggregate reassembly cap and rejects inconsistent over-budget active state before fragment storage or received-byte mutation, preserving failure-atomic output and valid fragment behavior; Increment 223 adds `ValidateReliablePayloadBudgetUVE` with a 1200-byte default datagram cap and custom-cap support; Increment 344 clamps caller-supplied payload maxima above the shared 1200-byte wire cap so custom limits cannot authorize oversized datagrams; Increment 229 adds `PlanReliablePayloadFragmentsUVE` for overflow-safe caller-owned fragment count/final-size planning with a 1024-fragment cap and per-fragment datagram budget; Increment 283 adds `AcceptReliablePayloadFragmentUVE`, a bounded caller-owned copied-byte reassembly state with deterministic Accepted/Duplicate/Complete/Conflict/Invalid statuses, out-of-order assembly, duplicate idempotence, message-conflict rejection, ordered output publication, and state reset without socket, timer, retransmission, peer, or transport ownership; Increment 347 validates active reassembly counters against the copied fragment vector before accepting another fragment, rejecting malformed fragment-count or byte-total accounting failure-atomically with focused regression coverage; Increment 356 rejects inconsistent caller-owned receive state, including an active zero latest sequence or nonzero inactive history, before sequence classification or state mutation, with failure-atomic malformed-state regressions; Increment 555 adds exact 16-byte little-endian `ReliablePayloadFragmentUVE` serialization/deserialization with nonzero message and bounded fragment metadata, exact payload-length checks, the existing 1200-byte fragment cap, and failure-atomic copied output; Increment 556 adds transport-neutral `SerializeReliablePacketFragmentUVE`/`DeserializeReliablePacketFragmentUVE` composition of the validated reliable header and payload-fragment envelope with exact bounded packet sizing and atomic two-part decode; Increment 557 adds `BuildReliablePayloadFragmentsUVE`, a bounded caller-owned splitter that materializes ordered copied fragments through the existing planner, preserves message/index/count metadata, supports out-of-order reassembly, and rejects empty, oversized, over-cap, or invalid inputs without publishing partial output; Increment 558 adds `ReliableRetryScheduleUVE`, a caller-owned persistent elapsed-time/retry state over the existing bounded exponential timeout and policy helpers, with atomic configuration, due evaluation, retry commitment, exhaustion, reset, and overflow/invalid-input rejection while owning no clock, timer, packet, socket, peer, retransmission queue, or transport lifecycle; sockets, fragmentation transport, reassembly state, caller integration of retransmission timers/backoff, peer authentication, replication, prediction, reconciliation, and security/performance strategy remain partial. |

<div align="center">

<h2><strong>CORE RUNTIME ENTRY RULES</strong></h2>

</div>

| Rule | Required discipline |
|---|---|
| **Service ownership** | Cross-subsystem runtime dependencies flow through `EngineServicesUVE`, not new globals. |
| **Threading** | Every public system states thread affinity; OpenGL/device work remains on the owning main/context thread. |
| **Persistence** | `.uve*` and derived-data changes need explicit versioning, validation, and backward-compatibility policy. |
| **Performance** | Optimization claims require profiling evidence. Hot paths require measured allocation and iteration discipline. |
| **Recovery** | Filesystem, serialization, and import failure paths must retain last-known-good data where the contract permits. |
