<div align="center">

<h1><strong>UNIVEX ENGINE — CORE & RUNTIME ROADMAP</strong></h1>

<strong>Native runtime contracts, scene ownership, assets, simulation, and platform foundations</strong>

</div>

> This roadmap protects the engine-owned C++ runtime. Editor features consume these contracts through `EngineServicesUVE`; they do not create a second scene, asset, or runtime lifecycle.

| Status | Completed foundation | Delivered capability | Ongoing boundary |
|---|---|---|---|
| **COMPLETED** | **Increment 1** | Engine loop, logging, timing, events, and `EngineCoreUVE`. | The documented `Init → Load → BeginFrame → Update → LateUpdate → Render → EndFrame → Shutdown` lifecycle remains the single runtime path. |
| **COMPLETED** | **Increment 2** | Memory manager with pool, stack, and heap allocators. | RAII and measured allocation behavior remain required for future low-level work. |
| **COMPLETED** | **Increment 3** | Work-stealing thread-pool job system. | Thread affinity and main-thread rendering constraints remain explicit. |
| **COMPLETED** | **Increment 4** | Configuration and command-line foundations. | Settings must remain versioned and safely recover from malformed/missing files. |
| **COMPLETED** | **Increments 5–6** | ECS, scene graph, prefabs, and scene serialization. | The runtime scene is authoritative; the editor only authors it through commands. |
| **COMPLETED** | **Increment 7** | Asset manager, generic importer, hot reload, and asset bundle foundations. | Asset identity remains GUID-based; future importers must preserve deterministic cache/invalidation rules. |
| **COMPLETED** | **Increment 8** | Virtual file system. | New content systems must use the existing virtual-path and mount contracts where appropriate. |
| **COMPLETED** | **Increments 15–16** | Physics/collision, raycasts, and physics materials. | Current collision/raycast implementation is the safe interaction foundation; advanced body/shape work remains partial. |
| **COMPLETED** | **Increment 17** | Input action and binding foundations. | Expanded gamepad/mobile input stays separate from editor-camera behavior. |
| **COMPLETED** | **Increment 18** | Audio system foundations. | Effects, streaming, side-chain policy, concrete platform backend selection, and editor mixer authoring remain partial; the existing listener/source runtime lifecycle and bounded clip identity resolution are documented below as completed slices. |
| **COMPLETED** | **Increment 19** | Save-game and checkpoint foundations. | Compression, encryption, cloud sync, migration, screenshots, and gameplay-state sections remain partial. |
| **COMPLETED** | **Increment 20** | GLFW window manager and desktop OpenGL backend. | Window/context ownership remains isolated from the renderer. |
| **COMPLETED** | **Increments 59–61** | Project index, import queue/cache, portable change watch, and targeted stale-cache marking. | No automatic reimport, source mutation, or native directory-watch backend is claimed. |
| **COMPLETED** | **Legacy Partial Slice — Typed UVE Envelope Importer Contracts** | The existing generic `AssetImporterUVE` now composes deterministic copy-and-register support for `.uvemodel`, `.uvetex`, `.uveshader`, and `.uvemat`, and EngineCore exposes the same registrations through `EngineServicesUVE`. | Typed envelope reimport is proven; raw FBX/OBJ/glTF/PNG/material-source conversion, parser licensing, derived-data generation, and format-specific diagnostics remain PARTIAL. |
| **COMPLETED** | **Legacy Partial Slice — Asset Load Failure Diagnostics v1** | `IAssetManagerUVE` and `AssetHandleUVE` expose a thread-safe copied reason for the most recent failed load/reload, with deterministic categories for unresolved paths, missing files, missing loaders, and loader rejection. | Diagnostics are observability only; AssetManager remains the sole owner of records/load state, and renderer fallback resources, source conversion, and automatic recovery remain separate PARTIAL work. |
| **COMPLETED** | **Legacy Partial Slice — Raw Asset Source Classification v1** | `IAssetImporterUVE` classifies normalized model, texture, material, audio, and existing UVE envelope extensions through a copied DTO, explicitly reports whether a format-specific parser is required, distinguishes unsupported extensions from custom importer registration, and exposes `rawAudio` through the editor bridge label seam. | This is a policy/diagnostics boundary only; it performs no conversion, decoder registration, metadata parsing, or filesystem read. Raw parser licensing, source-to-derived-data transforms, audio decoder backends, streaming preparation, and representative format fixtures remain PARTIAL; bounded PCM WAV metadata parsing now validates RIFF/WAVE/fmt/data coherence and returns copied duration/channel/rate facts without decoding samples. |
| **COMPLETED** | **Legacy Partial Slice — Text Import Parser v1** | `.txt` import now rejects NUL bytes, deterministically normalizes CR/CRLF to preserve/LF/CRLF policy, optionally ensures one trailing line ending, writes through a temporary file, and exposes a stable cache-version fingerprint through `TextImportSettingsUVE`. | The parser is intentionally byte-oriented and bounded: it does not infer encodings, rewrite Unicode, or claim raw model/texture/material conversion. Those external-library and derived-data decisions remain PARTIAL. |
| **COMPLETED** | **Phase 4 Slice — Resource Dependency Invalidation Plan v1** | `ResourceDependencyGraphUVE` now produces a copied, generation-stamped, deterministic breadth-first reverse-dependent closure with explicit maximum-result truncation and invalid/stale/unknown root diagnostics. | The plan is an invalidation input only; it does not own asset lifetime, perform reloads, or select raw parser libraries. Reload execution, derived-data rebuild policy, and external raw conversion remain separate boundaries. |
| **COMPLETED** | **Scene Component Slice — Camera Validation v1** | `CameraComponentUVE` exposes a shared projection-safe validity predicate for finite open-range FOV and positive ordered clip planes; scene restore rejects invalid payloads through existing rollback semantics. | Validation is a rejection boundary only. Camera projection matrices, render targets, advanced camera variants, and authoring repair UX remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — Light Validation v1** | `LightComponentUVE` exposes a shared renderer-safe validity predicate for finite non-negative HDR color/intensity, known light type, positive range, and open spot angle; scene restore rejects invalid payloads and LightSystemUVE asserts the runtime precondition before extraction. | Validation does not select lights, alter lighting equations, or own GPU resources. Light ranking, shadows, attenuation, authoring repair UX, and advanced lighting remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — Collider Validation v1** | `ColliderComponentUVE` exposes a shared physics-safe validity predicate for finite positive half-extents, nonzero collision layer, bounded friction/restitution, and positive finite density; scene restore rejects invalid payloads through existing rollback semantics. | Validation does not add shape types, change collision filtering, wire density to mass, or repair malformed data. Runtime material extraction retains its established friction/restitution clamp for legacy hand-authored values; collision masks, shape breadth, rigid-body depth, and editor repair UX remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — RigidBody Validation v1** | `RigidBodyComponentUVE` exposes a shared physics-safe validity predicate for finite non-negative mass, finite velocity, non-negative drag, and finite non-negative gravity scale; scene restore rejects invalid payloads and PhysicsSystemUVE asserts the integration precondition. | Zero mass remains the established immovable-body state. Validation does not add body types, mass/inertia solving, constraints, character controllers, or authoring repair UX. |
| **COMPLETED** | **Scene Component Slice — AudioSource Validation v1** | `AudioSourceComponentUVE` exposes a shared audio-safe validity predicate for finite non-negative volume, positive finite pitch, known attenuation curve, and spatial-only ordered distance bounds; scene restore rejects invalid payloads and AudioSourceSystemUVE asserts the synchronization precondition. | Empty asset paths remain the existing no-clip default. Validation remains pure and does not resolve clips, own voices, add mixer routing/effects, or choose a platform backend; optional clip identity resolution is owned by the separate AudioSystemUVE resolver seam. |
| **COMPLETED** | **Legacy Partial Closure — AudioSource Listener Runtime Binding v1** | Existing Increment 18 runtime proof binds validated AudioSourceComponentUVE entities to stable AudioSystemUVE voices, honors play-on-awake, synchronizes world positions and volume/pitch, computes listener-relative attenuation, updates the active camera as listener in EngineCoreUVE::LateUpdate(), and destroys voices when components/entities disappear. | AudioSourceSystemUVE owns only the entity-to-voice map; AudioSystemUVE owns listener/source state and attenuation. Effects, streaming, side-chain policy, concrete platform backend selection, and editor mixer authoring remain partial; clip identity resolution and mixer routing are closed bounded slices. |

<div align="center">

<h2><strong>PARTIAL — CORE & RUNTIME EXPANSION</strong></h2>

</div>

| Status | Area | Intended next capability | Completion proof / boundary |
|---|---|---|---|
| **PARTIAL** | Scene components and user-facing nodes | Introduce only supported components/nodes such as richer camera, mesh, light, collider, rigid-body, audio-source, animation, particle, and script attachments. | Camera, light, collider, rigid-body, and audio-source value validation plus invalid-scene rollback are complete; MeshComponent asset-reference coherence and Script/Particle authored-parameter validation plus rollback are now also complete; ScriptAssetLoaderUVE now reads bounded validated ScriptComponentUVE virtual paths through IFileSystemUVE and decodes copied ScriptGraphSchemaUVE data with deterministic failure diagnostics; validated ScriptComponent ownership still reconciles empty-path detach and graph-to-runtime attach without owning filesystem/asset state; ParticleRuntimeUVE now owns bounded CPU particle emission/update state with finite preflight, stable sequence IDs, semi-implicit integration, and lifetime culling; Increment 212 adds `IsValidParticleDrawCommandUVE`, a bounded finite copied particle draw-command gate for render-bridge handoff; shared SkeletonDefinitionUVE/PoseBufferUVE/AnimationEvaluationContextUVE contracts now converge Motion Query and Control Rig validation/evaluation; Increment 211 adds `ValidateSkeletonRetargetMapUVE`, a bounded copied one-to-one source/target joint-map validator against the shared skeleton topology; runtime asset handles now preserve last-known-good values across failed asynchronous replacement reloads with copied diagnostics and successful replacement invalidation semantics; Increment 228 adds `ValidateParticleRenderSnapshotUVE`, a bounded copied particle render-snapshot gate for source-count/truncation consistency, valid entities/sequences, finite positions/depth/lifetimes, and caller caps without GPU work; Increment 251 adds `PlanParticleGpuUploadUVE` for fixed-stride overflow-safe byte planning over validated copied draw commands with truncation consistency and failure-atomic rejection, without GPU allocation or submission; Increment 279 adds `EvaluateMeshRenderEligibilityUVE`, a bounded value-only scene-to-render gate for mesh/material GUID coherence, finite world-transform normalization, ordered finite mesh bounds, frustum visibility, finite near-plane sort depth, and copied `invalidRenderEligibility` queue diagnostics without asset loading, GPU allocation, or command submission. GPU simulation/backend/renderer integration beyond the validated snapshot/draw-command handoff, pose conversion/reference-pose adaptation, full animation playback/retargeting, editor command paths, and each remaining component still require real behavior and tests; no placeholder node catalog. |
| **PARTIAL** | Prefab maturity | Nested prefabs, override tracking, safe apply/revert, and deterministic conflict handling. | PrefabInstance source-reference validation and invalid-scene rollback are proven; bounded deterministic override records now persist with sorted property paths, explicit size/count caps, and backward-compatible empty defaults; caller-owned live-target Apply/Revert is failure-atomic and baseline-explicit; DetectPrefabOverrideConflictsUVE now performs a bounded read-only baseline preflight with copied expected/actual values and truncation evidence; baseline-checked source-prefab mutation through caller-owned targets is now proven with failure-atomic writes and post-success override clearing; Increment 209 adds `EvaluatePrefabRevisionUVE`, a bounded value-only Current/Stale/Invalid comparison for nonzero source and instance revisions; Increment 280 adds backward-compatible persisted `sourceRevision`/`instanceRevision` fields with legacy default 1, a revision-aware `InstantiateWithRevisionUVE` path that stamps both fields atomically, and strict nonzero validation without automatic merge or asset lookup. Automatic merge policy, conflict resolution UX, and asset-revision tracking remain partial. |
| **PARTIAL** | Asset-type importers | Extend the typed-envelope foundation with deliberately selected model, texture, audio, material, animation, and shader source-import contracts. | `.txt` bounded parsing, generation-aware dependency invalidation planning, and `.uvemodel`/`.uvetex`/`.uvemat`/`.uveshader` copy-and-register are complete; raw model/texture/material/animation source-parser licensing, source/derived-data transforms, and representative fixtures are still required; WAV PCM metadata, PNG IHDR metadata, JPEG frame metadata, bounded OBJ source statistics, glTF/GLB structural metadata, and MTL source statistics are now validated handoff slices, while Increment 216 adds a bounded RGBA8 decoded-pixel budget predicate with overflow-safe width×height×4 rejection and a 64 MiB default cap, Increment 217 adds `ResolveObjIndexUVE` for bounded positive 1-based and negative-relative face-index resolution with failure-atomic output, and Increment 218 adds `ValidateGltfAccessorSpanUVE` for overflow-safe bounded accessor byte-span validation with stride and element-count caps; Increment 219 adds `ValidateMtlTextureReferenceUVE` for bounded relative, NUL-free, traversal-safe texture-reference preflight without filesystem access, Increment 220 adds `ValidateJpegRgba8PixelBudgetUVE` for overflow-safe normalized RGBA8 decoded-pixel budgeting with a 64 MiB default cap, and Increment 230 adds `ClassifyGltfResourceUriUVE` for bounded traversal-safe relative-path or `data:` URI classification with percent-encoded traversal rejection and a 4096-byte cap, while Increment 231 adds `ResolveGltfResourceVirtualPathUVE` for failure-atomic relative-resource composition against an asset directory with VFS separator normalization and a combined 4096-byte bound, and Increment 232 adds `DecodeGltfDataUriUVE` for bounded percent-encoded or strict base64 byte publication with a 64 MiB cap and failure-atomic output, and Increment 233 adds `ResolveObjFaceVertexUVE` for bounded `v`, `v/vt`, `v//vn`, and `v/vt/vn` token resolution with positive/negative index support and failure-atomic copied output, and Increment 234 adds `ParseMtlMaterialPropertyUVE` for bounded finite scalar/vector3 facts and validated single-token texture references with exact arity and failure-atomic output, and Increment 235 adds `ParseMtlTextureMapUVE` for bounded `-s`/`-o` three-component transforms, `-clamp on|off`, validated path publication, and failure-atomic output, and Increment 236 adds `UnfilterPngRgba8ScanlineUVE` for bounded RGBA8 None/Sub/Up/Average/Paeth scanline reconstruction with previous-row shape validation and failure-atomic output; Increment 252 adds `DecodePngRgba8ImageUVE` for bounded 8-bit non-interlaced RGBA PNG chunk/CRC validation, zlib IDAT inflation, scanline reconstruction, and failure-atomic copied pixel publication; Increment 253 adds `DecodeJpegRgba8ImageUVE` for bounded baseline JPEG RGBA8 decoding via libjpeg with explicit error recovery, RGB-to-RGBA expansion, and failure-atomic copied pixel publication; Increment 254 extends `DecodeJpegRgba8ImageUVE` to bounded progressive JPEG RGBA8 decoding through the same recovery, budget, and copied-pixel path; Increment 255 extends `DecodePngRgba8ImageUVE` to bounded non-interlaced 8-bit RGB PNG decoding with RGB-to-opaque-RGBA8 expansion through the existing zlib and scanline path; Increment 256 extends it to bounded non-interlaced 8-bit grayscale PNG decoding with grayscale-to-opaque-RGBA8 expansion through the same path; Increment 257 extends it to bounded non-interlaced 8-bit indexed PNG decoding with strict PLTE/tRNS palette validation, palette-index bounds checks, and copied RGBA8 publication; Increment 258 extends it to bounded non-interlaced 16-bit RGBA PNG decoding with big-endian high-byte down-conversion to RGBA8; Increment 259 extends it to bounded non-interlaced 16-bit grayscale PNG decoding with high-byte grayscale replication to opaque RGBA8; Increment 260 extends it to bounded non-interlaced 16-bit RGB PNG decoding with high-byte RGB conversion to opaque RGBA8; Increment 261 extends it to bounded Adam7-interlaced 8-bit RGBA PNG decoding with overflow-safe pass planning, per-pass unfilter state, and final-coordinate RGBA8 scatter; Increment 262 extends the same bounded Adam7 path to 8-bit RGB PNG input with opaque-alpha normalization; Increment 263 extends it to 8-bit grayscale PNG input with replicated opaque RGBA8 output; Increment 264 extends it to 8-bit indexed PNG input with validated PLTE/tRNS palette mapping and palette-index bounds checks; Increment 265 extends the Adam7 path to 16-bit RGBA PNG input with big-endian high-byte RGBA8 conversion; Increment 266 extends the Adam7 path to 16-bit grayscale PNG input with high-byte grayscale replication to opaque RGBA8; Increment 267 extends the Adam7 path to 16-bit RGB PNG input with high-byte RGB conversion to opaque RGBA8; Increment 268 extends the Adam7 path to 16-bit grayscale-alpha PNG input with high-byte grayscale and alpha conversion to RGBA8; Increment 269 registers `RegisterPngImporterUVE`, which reads bounded raw PNG files, decodes through `DecodePngRgba8ImageUVE`, and persists validated RGBA8 `TextureAssetUVE` data to `.uvetex` with failure-atomic destination publication; Increment 270 adds `ValidateTextureUploadUVE`, rejects invalid texture descriptors and partial initial uploads before Null/OpenGL resource allocation, and makes Renderer3DUVE avoid caching failed texture handles; Increment 271 adds `ConvertObjMeshUVE` for bounded finite position/UV/normal parsing, supported OBJ face-token resolution, fan triangulation, copied `MeshAssetUVE` publication, computed fallback normals/bounds, and failure-atomic output without MTL, importer-registration, or GPU ownership; Increment 272 registers the bounded raw-filesystem OBJ importer, which converts through `ConvertObjMeshUVE`, publishes `.uvemodel` envelopes by temporary sibling rename, and leaves generic database registration with `AssetImporterUVE`; Increment 273 adds `ConvertMtlMaterialUVE` for one bounded `newmtl` block, validated Kd/Ke/Ks/Ns/d/Tr scalar/vector mapping, strict map syntax validation, and failure-atomic `MaterialAssetUVE` publication without fabricating texture/shader GUIDs; Increment 274 registers the bounded raw-filesystem MTL importer, converts through `ConvertMtlMaterialUVE`, publishes `.uvemat` envelopes by temporary sibling rename, and leaves generic database registration with `AssetImporterUVE`; Increment 275 adds `ConvertGltfPrimitiveUVE`, a bounded caller-owned glTF TRIANGLES primitive handoff that validates float32 position/normal/UV and U8/U16/U32 index accessor spans, reads little-endian data, computes fallback face normals and local bounds, generates tangents, and publishes `MeshAssetUVE` failure-atomically without JSON, URI, filesystem, material, image, buffer, or GPU ownership; Increment 276 adds the bounded raw `.gltf`/`.glb` filesystem bridge for one buffer, one mesh, one TRIANGLES primitive, POSITION plus optional NORMAL/TEXCOORD_0 and indices, safe relative/data-URI/BIN bytes, delegated `ConvertGltfPrimitiveUVE` conversion, and temporary-sibling `.uvemodel` publication; Increment 277 registers bounded `.jpg` and `.jpeg` filesystem bridges, decodes baseline/progressive JPEG through `DecodeJpegRgba8ImageUVE`, validates RGBA8 byte counts, and publishes `.uvetex` textures through temporary-sibling rename; Increment 278 adds `AudioAssetUVE` and `AssetKindUVE::Audio` for bounded interleaved normalized PCM16-derived samples, implements the cycle-safe audio-side `RegisterWavImporterUVE` bridge for `.wav` to `.uveaudio` publication through EngineCore registration, and preserves runtime stream/mixer/device ownership as deferred; backend-specific compression, mip generation, streaming, and broader resource lifetime policy remain partial; MTL shader compilation, texture loading, unmodeled map options, material conversion beyond bounded MTL property mapping, mesh/material/image/buffer parsing beyond the bounded OBJ converter, PNG zlib/IDAT/chunk/interlace decoding beyond the bounded non-interlaced 8-bit grayscale/RGB/indexed/RGBA and 16-bit grayscale/RGB/RGBA paths plus Adam7-interlaced 8-bit grayscale/RGB/indexed/RGBA and 16-bit grayscale/grayscale-alpha/RGB/RGBA; JPEG/other raw format conversion and GPU texture ownership, JPEG entropy/pixel decoding beyond the bounded baseline/progressive RGBA8 path, audio codecs beyond the bounded PCM16 WAV decoder, derived `.uveaudio`/`.uvetex` conversion, compression, deterministic full metadata import, filesystem reads, media-type interpretation, and streaming preparation remain partial. |
| **PARTIAL** | Physics depth | Rigid bodies, expanded shapes, character controller, trigger areas, broad-phase acceleration, and measured simulation hardening. | Existing deterministic rigid-body integration, AABB collision resolution, raycast layer queries, symmetric collider layer/mask filtering, AreaComponentUVE scene persistence, bounded read-only area-to-collider overlap queries, a generation-safe copied enter/exit lifecycle tracker with truncation-safe baseline retention, EngineCore typed queued Entered/Exited event-bus delivery, CharacterControllerUVE bounded kinematic substep movement with contact correction/sliding and caller-selected grounded/support-normal classification, validated Box/Sphere/Capsule collider descriptors mapped into a shared conservative AABB cache, exact value-only Sphere↔Sphere, Capsule↔Sphere, Capsule↔Capsule, Sphere↔Box, rotated local-Y Capsule↔Box, Sphere↔rotated Box, Capsule↔rotated Box, and oriented Box↔Box SAT penetration integrated after broad-phase candidate filtering with deterministic axis/depth, strict touching rejection, coincident-center +X fallback, normalized world-rotation centerline construction, tight rotated-capsule and rotated-box AABB extents, box-local exact sphere/capsule evaluation with world-axis restoration, deterministic 15-axis OBB SAT with strict touching rejection, capsule-centerline closest-point and closest-segment-pair evaluation, segment-to-AABB closest-distance evaluation, and diagonal false-positive rejection, a stateless per-query median-split BVH broad phase that preserves legacy pair ordering, an optional bounded generation-safe distance/hinge positional solver integrated after collision resolution, bounded read-only conservative SphereCastSystemUVE, axis-aligned BoxCastUVE, and local-Y CapsuleCastUVE over expanded collider world-AABBs with finite shape validation, layer/ignore filters, deterministic closest hits, moving-center/normal/material facts, and focused native regressions, opt-in CharacterControllerUVE `maximumStepHeight` lift/clearance/drop traversal with failure-atomic rollback and copied `stepUpUsed` evidence, a bounded value-only hinge motor/limit policy evaluator with capped target-speed correction, deterministic lower/upper limit correction, disabled no-op behavior, and failure-atomic finite-input validation (five focused native tests), an opt-in CharacterControllerUVE dynamic-body push path that mutates only validated non-kinematic positive-mass target velocity along the mover-to-target normal with bounded speed and clamp/count diagnostics (three focused controller regressions), and the persisted RigidBody angular state/PhysicsSystem semi-implicit torque plus normalized local-quaternion advancement are proven; Increment 208 adds `EvaluateGyroscopicTorqueUVE`, a bounded value-only diagonal-inertia gyroscopic torque evaluator with finite validation and caller-applied ownership; Increment 225 adds `EvaluateCcdEligibilityUVE`, a bounded finite displacement-threshold classifier returning Invalid, Disabled, or Enabled before caller-owned continuous sweeps; Increment 249 integrates the validated `EvaluateGyroscopicTorqueUVE` term into PhysicsSystemUVE angular-velocity advancement with deterministic nonzero-spin regression coverage; Increment 250 adds `BuildConstraintIslandPlanUVE`, a bounded failure-atomic deterministic connected-component planner over copied constraint entity edges with fixed-cap output. Increment 281 integrates the existing bounded plan into `PhysicsConstraintSystemUVE::SolveUVE`, orders occupied constraint slots by ascending island while preserving slot order within each island, and publishes copied `islandCount`/`islandPlanValid` diagnostics without changing positional equations or claiming parallel/backend execution. Exact oriented/rotated-box/rotated-capsule casts, sphere/capsule mixed pairs beyond current narrow phases, exact step geometry, gyroscopic coupling, full angular hinge motor/limit application, broader gyroscopic coupling beyond the diagonal-inertia integration, sweep execution beyond the existing controller TOI path, backend-wide continuous response, full locomotion integration, backend-wide CCD, BVH reuse/update policy, persistent/editor-authored constraints, constraint islands, and measured backend hardening remain partial. Any backend replacement still requires a migration/test plan. |
| **PARTIAL** | Audio depth | Source/listener components, mixer groups, effects, streaming, and platform backend selection. | AudioSource runtime binding and listener-relative attenuation are proven; AudioSystemUVE now supports bounded named mixer groups with Master default, validated volume/pitch multipliers, explicit source routing, final device-param scaling, copied deterministic diagnostics, and an optional borrowed clip resolver with failure-atomic rejection and accepted-path copying; decoder-independent PCM WAV metadata parsing now provides copied validated format/rate/channel/data-size/duration facts; Increment 210 adds bounded PCM16 WAV sample decoding into copied normalized float samples with malformed-input rejection and a 1,048,576-sample cap; Increment 206 adds a bounded backend-neutral `ApplyPcmGainEffectUVE` over copied normalized float samples with finite gain validation, hard output clamping, a 1,048,576-sample cap, and failure-atomic publication; Increment 221 adds `ValidateWavPcm16SampleWindowUVE` for bounded caller-owned PCM16 streaming/chunk windows with positive-size, end-bound, and cap validation; Increment 226 adds `ApplyPcmGainEffectChainUVE` for an ordered finite nonnegative eight-stage gain chain with per-stage hard clamping, sample-count cap enforcement, and failure-atomic output publication; Increment 237 adds `DecodeWavPcm16SampleWindowUVE` for direct bounded PCM16 byte-range decoding into copied normalized samples with failure-atomic output; Increment 238 adds `PlanPcm16StreamWindowUVE` for non-mutating contiguous refill-window planning with explicit end and loop-wrap facts under a bounded sample cap; Increment 239 adds `ApplyScheduledPcmGainEffectsUVE` for ordered bounded gain windows with overlap-by-caller-order, per-window hard clamping, finite validation, and failure-atomic copied output; Increment 240 adds `EvaluateAudioMixParametersUVE` for finite source/group/attenuation composition into clamped final gain and pitch, and routes AudioSystemUVE through its fail-closed policy; Increment 241 adds `ValidateAudioVoiceParamsUVE` for finite device-facing position, clamped gain, and nonnegative pitch validation, with NullAudioDeviceUVE rejecting invalid parameters before recording calls; Increment 282 adds failure-safe live authored-field refresh in `AudioSourceSystemUVE::SyncUVE`, replacing voices for audio path, looping, spatial, distance, or attenuation edits only after replacement creation and playback restart succeed, preserving the prior Stopped/Playing state and retaining the old voice on failure while volume, pitch, and position continue updating in place. Persistent cursor mutation, refill scheduling, persistent mixer routing, side-chain/effect-chain scheduling beyond copied primitives, concrete platform backends, and editor mixer authoring remain partial. |
| **PARTIAL** | Input breadth | Gamepad, touch, gyro, mobile gesture, remapping, and platform-specific input policy. | GamepadInputSystemUVE now provides a bounded injectable four-slot snapshot with six finite/clamped dead-zone-normalized axes, sixteen buttons, copied current/previous frames, deterministic edge queries, disconnect clearing, and invalid-index safety; InputSystemUVE now provides optional borrowed gamepad injection with per-input-frame copied current/previous snapshot history, deterministic button action trigger/held/release edges, scaled/clamped axis binding composition, null-safe fallback, and failure-atomic bounded action remapping with finite/index validation; MobileInputSystemUVE now provides a bounded backend-neutral ten-slot touch snapshot with stable-identifier deltas, finite positions, clamped pressure, and finite gyroscope rotation-rate values; MobileGestureRecognizerUVE now provides deterministic frame-driven tap/swipe classification with stable touch tracking, finite thresholds, dominant-axis direction, duplicate-frame suppression, and bounded reports; Increment 207 adds `NormalizeTouchCoordinateUVE`, a bounded value-only safe-area-relative normalized coordinate policy with finite validation, clamping, and failure-atomic invalid-input handling; Increment 222 adds `ApplyTouchOrientationUVE` with finite cardinal 0/90/180/270-degree normalized-coordinate mapping and failure-atomic invalid-input handling; Increment 227 adds `EvaluateTouchChordUVE` for exact-count, unique-identifier, finite-position multi-touch chord centroid evaluation with failure-atomic output; Increment 242 adds `EvaluateTouchLifecycleTransitionUVE` for copied per-slot None/Began/Moved/Ended/Replaced classification with stable-identifier and finite-field validation, and registers its native regression source in CMake; Increment 243 adds `EvaluateTouchChordLifecycleTransitionUVE` for exact-count copied previous/current multi-touch Began/Moved/Ended/Replaced/None classification with unique identifiers, finite-field validation, and failure-atomic output; Increment 244 adds `EvaluateGamepadConnectionTransitionUVE` for copied Connected/Disconnected/None edge classification with finite axis validation and failure-atomic output, providing a backend-neutral handoff seam without polling hardware; Increment 245 adds the borrowed `IWindowManagerUVE::AttachInputSystemUVE` desktop bridge, real GLFW keyboard/mouse/scroll polling into the existing InputSystemUVE injection seam, and poll-before-input-commit frame ordering, with NullWindowManagerUVE remaining inert; Increment 246 adds `EvaluateMobileLifecycleTransitionUVE` for copied Inactive/Active/Suspended/Terminated state classification into Activated/Resumed/Suspended/Deactivated/Terminated/Reinitialized/None transitions with invalid-state rejection and failure-atomic output; Increment 247 adds `ValidateTouchCoordinateViewportUVE` for copied finite positive viewport and ordered bounded safe-area validation without native metric acquisition; Increment 248 adds `EvaluateMobileInputPolicyUVE` for copied lifecycle/focus/background-allowance facts into Disabled/TouchOnly/TouchAndGyroscope availability with invalid-state rejection and failure-atomic output. Native Android/iOS callbacks, application/process ownership, background execution, mobile coordinate acquisition, platform gesture frameworks, and responsiveness measurements remain partial. |
| **PARTIAL** | Save-game depth | Version migration, compression, encryption, cloud hooks, screenshot thumbnails, and gameplay-domain state. | SaveGameSystemUVE now composes a bounded caller-owned SavePayloadMigrationRegistryUVE with exact source/target transform registration, deterministic shortest-path multi-hop planning with registration-order tie breaks, failure-atomic working-payload staging, copied applied-step diagnostics, and proven registered v0-to-v1 plus multi-hop load paths; Increment 205 adds save-only bounded RLE compression with magic/size validation, raw backward-compatible fallback, failure-atomic decompression, and a 64 MiB expanded-payload cap used by SaveUVE, LoadUVE, and metadata reads; Increment 214 adds `EvaluateSaveRevisionUVE`, a bounded base/local/remote Current-style revision classifier returning Unchanged, LocalAhead, RemoteAhead, Conflict, or Invalid; Increment 224 adds `EvaluateSaveSyncActionUVE` to map those statuses to NoOp, Upload, Download, Conflict, or Invalid without performing cloud I/O. Current v1 passes through and unsupported paths fail before scene creation. Encryption, cloud transport, conflict-resolution application, screenshot thumbnails, gameplay-domain migration, rewrite-back policy, and backend ownership remain partial. |
| **PARTIAL** | Platform abstraction | Desktop display modes, Android/iOS lifecycle, mobile safe areas, power management, and other native platform contracts. | Increment 203 adds `ValidateWindowDescUVE`, a bounded value-only desktop descriptor gate for positive dimensions, non-empty title, and a valid OpenGL major version; the unsigned minor-version field is inherently nonnegative. Increment 215 adds `ValidateMonitorSnapshotUVE`, a bounded value-only monitor snapshot gate for bounded names, positive dimensions, and at most one primary monitor. The contracts are covered by focused native regressions and do not own GLFW creation, monitor handles, hot-plug state, display-mode application, backend choice, or mobile lifecycle. Android/iOS lifecycle, mobile safe areas, power management, hot-plug refresh policy, and real platform display-mode enumeration/application remain partial. |
| **PARTIAL** | Networking | Reliable UDP channels, replication, prediction, and reconciliation. | Increment 204 adds a bounded value-only reliable sequence window with wrap-safe uint32 ordering, deterministic accepted/duplicate/too-old/invalid classification, 32-entry receive history, and cumulative plus forward selective ACK application; Increment 213 adds `EvaluateReliableRetransmitPolicyUVE`, a bounded caller-owned Waiting/Due/Exhausted/Invalid retry decision, and Increment 223 adds `ValidateReliablePayloadBudgetUVE` with a 1200-byte default datagram cap and custom-cap support; Increment 229 adds `PlanReliablePayloadFragmentsUVE` for overflow-safe caller-owned fragment count/final-size planning with a 1024-fragment cap and per-fragment datagram budget; thirteen focused native regressions cover the combined contract. Sockets, payload serialization, fragmentation transport, reassembly state, caller integration of retransmission timers/backoff, peer authentication, replication, prediction, reconciliation, and security/performance strategy remain partial. |

<div align="center">

<h2><strong>CORE RUNTIME ENTRY RULES</strong></h2>

</div>

| Rule | Required discipline |
|---|---|
| **Service ownership** | Cross-subsystem runtime dependencies flow through `EngineServicesUVE`, not new globals. |
| **Threading** | Every public system states thread affinity; OpenGL/device work remains on the owning main/context thread. |
| **Persistence** | `.uve*` and derived-data changes need explicit versioning, validation, and backward-compatibility policy. |
| **Performance** | Optimization claims require profiling evidence. Hot paths require measured allocation and iteration discipline. |
| **Recovery** | Filesystem, serialization, and import failure paths must retain last-known-good data where the contract permits. |
