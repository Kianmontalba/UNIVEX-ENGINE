<div align="center">

<h1><strong>UNIVEX ENGINE — CORE & RUNTIME ROADMAP</strong></h1>

<strong>Native runtime contracts, scene ownership, assets, simulation, and platform foundations</strong>

</div>

> This roadmap protects the engine-owned C++ runtime. Editor features consume these contracts through `EngineServicesUVE`; they do not create a second scene, asset, or runtime lifecycle.

| Status | Completed foundation | Delivered capability | Ongoing boundary |
|---|---|---|---|
| **COMPLETED** | **Increment 1** | Engine loop, logging, timing, events, and `EngineCoreUVE`. | The documented `Init → Load → BeginFrame → Update → LateUpdate → Render → EndFrame → Shutdown` lifecycle remains the single runtime path. |
| **COMPLETED** | **Increment 2** | Memory manager with pool, stack, and heap allocators. | RAII and measured allocation behavior remain required for future low-level work. |
| **COMPLETED** | **Increment 3** | Work-stealing thread-pool job system. | Thread affinity and main-thread rendering constraints remain explicit. |
| **COMPLETED** | **Increment 4** | Configuration and command-line foundations. | Settings must remain versioned and safely recover from malformed/missing files. |
| **COMPLETED** | **Increments 5–6** | ECS, scene graph, prefabs, and scene serialization. | The runtime scene is authoritative; the editor only authors it through commands. |
| **COMPLETED** | **Increment 7** | Asset manager, generic importer, hot reload, and asset bundle foundations. | Asset identity remains GUID-based; future importers must preserve deterministic cache/invalidation rules. |
| **COMPLETED** | **Increment 8** | Virtual file system. | New content systems must use the existing virtual-path and mount contracts where appropriate. |
| **COMPLETED** | **Increments 15–16** | Physics/collision, raycasts, and physics materials. | Current collision/raycast implementation is the safe interaction foundation; advanced body/shape work remains partial. |
| **COMPLETED** | **Increment 17** | Input action and binding foundations. | Expanded gamepad/mobile input stays separate from editor-camera behavior. |
| **COMPLETED** | **Increment 18** | Audio system foundations. | Effects, streaming, side-chain policy, concrete platform backend selection, and editor mixer authoring remain partial; the existing listener/source runtime lifecycle and bounded clip identity resolution are documented below as completed slices. |
| **COMPLETED** | **Increment 19** | Save-game and checkpoint foundations. | Compression, encryption, cloud sync, migration, screenshots, and gameplay-state sections remain partial. |
| **COMPLETED** | **Increment 20** | GLFW window manager and desktop OpenGL backend. | Window/context ownership remains isolated from the renderer. |
| **COMPLETED** | **Increments 59–61** | Project index, import queue/cache, portable change watch, and targeted stale-cache marking. | No automatic reimport, source mutation, or native directory-watch backend is claimed. |
| **COMPLETED** | **Legacy Partial Slice — Typed UVE Envelope Importer Contracts** | The existing generic `AssetImporterUVE` now composes deterministic copy-and-register support for `.uvemodel`, `.uvetex`, `.uveshader`, and `.uvemat`, and EngineCore exposes the same registrations through `EngineServicesUVE`. | Typed envelope reimport is proven; raw FBX/OBJ/glTF/PNG/material-source conversion, parser licensing, derived-data generation, and format-specific diagnostics remain PARTIAL. |
| **COMPLETED** | **Legacy Partial Slice — Asset Load Failure Diagnostics v1** | `IAssetManagerUVE` and `AssetHandleUVE` expose a thread-safe copied reason for the most recent failed load/reload, with deterministic categories for unresolved paths, missing files, missing loaders, and loader rejection. | Diagnostics are observability only; AssetManager remains the sole owner of records/load state, and renderer fallback resources, source conversion, and automatic recovery remain separate PARTIAL work. |
| **COMPLETED** | **Legacy Partial Slice — Raw Asset Source Classification v1** | `IAssetImporterUVE` classifies normalized model, texture, material, audio, and existing UVE envelope extensions through a copied DTO, explicitly reports whether a format-specific parser is required, distinguishes unsupported extensions from custom importer registration, and exposes `rawAudio` through the editor bridge label seam. | This is a policy/diagnostics boundary only; it performs no conversion, decoder registration, metadata parsing, or filesystem read. Raw parser licensing, source-to-derived-data transforms, audio decoder backends, streaming preparation, and representative format fixtures remain PARTIAL; bounded PCM WAV metadata parsing now validates RIFF/WAVE/fmt/data coherence and returns copied duration/channel/rate facts without decoding samples. |
| **COMPLETED** | **Legacy Partial Slice — Text Import Parser v1** | `.txt` import now rejects NUL bytes, deterministically normalizes CR/CRLF to preserve/LF/CRLF policy, optionally ensures one trailing line ending, writes through a temporary file, and exposes a stable cache-version fingerprint through `TextImportSettingsUVE`. | The parser is intentionally byte-oriented and bounded: it does not infer encodings, rewrite Unicode, or claim raw model/texture/material conversion. Those external-library and derived-data decisions remain PARTIAL. |
| **COMPLETED** | **Phase 4 Slice — Resource Dependency Invalidation Plan v1** | `ResourceDependencyGraphUVE` now produces a copied, generation-stamped, deterministic breadth-first reverse-dependent closure with explicit maximum-result truncation and invalid/stale/unknown root diagnostics. | The plan is an invalidation input only; it does not own asset lifetime, perform reloads, or select raw parser libraries. Reload execution, derived-data rebuild policy, and external raw conversion remain separate boundaries. |
| **COMPLETED** | **Scene Component Slice — Camera Validation v1** | `CameraComponentUVE` exposes a shared projection-safe validity predicate for finite open-range FOV and positive ordered clip planes; scene restore rejects invalid payloads through existing rollback semantics. | Validation is a rejection boundary only. Camera projection matrices, render targets, advanced camera variants, and authoring repair UX remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — Light Validation v1** | `LightComponentUVE` exposes a shared renderer-safe validity predicate for finite non-negative HDR color/intensity, known light type, positive range, and open spot angle; scene restore rejects invalid payloads and LightSystemUVE asserts the runtime precondition before extraction. | Validation does not select lights, alter lighting equations, or own GPU resources. Light ranking, shadows, attenuation, authoring repair UX, and advanced lighting remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — Collider Validation v1** | `ColliderComponentUVE` exposes a shared physics-safe validity predicate for finite positive half-extents, nonzero collision layer, bounded friction/restitution, and positive finite density; scene restore rejects invalid payloads through existing rollback semantics. | Validation does not add shape types, change collision filtering, wire density to mass, or repair malformed data. Runtime material extraction retains its established friction/restitution clamp for legacy hand-authored values; collision masks, shape breadth, rigid-body depth, and editor repair UX remain separate partial work. |
| **COMPLETED** | **Scene Component Slice — RigidBody Validation v1** | `RigidBodyComponentUVE` exposes a shared physics-safe validity predicate for finite non-negative mass, finite velocity, non-negative drag, and finite non-negative gravity scale; scene restore rejects invalid payloads and PhysicsSystemUVE asserts the integration precondition. | Zero mass remains the established immovable-body state. Validation does not add body types, mass/inertia solving, constraints, character controllers, or authoring repair UX. |
| **COMPLETED** | **Scene Component Slice — AudioSource Validation v1** | `AudioSourceComponentUVE` exposes a shared audio-safe validity predicate for finite non-negative volume, positive finite pitch, known attenuation curve, and spatial-only ordered distance bounds; scene restore rejects invalid payloads and AudioSourceSystemUVE asserts the synchronization precondition. | Empty asset paths remain the existing no-clip default. Validation remains pure and does not resolve clips, own voices, add mixer routing/effects, or choose a platform backend; optional clip identity resolution is owned by the separate AudioSystemUVE resolver seam. |
| **COMPLETED** | **Legacy Partial Closure — AudioSource Listener Runtime Binding v1** | Existing Increment 18 runtime proof binds validated AudioSourceComponentUVE entities to stable AudioSystemUVE voices, honors play-on-awake, synchronizes world positions and volume/pitch, computes listener-relative attenuation, updates the active camera as listener in EngineCoreUVE::LateUpdate(), and destroys voices when components/entities disappear. | AudioSourceSystemUVE owns only the entity-to-voice map; AudioSystemUVE owns listener/source state and attenuation. Effects, streaming, side-chain policy, concrete platform backend selection, and editor mixer authoring remain partial; clip identity resolution and mixer routing are closed bounded slices. |

<div align="center">

<h2><strong>PARTIAL — CORE & RUNTIME EXPANSION</strong></h2>

</div>

| Status | Area | Intended next capability | Completion proof / boundary |
|---|---|---|---|
| **PARTIAL** | Scene components and user-facing nodes | Introduce only supported components/nodes such as richer camera, mesh, light, collider, rigid-body, audio-source, animation, particle, and script attachments. | Camera, light, collider, rigid-body, and audio-source value validation plus invalid-scene rollback are complete; MeshComponent asset-reference coherence and Script/Particle authored-parameter validation plus rollback are now also complete; ScriptAssetLoaderUVE now reads bounded validated ScriptComponentUVE virtual paths through IFileSystemUVE and decodes copied ScriptGraphSchemaUVE data with deterministic failure diagnostics; validated ScriptComponent ownership still reconciles empty-path detach and graph-to-runtime attach without owning filesystem/asset state; ParticleRuntimeUVE now owns bounded CPU particle emission/update state with finite preflight, stable sequence IDs, semi-implicit integration, and lifetime culling; shared SkeletonDefinitionUVE/PoseBufferUVE/AnimationEvaluationContextUVE contracts now converge Motion Query and Control Rig validation/evaluation; runtime asset handles now preserve last-known-good values across failed asynchronous replacement reloads with copied diagnostics and successful replacement invalidation semantics; GPU simulation/backend/renderer integration, full animation playback, retargeting, editor command paths, and each remaining component still require real behavior and tests; no placeholder node catalog. |
| **PARTIAL** | Prefab maturity | Nested prefabs, override tracking, safe apply/revert, and deterministic conflict handling. | PrefabInstance source-reference validation and invalid-scene rollback are proven; bounded deterministic override records now persist with sorted property paths, explicit size/count caps, and backward-compatible empty defaults; caller-owned live-target Apply/Revert is failure-atomic and baseline-explicit; DetectPrefabOverrideConflictsUVE now performs a bounded read-only baseline preflight with copied expected/actual values and truncation evidence; baseline-checked source-prefab mutation through caller-owned targets is now proven with failure-atomic writes and post-success override clearing; automatic merge policy, conflict resolution UX, and asset-revision tracking remain partial. |
| **PARTIAL** | Asset-type importers | Extend the typed-envelope foundation with deliberately selected model, texture, audio, material, animation, and shader source-import contracts. | `.txt` bounded parsing, generation-aware dependency invalidation planning, and `.uvemodel`/`.uvetex`/`.uvemat`/`.uveshader` copy-and-register are complete; raw model/texture/material/animation source-parser licensing, source/derived-data transforms, and representative fixtures are still required; WAV PCM metadata, PNG IHDR metadata, JPEG frame metadata, bounded OBJ source statistics, glTF/GLB structural metadata, and MTL source statistics are now validated handoff slices, while OBJ/glTF index resolution, MTL texture/shader/material conversion, mesh/material/image/buffer conversion, JPEG/PNG pixel decoding, audio sample decoding, derived `.uveaudio`/`.uvetex` conversion, compression, deterministic full metadata import, and streaming preparation remain partial. |
| **PARTIAL** | Physics depth | Rigid bodies, expanded shapes, character controller, trigger areas, broad-phase acceleration, and measured simulation hardening. | Existing deterministic rigid-body integration, AABB collision resolution, raycast layer queries, symmetric collider layer/mask filtering, AreaComponentUVE scene persistence, bounded read-only area-to-collider overlap queries, a generation-safe copied enter/exit lifecycle tracker with truncation-safe baseline retention, EngineCore typed queued Entered/Exited event-bus delivery, CharacterControllerUVE bounded kinematic substep movement with contact correction/sliding and caller-selected grounded/support-normal classification, validated Box/Sphere/Capsule collider descriptors mapped into a shared conservative AABB cache, exact value-only Sphere↔Sphere, Capsule↔Sphere, Capsule↔Capsule, Sphere↔Box, rotated local-Y Capsule↔Box, Sphere↔rotated Box, Capsule↔rotated Box, and oriented Box↔Box SAT penetration integrated after broad-phase candidate filtering with deterministic axis/depth, strict touching rejection, coincident-center +X fallback, normalized world-rotation centerline construction, tight rotated-capsule and rotated-box AABB extents, box-local exact sphere/capsule evaluation with world-axis restoration, deterministic 15-axis OBB SAT with strict touching rejection, capsule-centerline closest-point and closest-segment-pair evaluation, segment-to-AABB closest-distance evaluation, and diagonal false-positive rejection, a stateless per-query median-split BVH broad phase that preserves legacy pair ordering, an optional bounded generation-safe distance/hinge positional solver integrated after collision resolution, bounded read-only conservative SphereCastSystemUVE, axis-aligned BoxCastUVE, and local-Y CapsuleCastUVE over expanded collider world-AABBs with finite shape validation, layer/ignore filters, deterministic closest hits, moving-center/normal/material facts, and focused native regressions, opt-in CharacterControllerUVE `maximumStepHeight` lift/clearance/drop traversal with failure-atomic rollback and copied `stepUpUsed` evidence, a bounded value-only hinge motor/limit policy evaluator with capped target-speed correction, deterministic lower/upper limit correction, disabled no-op behavior, and failure-atomic finite-input validation (five focused native tests), an opt-in CharacterControllerUVE dynamic-body push path that mutates only validated non-kinematic positive-mass target velocity along the mover-to-target normal with bounded speed and clamp/count diagnostics (three focused controller regressions), and the persisted RigidBody angular state/PhysicsSystem semi-implicit torque plus normalized local-quaternion advancement are proven; exact oriented/rotated-box/rotated-capsule casts, sphere/capsule mixed pairs beyond current narrow phases, exact step geometry, gyroscopic coupling, full angular hinge motor/limit application, backend-wide continuous response, full locomotion integration, backend-wide CCD, BVH reuse/update policy, persistent/editor-authored constraints, constraint islands, and measured backend hardening remain partial. Any backend replacement still requires a migration/test plan. |
| **PARTIAL** | Audio depth | Source/listener components, mixer groups, effects, streaming, and platform backend selection. | AudioSource runtime binding and listener-relative attenuation are proven; AudioSystemUVE now supports bounded named mixer groups with Master default, validated volume/pitch multipliers, explicit source routing, final device-param scaling, copied deterministic diagnostics, and an optional borrowed clip resolver with failure-atomic rejection and accepted-path copying; decoder-independent PCM WAV metadata parsing now provides copied validated format/rate/channel/data-size/duration facts; effects, sample decoding, streaming, side-chain policy, concrete platform backends, and editor mixer authoring remain partial. |
| **PARTIAL** | Input breadth | Gamepad, touch, gyro, mobile gesture, remapping, and platform-specific input policy. | GamepadInputSystemUVE now provides a bounded injectable four-slot snapshot with six finite/clamped dead-zone-normalized axes, sixteen buttons, copied current/previous frames, deterministic edge queries, disconnect clearing, and invalid-index safety; InputSystemUVE now provides optional borrowed gamepad injection with per-input-frame copied current/previous snapshot history, deterministic button action trigger/held/release edges, scaled/clamped axis binding composition, null-safe fallback, and failure-atomic bounded action remapping with finite/index validation; MobileInputSystemUVE now provides a bounded backend-neutral ten-slot touch snapshot with stable-identifier deltas, finite positions, clamped pressure, and finite gyroscope rotation-rate values; MobileGestureRecognizerUVE now provides deterministic frame-driven tap/swipe classification with stable touch tracking, finite thresholds, dominant-axis direction, duplicate-frame suppression, and bounded reports. Real window/backend polling, Android/iOS lifecycle and coordinate mapping, multi-touch chords, platform gesture frameworks, platform policy, and responsiveness measurements remain partial. |
| **PARTIAL** | Save-game depth | Version migration, compression, encryption, cloud hooks, screenshot thumbnails, and gameplay-domain state. | SaveGameSystemUVE now composes a bounded caller-owned SavePayloadMigrationRegistryUVE with exact source/target transform registration, deterministic shortest-path multi-hop planning with registration-order tie breaks, failure-atomic working-payload staging, copied applied-step diagnostics, and proven registered v0-to-v1 plus multi-hop load paths; current v1 passes through and unsupported paths fail before scene creation. Rewrite-back policy, compression, encryption, cloud hooks, screenshot thumbnails, gameplay-domain migration, and backend ownership remain partial. |
| **PARTIAL** | Platform abstraction | Desktop display modes, Android/iOS lifecycle, mobile safe areas, power management, and other native platform contracts. | Increment 203 adds `ValidateWindowDescUVE`, a bounded value-only desktop descriptor gate for positive dimensions, non-empty title, and a valid OpenGL major version; the unsigned minor-version field is inherently nonnegative. The contract is covered by four focused native regressions and does not own GLFW creation, monitor selection, backend choice, or mobile lifecycle. Android/iOS lifecycle, mobile safe areas, power management, and real platform display-mode enumeration/application remain partial. |
| **PARTIAL** | Networking | Reliable UDP channels, replication, prediction, and reconciliation. | Increment 204 adds a bounded value-only reliable sequence window with wrap-safe uint32 ordering, deterministic accepted/duplicate/too-old/invalid classification, 32-entry receive history, and cumulative plus forward selective ACK application; seven focused native regressions cover the contract. Sockets, payload serialization, retransmission timers, peer authentication, replication, prediction, reconciliation, and security/performance strategy remain partial. |

<div align="center">

<h2><strong>CORE RUNTIME ENTRY RULES</strong></h2>

</div>

| Rule | Required discipline |
|---|---|
| **Service ownership** | Cross-subsystem runtime dependencies flow through `EngineServicesUVE`, not new globals. |
| **Threading** | Every public system states thread affinity; OpenGL/device work remains on the owning main/context thread. |
| **Persistence** | `.uve*` and derived-data changes need explicit versioning, validation, and backward-compatibility policy. |
| **Performance** | Optimization claims require profiling evidence. Hot paths require measured allocation and iteration discipline. |
| **Recovery** | Filesystem, serialization, and import failure paths must retain last-known-good data where the contract permits. |
