<div align="center">

<h1><strong>UNIVEX ENGINE — AUTHORITATIVE CONTRACT INVENTORY</strong></h1>

<strong>Increment 202 machine-checked documentation boundary</strong>

</div>

> This inventory is a maintained index of authoritative contracts. It does not generate runtime code, transfer ownership, or replace the C++/C# source files named in the `Authority` column.

| ID | Authority | Anchor | Contract role | Allowed consumers | Ownership boundary |
|---|---|---|---|---|---|
| CORE-MQ-DATABASE | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_database_contract_uve.h | `struct MotionQueryDatabaseContractUVE final` | Shared Motion Query database/schema/settings validation contract. | Native Motion Query runtime, editor authoring, debugger, and integration tests. | C++ owns validation and copied contract state; managed code receives DTOs only. |
| EDITOR-CONSOLE | engine/editor/include/uve/editor/developer_console_uve.h | `class DeveloperConsoleUVE final` | Bounded native developer-console commands, CVARs, policy, diagnostics, and snapshots. | Native editor tooling and the value-only editor bridge. | Native console owns command/CVAR state; no arbitrary process, filesystem, network, ECS, or renderer access. |
| EDITOR-BRIDGE | engine/editor/include/uve/editor/editor_bridge_uve.h | `class EditorBridgeUVE final` | Revision-checked native-to-managed snapshot and named-command boundary. | Native editor services, stdio transport, and managed presentation. | C++ owns engine state; C# receives copied DTOs and never receives native pointers or renderer resources. |
| EDITOR-ASSET-BINDING | engine/editor/include/uve/editor/editor_bridge_uve.h | `struct EditorBridgeAssetBindingSnapshotUVE final` | Read-only mesh/material AssetGuid facts for the selected entity's Inspector presentation. | Native Inspector capture, stdio serialization, managed DTO parser, and Avalonia Inspector presentation. | GUID values are copied only; AssetManager, loading, resource ownership, ECS mutation, and import authority remain native. |
| EDITOR-CONTENT-IMPORT-ACTION | engine/editor/include/uve/editor/editor_bridge_uve.h | `struct EditorBridgeContentImportActionSnapshotUVE final` | Revision-synchronized, copied import/reimport capability facts plus explicit native `EditorBridgeRequestKindUVE::QueueContentBrowserImport` acknowledgement and job ID for the selected Content Browser entry. | Native Content Browser capture/dispatch, AssetImportQueueUVE, stdio serialization, managed DTO/command parser, Avalonia presentation, and bridge tests. | The request accepts bounded relative source/destination paths, reuses native importer classification, and returns a copied queue ID; no managed filesystem access, parser execution on the bridge thread, AssetDatabase pointer, or native resource ownership crosses the bridge. |
| ASSET-LOAD-DIAGNOSTICS | engine/asset/include/uve/asset/i_asset_manager_uve.h | `GetFailureReasonUVE` | Thread-safe copied failure reason for the most recent failed asset load/reload, with deterministic empty semantics for non-failed records. | Native asset consumers, editor diagnostics, and tests through `IAssetManagerUVE`/`AssetHandleUVE`. | AssetManager owns the record and mutex; consumers receive strings only and never acquire asset data or mutate load state. |
| ASSET-HANDLE-RELOAD | engine/asset/include/uve/asset/i_asset_manager_uve.h | `ReloadUVE` | Failure-atomic typed asset replacement: successful reloads publish the new value and clear diagnostics, while failed reloads preserve the loaded last-known-good value, keep handles ready, and expose a copied reload diagnostic; first-load failures remain failed with no value. | AssetManagerUVE, AssetHandleUVE, HotReloadUVE, typed asset consumers, load-completion events, and native manager tests. | AssetManager retains only its owned typed value and diagnostic; reload is asynchronous, callers must reacquire pointers after successful replacement, and the contract does not add decoder ownership, renderer fallback resources, or automatic retry policy. |
| AUDIO-SOURCE-RUNTIME-BINDING | engine/audio/include/uve/audio/audio_source_system_uve.h | `AudioSourceSystemUVE` | Existing Increment 18 runtime binding from validated AudioSourceComponentUVE entities to owned audio voices, including play-on-awake, world-position synchronization, listener-driven attenuation, repeated-sync stability, component/entity removal cleanup, and EngineCore active-camera listener updates. | EngineCoreUVE LateUpdate, AudioSystemUVE, IAudioDeviceUVE, scene entity ownership, audio runtime tests, and future mixer/backend adapters. | AudioSourceSystemUVE owns only the entity-to-voice map and borrows ECS/audio-system references per sync; AudioSystemUVE owns listener/source state and attenuation; clip identity resolution is now an optional AudioSystemUVE seam, while mixer effects, streaming, and concrete platform backend remain outside this contract. |
| AUDIO-SOURCE-LIVE-REFRESH-V1 | engine/audio/include/uve/audio/i_audio_source_system_uve.h | `SyncUVE` | Increment 282 adds failure-safe live authored-field refresh: audioAssetPath, looping, spatial, minDistance, maxDistance, and attenuationCurve edits create a replacement voice before the old voice is destroyed, preserve the previous Stopped/Playing state, and retain the old voice when replacement creation or playback restart fails; position, volume, and pitch continue updating in place. | AudioSourceSystemUVE, AudioSystemUVE, optional IAudioClipResolverUVE, NullAudioDeviceUVE, scene transforms/components, native audio tests, and future concrete backend adapters. | SyncUVE owns the per-entity cached descriptor and replacement decision; AudioSystemUVE owns voice creation, playback, and destruction; failure paths are failure-atomic for the live old voice, while mixer authoring, streaming, and platform backend selection remain outside this contract. |
| AUDIO-MIXER-GROUPS | engine/audio/include/uve/audio/audio_mixer_group_uve.h | `AudioMixerGroupUVE` | Bounded named mixer routing above IAudioDeviceUVE with a non-removable Master group, capped group count/name size, validated volume/pitch multipliers, safe source attach/detach, final gain/pitch scaling, and deterministic copied name-ordered diagnostics with truncation evidence. | AudioSystemUVE source creation/routing/update, audio tests, EngineCore/audio consumers, and future editor mixer authoring. | AudioSystemUVE owns source-to-group routing and final multiplier composition; AudioMixerGroupUVE owns copied group state only; the device remains a final-parameter sink, and effects, streaming, side-chain policy, and concrete backend ownership remain outside this increment; clip identity resolution is owned by the separate optional resolver seam. |
| AUDIO-CLIP-RESOLUTION | engine/audio/include/uve/audio/i_audio_clip_resolver_uve.h | `IAudioClipResolverUVE` | Optional borrowed clip identity resolver invoked before AudioSystemUVE voice creation, returning copied accepted/rejected status, canonical path, and diagnostic; accepted paths reach both source state and the backend voice descriptor, while rejection is failure-atomic and creates no voice. | AudioSystemUVE source creation, AssetDatabase/VFS-backed future adapters, audio tests, and future decoded clip handles. | The resolver owns path/database policy only; it does not decode PCM, own streaming buffers or voices, select a platform backend, mutate ECS, or silently repair rejected paths. |
| SAVE-PAYLOAD-MIGRATION | engine/save/include/uve/save/save_payload_migration_uve.h | `MigrateSavePayloadUVE` | Increment 323 makes SaveGameSystemUVE read only a bounded metadata schema discriminator before selecting migration, then migrate the actual framed two-section payload before final metadata decoding or scene creation; `GetSaveMetadataUVE` now applies the same migration to the real decompressed payload instead of a one-byte probe, while current-version pass-through, exact source/target registration, 32-entry cap, deterministic shortest-path multi-hop planning, copied diagnostics, and failure-atomic staged payload composition remain intact. | SaveGameSystemUVE LoadUVE/GetSaveMetadataUVE, checkpoint/save tests including a legacy field that is undecodable until migration, explicit registered v0-to-v1 transform integration, multi-hop migration tests, editor save diagnostics, and future migration registrations. | The seam owns schema transform dispatch and copied payload values only; transform callbacks do not receive filesystem/scene ownership, failed or empty intermediate transforms never mutate the original payload, and compression, encryption, cloud sync, screenshot thumbnails, gameplay-domain migration, rewrite policy, and backend ownership remain outside this increment. |
| RENDER-ASSET-DIAGNOSTICS | engine/render/include/uve/render/i_renderer_3d_uve.h | `struct Renderer3DFrameDiagnosticsUVE final` | Copied per-frame asset-resolution counts for invalid references, pending/failed mesh-material loads, and renderer texture fallbacks. | Renderer tests, engine diagnostics, and future editor presentation through the existing Renderer3D snapshot. | Renderer owns GPU fallback resources; the DTO contains counts only and cannot mutate ECS, AssetManager state, or GPU handles. |
| RENDER-PARTICLE-BRIDGE | engine/render/include/uve/render/particle_render_bridge_uve.h | `ParticleRenderBridgeUVE` | Bounded copied extraction of enabled CPU particle state into deterministic transparent render items with a per-call item cap and truncation fact, plus RenderQueueUVE append/sort integration. | RenderQueueUVE, Renderer3DUVE frame extraction, future particle renderer/backend adapters, renderer diagnostics, native tests, and OpenGL integration. | The bridge and queue own frame-local value DTOs only; they do not mutate ParticleRuntimeUVE, resolve assets, register GPU resources, perform camera culling, or own OpenGL state. |
| RENDERER3D-PARTICLE-FRAME | engine/render/include/uve/render/i_renderer_3d_uve.h | `RenderFrameWithParticleRuntimeUVE` | Explicit borrowed per-frame ParticleRuntimeUVE input seam that copies bounded particle items into Renderer3D's RenderQueueUVE, records a bounded GPU-independent particle command description, and publishes copied extraction/truncation diagnostics while preserving the legacy frame API. | Renderer3DUVE, EngineCore frame ownership, editor/runtime integration, native renderer tests, and future particle draw submission. | The runtime reference is borrowed for one call only; Renderer3DUVE retains no runtime pointer after return, and this seam performs no particle GPU allocation, camera culling, asset resolution, or OpenGL draw submission. |
| RENDER-PARTICLE-DRAW-COMMAND | engine/render/include/uve/render/particle_draw_command_uve.h | `ParticleDrawRecorderUVE` | Bounded copy-only conversion from sorted RenderQueueUVE particle items into deterministic draw-command DTOs with source count, hard cap, and truncation evidence. | Renderer3DUVE diagnostics, particle pipeline/shader/backend submission, native renderer tests, and OpenGL integration. | The recorder does not mutate the queue, allocate buffers, bind pipelines, resolve assets, issue draw calls, or own GPU/OpenGL state. |
| RENDER-PARTICLE-GPU-SUBMISSION | engine/render/include/uve/render/i_renderer_3d_uve.h | `Renderer3DFrameDiagnosticsUVE` | Bounded OpenGL-capable submission of recorded particle DTOs through a renderer-owned persistent vertex buffer, CPU-expanded world-space quads, lifetime-derived alpha, source-alpha blending, and copied per-frame submission diagnostics. | Renderer3DUVE MainColor pass, IRenderDeviceUVE buffer/pipeline ownership, built-in shader parity tests, native command recording, and real OpenGL/Xvfb smoke. | The v1 path is non-instanced and world-axis aligned, caps GPU submission at 16,384 commands, performs no camera-facing billboarding, texture/asset resolution, GPU simulation, compute dispatch, occlusion culling, or backend-specific resource ownership outside the RHI. |
| ASSET-SOURCE-CLASSIFICATION | engine/asset/include/uve/asset/i_asset_importer_uve.h | `struct AssetImportSourceClassificationUVE final` | Deterministic, value-only source extension classification with explicit raw model/texture/material/audio parser requirements, normalized extension handoff, unsupported-format diagnostics, and editor-facing raw-audio labeling. | Asset import UI, import queue policy, EngineCore service consumers, editor bridge snapshots, and native tests. | Classification performs no conversion, decoder registration, metadata parsing, or filesystem read; importer registration remains mutex-owned and parser authority stays explicit. |
| ASSET-PNG-METADATA | engine/asset/include/uve/asset/png_metadata_uve.h | `ParsePngMetadataUVE` | Bounded decoder-independent PNG signature/IHDR metadata parsing from caller-owned bytes, returning copied dimensions, bit depth, color type, alpha fact, and interlace method after strict supported-combination and method validation. | Texture importer metadata handoff, TextureAssetUVE preparation, renderer diagnostics, native PNG tests, and future `.uvetex` conversion/compression adapters. | The parser does not decode pixel chunks, verify CRCs, read files, allocate GPU resources, choose a compression library, or own renderer/streaming state. |
| ASSET-OBJ-METADATA | engine/asset/include/uve/asset/obj_metadata_uve.h | `ParseObjMetadataUVE` | Bounded decoder-independent OBJ source statistics from caller-owned text, with validated position/UV/normal declarations, face-token arity, polygon-to-triangle capacity, group/material library/use counts, and deterministic ignored-directive counts. | Raw model importer diagnostics, mesh source preflight, asset UI, native OBJ tests, and future mesh/material conversion adapters. | The parser returns counts only; it does not resolve indices, read external material libraries, allocate `MeshAssetUVE` storage, perform triangulation, convert geometry, or own renderer state. |
| ASSET-GLTF-METADATA | engine/asset/include/uve/asset/gltf_metadata_uve.h | `ParseGltfMetadataUVE` | Bounded decoder-independent glTF 2.0 JSON and GLB structural validation with copied node/mesh/material/image/buffer counts, strict asset-version checks, GLB header/JSON-chunk checks, and binary-tail evidence. | Raw model importer diagnostics, mesh source preflight, asset UI, native glTF/GLB tests, and future mesh/image/material conversion adapters. | The parser does not resolve buffers, decode images, convert meshes, load external resources, verify all binary semantics, or own renderer/GPU state. |
| ASSET-JPEG-METADATA | engine/asset/include/uve/asset/jpeg_metadata_uve.h | `ParseJpegMetadataUVE` | Bounded decoder-independent JPEG SOI/segment/SOF frame-header parsing from caller-owned bytes, returning copied dimensions, precision, component count, and progressive-frame evidence after strict segment and frame validation. | Texture importer metadata handoff, asset diagnostics, native JPEG tests, and future `.uvetex` conversion/compression adapters. | The parser does not decode entropy data, read files, allocate pixels or GPU resources, verify all JPEG coding semantics, or select a texture/renderer backend. |
| ANIMATION-CLIP-METADATA | engine/asset/include/uve/asset/animation_clip_metadata_uve.h | `ValidateAnimationClipMetadataUVE` | Bounded copied animation clip metadata validation for finite positive duration, capped sample rate and track count, and non-zero skeleton identity, providing a decoder/importer handoff without playback ownership. | Animation asset import preflight, skeleton/clip compatibility checks, native animation metadata tests, and future player/tree/retargeting adapters. | The validator does not own keyframes, skeleton storage, playback time, retargeting, filesystem state, GPU skinning, or renderer resources. |
| ASSET-MTL-METADATA | engine/asset/include/uve/asset/mtl_metadata_uve.h | `ParseMtlMetadataUVE` | Bounded decoder-independent Wavefront MTL source statistics from caller-owned text, with validated material declarations, texture-map paths, scalar/vector property arity, and deterministic ignored-directive counts. | Material importer diagnostics, source preflight, asset UI, native MTL tests, and future texture/shader/material conversion adapters. | The parser returns counts only; it does not load texture paths, resolve assets, compile shaders, write `MaterialAssetUVE`, or own renderer state. |
| AUDIO-WAV-METADATA | engine/audio/include/uve/audio/wav_metadata_uve.h | `ParseWavMetadataUVE` | Bounded decoder-independent PCM WAV metadata parsing from caller-owned bytes, requiring RIFF/WAVE structure, coherent PCM `fmt ` facts, aligned `data` payload, safe chunk padding/bounds, and finite positive duration; returns copied channel/rate/format/size metadata without samples or backend ownership. | Audio importer metadata handoff, AudioSystemUVE clip preparation, asset diagnostics, native WAV tests, and future `.uveaudio` conversion/streaming adapters. | The parser supports metadata only; it does not decode samples, allocate voices, register assets, select a library, perform filesystem I/O, or own streaming/backend state. |
| ASSET-TEXT-PARSER | engine/asset/include/uve/asset/i_asset_importer_uve.h | `struct TextImportSettingsUVE final` | Bounded text-source parser with NUL rejection, deterministic line-ending normalization, optional trailing newline, and stable cache-version fingerprint. | Asset import pipeline, importer tests, and EngineCore service consumers. | Parser rejects NUL bytes and normalizes CR/CRLF only; it does not infer encodings, rewrite Unicode, or become a general-purpose text runtime. |
| ASSET-RESOURCE-DEPENDENCY-GRAPH | engine/asset/include/uve/asset/resource_dependency_graph_uve.h | `struct ResourceDependencyInvalidationPlanUVE final` | Generation-aware, copied, deterministic breadth-first reverse-dependent closure for bounded asset invalidation planning. | Asset reload/invalidation consumers, Motion Query ingestion, editor diagnostics, and native tests. | The graph owns registration and dependency topology; plans copy handles and graph generation only, exclude the changed root, flag truncation, and never transfer asset/resource lifetime ownership. |
| SCENE-CAMERA-VALIDATION | engine/scene/include/uve/scene/components/camera_component_uve.h | `IsCameraComponentValidUVE` | Projection-safe camera component validity contract for finite open-range FOV and positive ordered near/far clip planes. | Scene serializer restore, CameraSystemUVE consumers, editor camera creation, and native tests. | Validation rejects malformed persisted values before restore; it does not own camera projection matrices, render targets, or silently repair invalid authoring data. |
| SCENE-LIGHT-VALIDATION | engine/scene/include/uve/scene/components/light_component_uve.h | `IsLightComponentValidUVE` | Renderer-safe light component validity contract for finite non-negative HDR color/intensity, known light type, positive range, and open spot-cone angle. | Scene serializer restore, LightSystemUVE extraction, editor light creation, and native tests. | Validation rejects malformed persisted/runtime values; it does not own light selection, lighting equations, GPU resources, or silently repair authoring data. |
| SCENE-COLLIDER-VALIDATION | engine/scene/include/uve/scene/components/collider_component_uve.h | `IsColliderComponentValidUVE` | Physics-safe collider validity contract for legacy positive box half-extents, supported Box/Sphere/Capsule shape descriptors, nonzero collision layer, bounded friction/restitution, and positive finite density. | Scene serializer restore, explicit collider validation consumers, conservative AABB cache generation, physics material tests, and native tests. | Collision masks remain opaque bit fields; validation does not claim exact sphere/capsule narrow phase, alter collision filtering, wire density to mass, or silently repair malformed authoring data. Runtime material extraction retains its established friction/restitution clamp for legacy hand-authored values. |
| SCENE-AREA-VALIDATION | engine/scene/include/uve/scene/components/area_component_uve.h | `IsAreaComponentValidUVE` | Bounded non-physical area volume contract for finite positive axis-aligned-box half-extents and nonzero collision layer with opaque mask bits. | Scene serializer save/restore, AreaOverlapSystemUVE query, editor area authoring, and native tests. | Area validation does not add shape types, enter/exit event ownership, rigid-body resolution, character behavior, or silently repair malformed authoring data. |
| PHYSICS-COLLISION-BVH | engine/physics/include/uve/physics/i_collision_system_uve.h | `ICollisionSystemUVE` | Stateless per-query median-split AABB broad phase over the shared collider cache, with bounded leaf/traversal policy, conservative candidate pruning, deterministic cache-index pair ordering, and legacy fallback for malformed geometry or traversal overflow. | CollisionSystemUVE detection, PhysicsSystemUVE sequential resolution, shared collider cache, collision tests, dynamic-BVH tests, and future measured broad-phase adapters. | CollisionSystemUVE owns no persistent tree or ECS state; its per-query path consumes copied cache data, does not change the public pair DTO, and preserves the existing AABB narrow phase, symmetric filtering, and pair orientation. Exact sphere/capsule narrow phases and backend replacement remain outside this seam. |
| PHYSICS-COLLISION-BVH-DYNAMIC | engine/physics/include/uve/physics/detail/collider_world_aabb_cache_uve.h | `class DynamicAabbBvhUVE final` | Bounded copied median-split BVH topology with refit-only per-proxy AABB updates keyed by full generational EntityUVE identity, stable cache-index candidate ordering, leaf/ancestor refit, invalid-AABB rejection, and stale-generation diagnostics. | Physics broad-phase adapters, collision tests, dynamic-BVH movement tests, and future persistent frame-cache integration. | The helper owns copied collider proxies and tree nodes only; updates never mutate ECS, rebuild topology, reorder cache indices, or accept stale entity generations. It remains an internal detail seam and does not claim persistent world lifetime, exact narrow phases, or thread-safe concurrent mutation. |
| PHYSICS-CONSTRAINTS | engine/physics/include/uve/physics/physics_constraint_system_uve.h | `class PhysicsConstraintSystemUVE final` | Bounded generation-safe 256-slot distance/hinge constraint registry with copied descriptors, deterministic slot-index positional solving, mass-weighted dynamic-body correction, stale-entity diagnostics, eight-iteration cap, optional post-collision PhysicsSystemUVE integration, and a separate bounded hinge motor/limit policy evaluator. | PhysicsSystemUVE optional collaborator, runtime gameplay physics, constraint tests, hinge motor/limit policy tests, and future scene/editor adapters. | The solver owns copied constraint descriptors and slot generations only; the separate policy evaluator returns copied angular-speed/angle corrections and never mutates ECS or constraint state. Full angular constraint application, persistence, networking, backend replacement, and dynamic constraint island policy remain outside this contract. |
| PHYSICS-ANGULAR-DYNAMICS | engine/physics/include/uve/physics/angular_dynamics_uve.h | `ComputeBoxInverseInertiaUVE` | Bounded value-only diagonal box inverse-inertia computation plus finite torque-timestep and angular-impulse integration over copied angular velocity; zero mass yields a zero inverse tensor and unsafe/overflowed inputs fail closed. | Physics tests, future RigidBodyComponent/PhysicsSystem integration, constraint adapters, gameplay force/impulse nodes, and measured angular-backend work. | The helper owns no ECS/body persistence, quaternion/orientation integration, gyroscopic coupling, torque accumulation, dynamic push, networking, or backend state; callers own all copied inputs and outputs. Hinge motor/limit policy is documented by the separate evaluator below. |
| PHYSICS-HINGE-MOTOR-LIMIT | engine/physics/include/uve/physics/hinge_motor_limit_uve.h | `EvaluateHingeMotorLimitUVE` | Bounded value-only hinge motor/limit policy over copied scalar state: finite target-speed correction capped by maximum torque × effective inverse inertia × timestep, deterministic no-overshoot behavior, lower/upper angle corrections, disabled-policy no-op semantics, and failure-atomic invalid-input rejection. | Physics constraint adapters, RigidBody/PhysicsSystem integration work, hinge motor/limit tests, gameplay constraint consumers, and future editor-authored constraint workflows. | The evaluator owns no body, transform, constraint, persistence, or backend state; callers apply returned angular-speed and angle corrections. Full hinge orientation extraction, angular constraint solve, motor torque accumulation, limit impulse application, persistence, networking, and backend replacement remain partial. |
| PHYSICS-COLLISION-FILTERING | engine/physics/include/uve/physics/i_collision_system_uve.h | `ICollisionSystemUVE` | Deterministic collider-vs-collider filtering that requires both colliders' masks to accept the other collider's layer before the existing world-AABB penetration result is emitted. | CollisionSystemUVE pair detection, PhysicsSystemUVE resolution input, scene collider persistence, raycast layer queries, and native tests. | Filtering is a symmetric broad-phase gate only; it does not add shape types, triggers/areas, character controllers, continuous collision detection, or mutate components during detection. |
| PHYSICS-COLLIDER-SHAPE-AABB | engine/physics/include/uve/physics/detail/collider_world_aabb_cache_uve.h | `BuildColliderWorldAabbCacheUVE` | Shared conservative world-AABB cache maps legacy Box, Sphere, and Capsule collider descriptors to deterministic broad-phase bounds consumed by collision, raycast, and conservative sphere-cast queries. | CollisionSystemUVE, RaycastSystemUVE, ShapeCastSystemUVE, CharacterControllerUVE, collider shape tests, and future exact narrow-phase adapters. | The cache is an internal implementation seam; exact sphere/capsule contacts, oriented cast bounds, box/capsule casts, CCD/TOI, BVH ownership, and backend replacement remain outside it. |
| PHYSICS-SHAPE-CAST-SPHERE | engine/physics/include/uve/physics/shape_cast_system_uve.h | `SphereCastUVE` | Bounded read-only conservative sphere, axis-aligned box, and local-Y capsule casts over copied collider world-AABBs, expanding each target by finite non-negative sphere radius, box half-extents, or capsule radius/height bounds, applying layer/ignore filters, selecting deterministic closest hits, returning moving-center/normal/material facts, and rejecting invalid query inputs. | ColliderWorldAabbCacheUVE, EntityManagerUVE read-only queries, physics shape-cast tests, gameplay query adapters, and future exact oriented/rotated-box/capsule cast backends. | The queries are conservative and axis-aligned; they do not claim exact oriented sphere/box/capsule casts, rotated capsule geometry, dynamic-body CCD, event publication, ECS mutation, or backend ownership. |
| PHYSICS-AABB-TOI | engine/math/include/uve/math/aabb_uve.h | `SweepAabbUVE` | Pure deterministic swept-AABB query returning normalized first-impact time and mover-to-target normal for non-overlapping AABBs over a bounded displacement interval. | CharacterControllerUVE TOI-assisted movement, ShapeCastSystemUVE conservative query adapters, AABB math tests, and future bounded kinematic sweep adapters. | The helper is conservative for expanded shape AABBs; it does not own exact sphere/capsule or oriented narrow phases, dynamic-body CCD, TOI event publication, BVH acceleration, or backend policy. |
| PHYSICS-CHARACTER-CONTROLLER | engine/physics/include/uve/physics/character_controller_uve.h | `class CharacterControllerUVE final` | Bounded main-thread kinematic movement for a caller-owned entity using capped finite substeps, existing symmetric collider layer/mask filtering, minimum-translation contact correction, tangential displacement projection, copied applied/remaining displacement, blocked/contact truncation diagnostics, caller-selected grounded/support-normal classification with finite maximum-slope clamping, an opt-in bounded `maximumStepHeight` lift/clearance/drop attempt with copied `stepUpUsed` evidence, and an opt-in per-contact dynamic-body push that caps target normal speed and reports pushed-body/clamp facts; Increment 333 rejects finite desired displacements whose squared length overflows before any ECS mutation; Increment 338 rejects backend collision pairs whose separation axis is non-finite, non-unit, or produces a non-finite correction before controller or dynamic-target mutation, with pair-atomic malformed-axis regression coverage. | SceneGraphUVE local-transform writes, CollisionSystemUVE pair detection, kinematic Collider/RigidBody components, validated dynamic-target velocity mutation, native controller tests, and future gameplay movement adapters. | The controller owns no ECS components or locomotion state; default MoveUVE and zero-height MoveWithToIUVE behavior remain backward-compatible. Positive `maximumStepHeight` uses failure-atomic swept-AABB clearance and bounded drop search; push touches only validated non-kinematic positive-mass target rigid bodies, preserves tangential velocity, caps normal speed, and leaves invalid/kinematic/static targets unchanged. Overflowed finite displacement is rejected as InvalidInput without transform publication; exact non-box step geometry, backend-wide continuous response, animation coupling, networking, and platform/input ownership remain outside the seam. |
| PHYSICS-AREA-OVERLAP-QUERY | engine/physics/include/uve/physics/area_overlap_system_uve.h | `AreaOverlapSystemUVE` | Bounded read-only area-to-collider world-AABB overlap query with deterministic copied area/other entity pairs, penetration depth, symmetric layer/mask acceptance, inspected counts, and hard-cap truncation evidence. | AreaOverlapSystemUVE consumers, scene area persistence, gameplay query adapters, native tests, and future trigger event workflows. | The query does not enter rigid-body collision resolution, mutate ECS state, publish global events, implement enter/exit lifecycle, add character controllers, or own a physics backend. |
| PHYSICS-AREA-OVERLAP-LIFECYCLE | engine/physics/include/uve/physics/area_overlap_lifecycle_tracker_uve.h | `AreaOverlapLifecycleTrackerUVE` | Deterministic copied Entered/Exited transition reports between complete area-overlap snapshots, using full generational entity handles, explicit reset, hard transition caps, and no inferred exits from truncated input. | Gameplay trigger adapters, future event-bus integration, area lifecycle tests, and editor diagnostics. | The tracker owns only copied pair baselines and reports; it does not publish global events, mutate ECS/physics state, resolve bodies, implement characters, or own query/backend lifetimes. |
| PHYSICS-AREA-OVERLAP-EVENT-BUS | engine/physics/include/uve/physics/area_overlap_events_uve.h | `AreaOverlapEnteredEventUVE` | Typed queued event DTOs for deterministic area lifecycle transitions, carrying copied validated area/other handles and penetration evidence through the existing `IEventSystemUVE` queue/dispatch contract. | EngineCoreUVE LateUpdate orchestration, gameplay trigger subscribers, native event/EngineCore tests, and future managed/editor adapters. | EngineCore owns query/tracker orchestration and queues value copies; `IEventSystemUVE` owns delivery; events do not mutate ECS/physics state, resolve bodies, implement characters, or expose backend ownership. |
| INPUT-GAMEPAD-SNAPSHOT | engine/input/include/uve/input/i_gamepad_input_system_uve.h | `IGamepadInputSystemUVE` | Bounded injectable gamepad state contract with four device slots, six normalized axes, sixteen buttons, copied current/previous frame snapshots, deterministic button edge queries, finite/clamped input, symmetric dead-zone normalization, disconnect clearing, and monotonic frame numbers. | Platform input adapters, InputSystemUVE action bindings, runtime gamepad consumers, native input tests, and future action-binding integration. | GamepadInputSystemUVE owns only copied live/current/previous value state; injection is thread-safe, UpdateUVE/query methods are main-thread-only, no window/backend polling, remapping, touch/gyro, or ECS/editor-camera ownership crosses this boundary. |
| INPUT-MOBILE-SNAPSHOT | engine/input/include/uve/input/i_mobile_input_system_uve.h | `IMobileInputSystemUVE` | Bounded backend-neutral mobile snapshot contract with ten touch slots, stable nonzero active identifiers, finite positions, clamped pressure, same-identifier per-frame touch deltas, finite gyroscope rotation-rate values, copied current/previous snapshots, invalid-slot safety, and monotonic frame numbers. Increment 337 makes `MobileInputSystemUVE::SetTouchStateUVE` reject active identifier zero before publishing or replacing copied state, with fresh and existing-contact regression coverage. | Future Android/iOS adapters, gameplay/mobile input consumers, native input tests, and future gesture interpretation. | MobileInputSystemUVE owns only copied live/current/previous values; platform adapters own coordinate-system mapping, lifecycle, polling, and device permissions, while no Android/iOS backend, gesture recognizer, ECS, or editor-camera ownership crosses this boundary. |
| INPUT-GESTURE-RECOGNIZER | engine/input/include/uve/input/mobile_gesture_recognizer_uve.h | `class MobileGestureRecognizerUVE final` | Bounded frame-driven tap/swipe classification over copied mobile touch snapshots, with finite positive thresholds, stable touch-identifier tracking, caller-supplied finite frame delta, deterministic dominant-axis direction, duplicate-frame suppression, reset, ten-event reports, and truncation evidence. | MobileInputSystemUVE/platform adapters, gameplay/mobile input consumers, native gesture tests, and future managed/mobile presentation. | The recognizer owns only capped per-touch tracking and copied gesture DTOs; it does not poll devices, map platform coordinates, own Android/iOS lifecycle or permissions, recognize multi-touch chords, mutate ECS, or replace a platform gesture framework. |
| INPUT-SENTINEL-SAFETY-V1 | engine/input/include/uve/input/i_input_system_uve.h | `SetKeyStateUVE` | Increment 287 hardens `InputSystemUVE` against the non-physical `KeyCodeUVE::Count` and `MouseButtonUVE::Count` sentinels: injection is inert, raw key/mouse edge queries fail closed, sentinel bindings evaluate as zero, and remapping rejects them without changing the existing action. | Native InputSystemUVETest sentinel injection/query, inert binding evaluation, and atomic remap rejection regressions; full native/managed/editor validation. | The contract owns only enum-boundary safety in the existing copied input/action snapshots; it does not add keys, buttons, chord/sequence semantics, platform polling, mobile lifecycle, or Visual Scripting nodes. |
| INPUT-ACTION-BINDINGS | engine/input/include/uve/input/input_system_uve.h | `class InputSystemUVE final` | Optional borrowed gamepad action-layer bindings for button and axis actions, including four-slot/button/axis validation, per-InputSystem-frame copied current/previous gamepad history, deterministic trigger/held/release edges, finite scaled/clamped axis composition, null-safe legacy keyboard/mouse behavior, and atomic replacement of bounded positive/negative binding vectors with finite/index validation. | InputActionUVE/InputBindingUVE registration and remapping, GamepadInputSystemUVE snapshot adapters, gameplay and Visual Scripting input consumers, native input tests, and future platform remapping adapters. | InputSystemUVE owns only copied frame snapshots, action evaluation, and mapping replacement; remap failure leaves the existing action unchanged, the injected gamepad service remains caller-owned and must be updated before the consuming InputSystemUVE frame, while touch/gyro/gesture, backend polling, and platform policy remain outside this contract. |
| SCENE-RIGIDBODY-VALIDATION | engine/scene/include/uve/scene/components/rigid_body_component_uve.h | `IsRigidBodyComponentValidUVE` | Physics-safe rigid-body validity contract for finite non-negative mass, finite linear/angular velocity and torque, non-negative diagonal inverse inertia, non-negative drag, and finite non-negative gravity scale. | Scene serializer restore, PhysicsSystemUVE integration, editor rigid-body creation, angular-state persistence, and native tests. | Zero mass remains a valid immovable-body state; angular state now round-trips with zero defaults for older payloads, while validation does not add body types, gyroscopic coupling, hinge motors/limits, dynamic push, networking, or silently repair malformed authoring data. |
| SCENE-AUDIO-SOURCE-VALIDATION | engine/scene/include/uve/scene/components/audio_source_component_uve.h | `IsAudioSourceComponentValidUVE` | Audio-source validity contract for finite non-negative volume, positive finite pitch, known attenuation curve, spatial-only ordered distance bounds, and an optional 1024-byte NUL-free authored asset path. | Scene serializer restore, AudioSourceSystemUVE synchronization, audio tests, and native tests including bounded-path regressions. | Empty asset paths remain valid as the existing no-clip default; validation does not resolve assets, own voices, add mixer routing, impose filesystem traversal policy, or silently repair malformed authoring data. |
| SCENE-MESH-VALIDATION | engine/scene/include/uve/scene/components/mesh_component_uve.h | `IsMeshComponentValidUVE` | Mesh/material asset-reference coherence contract that preserves the both-invalid unassigned state and rejects half-assigned GUID pairs before persistence/runtime extraction. | Scene serializer restore, MeshRendererUVE extraction, editor mesh binding, and native tests. | Validation does not resolve assets, require files to exist, own GPU resources, or change the renderer's invalid/pending/failed accounting and skip behavior. |
| SCENE-SCRIPT-VALIDATION | engine/scene/include/uve/scene/components/script_component_uve.h | `IsScriptComponentValidUVE` | Bounded project-relative script virtual-path contract with an explicit empty no-script state, canonical forward-slash segments, traversal/absolute/URI/Windows-drive rejection, NUL rejection, and maximum input size. Increment 339 rejects colon-containing forms before path publication, with Windows-drive, URI, and colon-containing relative-path regressions. | Scene serializer restore, ScriptComponentRuntimeOwnershipUVE, editor script attachment, and native tests. | Validation does not resolve or execute scripts, access the filesystem, own VM state, or silently normalize malformed authoring data. |
| SCENE-PARTICLE-VALIDATION | engine/scene/include/uve/scene/components/particle_emitter_component_uve.h | `IsParticleEmitterComponentValidUVE` | Bounded authored particle-emitter budget contract requiring a positive count no greater than the explicit one-million safety ceiling. | Scene serializer restore, ParticleRuntimeUVE attach validation, editor emitter authoring, and native tests. | Validation does not allocate particles, select a GPU/CPU backend, own VFX resources, or claim a particle runtime exists. |
| SCENE-PARTICLE-RUNTIME-OWNERSHIP | engine/scene/include/uve/scene/particle_runtime_uve.h | `ParticleRuntimeUVE` | Bounded per-entity emitter ownership with aggregate budget enforcement, enabled/live-count lifecycle state, deterministic generational snapshots, finite emission validation, stable particle sequence IDs, bounded semi-implicit CPU integration, and lifetime culling. | Scene/entity runtime ownership, ParticleRenderBridgeUVE, future GPU simulation adapters, renderer integration, editor diagnostics, and native tests. | Runtime owns only explicitly emitted CPU particle state and copied budget/lifecycle facts; it does not own GPU resources, renderer registration, assets, entity lifetime, or backend selection. |
| SCENE-PREFAB-INSTANCE-VALIDATION | engine/scene/include/uve/scene/components/prefab_instance_component_uve.h | `IsPrefabInstanceComponentValidUVE` | Prefab-instance source-reference contract requiring a non-invalid source GUID while preserving nested instances as serialized data without recursive plain-load behavior. | Scene serializer restore, PrefabSystemUVE instantiation, nested-prefab tests, and native tests. | Validation does not resolve the asset database, instantiate entities, apply overrides, or transfer prefab ownership to the serializer. |
| SCENE-PREFAB-OVERRIDE-PERSISTENCE | engine/scene/include/uve/scene/components/prefab_instance_component_uve.h | `PrefabPropertyOverrideUVE` | Deterministic bounded override persistence using strictly increasing property paths, non-empty serialized values, explicit path/value byte caps, and a maximum override count. | Scene serializer capture/restore, prefab instance round-trip tests, Apply/Revert target resolution, conflict handling, and native tests. | Persistence does not resolve component types, mutate source prefabs, compute conflicts, or own live target lifetime. |
| SCENE-PREFAB-OVERRIDE-APPLY-REVERT | engine/scene/include/uve/scene/components/prefab_instance_component_uve.h | `ApplyPrefabOverridesUVE` | Increment 322 makes application two-phase: all existing target values are read and staged before the first override write, so read/staging allocation failures remain read-only; write failures still reverse-rollback every prior write. Baseline-driven revert clears records only after successful restoration, and baseline-checked `CommitPrefabOverridesToSourceUVE` baking clears instance records only after source writes succeed. | Prefab instance authoring, editor property mutation, caller-owned source-prefab adapters, runtime target adapters, native tests including later-staging-read/zero-write regression, and future conflict workflows. | The target owns property resolution and mutation; commit first performs read-only baseline conflict preflight, then reuses the staged-read/failure-atomic write path. The helper never resolves assets, owns ECS entities, invents baseline data, or auto-merges conflicts. |
| SCENE-PREFAB-OVERRIDE-CONFLICT | engine/scene/include/uve/scene/components/prefab_instance_component_uve.h | `DetectPrefabOverrideConflictsUVE` | Bounded read-only comparison of a caller-supplied source-prefab baseline against current live target values, returning deterministic inspected count, copied expected/actual values, conflict status, and hard-cap truncation evidence. | Prefab authoring/editor conflict UI, ApplyPrefabOverridesUVE preflight, runtime target adapters, native tests, and future source-prefab workflows. | Detection performs reads only and never writes the live target or source prefab; it does not resolve assets, auto-merge values, choose conflict winners, or expose a source-prefab mutation API. |
| SCRIPT-BUILTIN-NODE-CATALOG | engine/scripting/include/uve/scripting/script_builtin_nodes_uve.h | `RegisterBuiltInScriptNodesUVE` | Production palette contract for 161 built-in graph descriptors: eleven Flow nodes, four Conversion nodes, eighteen Float math nodes, ten Vector2 math nodes, eleven Vector3 math nodes, nine Rotation math nodes, eleven Transform nodes, ten Boolean logic nodes, two Entity Query nodes, two Engine nodes, twenty-one Variable nodes including twelve typed Array/Map/Set/Struct Make/Get/Set descriptors, six Entity lifecycle nodes, eight Input nodes for keyboard/mouse/gamepad/action queries, and seven Camera nodes for camera query/control, ten Animation nodes, and ten Motion Query nodes for bounded query construction/search/control, and eleven Physics query/force nodes for bounded casts, overlap, and body mutation, all with exact typed pins, deterministic display order, and dedicated category icon identities. | EditorBridgeUVE production registry initialization, canvas palette snapshots, graph validation/compiler tests, managed search filtering/insertion, typed VM dispatch for supported data nodes, and native tests. | Descriptors define graph typing and presentation only; flow execution is owned by explicit bounded VM instruction contracts, while nested/subgraph traversal and broader engine-call bindings remain outside this catalog seam. |
| SCRIPT-ENTITY-LIFECYCLE-NODE-FAMILY | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineCallBindingsUVE` | Increment 327 adds six typed Entity lifecycle nodes—`entity.spawn`, `entity.destroy`, `entity.find`, `entity.get_entity`, `entity.add_component`, and `entity.remove_component`—with deterministic Entity/node.entity presentation, copied Entity/Component/Number inputs and Entity/Boolean outputs, and explicit caller-owned Spawn/Destroy/Find-by-Component/Get-by-Handle/Add/Remove callbacks. | Native catalog/compiler/bytecode/VM/runtime tests, EditorBridge palette snapshots, ScriptRuntimeUVE callers, scene-owned adapters, and future managed search/insertion consumers. | Callbacks receive copied values and return fail-closed booleans; the VM retains no scene/entity-manager/component pointer, reflection registry, or service lifetime, while Has/Get Component remain the read-only copied-fact query family. |
| SCRIPT-INPUT-NODE-FAMILY | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineCallBindingsUVE` | Increment 328 adds eight typed Input nodes—`input.key_pressed`, `input.key_released`, `input.key_down`, `input.mouse_position`, `input.mouse_button`, `input.gamepad_button`, `input.get_axis`, and `input.get_action`—with finite integral keyboard/mouse/gamepad/action tokens, copied Boolean/Number/Vector2 outputs, strict gamepad bounds, and explicit caller-owned query callbacks. | Native catalog/compiler/VM tests, EditorBridge palette snapshots, InputSystemUVE/GamepadInputSystemUVE adapters, managed search/insertion consumers, and future action-layer integration. | The VM accepts only copied numeric tokens and finite callback results; it does not poll devices, own input backends, retain platform handles, carry strings/action maps, or mutate input state. |
| SCRIPT-CAMERA-NODE-FAMILY | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineCallBindingsUVE` | Increment 328 adds seven typed Camera nodes—`camera.get_camera`, `camera.set_position`, `camera.set_rotation`, `camera.look_at`, `camera.set_fov`, `camera.shake`, and `camera.set_active`—with copied Entity/Vector3/Rotation/Number/Boolean pins, finite FOV/amplitude/duration validation, bounded Entity one-hop staging from `camera.get_camera`, and explicit caller-owned query/control callbacks. | Native catalog/compiler/VM tests, EditorBridge palette snapshots, CameraSystemUVE/render adapters, managed search/insertion consumers, and future gameplay camera workflows. | Camera callbacks receive copied values and return fail-closed booleans or a copied Entity; the VM owns no render camera, ECS pointer, viewport session, transform authority, or camera service lifetime. |
| SCRIPT-ANIMATION-NODE-FAMILY | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineCallBindingsUVE` | Increment 329 adds ten typed Animation nodes—`animation.play`, `animation.stop`, `animation.pause`, `animation.blend`, `animation.blend_space`, `animation.set_speed`, `animation.set_weight`, `animation.montage`, `animation.get_current_animation`, and `animation.is_playing`—with copied Actor/token/Number inputs, bounded clip and blend-weight validation, copied Boolean/Number outputs, and explicit caller-owned control/state callbacks. | Native catalog/compiler/VM tests, EditorBridge palette snapshots, core animation evaluator adapters, managed search/insertion consumers, and future animation playback/session workflows. | Animation callbacks receive only copied generational Entity values and finite numeric tokens/scalars; the VM owns no clip, player, state-machine evaluator, animation tree, pose buffer, or plugin service lifetime. |
| SCRIPT-MOTION-QUERY-NODE-FAMILY | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_uve.h | `TryBuildMotionQueryUVE` | Increment 329 adds ten typed Motion Query nodes—`motion.query.build`, `motion.query.search`, `motion.query.get_best_match`, `motion.query.set_trajectory`, `motion.query.set_pose`, `motion.query.set_velocity`, `motion.query.set_facing`, `motion.query.set_yaw`, `motion.query.transition`, and `motion.query.motion_warp`—with copied Actor/vector/pose/token inputs, finite and bounded candidate/weight/time validation, copied Boolean/Number outputs, and caller-owned Motion Query callbacks aligned with the existing value-only runtime authorities. | Native catalog/compiler/VM tests, EditorBridge palette snapshots, Motion Query runtime adapters, managed search/insertion consumers, and future database/index/evaluator workflows. | The bridge does not own databases, search indexes, clips, history, playback, or plugin state; it passes copied values to caller-owned services and fail-closes on missing bindings, invalid inputs, rejected callbacks, or invalid numeric outputs. |
| SCRIPT-PHYSICS-NODE-FAMILY | engine/physics/include/uve/physics/i_raycast_system_uve.h | `RaycastUVE` | Increment 330 adds eleven typed Physics nodes—`physics.raycast`, `physics.sphere_cast`, `physics.box_cast`, `physics.capsule_cast`, `physics.overlap`, `physics.apply_force`, `physics.apply_impulse`, `physics.set_velocity`, `physics.get_velocity`, `physics.enable_gravity`, and `physics.is_colliding`—with copied Vector3/Entity/Number/Boolean pins, bounded layer/distance/shape/count validation, caller-owned query and body-mutation callbacks, one-hop Entity staging into Body/Ignore consumers, and fail-closed direct/scheduler VM dispatch. | Native catalog/compiler/VM tests, EditorBridge palette snapshots, RaycastSystemUVE/AreaOverlapSystemUVE adapters, scene-owned body/rigid-body adapters, managed search/insertion consumers, and future measured cast backends. | The VM owns no IEntityManagerUVE, physics system, collider storage, rigid-body state, cast backend, or broad-phase lifetime; the current native Raycast authority remains a closest-hit point query, sphere/box/capsule casts are callback-owned extension seams, and all returned hit/count/mutation facts are copied and finite/bounded before storage. |
| SCRIPT-ENTITY-QUERY-NODES | engine/scripting/include/uve/scripting/script_vm_uve.h | `ExecuteScriptBytecodeUVE` | Bounded `Has Component` and `Get Component` nodes using appended Entity/Component typed value alternatives and a copied capped `{entity, componentTypeId, present}` fact table; missing facts fail deterministically and no live ECS pointer crosses the VM boundary. | Native catalog/VM tests, ScriptEntityQueryAdapterUVE, runtime state validation, editor bridge palette snapshots, and future scene-query workflows. | Query execution consumes only copied facts populated by an explicit adapter; it never owns or retains IEntityManagerUVE/component storage, resolves arbitrary reflection, mutates ECS state, or transfers scene ownership into ScriptRuntimeUVE. |
| SCRIPT-ENTITY-QUERY-ADAPTER | engine/scripting/include/uve/scripting/script_entity_query_adapter_uve.h | `PopulateComponentFactsUVE` | Scene-owned bridge that maps caller-registered script component IDs to native `std::type_index` bindings, validates an alive generational entity, reads component presence through `IEntityManagerUVE`, and atomically writes binding-ordered copied facts into the bounded VM context. | ScriptRuntimeUVE query-aware ticks, scene adapters, native scripting/ECS integration tests, and future engine-call binding workflows. | Adapter borrows the entity manager per call and copies only presence facts; it does not expose component pointers, mutate ECS, own type registration globally, or execute bytecode. |
| SCRIPT-FLOW-PIN-SEMANTICS | engine/scripting/include/uve/scripting/script_graph_uve.h | `ExecutionLinkCardinality` | Increment 276 adds deterministic `flow.sequence` and `flow.branch` descriptors with explicit execution pins and one-upstream/one-downstream execution-link cardinality validation; flow.branch compiler/runtime support and broader nested traversal remain independently bounded. | Native catalog/graph/compiler tests, EditorBridge palette snapshots, managed read-only palette/search consumers, and future graph compiler lowering. | This increment defines authoring/presentation and link validation only; it does not own bytecode execution, graph traversal, runtime/ECS state, or hidden engine-call authority. |
| SCRIPT-FLOW-LOOP-NODE-FAMILY | engine/scripting/src/script_builtin_nodes_uve.cpp | `flow.event` | Increment 325 adds Event, Loop, For Loop, While Loop, and Delay as one bounded Flow family with exact execution/data pins, deterministic 106–110 display orders, compiler lowering to existing v5 FlowControlDispatch, capped iteration routing, For Loop Index output, and persistent frame-gated Delay behavior, and debugger stepping support for the shared FlowControlDispatch contract. | Native catalog/compiler/bytecode/VM/runtime/debugger tests, EditorBridge palette snapshots, managed read-only palette/search consumers, and future gameplay control-flow nodes. | The family owns only copied execution routing and bounded context state; Event is an explicit per-execution trigger, loops do not traverse arbitrary subgraphs, Delay does not access wall-clock time, and no node owns ECS, scene, asset, or service lifetime. |
| SCRIPT-FLOW-LOOP-STATE | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptVmLoopStateUVE` | Increment 325 adds capped per-node loop iteration state and frame-gated Delay state to the caller-owned VM context. Count/Frames accept finite non-negative integers within 4096; context-free loop/Delay execution fails closed instead of fabricating persistence or spinning, while a persistent context may resume Delay across ticks. | ScriptVmExecutionContextUVE, ScriptRuntimeUVE per-instance ticks, debugger trace consumers, native Flow tests, and future bounded scheduling work. | State is copied, node-ID keyed, and capped at 256 loop and 256 Delay records; it is reset on completion, never stores wall-clock timestamps, never schedules threads, and never crosses into managed/native service ownership. |
| SCRIPT-VARIABLE-COLLECTION-NODE-FAMILY | engine/scripting/src/script_builtin_nodes_uve.cpp | `variable.make_array` | Increment 326 adds twelve typed Variable descriptors for Array, Map, Set, and Struct Make/Get/Set operations with deterministic 809–820 display orders, exact collection pins, bounded one-hop Get→Set staging, and editor palette coverage. | Native catalog/compiler/VM/runtime tests, EditorBridge palette snapshots, managed read-only palette/search consumers, and future collection-aware graph nodes. | The descriptors own graph typing and presentation only; collection values are copied through the VM and do not imply strings, arbitrary reflection, persistence, global/shared scope, or ECS/service authority. |
| SCRIPT-VARIABLE-COLLECTION-VALUE-KERNEL | engine/scripting/include/uve/scripting/script_collection_value_uve.h | `IsValidScriptArrayValueUVE` | Increment 326 adds finite value-only Array, Map, Set, and Struct contracts over Number/Boolean/Vector2/Vector3 elements, with a 64-entry cap, strictly increasing finite Map keys, canonical unique Set ordering, and strictly increasing nonzero Struct field IDs. | Native collection kernel/VM/runtime tests, typed local-variable storage, compiler transfers, EditorBridge palette snapshots, and future bounded collection operations. | The kernel owns copied values and deterministic validation only; it does not own strings, object references, reflection schemas, asset/ECS state, serialization persistence, shared scope, or arbitrary mutation authority. |
| SCRIPT-FLOW-STAGED-INSTRUCTION-CAP | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `kMaximumInstructionsUVE` | Increment 300 bounds IR-to-bytecode lowering at 256 instructions, accepts the exact boundary, and emits one deterministic `InstructionLimitExceeded` diagnostic before lowering when exceeded; the serialized bytecode cap remains an independent 4096-instruction boundary. | Native IR/bytecode lowering tests, runtime attach/reload validation, debugger consumers, managed read-only diagnostics, and future graph compiler expansion. | The cap owns only bounded lowering cost and copied diagnostics; it does not schedule graphs, mutate authoring, own runtime state, or replace the serialized bytecode and VM instruction-budget limits. |
| SCRIPT-CONDITIONAL-JUMP-BYTECODE | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `ConditionalJump` | Increment 277 introduced the version-2 ConditionalJump IR/bytecode kind with bounded true/false targets; the current v3 format preserves that payload while adding SequenceDispatch, with strict target validation and legacy v1 data-only decode/VM/debugger compatibility. | Native bytecode round-trip/decoder tests, ConditionalJump VM execution, debugger stepping, future compiler lowering, and runtime compatibility gates. | The contract owns only copied instruction metadata and byte serialization; it does not own graph traversal, ECS/scene state, or hidden engine-call authority. |
| SCRIPT-VECTOR3-NODE-FAMILY | engine/scripting/include/uve/scripting/script_builtin_nodes_uve.h | `RegisterBuiltInScriptNodesUVE` | The eleven existing Vector3 graph node descriptors remain stable with exact typed pins, deterministic display order, and the established Math/icon presentation identity, including the Increment 316 Distance, Direction, and Lerp additions. | EditorBridgeUVE production registry initialization, canvas palette snapshots, graph validation/compiler tests, managed search filtering, and native tests. | This family retains its existing IDs and descriptors; it does not own runtime state or alter VM bytecode semantics. |
| SCRIPT-VECTOR3-VALUE-KERNEL | engine/scripting/include/uve/scripting/script_vector3_value_uve.h | `EvaluateScriptVector3AddUVE` | Pure typed Vector3 value/evaluation contract reusing Math::Vector3UVE for Make, Add, Subtract, scalar Multiply, Dot, Cross, Length, guarded Normalize, Distance, Direction, and Lerp with finite-input/output checks plus zero-length direction rejection. | Native scripting math tests, typed runtime state storage, typed VM dispatch, and managed editor descriptors that consume the same pin types. | Kernel does not mutate graphs, allocate runtime state, execute bytecode, or replace the existing scalar ScriptRuntimeStateUVE storage. |
| SCRIPT-ROTATION-NODE-FAMILY | engine/scripting/src/script_builtin_nodes_uve.cpp | `math.rotation.make` | Increment 323 adds the complete nine-node Rotation/Quaternion palette family—Make/Break Rotation, Degrees, Radians, Euler, Quaternion, Look At, Slerp, and Rotate—with exact typed pins, deterministic display order, compiler staging, VM execution, and editor-discoverable category/icon identity. | Native catalog, graph validation/compiler/bytecode/VM tests, runtime finite-value validation, and future managed palette/search consumers. | The descriptors and execution path own typed graph/runtime semantics only; they do not own transform composition, asset state, ECS mutation, arbitrary reflection, or engine-service bindings. |
| SCRIPT-ROTATION-VALUE-KERNEL | engine/scripting/include/uve/scripting/script_rotation_value_uve.h | `ScriptRotationValueUVE` | Increment 323 adds pure Rotation value evaluation over checked quaternion math for axis-angle Make/Break, Degrees/Radians conversion, Euler and raw Quaternion construction, Look At, shortest-path Slerp, and Vector3 rotation with finite/degenerate failure codes and normalized outputs. | Native scripting math tests, QuaternionUVE helper tests, typed VM dispatch, runtime state finite checks, and future Transform/Animation node families. | The kernel owns copied value results and failure codes only; Math owns quaternion operations, and no graph, ECS, asset, renderer, or service ownership crosses the boundary. |
| SCRIPT-TRANSFORM-NODE-FAMILY | engine/scripting/src/script_builtin_nodes_uve.cpp | `math.transform.make` | Increment 324 adds the eleven-node Transform family—Make/Break Transform, Get/Set Position, Get/Set Rotation, Get/Set Scale, Translate, Rotate, and Transform Point—with exact typed pins, display orders 600–610, explicit category/icon identity, compiler staging, typed VM dispatch, and editor palette coverage. Entity Query display orders move to 620/621 to keep palette sorting collision-free. | Native catalog, graph/compiler/bytecode/VM/runtime tests, EditorBridge palette snapshots, managed palette/search consumers, and future entity/animation service families. | The family owns copied Transform values and pure typed node semantics only; it does not mutate ECS/scene state, resolve assets, perform reflection, or provide engine-service authority. |
| SCRIPT-TRANSFORM-VALUE-KERNEL | engine/scripting/include/uve/scripting/script_transform_value_uve.h | `ScriptTransformValueUVE` | Increment 324 adds a finite, normalized TRS value kernel for Make/Break, component access/mutation, Translate, quaternion composition, and Matrix4x4UVE-backed Transform Point evaluation with explicit non-finite and degenerate-rotation failure codes. | Native Transform evaluator/VM tests, runtime finite-value validation, typed graph transfers, and future animation/entity transform consumers. | The kernel owns copied value results and failure codes only; Matrix4x4UVE and QuaternionUVE own math primitives, and no graph, ECS, renderer, asset, or service ownership crosses the boundary. |
| SCRIPT-VECTOR3-RUNTIME-STATE | engine/scripting/include/uve/scripting/script_runtime_uve.h | `vector3Values` | Bounded copied typed Vector3 runtime storage alongside legacy int64 scalar slots, with finite-component validation, independent capacity, deterministic value equality, and compatible reload preservation. | ScriptRuntimeUVE state update/get, compatible reload tests, per-entity typed tick execution, and future typed node families. | Storage does not decode bytecode, mutate graphs, or replace legacy scalar slots; graph execution occurs through the explicit VM context. |
| SCRIPT-TYPED-VM-EXECUTION | engine/scripting/include/uve/scripting/script_vm_uve.h | `ExecuteScriptBytecodeUVE` | Explicit bounded variant-valued VM context and deterministic dependency-aware dispatch for the 161-node catalog’s supported Flow (including Event/Loop/For Loop/While Loop/Delay), Float, Vector2, Vector3, Rotation, Transform, Boolean, Conversion, typed Variable collections, Entity Query data nodes, Entity lifecycle nodes, Input query nodes, and Camera query/control nodes, Animation control/state nodes, and Motion Query build/search/control nodes, Physics query/force nodes, appended Entity/Component/Rotation/Transform/Array/Map/Set/Struct value alternatives, copied query facts, caller-owned lifecycle/input/camera/animation/Motion Query/Physics callbacks, typed transfer bindings including bounded Input Boolean/Number/Vector2 and Entity staging into Camera/Animation/Motion Query Actor and Physics Body/Ignore pins, finite scalar/vector/quaternion/TRS/collection safeguards, bounded collection and loop/frame state, instruction budgets, and node-execution diagnostics. | Native VM tests, ScriptRuntimeUVE per-entity execution, debugger stepping, scene-owned lifecycle/input/camera/animation/Motion Query/Physics adapters, and managed read-only diagnostics. | New value alternatives are appended after the existing float/Vector3/bool indices; VM owns only caller-provided copied bindings/facts and no ECS entities, scene lifetime, graph authoring, engine services, device handles, render camera, animation player, Motion Query database/index, PhysicsSystemUVE/collider storage, or hidden engine-call bindings. |
| SCRIPT-CONDITIONAL-JUMP-VM | engine/scripting/include/uve/scripting/script_vm_uve.h | `ExecuteScriptBytecodeUVE` | Increment 278 evaluates copied Boolean conditions for v2 ConditionalJump instructions, selects bounded true/false instruction targets, terminates self-loops through the instruction budget, emits NodeExecuted/Failed/Completed trace evidence, and exposes an explicit copied-context `ScriptDebuggerUVE::AttachWithContextUVE` stepping seam. ScriptRuntimeUVE consumes the same VM path for attached contexts. | Native VM/runtime/debugger tests, bytecode execution, managed read-only diagnostics, and future graph compiler lowering. | Branch execution reads only caller-provided value bindings and bytecode targets; it does not own ECS/scene services, mutate graph authoring, infer hidden engine calls, or provide flow.sequence lowering. |
| SCRIPT-SEQUENCE-DISPATCH | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `SequenceDispatch` | Increment 279 lowers one validated flow.sequence node into a v3 SequenceDispatch instruction with bounded ordered first/second targets, direct non-flow target restrictions, deterministic missing-output continuation, VM instruction-budget loop safety, and debugger stepping evidence; nested flow/subgraph traversal remains deferred. | Native graph/compiler/bytecode/VM/debugger tests, ScriptRuntimeUVE through the shared VM path, and future graph traversal/lowering work. | SequenceDispatch owns only copied instruction targets and execution control; it does not own graph lifetime, ECS/scene state, nested traversal, or engine-call bindings. |
| SCRIPT-FLOW-BRANCH-DISPATCH | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `ConditionalJump` | Increment 280 lowers one validated flow.branch node into a ConditionalJump instruction with caller-bound Boolean Condition, bounded True/False direct non-flow targets, explicit data-condition staging deferral, and missing-output continuation; nested flow traversal remains deferred. | Native graph/compiler/bytecode/VM/debugger tests, ScriptRuntimeUVE through the shared VM path, and future graph data-condition staging. | The contract owns only copied instruction targets; it does not own graph lifetime, ECS/scene state, data-condition staging, nested traversal, or engine-call bindings. |
| SCRIPT-FLOW-DATA-CONDITION-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 281 stages one direct graph Boolean output from an approved built-in Boolean producer into flow.branch Condition by moving the producer before the branch and emitting an immediate TransferValue; custom producers and producer data dependencies remain explicit UnsupportedRuntimeNode deferrals. | Native graph/compiler/bytecode/VM tests, ScriptRuntimeUVE through the shared VM path, debugger trace/step consumers, and future dependency-aware graph scheduling. | Staging owns only copied instruction order and link metadata; it does not own graph lifetime, producer evaluation state, ECS/scene state, arbitrary dependency traversal, custom node authority, or engine-call bindings. |
| SCRIPT-FLOW-CONDITION-DEPENDENCY-SCHEDULING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 282 extends flow.branch data-condition staging by deterministically ordering at most one approved built-in Boolean producer dependency before the condition producer, emitting both typed transfers, and rejecting cycles or deeper dependency chains without partial IR. | Native graph/compiler/bytecode/VM tests, ScriptRuntimeUVE through the shared VM path, debugger trace/step consumers, and future general graph scheduling. | Scheduling owns only copied bounded instruction order and link metadata; it does not own graph lifetime, arbitrary dependency traversal, node evaluation state, ECS/scene state, custom node authority, or engine-call bindings. |
| SCRIPT-FLOW-BOOLEAN-CHAIN-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 290 stages one direct `logic.boolean.*.Result` output before one downstream Boolean consumer input (`Value`, `A`, or `B`), with producer-before-transfer-before-consumer ordering and explicit rejection of a second staged consumer or composed/deeper Boolean chains; flow.branch condition dependency links retain their dedicated scheduling seam. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The seam owns only copied bounded instruction order and Boolean link metadata; it does not own Boolean producer state, graph lifetime, arbitrary topological scheduling, output storage, ECS/scene state, or hidden evaluation history. |
| SCRIPT-FLOW-COMPARISON-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 305 adds six finite Number-to-Boolean comparison nodes (`Equal`, `Not Equal`, `Greater`, `Less`, `Greater Equal`, and `Less Equal`) with up to two direct Number producers staged onto one comparison A/B pair, deterministic producer order, copied Boolean Result output, and explicit rejection of composed dependencies or multiple comparison consumers. | Native built-in catalog, graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The comparison seam owns only typed Number inputs, Boolean output, bounded staging metadata, and deterministic comparison semantics; it does not own variables, arbitrary scheduling, floating-point tolerance policy beyond exact finite comparisons, ECS/scene state, or service bindings. |
| SCRIPT-FLOW-SCALAR-MATH-UTILITY | engine/scripting/src/script_vm_uve.cpp | `ExecuteFloatNodeUVE` | Increment 306 adds six direct finite Number utility nodes (`Modulo`, `Abs`, `Min`, `Max`, `Clamp`, and `Power`) with copied Number Result output; modulo rejects zero-near divisors, clamp rejects reversed bounds, and non-finite inputs or results fail closed. | Native built-in catalog, compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The utility seam owns only copied typed Number inputs/results and deterministic bounded math behavior; it does not own graph scheduling, staged producer dependencies, variable storage, tolerance policy beyond the explicit modulo guard, ECS/scene state, or service bindings. |
| SCRIPT-FLOW-COMPARISON-CONDITION-STAGING | engine/scripting/src/script_compiler_ir_uve.cpp | `ConditionalJump` | Increment 307 proves one direct finite Number comparison Result can stage into `flow.branch.Condition`, preserving producer-before-transfer-before-ConditionalJump ordering and deterministic true/false continuation; composed/deeper condition dependencies remain deferred. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, debugger trace/step consumers, and existing editor read-only branch diagnostics. | The seam owns only copied comparison-result transfer and branch target selection; it does not own arbitrary graph scheduling, variables, producer history, branch side effects, ECS/scene state, or custom Boolean producers. |
| SCRIPT-FLOW-LOCAL-VARIABLE-RUNTIME | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptVmExecutionContextUVE` | Increment 308 adds nine typed local-variable nodes: Make/Get/Set for Number, Boolean, and Vector3. Each graph instance owns a bounded 256-slot value-only store; Make initializes a slot once with a finite/default value, Set requires an existing matching type, Get returns a copied matching value, and compatible runtime reload preserves the context. | Native catalog/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE tick/state/reload paths, EditorBridgeUVE copied runtime snapshots, managed Avalonia snapshot parsing, and existing editor read-only palette discovery. | The contract owns only per-instance copied typed values, integral slot identity, finite/default validation, bounded capacity, and one-hop Get-result staging; it does not own strings, arrays/maps/sets/struct reflection, persistence, global/shared scope, ECS state, arbitrary graph scheduling, or service bindings. |
| SCRIPT-FLOW-VECTOR2-FUNCTION-LIBRARY | engine/scripting/include/uve/scripting/script_vector2_value_uve.h | `EvaluateScriptVector2NormalizeUVE` | Increment 309 adds a bounded Vector2 function family: Make, Add, Subtract, Multiply, Length, and Normalize. Inputs and copied outputs must be finite; Length uses stable two-component hypot semantics and Normalize rejects zero-near vectors deterministically. Direct Vector2 producers can feed one downstream Vector2 input, while Vector2 Length can feed one approved Number input. | Native Vector2 value, catalog, compiler, VM, runtime, debugger, and editor palette tests, ScriptRuntimeUVE through the shared VM path, and existing managed read-only palette consumers. | The contract owns only copied Vector2 values, finite arithmetic, zero-length normalization diagnostics, and bounded one-hop staging; it does not own Vector4/rotation/transform types, containers, reflection, arbitrary graph scheduling, ECS/scene state, or service bindings. |

| SCRIPT-FLOW-QUERY-BOOLEAN-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 295 extends one-hop Boolean staging to one copied `query.entity.has_component.Result` producer feeding one downstream Boolean `Value`, `A`, or `B` input, preserving producer-before-transfer-before-consumer ordering and explicit rejection of a second staged consumer or composed/deeper Boolean chains. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptEntityQueryAdapterUVE copied-fact refresh, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The seam owns only copied instruction order, Boolean link metadata, and query-result transfer; it does not own ECS/entity-manager state, component pointers, graph lifetime, arbitrary scheduling, output storage, or hidden evaluation history. |
| SCRIPT-FLOW-QUERY-CONDITION-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 296 proves the existing dependency-aware flow.branch condition seam with one copied `query.entity.has_component.Result` producer ordered before an immediate Condition transfer and ConditionalJump, preserving branch targets and rejecting arbitrary query scheduling or deeper condition dependencies. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptEntityQueryAdapterUVE copied-fact refresh, ScriptRuntimeUVE through the shared VM path, and existing editor read-only branch diagnostics. | The seam owns only copied condition order and link metadata; it does not own ECS/entity-manager state, component pointers, graph lifetime, arbitrary topological scheduling, branch target ownership, output storage, or hidden evaluation history. |
| SCRIPT-FLOW-QUERY-COMPONENT-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 297 stages one direct copied `query.entity.get_component.Result` token before one downstream `query.entity.has_component.Component` input, with producer-before-transfer-before-consumer ordering and explicit rejection of additional Component consumers or composed/deeper query dependencies. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptEntityQueryAdapterUVE copied-fact refresh, ScriptRuntimeUVE through the shared VM path, and existing editor read-only query diagnostics. | The seam owns only copied Component token order and link metadata; it does not own ECS/entity-manager state, component pointers, graph lifetime, arbitrary scheduling, output storage, or hidden evaluation history. |
| SCRIPT-FLOW-STAGED-TRANSFER-TRACE | engine/scripting/include/uve/scripting/script_vm_uve.h | `StagedValueTransferred` | Increment 298 carries compiler-marked staging transfers through v4 IR/bytecode and emits a dedicated copied `StagedValueTransferred` trace event in both the VM and debugger, while v1–v3 bytecode remains decodable with ordinary transfer semantics. | Native compiler/bytecode/VM/debugger tests, ScriptRuntimeUVE through the shared VM path, debugger trace snapshots, and existing editor read-only trace presentation. | The marker owns only copied instruction provenance and trace classification; it does not own graph scheduling, node evaluation, runtime value storage, ECS/scene state, debugger control flow, or hidden engine-service authority. |
| SCRIPT-FLOW-GRAPH-LINK-COUNT-CAP | engine/scripting/include/uve/scripting/script_graph_uve.h | `LinkCountExceeded` | Increment 301 bounds `ScriptGraphUVE::ValidateUVE` to 256 links after the 64-node guard, accepts the exact boundary with valid typed links, and returns one deterministic graph-level `LinkCountExceeded` diagnostic before traversal when exceeded. | Native graph validation/compiler tests, EditorBridge validation serialization, managed read-only diagnostics, and future bounded graph compilation. | The cap owns only validation cost and copied diagnostics; it does not limit authoring storage, mutate graphs, schedule nodes, own runtime values, or replace bytecode instruction limits. |
| SCRIPT-FLOW-GRAPH-NODE-COUNT-CAP | engine/scripting/include/uve/scripting/script_graph_uve.h | `NodeCountExceeded` | Increment 299 bounds `ScriptGraphUVE::ValidateUVE` to 64 nodes, accepts the exact boundary, and returns one deterministic graph-level `NodeCountExceeded` diagnostic before node/link traversal when exceeded. | Native graph validation/compiler tests, EditorBridge validation serialization, managed read-only diagnostics, and future bounded graph compilation. | The cap owns only validation cost and copied diagnostics; it does not limit authoring storage, mutate graphs, schedule nodes, own runtime values, or replace bytecode instruction limits. |
| SCRIPT-FLOW-DATA-LINK-CARDINALITY | engine/scripting/include/uve/scripting/script_graph_uve.h | `DataLinkCardinality` | Increment 292 enforces at most one non-execution source per data input pin, preserves multiple links to distinct data inputs, and emits deterministic copied diagnostics without changing execution-link cardinality or typed TransferValue staging behavior. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, editor read-only graph diagnostics, and managed validation consumers through existing serialized diagnostic values. | Validation owns only copied link-cardinality facts and diagnostics; it does not own graph lifetime, node evaluation, output fan-out policy, execution scheduling, runtime value storage, ECS/scene state, or editor mutation. |
| SCRIPT-FLOW-NUMBER-DATA-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 286 stages one direct `engine.get_time.Value` output before one `math.float.*` A/B consumer and emits an immediate typed TransferValue, while composed/deeper Number dependencies and additional staged consumers are explicitly rejected without partial IR. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through shared VM options, and existing editor read-only graph diagnostics. | The staging seam owns only copied instruction ordering and link metadata; it does not own graph lifetime, arbitrary topological scheduling, producer history, output storage, engine clock/service state, ECS/scene state, or hidden node evaluation. |
| SCRIPT-FLOW-NUMBER-PRODUCER-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 288 extends direct Number staging to finite built-in `math.vector3.dot.Result` and `math.vector3.length.Length` outputs feeding one `math.float.*` A/B input, with producer-before-transfer-before-consumer ordering and explicit rejection of composed/deeper Number chains or additional staged consumers. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The seam owns only copied bounded instruction order and link metadata; it does not own vector/scalar producer state, graph lifetime, arbitrary topological scheduling, output storage, ECS/scene state, asset/resource state, or hidden evaluation history. |
| SCRIPT-FLOW-FLOAT-CHAIN-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 289 extends direct Number staging to one approved `math.float.*.Result` output feeding one downstream `math.float.*` A/B input, with producer-before-transfer-before-consumer ordering and explicit rejection of a second staged consumer or composed/deeper Float chains. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The seam owns only copied bounded instruction order and link metadata; it does not own scalar producer state, graph lifetime, arbitrary topological scheduling, output storage, ECS/scene state, or hidden evaluation history. |
| SCRIPT-FLOW-VECTOR3-DATA-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 287 stages one direct built-in Vector3 producer output (`Vector` or `Result`) before one `math.vector3.*` A/B/Vector consumer and emits an immediate typed TransferValue with finite copied Vector3 execution proof; composed/deeper vector dependencies and additional staged consumers are explicitly rejected without partial IR. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The staging seam owns only copied instruction ordering and link metadata; it does not own graph lifetime, arbitrary topological scheduling, producer history, typed output storage, ECS/scene state, asset/resource state, or hidden node evaluation. |
| SCRIPT-FLOW-VECTOR3-SCALE-STAGING | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `TransferValue` | Increment 291 stages one direct finite Number producer (`engine.get_time.Value` or `math.float.*.Result`) before `math.vector3.multiply.Scale`, with producer-before-transfer-before-consumer ordering and explicit rejection of additional Scale consumers or composed/deeper Number dependencies. | Native graph/compiler/bytecode/VM/runtime/debugger tests, ScriptRuntimeUVE through the shared VM path, and existing editor read-only graph diagnostics. | The seam owns only copied bounded instruction order and Scale link metadata; it does not own scalar/vector producer state, graph lifetime, arbitrary topological scheduling, output storage, ECS/scene state, or hidden evaluation history. |
| SCRIPT-ENGINE-CALL-BINDINGS | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineCallBindingsUVE` | Increment 283 exposes one explicit `engine.log` binding that accepts a copied finite Number through a caller-owned noexcept callback, emits normal VM trace evidence, and fails deterministically when the binding, input, context, or callback acceptance is absent; no service is retained by the VM or runtime. | Built-in node catalog/compiler tests, bytecode/VM/runtime tests, debugger trace consumers, ScriptRuntimeUVE tick options, and managed read-only node discovery. | The binding table is borrowed for one execution call; it owns no callback target, service, ECS/scene state, graph lifetime, thread/executor policy, or arbitrary engine-call authority, and it does not imply string logging, time access, mutation, or hidden capabilities. |
| SCRIPT-ENGINE-TIME-BINDINGS | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineGetTimeFunctionUVE` | Increment 284 exposes one explicit `engine.get_time` binding that writes a finite copied Number into the node output context, emits normal VM trace evidence, and rejects unbound, callback-rejected, non-finite, or capacity-exhausted results without reading a hidden clock. | Built-in node catalog/compiler tests, bytecode/VM/runtime tests, ScriptRuntimeUVE tick options, debugger trace consumers, and managed read-only node discovery. | The callback is borrowed for one execution call and owns no clock, service, thread/executor, ECS/scene state, graph lifetime, output storage, or arbitrary engine-call authority. |
| SCRIPT-VM-EXECUTION-TRACE | engine/scripting/include/uve/scripting/script_vm_uve.h | `AppendTraceEventUVE` | Bounded copied per-instruction trace records for node execution, value transfer, query refresh, completion, and failure; capped at 512 events with 256-byte message truncation, while `PrependTraceEventsUVE` inserts query-refresh evidence before VM events. | Native VM/runtime tests, debugger stepping, and managed read-only diagnostics. | Trace is copied value data; the VM does not own ECS, scene, graph, or engine-service lifetime and exposes no hidden engine-call authority. |
| SCRIPT-DEBUGGER-TRACE-INTEGRATION | engine/scripting/include/uve/scripting/script_source_mapping_uve.h | `BuildPresentationUVE` | Read-only debugger presentation contract that copies bounded ScriptVmTraceEventUVE history and truncation evidence alongside source-mapped node labels, breakpoints, and watches; EditorBridgeUVE serializes the same event fields for the Avalonia host. | Native debugger/source-mapping tests, EditorBridge stdio JSON tests, managed BridgeSnapshotParser, and Avalonia debugger status presentation. | Presentation copies trace values only; it does not execute bytecode, own graph/runtime/ECS state, mutate breakpoints, or retain native debugger ownership across the bridge. |
| SCRIPT-VECTOR3-VM-EXECUTION | engine/scripting/include/uve/scripting/script_vm_uve.h | `ExecuteScriptBytecodeUVE` | The existing eight Vector3 node IDs retain deterministic dependency-aware dispatch, typed transfer behavior, instruction budgets, and node-execution diagnostics. | Native VM tests, ScriptRuntimeUVE per-entity execution, debugger stepping, and managed read-only diagnostics. | This family remains compatible with the existing float/Vector3 value alternatives; exact node-family expansion beyond Float/Boolean remains future work. |
| SCRIPT-RUNTIME-TYPED-EXECUTION | engine/scripting/include/uve/scripting/script_runtime_uve.h | `TickWithEntityQueryDetailedUVE` | Per-entity copied VM context synchronized through ScriptRuntimeUVE state updates and consumed by deterministic ticks, including an explicit query-aware path that refreshes copied facts before execution and reports adapter rejection as per-entity NodeExecutionFailed diagnostics. | ScriptRuntimeUVE attach/state/reload/tick lifecycle, ScriptEntityQueryAdapterUVE population, EditorBridge diagnostic tick, native runtime tests, and future managed runtime diagnostics. | Runtime owns the copied per-entity context and VM result; the query-aware overload borrows IEntityManagerUVE only for the call and does not transfer ECS/entity lifetime, graph ownership, engine services, or hidden engine-call authority to the VM. |
| SCRIPT-COMPONENT-RUNTIME-OWNERSHIP | engine/scripting/include/uve/scripting/script_component_runtime_ownership_uve.h | `ReconcileUVE` | Validated ScriptComponent path ownership bridge: empty path idempotently detaches, non-empty path compiles/binds a caller-supplied graph, and active duplicate runtime instances are rejected without mutation. | Scene/editor component attachment, ScriptGraphRuntimeBindingUVE, ScriptRuntimeUVE attach/detach, native ownership tests, and future asset-loader reconciliation. | The bridge validates only the virtual path and graph contract; it does not read files, resolve asset GUIDs, own bytecode, replace active scripts, or own ECS/entity lifetime. |
| SCRIPT-ASSET-GRAPH-LOADER | engine/scripting/include/uve/scripting/script_asset_loader_uve.h | `ScriptAssetLoaderUVE` | Bounded VFS-backed loading of a validated ScriptComponentUVE path into the existing ScriptGraphSchemaUVE format, with explicit no-script, missing, oversize, embedded-NUL, decode, source-byte-count, and copied diagnostic outcomes; repeated calls observe updated VFS content deterministically. | ScriptComponent asset reconciliation, graph editor/serializer consumers, ScriptGraphRuntimeBindingUVE preflight, VFS mounts, native tests, and future asset-manager integrations. | Loader borrows IFileSystemUVE per call and returns copied schema/diagnostics; it owns no files, bytes, graphs, bytecode, ECS entities, runtime attachment, asset handles, or hot-reload last-known-good state. |
| MQ-REGISTRY-CACHE | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_asset_registry_cache_uve.h | `class MotionQueryAssetRegistryUVE final` | Generation-aware asset registry and bounded derived-data cache. | Motion Query ingestion, runtime integration, and native tests. | Asset manager remains authoritative; plugin stores exact handles and copied derived data only. |
| MQ-INGESTION | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_asset_ingestion_uve.h | `class MotionQueryAssetIngestionCoordinatorUVE final` | Event-driven reload invalidation and rebuild state coordinator. | Asset events, Motion Query derived-data providers, diagnostics, and native tests. | Coordinator is non-owning; event subscriptions are RAII and snapshots are copied value data. |
| MQ-SEARCH | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_search_index_uve.h | `class MotionQuerySearchIndexUVE final` | Bounded copied feature index and deterministic nearest-candidate search contract. | Motion Query animation node and native runtime tests. | Index owns copied entries only; it does not own databases, ECS entities, assets, or renderer resources. |
| MQ-ANIMATION | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_animation_node_uve.h | `EvaluateMotionQueryAnimationNodeUVE` | Runtime motion-query evaluation and optional copied debug publication. | Native animation runtime and optional non-owning debug sink. | Evaluator consumes value-oriented inputs; debug publication never transfers runtime ownership. |
| MQ-DATABASE-FACTORY | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_database_contract_uve.h | `CreateDefaultMotionQueryDatabaseContractUVE` | Deterministic copied default Motion Query database contract factory with caller-provided database identity, generation, schema identity, bounded candidate capacity, one valid candidate/trajectory fixture, and shared contract validation evidence. | Editor bridge fixtures, future asset/import authoring callers, runtime contract tests, and native validation. | Factory owns no assets, files, ECS entities, renderer resources, or runtime database lifetime; it returns copied value data and delegates acceptance to the existing contract validator. |
| ANIMATION-RUNTIME-CONVERGENCE | engine/core/include/uve/core/time_pose_contract_uve.h | `ValidateSkeletonDefinitionUVE` | Shared bounded copy-only skeleton joint topology, skeleton-matched local pose buffer, and finite evaluation-time context consumed by Motion Query and Control Rig plugin DTOs. | MotionQueryUVE validation/matching, ControlRigUVE validation/evaluation result, core animation tests, plugin tests, and future animation player/tree/retargeting adapters. | Contracts carry identifiers and copied transforms only; no asset/clip/resource ownership, ECS mutation, full playback, IK policy, retargeting, GPU skinning, or editor authoring crosses this boundary. |
| CONTROL-RIG-TWOBONE-ORIENTATION | engine/plugins/Animation/control_rig/Source/Runtime/include/uve/plugins/control_rig_uve.h | `SolveTwoBoneIKUVE` | Increment 302 extends the existing bounded TwoBoneIK solve with deterministic finite root and mid rotations that align each control's local +X bone axis to the solved root-to-mid and mid-to-end directions, blended by the existing clamped weight while preserving position, pole, reachability, and no-history ownership. | ControlRigUVE validation/evaluation, TwoBoneIK native tests, shared pose/quaternion contracts, and future animation editor adapters. | The orientation solve owns only copied pose rotations and solved positions; it does not own skeleton assets, animation clips, ECS entities, renderer resources, runtime handles, temporal history, or editor persistence. |
| CONTROL-RIG-EVALUATION | engine/plugins/Animation/control_rig/Source/Runtime/include/uve/plugins/control_rig_uve.h | `BlendControlRigPoseUVE` | Deterministic bounded Control Rig pose blending with finite weight clamping, normalized rotation fallback, and weighted two-bone constraint evaluation over copied controls and shared pose/evaluation contracts. | ControlRigUVE validation, TwoBoneIK/AimLookAt evaluation, native Control Rig tests, and future animation editor adapters. | Control Rig owns copied control/constraint results only; it does not own skeleton assets, animation clips, ECS entities, renderer resources, runtime handles, or editor persistence. |
| CONTROL-RIG-AIM-POLE-VECTOR | engine/plugins/Animation/control_rig/Source/Runtime/include/uve/plugins/control_rig_uve.h | `ControlRigConstraintUVE` | Increment 293 extends AimLookAt with an optional validated pole control whose copied source-to-pole direction supplies the deterministic up reference; missing pole references reject validation and degenerate pole directions fall back to world-up without retaining history. | ControlRigUVE validation/evaluation, AimLookAt runtime tests, shared pose contracts, and future Control Rig editor adapters. | The pole reference owns only copied control identity and evaluated pose direction; it does not own controls, skeleton/assets, ECS entities, renderer resources, temporal history, or editor persistence. |
| CONTROL-RIG-AIM-ANGLE-LIMITS | engine/plugins/Animation/control_rig/Source/Runtime/include/uve/plugins/control_rig_uve.h | `ControlRigConstraintUVE` | Increment 294 appends finite `minAimAngleDegrees`/`maxAimAngleDegrees` bounds with backward-compatible 0–180° defaults, validates ordered limits, and clamps the copied AimLookAt quaternion around its solved axis without adding temporal history or changing the default solve. | ControlRigUVE validation/evaluation, AimLookAt/pole-vector native tests, shared pose contracts, and future Control Rig editor adapters. | Angle limits own only copied scalar configuration and bounded output rotation; they do not own controls, skeleton/assets, ECS entities, renderer resources, scheduler state, or hidden evaluation history. |
| CONTROL-RIG-SPRING-CONSTRAINT | engine/plugins/Animation/control_rig/Source/Runtime/include/uve/plugins/control_rig_uve.h | `SolveSpringPositionUVE` | Increment 285 adds one bounded SpringPosition constraint that applies a finite exponential position response toward a copied target using animation delta time, stiffness 0–64, damping 0–1, and clamped influence weight, while preserving source rotation/scale and rejecting steps above 0.25 seconds. | ControlRigUVE validation/evaluation, Animation plugin tests, shared pose/time contracts, and future Control Rig editor adapters. | The spring solver owns only copied pose/result values; it does not retain velocity/history, own scene/entity/asset state, choose a scheduler, mutate ECS, or claim full physical spring integration, collision response, or network determinism. |
| MQ-EDITOR-AUTHORING | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_editor_authoring_uve.h | `class MotionQueryEditorAuthoringSessionUVE final` | Native authoring drafts, validation, revision guards, and copied editor snapshots. | Native editor bridge, editor tests, and managed presentation through DTOs. | Authoring session owns bounded copied drafts; scene files, ECS, renderer resources, and C# remain outside its authority. |
| MQ-EDITOR-CLIPBOARD | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_editor_authoring_uve.h | `CopyDatabase` | One-entry copied database clipboard with explicit destination resource/display/context identity, duplicate guards, strict validation, revision-safe mutation, and snapshot availability facts. | Motion Query editor authoring/clipboard workflow, native tests, editor bridge DTO consumers, and future managed commands. | Clipboard owns copied contract data only; paste remaps identity and clears event history, never transfers resource/file/ECS ownership, and does not provide multi-entry or OS clipboard integration. |
| MQ-EDITOR-HISTORY | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_editor_authoring_uve.h | `kMotionQueryEditorMaximumHistoryEntriesUVE` | Bounded snapshot-based Undo/Redo commands with a 32-entry cap, revision guards, deterministic restoration of databases/selection/clipboard, redo invalidation after new mutations, and copied capability flags. | Motion Query authoring commands, native editor tests, editor bridge DTO consumers, and future managed undo/redo presentation. | History stores copied value snapshots only; it does not persist to disk, own assets/ECS/renderer resources, bypass stale-revision checks, or provide cross-session history. |
| MQ-EDITOR-COMMAND-METADATA | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_editor_authoring_uve.h | `GetMotionQueryEditorCommandMetadataUVE` | Stable ordered metadata for all 14 authoring commands, including stable legacy names/labels, payload kind, mutation/resource/payload requirements, and Undo capability facts, copied through the native editor bridge for managed discovery. | Native Motion Query authoring, editor bridge JSON DTOs, managed snapshot parser, and native/managed editor tests. | Metadata is a static value catalog only; it does not execute commands, own reflection, infer arbitrary plugins, or replace authoritative request validation. |
| MQ-EDITOR-UTILS | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_editor_authoring_uve.h | `ValidateMotionQueryEditorCandidateIdentifiersUVE` | Bounded editor utility authority for display-name validity, ASCII outer-whitespace schema-ID normalization, and candidate identifier preflight with stable index/code/message results; authoring commands use the helpers before shared runtime validation. | Native Motion Query authoring commands and focused editor utility tests. | Utilities operate on copied authoring values only; they do not own assets, files, ECS/renderer resources, or runtime validation policy outside the mirrored identifier bounds. |
| MQ-REPLAY-FIXTURE | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_trace_replay_uve.h | `struct MotionQueryTraceReplayFixtureUVE final` | Versioned bounded value-only replay fixture, deterministic comparator, compatibility identity, and field-level mismatch facts. | Native regression harness, baseline registry, editor bridge, stdio transport, and managed parser through copied DTOs. | Fixture data excludes raw runtime handles, timestamps, database ownership, and unbounded diagnostics; comparison is deterministic and sequence-ordered. |
| MQ-REPLAY-BASELINE | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_trace_replay_baseline_registry_uve.h | `class MotionQueryTraceReplayBaselineRegistryUVE final` | Bounded named baseline copies with sorted ordering, mutation generation, and stale-selection guards. | Native editor bridge, named regression workflow, and CI fixture tests. | Registry owns copied fixtures only; it does not own files, assets, live-debug sessions, ECS state, or renderer resources. |
| MQ-LIVE-DEBUG | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_live_debug_trace_persistence_uve.h | `SerializeMotionQueryLiveDebugTraceUVE` | Increment 303 closes the bounded live-debug persistence contract: schema-versioned strict JSON envelopes, copied filter/truncation annotations, 512-event and 1 MiB payload limits, monotonic sequence/frame validation, runtime identity omission, deterministic schema/field/parse diagnostics, and offline session import/export through the existing debugger authority. | Native Motion Query evaluator, debugging/session tests, editor bridge, stdio transport, Avalonia presentation, offline inspection, and future trace analysis/replay consumers. | Persistence owns copied trace evidence and schema validation only; it does not own live runtime identity, databases, ECS state, filesystem policy, trace-provider scheduling, analyzer ownership, or managed UI state. |
| MQ-LIVE-DEBUG-TRACE-ANALYSIS | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_live_debug_trace_persistence_uve.h | `AnalyzeMotionQueryLiveDebugTraceUVE` | Increment 304 adds deterministic bounded copied analysis over live-debug traces: validates the same sequence/frame/cost/event invariants, reports event count, double-precision total cost, maximum evaluated candidates, and lexicographically ordered kind counts, and fails closed at the 512-event cap. | Native Motion Query debugging/analyzer tests, editor offline inspection, managed read-only presentation, and future trace-provider/replay consumers. | Analysis owns only copied aggregate evidence; it does not own trace persistence, runtime scheduling, database handles, ECS state, filesystem policy, or managed UI state. |
| MQ-REPLAY-REGRESSION | engine/plugins/Animation/motion_query/Source/Editor/include/uve/plugins/motion_query_trace_replay_regression_uve.h | `CompareMotionQueryLiveDebugSnapshotAgainstNamedBaselineUVE` | Generation/compatibility-guarded comparison of complete copied live-debug evidence against a named baseline. | Native regression tests, editor bridge workflow, and CI validation. | The harness rejects filtered/empty evidence and returns copied result facts; it never transfers logger, runtime, filesystem, or managed ownership. |
| MANAGED-BRIDGE | editor/managed/UniVex.EditorHost/BridgeProtocolClient.cs | `public sealed class BridgeProtocolClient` | Managed transport/parser boundary for immutable bridge DTOs. | Avalonia editor host and managed tests. | Managed layer owns presentation/session copies only; it cannot write scenes or call native pointers. |
| SCRIPT-FLOW-CONTROL-DISPATCH | engine/scripting/include/uve/scripting/script_compiler_ir_uve.h | `FlowControlDispatch` | Increment 311 adds the version-5 flow-control IR/bytecode contract for Return, Do Once, Gate, and Switch with bounded true/false/default instruction targets; Return terminates, Do Once persists a bounded per-node latch with Reset, Gate persists bounded per-node open state with Open/Close, and Switch routes finite Number values 0.0/other to Case0/Case1 with Default fallback. The built-in catalog expands to 53 descriptors with deterministic display orders 102–105. | Native catalog, compiler, bytecode round-trip, VM, runtime-state, and editor palette tests; ScriptRuntimeUVE validation; future debugger stepping and managed read-only flow presentation. | The contract owns only copied instruction metadata, bounded per-instance flow state, typed Number lookup, and deterministic control routing; it does not own arbitrary graph scheduling, nested subgraphs, ECS/scene state, engine-call authority, unbounded state, or managed mutation. |
| SCRIPT-TYPED-CONVERSION-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ExecuteScriptBytecodeUVE` | Increment 312 adds four explicit, exact-pin conversion nodes: finite Number→Boolean (`0.0` false, non-zero true), Boolean→Number (`0.0`/`1.0`), Vector2→Vector3 with an optional finite Number Z default of `0`, and Vector3→Vector2 dropping Z. Conversion links remain exact-type graph links, compiler staging is bounded to one conversion node and at most two direct inputs, and composed conversion chains are deferred. | Native catalog/compiler/VM/bytecode/runtime/editor-palette tests, ScriptGraphCanvas default metadata, ScriptRuntimeUVE through the shared VM path, and managed read-only palette consumers. | The contract owns only copied finite typed values, explicit conversion semantics, one bounded staging seam, and descriptor defaults; it does not relax graph type compatibility, persist new variable alternatives, own ECS/scene state, infer arbitrary conversions, or create managed mutation authority. |
| MOTION-QUERY-PLUGIN-CONFIGURATION-V1 | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_plugin_module_uve.h | `MotionQueryPluginConfigurationUVE` | Increment 313 adds a bounded copied configuration DTO and strict JSON parser for the Motion Query plugin descriptor, including semantic major.minor.patch version parsing, generic native-manifest validation, bounded relative `Source/Runtime`, `Source/Editor`, `Source/Shared`, and `Tests` layout paths, and explicit parity validation against the native plugin descriptor. | Native Motion Query plugin-module tests, plugin registry lifecycle tests, CMake-linked plugin target, and future editor/plugin discovery consumers. | The contract owns copied configuration text/manifest/layout facts and deterministic diagnostics; it does not read/write files, own plugin registry state, load assets, perform filesystem traversal, expose feature implementation authority, or mutate managed/editor state. |
| PLUGIN-ABI-LIFECYCLE-V1 | engine/plugins/include/uve/plugins/plugin_manifest_validation_uve.h | `NegotiateNativePluginAbiUVE` | Legacy partial closure adds explicit copied engine/plugin protocol negotiation and failure-atomic manifest removal that rejects invalid protocols, unknown identifiers, and removal while a generation-checked registration scope remains open. | Native plugin manifest/registry tests, Motion Query module registration, and future dynamic-loader adapters. | The contract owns protocol/result values and static registry membership only; it does not load dynamic libraries, own OS module handles, bypass capability policy, or expose plugin implementation callbacks. |
| EDITOR-CONSOLE-AUTHORIZATION-V1 | engine/editor/include/uve/editor/developer_console_uve.h | `DeveloperConsoleAccessUVE` | Legacy partial closure adds bounded native console authorization states—Denied, ReadOnly, and Full—with fail-closed command/CVAR registration, execution, mutation, and clear gates; Shipping is permanently Denied, while copied access state reaches the bridge and managed status presentation with legacy Full fallback. | Native DeveloperConsoleUVE/EditorBridge tests, stdio bridge serialization, managed .NET 8 snapshot/parser tests, and Avalonia status presentation. | Authorization owns copied access facts and native mutation gates only; it does not authenticate users, persist credentials, expose secrets, grant managed native ownership, or replace platform/editor session security. |
| MQ-ANIMATION-SETTINGS | engine/plugins/Animation/motion_query/Source/Runtime/include/uve/plugins/motion_query_animation_node_uve.h | `ValidateMotionQueryAnimationNodeSettingsUVE` | Increment 314 extracts one public value-only validation contract for animation-node search/evaluation settings, composing the authoritative LOD result budget, finite non-negative matching weights, supported continuity policy/age, and bounded transition settings including minimum-hold/maximum-window ordering. | Motion Query animation-node evaluation, native runtime tests, editor/read-only settings consumers, and future animation graph adapters. | The validator owns no database, search index, clips, pose, history, asset, ECS, renderer, registry, or managed mutation state; it preserves the existing evaluator result/telemetry path and returns copied diagnostics only. |
| SCRIPT-SCALAR-MATH-V2 | engine/scripting/include/uve/scripting/script_builtin_nodes_uve.h | `RegisterBuiltInScriptNodesUVE` | Increment 315 extends the production visual-scripting catalog to 65 nodes with eight Float functions: Lerp, Remap, Sin, Cos, Tan, Sqrt, Random, and Random Range. The VM consumes finite Number inputs, rejects invalid ranges/domain errors, rejects non-finite outputs, and derives Random/Random Range values from a copied finite Number Seed through a deterministic bit-mixed unit-float policy. | Native catalog/compiler/VM tests, editor bridge palette snapshots, ScriptRuntimeUVE through the shared VM path, managed palette/search consumers, and future numeric node families. | The catalog owns descriptor typing/order/presentation only; Float execution owns copied values and deterministic diagnostics, never graph authoring, ECS/scene state, assets, renderer resources, external RNG state, or hidden engine services. |
| SCRIPT-VECTOR-MATH-V2 | engine/scripting/include/uve/scripting/script_vector2_value_uve.h | `EvaluateScriptVector2DotUVE` | Increment 316 adds seven bounded vector nodes: Vector2 Dot, Distance, Direction, and Lerp, plus Vector3 Distance, Direction, and Lerp. Dot/Distance return finite copied Number values; Direction normalizes a copied To−From vector and rejects zero-length input; Lerp accepts finite A/B/Alpha values without clamping Alpha and rejects non-finite results. Compiler staging accepts only one direct producer per existing typed lane, with new Number outputs approved for Number consumers and new Vector outputs approved for Vector2/Vector3 consumers. | Native catalog/compiler/VM/kernel tests, EditorBridgeUVE palette snapshots, ScriptRuntimeUVE through the shared VM path, managed palette/search consumers, and future vector/rotation families. | The contract owns pure copied vector math and bounded descriptor/staging integration; it does not add rotation or quaternion value ownership, arbitrary graph scheduling, mutation, ECS access, assets, renderer resources, or hidden engine services. |
| MOTION-QUERY-MANAGED-AUTHORING-V1 | editor/managed/UniVex.EditorHost/MainWindow.axaml.cs | `RenderMotionQuery` | Increment 317 adds a bounded Avalonia authoring surface over copied native Motion Query database rows: select/remove existing resources, update display name and schema ID through bounded text commands, update maximum candidates through a positive bounded integer command, and request native validation while preserving bridge revision guards and centralized failure presentation. | Managed .NET 8 host tests, BridgeProtocolClient payload tests, native MotionQueryEditorAuthoringSessionUVE validation/rollback, EditorBridgeUVE copied snapshots, and future database editor/view-model work. | The managed host owns presentation and user input only; C++ retains database contracts, validation, mutation, revisions, history, resource identity, registration, candidate payload construction, and runtime/editor authority. |
| MOTION-QUERY-MANAGED-AUTHORING-METADATA-V1 | editor/managed/UniVex.EditorHost/MainWindow.axaml.cs | `RenderMotionQuery` | Increment 318 extends the managed Motion Query authoring presentation with copied database state facts—candidate count, maximum limit, schema ID, validity, dirty state—and the native 14-command metadata catalog, while retaining revision-guarded command dispatch and native ownership. No typed registration or full candidate payload is added in this increment because the native contract requires bounded context, schema arrays, settings, feature vectors, trajectories, and pose data. | Managed .NET 8 host build/tests, existing snapshot parser metadata assertions, EditorBridgeUVE copied authoring snapshots, native MotionQueryEditorAuthoringSessionUVE metadata authority, and future typed payload work. | The managed host owns presentation only; C++ retains contract validation, mutation, revisions, history, resource identity, registration, candidate construction, and runtime/editor authority. |
| MOTION-QUERY-BRIDGE-PASTE-TARGET-V1 | engine/editor/src/editor_bridge_stdio_uve.cpp | `ParseMotionQueryCommandUVE` | Increment 319 adds a bounded typed Motion Query Copy/Paste bridge payload. The managed host serializes a paste target containing a non-zero resource handle, bounded non-empty display name, bounded non-empty database ID, and non-zero context generation; the native parser rejects malformed or oversized fields before forwarding to the native authoring session, which retains contract validation, identity remapping, copied candidate ownership, undo history, and revision authority. | Native EditorBridgeUVE Copy/Paste regression, managed BridgeProtocolClient payload test, MotionQueryEditorAuthoringSessionUVE, EditorBridgeUVE copied authoring snapshots, and future asset/database authoring consumers. | The bridge transports identity/context metadata only; it does not serialize candidate feature vectors, trajectories, poses, registration descriptors, filesystem state, asset loads, renderer resources, or managed-owned database state. |
| MOTION-QUERY-AUTHORING-HISTORY-V2 | editor/managed/UniVex.EditorHost/EditorBridgeSnapshot.cs | `BridgeMotionQueryAuthoringSnapshot` | Increment 320 completes the managed presentation workflow for native Motion Query authoring history and clipboard state: copied `clipboardAvailable`, `canUndo`, and `canRedo` facts drive bounded Avalonia Undo, Redo, Refresh, Copy, and Paste controls, while command dispatch remains revision-guarded and native-authoritative. | Managed parser state assertions, grouped Undo/Redo/Read Snapshot protocol proof, native MotionQueryEditorAuthoringSessionUVE history/clipboard authority, and EditorBridgeUVE authoring snapshots. | Full typed database registration and feature-vector/trajectory/pose candidate authoring remain separate objectives because their payload schemas and ownership contracts are not dependency-complete in this increment. |
| MOTION-QUERY-AUTHORING-TYPED-PAYLOAD-V3 | engine/editor/src/editor_bridge_stdio_uve.cpp | `ParseMotionQueryCommandUVE` | Increment 321 closes the deferred typed Motion Query authoring wire schema. The managed host now serializes and the native bridge strictly parses bounded database registration and candidate payloads containing resource identity, display/context identity, schema version/ID, trajectory offsets, feature-channel IDs, candidate limits, event facts, candidate identifiers/source clips/sample times, vectors, quaternions, trajectories, skeleton joints, local poses, and evaluation time state. The bridge also completes named Copy/Paste/Undo/Redo command mappings and preserves separate top-level bridge and nested authoring revision guards. | Native editor application bridge probe covering registration, candidate add/remove, and undo; managed .NET 8 payload serialization tests; Avalonia registration and candidate controls; MotionQueryEditorAuthoringSessionUVE contract validation, rollback, capacity, and history authority; native CTest and OpenGL smoke. | The bridge owns only bounded copied transport DTOs and parser rejection; C++ remains authoritative for contract validation, mutation, resource identity, revisions, history, filesystem/assets, runtime evaluation, and managed presentation state. |
| MOTION-QUERY-CONTENT-BROWSER-FOCUS-V1 | engine/editor/include/uve/editor/editor_uve.h | `ContentBrowserTypeFocusUVE` | Increment 322 adds `.uvemotionquery` as a primary Content Browser asset type and a dedicated native/bridge/managed `Motion Query` focus. The filter is current-folder, copied-state presentation only: it classifies case-insensitively, preserves independent Registered correlation, emits stable labels, and never loads assets, performs filesystem I/O, or mutates the asset registry. | Native EditorUVE classifier/focus tests, native EditorBridgeUVE refresh-directory-focus test with a real temporary `.uvemotionquery` entry, managed BridgeProtocolClient focus-token test, and real editor smoke/probe. | The contract owns only extension classification, focus state, copied labels, and deterministic current-folder filtering; Motion Query asset parsing, database loading, registration, runtime evaluation, and filesystem mutation remain separate authorities. |
| WINDOW-DESC-VALIDATION | engine/window/include/uve/window/window_desc_validation_uve.h | `ValidateWindowDescUVE` | Increment 203 adds bounded desktop window descriptor validation: positive width/height, the shared `kMaximumDisplayModeAxisUVE` 16K cap on both axes, non-empty title, and an OpenGL major version of at least 1; the cap prevents uint32-to-GLFW signed-int narrowing overflow and the unsigned minor-version field is inherently nonnegative. Increment 335 adds explicit over-cap width/height regressions. | Native WindowDescValidationUVETest defaults, valid custom descriptor, invalid dimensions/title, and invalid major-version regressions; window library registration; full native/managed/editor smoke matrix. | The contract is value-only and failure-free: it does not create GLFW windows, enumerate or select display modes, own platform backends, manage Android/iOS lifecycle, or mutate the descriptor. |
| NETWORK-RELIABLE-HEADER-WIRE-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `SerializeReliablePacketHeaderUVE` | Increment 290 adds exact 12-byte little-endian serialization and deserialization for `ReliablePacketHeaderUVE`, validates nonzero sequence and acknowledged-sequence fields, rejects malformed sizes, and publishes copied outputs failure-atomically. | Reliable packet assembly preparation, ACK/header diagnostics, native networking tests, and future transport adapters. | The contract owns only fixed header bytes and value validation; it does not serialize payloads, open sockets, schedule timers/retries, own peers, authenticate packets, replicate entities, or manage transport threads. |
| NETWORK-RELIABLE-WINDOW-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `AcceptReliableSequenceUVE` | Increment 204 adds a bounded value-only reliable sequence window: sequence zero is invalid, uint32 ordering is wrap-safe, new packets advance a 32-entry receive history, duplicates and packets older than the retained window are classified deterministically, and the paired ACK helper applies a cumulative base plus 32 forward selective ACK bits to a caller-owned pending mask. | Native ReliablePacketWindowUVETest first/new/out-of-order, duplicate/too-old, wrap-around, cumulative ACK, selective ACK, and invalid-header regressions; native CTest and full editor validation. | The contract owns copied sequence/ACK facts only; it does not open sockets, serialize payloads, schedule retransmission timers, replicate entities, predict gameplay, authenticate peers, or own transport threads. |
| SAVE-CHECKPOINT-RESERVED-SLOTS-V1 | engine/save/include/uve/save/i_checkpoint_manager_uve.h | `CheckpointUVE` | Increment 289 separates automatic and explicit/manual checkpoint persistence: `UpdateUVE()` writes `kAutoSaveSlotIndexUVE`, `CheckpointUVE()` writes `kManualCheckpointSlotIndexUVE`, both reserved slots remain accepted by all save/load/metadata/delete methods, both are hidden from numbered-slot enumeration, and metadata preserves the actual reserved slot identity. | CheckpointManagerUVE, SaveGameSystemUVE reserved-slot persistence, native checkpoint/save tests, and future save UI adapters. | The contract owns no cloud transport, encryption, gameplay-domain state, conflict resolution, filesystem backend beyond the existing save system, or numbered-slot enumeration of reserved checkpoints. |
| SAVE-PAYLOAD-COMPRESSION-V1 | engine/save/include/uve/save/save_payload_compression_uve.h | `CompressSavePayloadUVE` | Increment 205 adds bounded save-only RLE compression for the fixed two-section payload: repetitive payloads receive a magic/size/run stream, incompressible payloads fall back to legacy raw bytes, decompression is failure-atomic, and expanded data is capped at 64 MiB. Increment 284 extends compressed writes with a versioned non-cryptographic payload fingerprint, verifies decompressed bytes failure-atomically, and preserves legacy `UVSC` compressed and raw `.uvesave` payload reads. Existing raw `.uvesave` payloads remain readable. | Six focused codec regressions plus existing save-game round-trip, metadata, migration, truncation, and atomic-write tests; full native/managed/editor validation. | The codec owns only bounded save payload bytes; the universal UVE envelope remains compression method `None`, and encryption, cloud transport, thumbnails, gameplay-domain state, sockets, and backend ownership remain outside this contract. |
| AUDIO-PCM-GAIN-EFFECT-V1 | engine/audio/include/uve/audio/pcm_gain_effect_uve.h | `ApplyPcmGainEffectUVE` | Increment 206 adds a bounded backend-neutral PCM gain kernel over copied normalized float samples: finite nonnegative gain, hard [-1, 1] output clamping, a 1,048,576-sample cap, and failure-atomic output publication. | Three focused native regressions cover scaling/clamping, invalid/non-finite input preservation, and sample-cap rejection; full native/managed/editor validation is required before merge. | The helper owns only copied sample transformation; it does not decode files, stream audio, create devices or voices, route mixer groups, schedule playback, or provide Visual Scripting Audio nodes. |
| INPUT-TOUCH-COORDINATE-POLICY-V1 | engine/input/include/uve/input/touch_coordinate_transform_uve.h | `NormalizeTouchCoordinateUVE` | Increment 207 adds bounded value-only touch coordinate normalization: finite positive viewport dimensions, nonnegative safe-area insets that leave usable space, safe-area-relative normalized coordinates clamped to [0,1], and failure-atomic invalid-input handling. | Three focused native regressions cover safe-area mapping, outside-pixel clamping, and invalid viewport/non-finite pixel preservation; full native/managed/editor validation is required before merge. | The contract owns only coordinate math; it does not poll platform APIs, own Android/iOS lifecycle, choose orientation, mutate touch snapshots, or provide platform gesture policy. |
| PHYSICS-GYROSCOPIC-TORQUE-V1 | engine/physics/include/uve/physics/angular_dynamics_uve.h | `EvaluateGyroscopicTorqueUVE` | Increment 208 adds a bounded value-only diagonal-inertia gyroscopic torque evaluator using `angularVelocity x (inertia * angularVelocity)`, with finite nonnegative inverse-inertia validation and copied output. | Seven focused angular-dynamics native regressions cover existing inertia/torque/impulse behavior plus gyroscopic cross-term and invalid-input cases; full native/managed/editor validation is required before merge. | The helper returns a caller-applied torque term only; it does not mutate rigid bodies, integrate angular state, own constraint islands, choose solver order, or replace backend physics. |
| PREFAB-REVISION-POLICY-V1 | engine/scene/include/uve/scene/prefab_revision_policy_uve.h | `EvaluatePrefabRevisionUVE` | Increment 209 adds bounded prefab source-revision classification: nonzero equal revisions are Current, nonzero mismatches are Stale, and either zero revision is Invalid. | Three focused native regressions cover current, stale, and invalid revision states; full native/managed/editor validation is required before merge. | The contract owns only copied revision facts; it does not read asset files, mutate prefab instances, merge overrides, resolve conflicts, or provide automatic retry/revision tracking persistence. |
| AUDIO-WAV-PCM16-DECODER-V1 | engine/audio/include/uve/audio/wav_pcm16_decoder_uve.h | `DecodeWavPcm16SamplesUVE` | Increment 210 adds bounded RIFF/WAVE PCM16 sample decoding into copied normalized float samples: metadata validation is reused, malformed/truncated chunks fail closed, output publication is failure-atomic, and decoding is capped at 1,048,576 samples. | Two focused native regressions cover normalized sample conversion and unsupported/truncated input with output preservation; full native/managed/editor validation is required before merge. | The contract supports only validated PCM16 WAV bytes; it does not decode other bit depths/codecs, own streams/devices/voices, schedule playback, or create derived audio assets. |
| ANIMATION-RETARGET-MAP-V1 | engine/core/include/uve/core/time_pose_contract_uve.h | `ValidateSkeletonRetargetMapUVE` | Increment 211 adds bounded copied source-to-target skeleton joint-map validation: both skeleton topologies must validate, mapped joints must exist, identifiers must remain bounded, and source and target joints must each be one-to-one. | Four focused native animation-contract regressions cover valid mapping, unknown-joint rejection, duplicate mapping rejection, and existing shared skeleton/pose/time behavior; full native/managed/editor validation is required before merge. | The validator owns no pose conversion, reference-pose adaptation, IK/control generation, asset/resource loading, runtime playback, ECS mutation, or editor persistence. |
| PARTICLE-DRAW-COMMAND-VALIDATION-V1 | engine/render/include/uve/render/particle_draw_command_uve.h | `IsValidParticleDrawCommandUVE` | Increment 212 adds bounded value-only validation for copied particle draw commands: position, remaining lifetime, and sort depth must be finite, lifetime must be nonnegative, and sequence identity must be nonzero. | Five focused native render regressions cover valid commands, invalid finite fields, recorder copying, hard caps, and queue truncation facts; full native/managed/editor validation is required before merge. | The predicate does not simulate particles, allocate GPU buffers, bind pipelines, submit commands, resolve assets, or mutate render queues. |
| NETWORK-RETRANSMIT-BACKOFF-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `ComputeReliableRetryTimeoutUVE` | Increment 285 adds a bounded caller-owned exponential retry-timeout calculator with finite positive base/max validation, retry-zero base semantics, overflow-safe doubling, maximum-timeout clamping, retry-exponent bounds, and failure-atomic output. | Reliable retransmit policy preparation, native networking tests, and future caller timer/backoff integration. | The helper owns no clock, timer, socket, retry queue, packet, peer, authentication, or transport lifecycle, and does not schedule or perform retransmission. |
| NETWORK-RETRANSMIT-POLICY-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `EvaluateReliableRetransmitPolicyUVE` | Increment 213 adds bounded retransmission classification for a caller-supplied elapsed time, positive timeout, retry count, and maximum retry count: Waiting precedes timeout, Due follows timeout, Exhausted occurs at the retry limit, and invalid finite/order inputs fail closed. | Nine focused native networking regressions cover the existing receive/ACK window plus all retransmission statuses; full native/managed/editor validation is required before merge. | The policy owns no clock, timer, socket, packet payload, retry queue, authentication, backoff, or transport lifecycle. |
| SAVE-REVISION-POLICY-V1 | engine/save/include/uve/save/save_revision_policy_uve.h | `EvaluateSaveRevisionUVE` | Increment 214 adds bounded save revision classification from nonzero base, local, and remote revision identities: Unchanged, LocalAhead, RemoteAhead, or Conflict; zero revision facts fail closed as Invalid. | Two focused native save regressions cover all classifications and zero-fact rejection; full native/managed/editor validation is required before merge. | The classifier owns no cloud transport, filesystem state, encryption, merge application, conflict resolution, rewrite-back policy, or gameplay-domain payload. |
| WINDOW-DISPLAY-MODE-VALIDATION-V1 | engine/window/include/uve/window/display_mode_validation_uve.h | `ValidateDisplayModeDescUVE` | Increment 286 adds bounded caller-owned desktop display-mode descriptor validation with positive dimensions, a 16K per-axis cap, and a zero-to-1000 Hz refresh policy; zero refresh requests backend/default selection. | Native display-mode validation regressions, future mode-enumeration/application adapters, and platform diagnostics. | The validator owns no native display mode, monitor handle, hot-plug lifecycle, window mutation, backend selection, or mobile lifecycle. |
| WINDOW-MONITOR-SNAPSHOT-VALIDATION-V1 | engine/window/include/uve/window/monitor_info_validation_uve.h | `ValidateMonitorSnapshotUVE` | Increment 215 adds bounded value-only monitor snapshot validation: every entry requires a bounded non-empty name and positive dimensions, and at most one entry may be marked primary. | Two focused native window regressions cover valid snapshots, empty snapshots, invalid entries, and duplicate-primary rejection; full native/managed/editor validation is required before merge. | The validator owns no monitor handles, hot-plug lifecycle, display-mode application, window placement, backend selection, or mobile platform state. |
| ASSET-PNG-RGBA8-PIXEL-BUDGET-V1 | engine/asset/include/uve/asset/png_metadata_uve.h | `ValidatePngRgba8PixelBudgetUVE` | Increment 216 adds a bounded RGBA8-only decoded-pixel budget predicate over copied PNG metadata: width×height×4 is checked without overflow against a caller cap, with a 64 MiB default and failure for zero dimensions, non-RGBA8 facts, zero caps, and oversized images. | PNG texture-import preflight, native PNG metadata tests, future pixel decoder/`.uvetex` conversion adapters, and asset diagnostics. | The predicate performs no pixel decoding, allocation, CRC verification, filesystem I/O, compression selection, GPU resource creation, or renderer/streaming ownership. |
| ASSET-OBJ-INDEX-RESOLUTION-V1 | engine/asset/include/uve/asset/obj_metadata_uve.h | `ResolveObjIndexUVE` | Increment 217 adds bounded OBJ face-attribute index resolution from standard positive 1-based or negative-relative indices to zero-based copied indices, with zero, empty-domain, out-of-range, and extreme-value rejection and failure-atomic output. | OBJ importer face-token handoff, native OBJ metadata tests, future bounded mesh conversion adapters, and asset diagnostics. | The resolver owns no face-token parsing, polygon triangulation, complete mesh assembly, MTL loading, filesystem I/O, vertex allocation, renderer state, or material conversion. |
| ASSET-GLTF-ACCESSOR-SPAN-V1 | engine/asset/include/uve/asset/gltf_metadata_uve.h | `ValidateGltfAccessorSpanUVE` | Increment 218 adds bounded overflow-safe glTF accessor byte-span validation over copied buffer length, offset, element count, stride, and element size, with a one-million-element default cap, zero-count support, and rejection for invalid ordering or out-of-bounds spans. | glTF importer accessor preflight, native glTF metadata tests, future buffer/image/mesh conversion adapters, and asset diagnostics. | The validator owns no JSON accessor parsing, buffer reads, component decoding, image resolution, mesh allocation, external-resource loading, filesystem I/O, or renderer state. |
| ASSET-MTL-TEXTURE-REFERENCE-V1 | engine/asset/include/uve/asset/mtl_metadata_uve.h | `ValidateMtlTextureReferenceUVE` | Increment 219 adds bounded MTL texture-reference preflight for non-empty relative tokens with a 256-byte default cap, NUL rejection, absolute/drive-qualified path rejection, and `.`/`..`/empty-segment traversal rejection. | MTL importer handoff, native MTL metadata tests, future texture/material conversion adapters, asset diagnostics, and editor import policy. | The validator performs no path canonicalization, filesystem access, asset lookup, texture decode, shader compilation, material conversion, or renderer ownership. |
| ASSET-JPEG-RGBA8-PIXEL-BUDGET-V1 | engine/asset/include/uve/asset/jpeg_metadata_uve.h | `ValidateJpegRgba8PixelBudgetUVE` | Increment 220 adds bounded normalized RGBA8 decoded-pixel budgeting from copied JPEG SOF facts: 8-bit precision and one-to-four components are required, width×height×4 is checked overflow-safely against a 64 MiB default cap, and invalid/oversized facts fail closed. | JPEG texture-import preflight, native JPEG metadata tests, future entropy decoder/`.uvetex` conversion adapters, and asset diagnostics. | The predicate performs no entropy decoding, pixel allocation, codec selection, filesystem I/O, color conversion, compression, GPU resource creation, or renderer ownership. |
| AUDIO-WAV-PCM16-SAMPLE-WINDOW-V1 | engine/audio/include/uve/audio/wav_pcm16_decoder_uve.h | `ValidateWavPcm16SampleWindowUVE` | Increment 221 adds bounded PCM16 streaming/chunk-window preflight over copied total/start/requested sample counts, requiring positive request size, an in-range end, and a caller cap defaulting to 1,048,576 samples with failure-safe subtraction-based bounds checks; Increment 334 aligns `PlanPcm16StreamWindowUVE` with `AdvancePcm16StreamCursorUVE` by rejecting total streams above the caller cap before publishing a refill plan, while preserving atomic invalid-input handling and looping facts. | Future audio stream/chunk adapters, WAV decoder tests, audio import preparation, and native audio diagnostics. | The validator and planner own no file cursor mutation, refill scheduler, seek state, decoded buffer, voice, device, platform backend, or stream lifetime. |
| INPUT-TOUCH-ORIENTATION-V1 | engine/input/include/uve/input/touch_coordinate_transform_uve.h | `ApplyTouchOrientationUVE` | Increment 222 adds bounded value-only cardinal touch orientation mapping for normalized coordinates: Deg0, Deg90, Deg180, and Deg270 transform finite [0,1] inputs deterministically, while invalid finite ranges and unknown enum values fail closed without mutating output. | Mobile touch adapters, safe-area normalization handoff, native input tests, gameplay/mobile input consumers, and future Android/iOS policy adapters. | The policy owns no device orientation query, platform polling, lifecycle, permissions, touch snapshot mutation, gesture recognition, ECS state, or editor-camera behavior. |
| NETWORK-RELIABLE-PAYLOAD-BUDGET-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `ValidateReliablePayloadBudgetUVE` | Increment 223 adds bounded caller-owned payload-size preflight with a 1200-byte default datagram cap, zero-length payload acceptance for control packets, custom cap support, and fail-closed oversized-payload rejection. | Reliable transport handoff, packet assembly diagnostics, future fragmentation/serialization adapters, native networking tests, and replication preflight. | The predicate owns no payload bytes, serializer, compressor, fragmenter, socket, peer identity, clock, retry timer, authentication, replication, or prediction state. |
| SAVE-SYNC-ACTION-POLICY-V1 | engine/save/include/uve/save/save_revision_policy_uve.h | `EvaluateSaveSyncActionUVE` | Increment 224 adds a bounded status-to-action policy mapping Unchanged→NoOp, LocalAhead→Upload, RemoteAhead→Download, Conflict→Conflict, and Invalid→Invalid from caller-owned revision facts. | Save/cloud synchronization planning, native revision tests, future transport adapters, editor diagnostics, and conflict-resolution UX preflight. | The policy performs no cloud transport, upload/download, encryption, merge application, rewrite-back, save persistence, filesystem I/O, or gameplay-state mutation. |
| PHYSICS-CCD-ELIGIBILITY-V1 | engine/physics/include/uve/physics/character_controller_uve.h | `EvaluateCcdEligibilityUVE` | Increment 225 adds bounded finite displacement-threshold classification for continuous-collision eligibility: non-finite displacement or non-positive threshold returns Invalid, finite movement below threshold returns Disabled, and movement at/above threshold returns Enabled; Increment 333 also classifies finite vectors whose squared length overflows as Invalid instead of silently treating their length as zero. | Character-controller TOI handoff, future dynamic-body CCD adapters, native physics tests, and movement diagnostics including overflow-length regressions. | The policy performs no sweep, broad-phase query, TOI solve, collision response, rigid-body mutation, backend integration, or persistent controller-state ownership. |
| AUDIO-PCM-GAIN-CHAIN-V1 | engine/audio/include/uve/audio/pcm_gain_effect_uve.h | `ApplyPcmGainEffectChainUVE` | Increment 226 adds ordered copied-sample processing through up to eight finite nonnegative PCM gain stages, reusing hard [-1,1] per-stage clamping, the one-million-sample cap, and failure-atomic output publication; Increment 327 wraps all chain allocations, including empty-chain copying, so `std::bad_alloc` returns false without terminating or publishing partial output, while valid stage ordering remains unchanged. | Audio effect-chain preparation, mixer diagnostics, streaming chunk processing, native audio tests including invalid-stage regression, and future concrete backend adapters. | The chain owns no mixer schedule, routing graph, stream refill/seek, voice, device, thread, asset, or sample-buffer lifetime. |
| INPUT-TOUCH-CHORD-V1 | engine/input/include/uve/input/mobile_gesture_recognizer_uve.h | `EvaluateTouchChordUVE` | Increment 227 adds exact-count multi-touch chord preflight over one copied snapshot: it requires two to ten active touches, unique nonzero identifiers, finite positions, and publishes a finite centroid failure-atomically. | Mobile gesture adapters, pinch/rotate gesture preparation, input diagnostics, native mobile tests, and future platform gesture frameworks. | The predicate owns no touch polling, frame history, gesture lifecycle, platform framework, coordinate conversion, event emission, ECS state, or application lifecycle. |
| RENDER-PARTICLE-SNAPSHOT-GATE-V1 | engine/render/include/uve/render/particle_render_bridge_uve.h | `ValidateParticleRenderSnapshotUVE` | Increment 228 adds bounded copied particle render-snapshot validation for source-count/truncation consistency, caller item caps, valid entities/sequences, finite positions/depth/lifetimes, and fail-closed invalid batches. | Particle render bridge handoff, draw-command recording, renderer diagnostics, native render tests, and future GPU submission adapters. | The gate performs no GPU allocation, asset lookup, renderer registration, camera culling, sort/submission, particle-runtime mutation, or device ownership. |
| NETWORK-RELIABLE-REASSEMBLY-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `AcceptReliablePayloadFragmentUVE` | Increment 283 adds bounded caller-owned reliable payload reassembly with a 1024-fragment cap, 1200-byte per-fragment cap, deterministic Accepted/Duplicate/Complete/Conflict/Invalid statuses, out-of-order copied-byte assembly, duplicate idempotence, message-conflict rejection, ordered output publication, and state reset. | Reliable packet fragmentation planning, native networking tests, and future transport/reassembly integration. | The reassembly state owns only copied fragment bytes and message metadata; it does not own sockets, packet headers, peers, retransmission timers, authentication, serialization, replication, prediction, reconciliation, or network threads. |
| NETWORK-FRAGMENT-PLAN-V1 | engine/network/include/uve/network/reliable_packet_window_uve.h | `PlanReliablePayloadFragmentsUVE` | Increment 229 adds bounded caller-owned payload fragment planning: overflow-safe ceil-division for fragment count, per-fragment datagram budget (≤1200 bytes), final-fragment sizing, and failure-atomic output under a 1024-fragment cap. | Reliable UDP fragmentation preparation, datagram sizing, native network tests, and future transport adapters. | The planner performs no serialization, socket I/O, reassembly, authentication, retry scheduling, or packet-buffer ownership. |
| GLTF-RESOURCE-URI-POLICY-V1 | engine/asset/include/uve/asset/gltf_metadata_uve.h | `ClassifyGltfResourceUriUVE` | Increment 230 adds bounded glTF image/buffer URI classification: safe relative paths and `data:` URIs are distinguished, absolute/scheme paths, empty segments, traversal, percent-encoded dot/separator escapes, control bytes, malformed escapes, and tokens over 4096 bytes fail closed. | glTF image/buffer import preparation, URI diagnostics, native asset tests, and future filesystem/data decoders. | The classifier performs no filesystem resolution, path canonicalization, data-URI decoding, image/buffer parsing, allocation, or derived-asset conversion. |
| GLTF-RESOURCE-VFS-JOIN-V1 | engine/asset/include/uve/asset/gltf_metadata_uve.h | `ResolveGltfResourceVirtualPathUVE` | Increment 231 adds bounded failure-atomic relative glTF resource composition against an asset virtual path: it normalizes resource backslashes to VFS separators and rejects data URIs, invalid/traversal paths, and combined outputs over 4096 bytes. | glTF image/buffer VFS handoff, native asset tests, and future resource readers. | The join policy performs no filesystem I/O, mount lookup, data-URI decoding, image/buffer parsing, allocation ownership, or derived-asset conversion. |
| GLTF-DATA-URI-DECODE-V1 | engine/asset/include/uve/asset/gltf_metadata_uve.h | `DecodeGltfDataUriUVE` | Increment 232 adds bounded glTF data-URI payload decoding for percent-encoded bytes and strict base64 bytes, with a 64 MiB caller cap, malformed-input rejection, and failure-atomic output publication. | glTF embedded image/buffer payload preparation, native asset tests, and future media-specific decoders. | The decoder does not interpret media types, create image/buffer objects, parse image/buffer formats, perform filesystem I/O, or convert derived assets. |
| OBJ-FACE-VERTEX-RESOLUTION-V1 | engine/asset/include/uve/asset/obj_metadata_uve.h | `ResolveObjFaceVertexUVE` | Increment 233 adds bounded complete OBJ face-vertex token resolution for `v`, `v/vt`, `v//vn`, and `v/vt/vn`: positive 1-based and negative-relative indices are copied to zero-based fields, optional attributes are represented explicitly, malformed/out-of-range tokens fail closed, and output publication is failure-atomic. | OBJ mesh-import preparation, face diagnostics, native asset tests, and future triangulation/mesh adapters. | The resolver performs no face triangulation, mesh allocation, material-library loading, filesystem I/O, or derived mesh conversion. |
| MTL-MATERIAL-PROPERTY-V1 | engine/asset/include/uve/asset/mtl_metadata_uve.h | `ParseMtlMaterialPropertyUVE` | Increment 234 adds bounded MTL property facts for finite scalar directives, three-component vector directives, and one validated texture-reference token with exact arity and failure-atomic copied output. | MTL source-import preparation, native material tests, and future material/texture adapters. | The parser does not handle map options, load textures, compile shaders, build material assets, perform filesystem I/O, or convert renderer state. |
| MTL-TEXTURE-MAP-OPTIONS-V1 | engine/asset/include/uve/asset/mtl_metadata_uve.h | `ParseMtlTextureMapUVE` | Increment 235 adds bounded MTL texture-map option parsing for one validated relative reference, three-component `-s` scale and `-o` offset values, and `-clamp on or off`, with deterministic defaults and failure-atomic copied output. | MTL map-option preflight, native asset tests, and future texture/material adapters. | The parser does not handle unmodeled map options, load textures, perform filesystem I/O, compile shaders, construct materials, or convert renderer state. |
| PNG-RGBA8-SCANLINE-UNFILTER-V1 | engine/asset/include/uve/asset/png_metadata_uve.h | `UnfilterPngRgba8ScanlineUVE` | Increment 236 adds bounded RGBA8 PNG scanline reconstruction for None, Sub, Up, Average, and Paeth filters with a 4 MiB row cap, previous-row shape validation, and failure-atomic output publication. | PNG decoded-row preparation, native image tests, and future IDAT/texture adapters. | The primitive does not decompress zlib/IDAT data, parse PNG chunks, handle interlace passes, allocate images/GPU resources, perform filesystem I/O, or convert textures. |
| WAV-PCM16-SAMPLE-WINDOW-DECODE-V1 | engine/audio/include/uve/audio/wav_pcm16_decoder_uve.h | `DecodeWavPcm16SampleWindowUVE` | Increment 237 adds direct bounded PCM16 WAV sample-window decoding into copied normalized float samples, with validated range/cap checks, malformed-input rejection, and failure-atomic output publication. | Audio streaming preparation, native audio tests, and future refill/seek adapters. | The decoder does not own persistent cursors, refill scheduling, mixer routing, voices, devices, platform backends, or filesystem paths. |
| WAV-PCM16-STREAM-CURSOR-ADVANCE-V1 | engine/audio/include/uve/audio/wav_pcm16_decoder_uve.h | `AdvancePcm16StreamCursorUVE` | Increment 288 adds bounded caller-owned PCM16 cursor advancement: non-looping cursors clamp at totalSamples, looping cursors wrap after the end, an end cursor normalizes deterministically for looping, totals/cursors/caps are validated, and output publication is failure-atomic. | Audio stream adapters, refill-window consumers, native decoder tests, and future persistent stream runtime integration. | The helper does not decode bytes, mutate external state, schedule refills, own files/cursors/devices/voices, route mixers, or select a platform backend. |
| WAV-PCM16-STREAM-WINDOW-PLAN-V1 | engine/audio/include/uve/audio/wav_pcm16_decoder_uve.h | `PlanPcm16StreamWindowUVE` | Increment 238 adds bounded non-mutating PCM16 refill-window planning with contiguous start/count/next-cursor facts, explicit end-of-stream signaling, loop-wrap signaling, and a caller-selected sample cap. | Audio stream preparation, refill adapters, native audio tests, and future persistent stream cursors. | The planner does not mutate cursors, decode bytes, schedule refills, own voices/devices, route mixers, or select platform backends. |
| AUDIO-SCHEDULED-GAIN-WINDOW-V1 | engine/audio/include/uve/audio/pcm_gain_effect_uve.h | `ApplyScheduledPcmGainEffectsUVE` | Increment 239 adds ordered bounded PCM gain-window processing over copied samples, with overlap-by-caller-order, per-window hard clamping, finite/nonnegative validation, an eight-window cap, and failure-atomic output publication. | Audio effect scheduling preparation, native gain tests, and future mixer/effect adapters. | The helper does not own mixer routing, device state, persistent streams, voices, effect scheduling clocks, or platform backends. |
| AUDIO-FINAL-MIX-EVALUATOR-V1 | engine/audio/include/uve/audio/audio_mixer_group_uve.h | `EvaluateAudioMixParametersUVE` | Increment 240 adds bounded finite source-volume/source-pitch, mixer multiplier, and attenuation composition into clamped device-facing gain and pitch, with failure-atomic output and AudioSystemUVE fail-closed integration. | Audio mixer parameter policy, native mixer tests, and future backend adapters. | The evaluator does not perform device I/O, mutate voices, own persistent mixer state, schedule streams/effects, or select platform backends. |
| AUDIO-VOICE-PARAMS-VALIDATION-V1 | engine/audio/include/uve/audio/audio_voice_desc_uve.h | `ValidateAudioVoiceParamsUVE` | Increment 241 adds bounded final voice-parameter validation for finite position components, gain in [0, 1], and nonnegative finite pitch; NullAudioDeviceUVE rejects invalid parameters before recording calls. | Backend-neutral audio device boundaries, native NullAudioDevice tests, and future concrete backend adapters. | The validator performs no device I/O, voice mutation, persistent mixer/stream ownership, scheduling, or platform backend selection. |
| TOUCH-LIFECYCLE-TRANSITION-V1 | engine/input/include/uve/input/i_mobile_input_system_uve.h | `EvaluateTouchLifecycleTransitionUVE` | Increment 242 adds bounded per-slot touch lifecycle transition classification (None/Began/Moved/Ended/Replaced) from copied previous/current TouchPointStateUVE frames, with stable-identifier and finite-field validation, and failure-atomic output. | Mobile input lifecycle tracking, native input tests, and future platform touch adapters. | The classifier does not poll hardware, own platform lifecycle state, mutate snapshots, or select a platform backend. |
| TOUCH-CHORD-LIFECYCLE-V1 | engine/input/include/uve/input/mobile_gesture_recognizer_uve.h | `EvaluateTouchChordLifecycleTransitionUVE` | Increment 243 adds bounded exact-count copied previous/current multi-touch chord lifecycle classification (None/Began/Moved/Ended/Replaced) with unique stable identifiers, finite touch-field validation, and failure-atomic output. | Mobile gesture lifecycle preparation, native gesture tests, and future platform gesture adapters. | The classifier does not poll touch hardware, own gesture-framework state, mutate snapshots, retain lifecycle history, or select a platform backend. |
| GAMEPAD-CONNECTION-TRANSITION-V1 | engine/input/include/uve/input/i_gamepad_input_system_uve.h | `EvaluateGamepadConnectionTransitionUVE` | Increment 244 adds bounded copied gamepad Connected/Disconnected/None edge classification with finite axis validation and failure-atomic output. | Backend-neutral gamepad lifecycle preparation, native input tests, and future GLFW/mobile device adapters. | The classifier does not poll hardware, own device handles, mutate snapshots, map native layouts, or select a platform backend. |
| DESKTOP-INPUT-BRIDGE-V1 | engine/window/include/uve/window/i_window_manager_uve.h | `AttachInputSystemUVE` | Increment 245 adds a borrowed desktop input bridge from the WindowManagerUVE backend into the existing InputSystemUVE injection seam, with GLFW keyboard/mouse/scroll polling and poll-before-input-commit frame ordering. | WindowManagerUVE, InputSystemUVE, EngineCoreUVE, native window/input tests, and future desktop backend adapters. | The window manager borrows but never owns the input sink; GLFW remains confined to the PIMPL, NullWindowManagerUVE stays inert, and the bridge does not provide Android/iOS lifecycle, mobile polling, or platform gesture-framework ownership. |
| MOBILE-LIFECYCLE-TRANSITION-V1 | engine/input/include/uve/input/i_mobile_input_system_uve.h | `EvaluateMobileLifecycleTransitionUVE` | Increment 246 adds bounded copied Inactive/Active/Suspended/Terminated mobile application-lifecycle state classification into Activated/Resumed/Suspended/Deactivated/Terminated/Reinitialized/None transitions with invalid-state rejection and failure-atomic output. | Android/iOS lifecycle adapter preparation, native mobile input tests, and future platform lifecycle integrations. | The classifier does not receive native callbacks, own process/application state, mutate touch snapshots, manage suspension resources, or select a mobile backend. |
| TOUCH-VIEWPORT-SAFE-AREA-VALIDATION-V1 | engine/input/include/uve/input/touch_coordinate_transform_uve.h | `ValidateTouchCoordinateViewportUVE` | Increment 247 adds bounded copied finite positive viewport and ordered safe-area validation for mobile coordinate adapters. | Mobile coordinate normalization preparation, native touch-coordinate tests, and future Android/iOS safe-area adapters. | The validator does not acquire native metrics, own orientation/lifecycle state, map platform pixels, or mutate touch snapshots. |
| MOBILE-INPUT-POLICY-V1 | engine/input/include/uve/input/i_mobile_input_system_uve.h | `EvaluateMobileInputPolicyUVE` | Increment 248 adds bounded copied lifecycle/focus/background-allowance policy classification into Disabled/TouchOnly/TouchAndGyroscope availability with invalid-state rejection and failure-atomic output. | Mobile input policy preparation, native mobile input tests, and future Android/iOS focus/background integrations. | The policy evaluator does not receive native callbacks, mutate touch or gyroscope snapshots, manage background execution, or select a platform backend. |
| PHYSICS-GYROSCOPIC-INTEGRATION-V1 | engine/physics/src/physics_system_uve.cpp | `PhysicsSystemUVE::StepUVE` | Increment 249 integrates the validated diagonal-inertia gyroscopic torque term into PhysicsSystemUVE angular-velocity advancement with finite fail-closed helper usage and deterministic nonzero-spin regression coverage. | Rigid-body angular simulation, PhysicsSystem native tests, and future broader gyroscopic/constraint integration. | The increment does not add full orientation-dependent inertia tensors, angular constraint islands, backend-wide CCD, or platform-specific physics backends. |
| PHYSICS-CONSTRAINT-ISLAND-PLAN-V1 | engine/physics/include/uve/physics/physics_constraint_system_uve.h | `BuildConstraintIslandPlanUVE` | Increment 250 adds a bounded failure-atomic deterministic connected-component planner over copied constraint entity edges with fixed-cap entity/island output. | Constraint-system preparation, native physics tests, and future deterministic island solver partitioning. | The planner does not mutate the constraint registry, partition or execute solver iterations, own entities, persist islands, or select a physics backend. |
| RENDER-PARTICLE-GPU-UPLOAD-PLAN-V1 | engine/render/include/uve/render/particle_draw_command_uve.h | `PlanParticleGpuUploadUVE` | Increment 251 adds bounded fixed-stride overflow-safe byte planning over validated copied particle draw commands with source/truncation consistency and failure-atomic rejection. | Particle render handoff, native renderer tests, and future production GPU buffer submission. | The planner does not allocate GPU buffers, resolve materials, bind pipelines, submit OpenGL commands, or mutate the particle recording. |
| ASSET-OBJ-MESH-CONVERSION-V1 | engine/asset/include/uve/asset/obj_mesh_converter_uve.h | `ConvertObjMeshUVE` | Increment 271 adds bounded failure-atomic OBJ-to-MeshAssetUVE conversion with finite position/UV/normal parsing, supported `v`/`v/vt`/`v//vn`/`v/vt/vn` token resolution, positive/negative indices, polygon fan triangulation, computed fallback face normals, copied local bounds, and generated tangents. | MeshAssetUVE CPU envelope preparation, native OBJ/mesh tests, and future raw OBJ importer registration. | The converter caps source/output sizes, does not load MTL files, resolve textures, preserve groups/material assignments, register `.obj`, perform filesystem I/O, or allocate GPU resources. |
| ASSET-GLTF-PRIMITIVE-CONVERSION-V1 | engine/asset/include/uve/asset/gltf_mesh_converter_uve.h | `ConvertGltfPrimitiveUVE` | Increment 275 adds bounded failure-atomic glTF TRIANGLES primitive conversion from caller-owned validated float32 position/normal/UV and U8/U16/U32 index accessor views into MeshAssetUVE vertices, indices, fallback face normals, local bounds, and generated tangents. | MeshAssetUVE CPU envelope preparation, native glTF/mesh tests, and future raw glTF importer integration. | The converter owns no JSON or GLB parsing, URI or buffer resolution, filesystem I/O, material/image decoding, multiple-primitive/scene assembly, skinning/LOD data, asset registration, or GPU resources. |
| ASSET-GLTF-FILESYSTEM-IMPORT-V1 | engine/asset/include/uve/asset/gltf_importer_uve.h | `RegisterGltfImporterUVE` | Increment 276 registers bounded `.gltf` and `.glb` filesystem bridges for one buffer, one mesh, one TRIANGLES primitive, safe relative/data-URI/BIN bytes, delegated `ConvertGltfPrimitiveUVE` conversion, and temporary-sibling `.uvemodel` publication. | Raw glTF/GLB source import, typed mesh-envelope loading, native importer/converter tests, and future broader glTF scene/material/image integration. | The bridge owns no multi-buffer or multi-primitive assembly, scene/node transforms, materials, images, skins, LODs, VFS mounts, GPU resources, background workers, or asset-manager state. |
| ASSET-OBJ-FILESYSTEM-IMPORT-V1 | engine/asset/include/uve/asset/obj_importer_uve.h | `RegisterObjImporterUVE` | Increment 272 registers the bounded raw-filesystem OBJ importer, reads capped source bytes, converts supported forms through `ConvertObjMeshUVE`, persists one validated MeshAssetUVE as a `.uvemodel` destination through temporary sibling rename, and leaves generic AssetImporterUVE database registration unchanged. | Raw model source import, typed mesh-envelope loading, native importer/converter tests, and future MTL/material integration. | The importer owns no VFS mounts, bundle resolution, MTL files, texture loading, group/material preservation, GPU resources, background workers, or asset-manager state. |
| ASSET-MTL-MATERIAL-CONVERSION-V1 | engine/asset/include/uve/asset/mtl_material_converter_uve.h | `ConvertMtlMaterialUVE` | Increment 273 adds bounded failure-atomic conversion of one `newmtl` block into MaterialAssetUVE, reusing validated Kd/Ke/Ks/Ns/d/Tr scalar/vector facts and strict map syntax; Kd/Ke/Ks/Ns/d/Tr map to representable PBR fields while texture maps remain syntax-only evidence. | MaterialAssetUVE CPU envelope preparation, native MTL/material tests, and future raw MTL importer registration. | The converter caps source size, rejects multiple/implicit materials and invalid dissolve ranges, fabricates no texture/shader GUIDs, performs no filesystem or texture I/O, and owns no importer or GPU resources. |
| ASSET-MTL-FILESYSTEM-IMPORT-V1 | engine/asset/include/uve/asset/mtl_importer_uve.h | `RegisterMtlImporterUVE` | Increment 274 registers the bounded raw-filesystem MTL importer, reads capped source bytes, converts supported forms through `ConvertMtlMaterialUVE`, persists one validated MaterialAssetUVE as a `.uvemat` destination through temporary sibling rename, and leaves generic AssetImporterUVE database registration unchanged. | Raw material source import, typed material-envelope loading, native importer/converter tests, and future texture/shader resolution. | The importer owns no VFS mounts, bundle resolution, texture or shader lookup, unsupported map options, multiple-material publication, GPU resources, background workers, or asset-manager state. |
| ASSET-JPEG-FILESYSTEM-IMPORT-V1 | engine/asset/include/uve/asset/jpeg_importer_uve.h | `RegisterJpegImporterUVE` | Increment 277 registers bounded `.jpg` and `.jpeg` filesystem bridges, decodes baseline/progressive JPEG through `DecodeJpegRgba8ImageUVE`, validates RGBA8 byte counts, and publishes `.uvetex` textures through temporary-sibling rename. | Raw JPEG texture source import, typed texture-envelope loading, native importer/decoder tests, and future texture/GPU integration. | The bridge owns no JPEG codec implementation, filesystem mounts, GPU resources, background workers, texture compression/mipmap/streaming policy, or asset-manager state. |
| ASSET-AUDIO-ENVELOPE-V1 | engine/asset/include/uve/asset/audio_asset_uve.h | `AudioAssetUVE` | Increment 278 adds the bounded typed PCM16-derived audio envelope with explicit channel count, sample rate, normalized interleaved samples, finite/range/cap validation, failure-atomic load, and `AssetKindUVE::Audio` serialization for `.uveaudio` output. | Typed audio asset persistence, native audio-envelope tests, and future audio runtime clip resolution. | The envelope owns no WAV parsing, codec selection, stream cursor, mixer routing, voice/device/backend lifetime, compression, streaming, or GPU resources. |
| ASSET-WAV-FILESYSTEM-IMPORT-V1 | engine/audio/include/uve/audio/wav_importer_uve.h | `RegisterWavImporterUVE` | Increment 278 registers the bounded `.wav` bridge, reads capped source bytes, validates PCM metadata, decodes normalized PCM16 samples through the existing decoder, and publishes `.uveaudio` through a temporary sibling before EngineCore-owned importer registration reaches the generic database pipeline. | Raw WAV source import, typed audio-envelope loading, native importer/decoder tests, and future audio clip/runtime integration. | The bridge owns no asset-target-to-audio dependency cycle, codec implementation, stream cursor, refill scheduling, mixer/voice/device/backend lifetime, compression, or background worker. |
| SCENE-MESH-RENDER-ELIGIBILITY-V1 | engine/render/include/uve/render/mesh_render_eligibility_uve.h | `EvaluateMeshRenderEligibilityUVE` | Increment 279 adds a bounded value-only scene-to-render eligibility evaluator for mesh/material GUID coherence, finite world-transform normalization, ordered finite local/world bounds, frustum visibility, finite near-plane sort depth, and copied matrix/bounds facts; MeshRendererUVE reports non-asset rejections through `RenderQueueUVE::invalidRenderEligibility`. | MeshComponentUVE/WorldTransformComponentUVE handoff, RenderQueueUVE diagnostics, renderer extraction tests, and future GPU submission integration. | The evaluator performs no asset loading, material resolution, GPU allocation, command submission, scene mutation, or renderer-resource lifetime ownership. |
| SCENE-PREFAB-REVISION-PERSISTENCE-V1 | engine/scene/include/uve/scene/i_prefab_system_uve.h | `InstantiateWithRevisionUVE` | Increment 280 adds backward-compatible persisted `sourceRevision` and `instanceRevision` fields to PrefabInstanceComponentUVE with legacy default 1, stamps both fields through the concrete revision-aware instantiation path, and rejects zero source revisions before loading or entity mutation. | Prefab revision-aware instantiation, scene serializer round-trip/legacy-load tests, and future explicit refresh/merge policy. | The extension performs no asset revision lookup, automatic merge, conflict resolution, source-prefab mutation, cloud sync, or editor UX; legacy interface implementations retain the old `InstantiateUVE` behavior through the non-pure default method. |
| PHYSICS-CONSTRAINT-ISLAND-EXECUTION-V1 | engine/physics/include/uve/physics/physics_constraint_system_uve.h | `SolveUVE` | Increment 281 consumes the bounded deterministic connected-component plan, orders occupied constraint slots by ascending island while preserving slot order within each island, and exposes copied `islandCount` and `islandPlanValid` diagnostics before positional solving; Increment 326 makes overflowed finite constraint geometry fail closed as a skipped constraint and preflights both proposed local transform deltas through `IsTransformComponentValidUVE` before either body write, preventing non-finite publication or one-sided pair mutation. | Constraint-system solver integration, deterministic island regressions, overflowed-geometry and pair-atomic correction regressions, and future parallel/backend execution. | The contract changes no distance/hinge equations and owns no parallel scheduling, backend solver, island persistence, CCD sweep, or entity lifetime. |
| RENDER-TEXTURE-UPLOAD-V1 | engine/render/include/uve/render/render_resource_descs_uve.h | `ValidateTextureUploadUVE` | Increment 270 adds a bounded value-only texture descriptor and level-0 initial-data validator requiring positive dimensions, nonzero mip count, known format, overflow-safe expected byte sizing, and exact non-empty upload size; NullRenderDeviceUVE and GlRenderDeviceUVE reject invalid inputs before allocation, while Renderer3DUVE avoids caching an invalid texture handle and falls back atomically. | TextureAssetUVE-to-RHI upload, renderer texture cache/reload ownership, Null/OpenGL backend tests, and future GPU texture integration. | The validator performs no allocation or backend calls; it does not generate mipmaps, compress textures, stream data, own asset handles, or define broader backend resource lifetime. |
| PNG-RGBA8-IMAGE-DECODE-V1 | engine/asset/include/uve/asset/png_metadata_uve.h | `DecodePngRgba8ImageUVE` | Increment 252 adds bounded 8-bit non-interlaced RGBA PNG chunk/CRC validation, zlib IDAT inflation, scanline reconstruction, and failure-atomic copied pixel publication under the existing decoded-pixel budget; Increment 255 extends the same bounded path to non-interlaced 8-bit RGB PNG input with RGB-to-opaque-RGBA8 expansion; Increment 256 extends it to non-interlaced 8-bit grayscale PNG input with grayscale-to-opaque-RGBA8 expansion; Increment 257 extends it to non-interlaced 8-bit indexed PNG input with strict PLTE/tRNS palette validation, palette-index bounds checks, and copied RGBA8 publication; Increment 258 extends it to non-interlaced 16-bit RGBA PNG input with big-endian high-byte down-conversion to RGBA8; Increment 259 extends it to non-interlaced 16-bit grayscale PNG input with high-byte grayscale replication to opaque RGBA8; Increment 260 extends it to non-interlaced 16-bit RGB PNG input with high-byte RGB conversion to opaque RGBA8; Increment 261 extends it to Adam7-interlaced 8-bit RGBA PNG input with overflow-safe seven-pass planning, per-pass unfilter state, final-coordinate RGBA8 scatter, and failure-atomic copied output; Increment 262 extends the same Adam7 pass decoder to 8-bit RGB PNG input with opaque-alpha normalization and failure-atomic copied output; Increment 263 extends it to 8-bit grayscale PNG input with replicated opaque RGBA8 output and failure-atomic copied publication; Increment 264 extends it to 8-bit indexed PNG input with validated PLTE/tRNS palette mapping, palette-index bounds checks, and failure-atomic copied RGBA8 publication; Increment 265 extends the same Adam7 pass decoder to 16-bit RGBA PNG input with big-endian high-byte RGBA8 conversion and failure-atomic copied output; Increment 266 extends the Adam7 pass decoder to 16-bit grayscale PNG input with high-byte grayscale replication to opaque RGBA8 and failure-atomic copied output; Increment 267 extends the Adam7 pass decoder to 16-bit RGB PNG input with high-byte RGB conversion to opaque RGBA8 and failure-atomic copied output; Increment 268 extends the Adam7 pass decoder to 16-bit grayscale-alpha PNG input with high-byte grayscale and alpha conversion to RGBA8 and failure-atomic copied output. | Texture source import, native asset tests, and future derived texture/GPU upload integration. | The decoder rejects unsupported PNG modes, owns no filesystem path, does not decode JPEG or other image formats, and does not allocate GPU resources or submit textures. |
| PNG-FILESYSTEM-IMPORT-V1 | engine/asset/include/uve/asset/png_importer_uve.h | `RegisterPngImporterUVE` | Increment 269 registers a bounded raw-filesystem PNG importer that reads capped source bytes, decodes supported forms through `DecodePngRgba8ImageUVE`, persists validated RGBA8 `TextureAssetUVE` data to a `.uvetex` destination through temp-file publication, and leaves generic `AssetImporterUVE` database registration unchanged. | Raw texture source import, typed texture-envelope loading, and future derived texture/GPU upload integration. | The importer owns no VFS mounts, bundle resolution, GPU resources, background workers, texture compression, JPEG/other format conversion, or asset-manager state. |
| JPEG-RGBA8-IMAGE-DECODE-V1 | engine/asset/include/uve/asset/jpeg_metadata_uve.h | `DecodeJpegRgba8ImageUVE` | Increment 253 adds bounded baseline JPEG RGBA8 decoding via libjpeg with explicit error recovery, RGB-to-RGBA expansion, and failure-atomic copied pixel publication; Increment 254 extends the same bounded recovery and budget path to progressive JPEG RGBA8 input. | Texture source import, native asset tests, and future derived texture/GPU upload integration. | The decoder rejects unsupported/unsafe input atomically, owns no filesystem path, does not decode PNG or other image formats, and does not allocate GPU resources or submit textures. |
| SCRIPT-AUDIO-SET-VOLUME-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioScalarControlFunctionUVE` | Increment 291 adds the bounded Visual Scripting `audio.set_volume` contract: a caller-owned source Entity and finite volume in [0, 1] are copied into a typed callback, callback acceptance is required, and a copied Boolean `Result` is published failure-atomically through both VM execution paths; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, and fail-closed regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, volume validation, callback dispatch, and Boolean result publication; it does not own audio assets, voice handles, playback scheduling, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| SCRIPT-AUDIO-SET-PITCH-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioScalarControlFunctionUVE` | Increment 292 adds the bounded Visual Scripting `audio.set_pitch` contract: a caller-owned source Entity and finite positive pitch are copied into a typed callback, callback acceptance is required, and a copied Boolean `Result` is published failure-atomically through both VM execution paths; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, and fail-closed regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, positive-finite pitch validation, callback dispatch, and Boolean result publication; it does not own audio assets, voice handles, playback scheduling, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| INPUT-AXIS1D-TRIGGER-EVENT-V1 | engine/input/include/uve/input/input_action_triggered_event_uve.h | `kInputActionAxisTriggerThresholdUVE` | Increment 293 adds a bounded Axis1D trigger-event policy: `InputSystemUVE::UpdateUVE` queues a copied `InputActionTriggeredEventUVE` only when a clamped absolute axis value crosses above the fixed 0.5 threshold from a previous value at or below it, publishes the current signed axis value, and samples previous gamepad axes through the existing copied snapshot path; sub-threshold changes remain quiet. | Native InputSystemUVE Axis1D keyboard, below-threshold gamepad, previous-gamepad-axis, existing button-event, and sentinel/remap regressions; the existing event bus remains the delivery authority. | The contract owns only fixed threshold classification, copied current/previous snapshot comparison, and event payload publication; it does not poll platform devices, own Android/iOS lifecycle, interpret gestures, expose configurable hysteresis, schedule background work, or mutate ECS state. |
| SCRIPT-AUDIO-SET-3D-POSITION-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioPositionControlFunctionUVE` | Increment 294 adds the bounded Visual Scripting `audio.set_3d_position` contract: a caller-owned source Entity and finite copied Vector3 position are dispatched into a typed callback, callback acceptance is required, and a copied Boolean `Result` is published failure-atomically through both VM execution paths; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, finite-component rejection, and editor palette regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, finite Vector3 validation, callback dispatch, and Boolean result publication; it does not own audio assets, voice handles, playback scheduling, listener transforms, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| SCRIPT-AUDIO-PLAY-SOUND-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioTriggerFunctionUVE` | Increment 295 adds the bounded Visual Scripting `audio.play_sound` contract: a caller-owned source Entity is copied into a typed trigger callback, callback acceptance is required, and a copied Boolean `Result` is published failure-atomically through both VM execution paths; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, and missing-binding fail-closed regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, trigger callback dispatch, and Boolean result publication; it does not own audio assets, voice handles, playback scheduling, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| SCRIPT-AUDIO-STOP-SOUND-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioTriggerFunctionUVE` | Increment 296 adds the bounded Visual Scripting `audio.stop_sound` contract: a caller-owned source Entity is copied into a separate stop trigger callback, callback acceptance is required, and a copied Boolean `Result` is published failure-atomically through both VM execution paths; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, and missing-binding fail-closed regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, stop callback dispatch, and Boolean result publication; it does not own audio assets, voice handles, playback scheduling, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| SCRIPT-AUDIO-IS-PLAYING-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioStateQueryFunctionUVE` | Increment 297 adds the bounded Visual Scripting `audio.is_playing` contract: a caller-owned source Entity is copied into a typed playback-state query callback, the callback-returned Boolean state is copied into the `Result` output through both VM execution paths, and missing bindings fail closed; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, true/false query publication, and missing-binding fail-closed regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, read-only state-query dispatch, and Boolean result publication; it does not own voice handles, playback scheduling, audio assets, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| SCRIPT-AUDIO-SET-ATTENUATION-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptAudioAttenuationControlFunctionUVE` | Increment 298 adds the bounded Visual Scripting `audio.set_attenuation` contract: a caller-owned source Entity plus finite positive min distance, max distance greater than min, and a Linear (0) or InverseSquare (1) model token are dispatched into a typed attenuation callback, callback acceptance is required, and a copied Boolean `Result` is published failure-atomically through both VM execution paths; the catalog and one-hop Entity compiler staging identify the node as an Audio family capability. | Native built-in catalog, compiler staging, direct VM, scheduler/context-free VM, valid/invalid range and model regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared catalog/VM contract. | The contract owns only bounded copied source identity, finite ordered distance validation, model-token validation, callback dispatch, and Boolean result publication; it does not own audio assets, voice handles, playback scheduling, mixer routing, persistent AudioSystem state, UI state, debugger/session state, or platform audio backends. |
| SCRIPT-DEBUG-PRINT-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptEngineLogFunctionUVE` | Increment 299 adds the bounded Visual Scripting `debug.print` contract: a caller-owned finite Number Value is copied into the existing typed log callback, callback acceptance is required, and missing input, missing binding, non-finite input, or callback rejection fail closed through both VM execution paths; the catalog and editor palette identify the node as a Debug family capability. | Native built-in catalog, direct VM, scheduler/context-free VM, finite-input and callback dispatch regressions, and editor palette snapshot; ScriptRuntimeUVE and editor palette consumers use the shared callback contract. | The contract owns only finite copied Number validation, callback dispatch, and failure diagnostics; it does not own string payloads, console storage, severity routing, file logging, debugger/session state, UI state, ECS state, or platform/backend services. |
| SCRIPT-DEBUG-WARNING-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptDebugWarningFunctionUVE` | Increment 300 adds the bounded Visual Scripting `debug.warning` contract: a caller-owned finite Number Value is copied into a distinct typed warning callback, callback invocation must accept and publish a Boolean Result, and missing input, missing binding, non-finite input, callback rejection, or rejected output capacity fail closed through both VM execution paths; the catalog and editor palette identify the node as a Debug family capability. | Native built-in catalog, direct VM, scheduler/context-free VM, finite-input, accepted-result, callback-rejection, and editor palette regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared binding contract. | The contract owns only finite copied Number validation, distinct warning callback dispatch, Boolean Result publication, and failure diagnostics; it does not own severity storage, console buffers, string payloads, file logging, debugger/session state, UI state, ECS state, or platform/backend services. |
| SCRIPT-DEBUG-ERROR-V1 | engine/scripting/include/uve/scripting/script_vm_uve.h | `ScriptDebugErrorFunctionUVE` | Increment 301 adds the bounded Visual Scripting `debug.error` contract: a caller-owned finite Number Value is copied into a distinct typed error callback, callback invocation must accept and publish a Boolean Result, and missing input, missing binding, non-finite input, callback rejection, or rejected output capacity fail closed through both VM execution paths; the catalog and editor palette identify the node as a Debug family capability. | Native built-in catalog, direct VM, scheduler/context-free VM, finite-input, accepted-result, callback-rejection, and editor palette regression coverage; ScriptRuntimeUVE and editor palette consumers use the shared binding contract. | The contract owns only finite copied Number validation, distinct error callback dispatch, Boolean Result publication, and failure diagnostics; it does not own severity storage, console buffers, string payloads, file logging, debugger/session state, UI state, ECS state, or platform/backend services. |
| SAVE-METADATA-PLAYTIME-VALIDATION-V1 | engine/save/src/save_game_system_uve.cpp | `IsValidSaveMetadataUVE` | Increment 302 adds bounded SaveGameSystemUVE metadata preflight: caller-supplied playtime must be finite and non-negative, otherwise SaveUVE fails before scene serialization, save-directory creation, temporary-file writes, or slot replacement; existing timestamp, slot, and schema overwrites remain authoritative. | Native SaveGameSystemUVE regressions for negative and NaN playtime, existing atomic-save coverage, full CTest, and managed/editor smoke consumers. | The contract owns only copied scalar playtime validation and early failure ordering; it does not own engine-version compatibility policy, gameplay-domain state, screenshots, encryption, cloud transport, or save backend selection. |
| INPUT-GAMEPAD-GETTER-SYNCHRONIZATION-V1 | engine/input/src/gamepad_input_system_uve.cpp | `GamepadInputSystemUVE::GetSnapshotUVE` | Increment 303 hardens the bounded GamepadInputSystemUVE read contract by taking the existing state mutex in snapshot, axis, button, and edge-query getters, synchronizing copied reads with live injection and frame commits without changing deadzone normalization, frame history, sentinel handling, or platform polling ownership. | Native gamepad/input regression coverage, full serial CTest, managed/editor smoke, and existing injected GamepadInputSystemUVE consumers. | The contract owns only synchronization of copied getter reads against the existing in-process state mutex; it does not own device polling, platform callbacks, remapping policy, haptics, mobile lifecycle, or ECS state. |
| NETWORK-ACK-HEADER-SEQUENCE-VALIDATION-V1 | engine/network/src/reliable_packet_window_uve.cpp | `ApplyReliableAcknowledgementsUVE` | Increment 304 hardens the bounded reliable acknowledgement contract by requiring both packet sequence and acknowledged sequence to be nonzero, rejecting reserved zero packet headers before cumulative or selective acknowledgement application and preserving failure-atomic pending-mask state. | Native reliable packet window regressions, full serial CTest, managed/editor smoke, and caller-owned reliable transport consumers. | The contract owns only copied header validity and pending-mask mutation; it does not own sockets, packet transport, timers, peers, authentication, replication, prediction, reconciliation, or security policy. |
| PHYSICS-MATERIAL-FINITE-READ-BOUNDARY-V1 | engine/physics/include/uve/physics/physics_material_uve.h | `MaterialOfUVE` | Increment 305 hardens the shared PhysicsMaterialUVE read boundary: finite friction and restitution are clamped to [0,1] with non-finite values failing closed to zero, while density must be finite and positive or falls back to unit density; the copied result remains shared by physics resolution and raycast reporting. | Native PhysicsMaterialUVE regressions, full serial CTest, managed/editor smoke, and existing PhysicsSystemUVE/RaycastSystemUVE consumers. | The contract owns only copied material sanitization at the read boundary; it does not own collider authoring, solver/backend policy, per-pair combine modes, persistence, or platform physics ownership. |
| ASSET-GENERIC-ENVELOPE-ATOMIC-PUBLISH-V1 | engine/asset/src/asset_importer_uve.cpp | `GenericFileImportUVE` | Increment 306 hardens generic UVE-envelope import publication by copying source bytes to a temporary sibling and renaming only after a successful complete copy, preserving an existing destination when copy or final publication fails; format-specific parsing and backend ownership remain outside this contract. | Native AssetImporterUVE generic-envelope and typed-envelope regression coverage, full serial CTest, managed/editor smoke, and caller-owned asset database registration. | The contract owns only bounded file-copy publication ordering; it does not own raw FBX/audio/glTF scene parsing, shader/material resolution, compression, GPU resources, streaming, or asset database policy. |
| PLATFORM-MONITOR-SNAPSHOT-CAP-V1 | engine/window/include/uve/window/monitor_info_validation_uve.h | `ValidateMonitorSnapshotUVE` | Increment 307 adds a bounded 32-entry cap to copied monitor snapshots before validation publication, while retaining non-empty bounded names, positive dimensions, and at-most-one-primary validation; the contract owns no monitor handles, enumeration, hot-plug refresh, display-mode application, or mobile lifecycle. | Native monitor validation regressions, full serial CTest, managed/editor smoke, and window/platform callers that consume copied snapshots. | The contract owns only bounded copied monitor-snapshot validation; native display enumeration, hot-plug state, window creation, power management, mobile safe areas, and platform lifecycle remain outside the boundary. |
| NETWORK-ACK-FULL-WINDOW-MASK-V1 | engine/network/src/reliable_packet_window_uve.cpp | `ApplyReliableAcknowledgementsUVE` | Increment 308 hardens cumulative acknowledgement application at the 32-entry pending window boundary: distance 31 publishes an explicit all-bits mask instead of shifting a 32-bit value by 32, preserving failure-safe copied pending-mask mutation; Increment 325 additionally ignores selective ACK arithmetic that wraps to reserved sequence zero at the uint32 boundary, preserving valid neighboring acknowledgements and preventing an unrelated pending bit from being cleared. | Native reliable packet window regressions including cumulative/full-window, reserved-zero header, and selective-wrap cases; full serial CTest, managed/editor smoke, and caller-owned reliable transport consumers. | The contract owns only bounded copied ACK-mask mutation and sequence arithmetic safety; it does not own sockets, packet transport, timers, peers, authentication, replication, prediction, reconciliation, or security policy. |
| ASSET-WAV-RIFF-BOUNDARY-V1 | engine/audio/src/wav_metadata_uve.cpp | `ParseWavMetadataUVE` | Increment 309 confines bounded PCM WAV metadata parsing to the declared RIFF payload, rejects incomplete in-bound chunk headers/payloads, and ignores unrelated bytes after the RIFF container while preserving failure-safe metadata publication. | Native WAV metadata, PCM16 decoder, and WAV importer regressions; full serial CTest; managed/editor smoke; and future caller-owned audio import consumers. | The parser owns only copied RIFF/WAVE PCM metadata validation; it does not own filesystem reads, codecs beyond PCM metadata/sample decoding, stream cursors, voice/device/backend lifetime, compression, or background workers. |
| AUDIO-PCM16-FULL-DECODE-ALLOC-FAILURE-V1 | engine/audio/src/wav_pcm16_decoder_uve.cpp | `DecodeWavPcm16SamplesUVE` | Increment 310 hardens bounded full PCM16 sample decoding so allocation failure is caught and returned as false before copied output publication, preserving the caller's prior output and existing sample-count cap. | Native WAV PCM16 decoder regressions, full serial CTest, managed/editor smoke, and caller-owned audio import consumers. | The decoder owns no stream cursor, refill scheduler, voice/device/backend lifetime, filesystem path, compression, or background worker. |
| SCENE-TRANSFORM-AUTHORED-VALIDATION-V1 | engine/scene/include/uve/scene/components/transform_component_uve.h | `IsTransformComponentValidUVE` | Increment 311 adds bounded authored local-transform validation: position and scale components must be finite, and the quaternion must be finite with unit-length tolerance before scene restore or SceneGraphUVE attach/set preconditions; invalid restore payloads fail atomically. | Native SceneGraphUVE and SceneSerializerUVE regressions, full serial CTest, managed/editor smoke, and caller-owned scene transform consumers. | The validator owns only copied authored-value validity; it does not normalize or repair transforms, own hierarchy/world propagation, physics/render matrices, animation, persistence storage, or backend resources. |
| SCENE-PREFAB-COMMIT-REVISION-V1 | engine/scene/include/uve/scene/components/prefab_instance_component_uve.h | `CommitPrefabOverridesToSourceUVE` | Increment 312 adds an optional caller-supplied nonzero committed revision; after baseline conflict validation and failure-atomic source writes succeed, the helper copies that revision into both sourceRevision and instanceRevision before clearing overrides, while the legacy no-revision path remains unchanged. | Native prefab apply/commit/revision regressions, full serial CTest, managed/editor smoke, and caller-owned source-prefab adapters. | The helper owns only copied revision-field publication ordering; it does not discover asset revisions, mutate asset databases, auto-merge, resolve conflicts, load nested prefabs, or own editor UX. |
| PHYSICS-SHAPE-CAST-EXPANDED-AABB-FINITE-V1 | engine/physics/src/shape_cast_system_uve.cpp | `IsFiniteAabbUVE` | Increment 313 rejects non-finite or inverted conservative expanded AABBs produced by large finite collider bounds or cast extents before ray intersection, preserving failure-closed hit publication and existing deterministic ordering for valid bounds. | Native shape-cast regressions, full serial CTest, managed/editor smoke, and caller-owned physics query consumers. | The guard owns no collision backend, broad-phase cache lifetime, continuous collision response, sweep solver, rigid-body integration, material backend, or GPU/resource state. |
| INPUT-ACTION-NAME-CAP-V1 | engine/input/src/input_system_uve.cpp | `RegisterActionUVE` | Increment 314 rejects empty, NUL-containing, or over-128-byte action names before copied action-map mutation, while preserving valid boundary-length registration and legacy binding/remap/query semantics. | Native InputSystemUVE action tests, full serial CTest, managed/editor smoke, and caller-owned input action consumers. | Registration owns only bounded copied action-name keys; it does not own physical device state, action-map persistence, input rebinding UI, event transport beyond existing dispatch, or platform input backends. |
| SAVE-METADATA-FIXED-FRAME-V1 | engine/save/src/save_game_system_uve.cpp | `SplitSaveMetadataOnlyUVE` | Increment 315 makes metadata-only save reads validate the uint64 world-section length and require exact decompressed fixed-layout consumption, rejecting truncated or trailing world bytes without copying the world section; Increment 336 makes the shared framed-byte slice reject uint64 lengths larger than the remaining buffer before addition, narrowing, or iterator arithmetic, with a maximum-world-length regression covering both LoadUVE and GetSaveMetadataUVE. | Native SaveGameSystemUVE framing/metadata regressions, full serial CTest, managed/editor smoke, and caller-owned save metadata consumers. | The helper owns only copied payload-boundary validation; it does not deserialize world state, mutate entities, rewrite saves, own compression/encryption, access cloud storage, or select filesystem backends. |
| NETWORK-REASSEMBLY-AGGREGATE-CAP-V1 | engine/network/src/reliable_packet_window_uve.cpp | `AcceptReliablePayloadFragmentUVE` | Increment 316 derives a 1,228,800-byte aggregate reassembly cap from the existing fragment and count limits, rejecting inconsistent over-budget active state before copied fragment storage or received-byte mutation while preserving valid duplicate/conflict/complete behavior. | Native reliable packet/reassembly regressions, full serial CTest, managed/editor smoke, and caller-owned network transport consumers. | The helper owns only bounded copied fragment state and aggregate accounting; it does not own sockets, packet serialization, timers, retransmission, peers, authentication, replication, prediction, reconciliation, or backend transport lifetime. |
| PLATFORM-MONITOR-ENUMERATION-VALIDATION-V1 | engine/window/src/window_manager_uve.cpp | `EnumerateMonitorsUVE` | Increment 317 routes copied GLFW monitor enumeration through `ValidateMonitorSnapshotUVE` and returns an empty snapshot for null/negative enumeration results or invalid monitor entries, preventing backend state from bypassing cardinality/name/primary validation. | Native WindowManagerUVE and monitor-validation regressions, full serial CTest, managed/editor smoke, and caller-owned platform consumers. | The bridge owns only copied monitor snapshots and fail-closed validation; it does not own monitor handles, hot-plug refresh, display-mode application, window lifecycle, mobile safe areas, or platform backend selection. |
| AUDIO-MIXER-NAME-LOOKUP-CAP-V1 | engine/audio/src/audio_mixer_group_uve.cpp | `HasGroupUVE` | Increment 318 applies the existing 64-byte non-empty NUL-free mixer-group name contract to all lookup, mutation, source-routing, and resolution APIs, rejecting malformed names before temporary map-key copies while preserving Master/default semantics. | Native AudioMixerGroupUVE and AudioSystemUVE routing regressions, full serial CTest, managed/editor smoke, and caller-owned audio runtime consumers. | The mixer owns only bounded copied group names and source counts; it does not own clip decoding, stream scheduling, device/backend lifetime, ECS state, editor authoring, or platform audio selection. |
| ASSET-BUNDLE-FRAMING-CAPS-V1 | engine/asset/src/asset_bundle_uve.cpp | `ScanBundleEntriesUVE` | Increment 319 enforces a 4096-entry bundle cap and a 256-byte non-empty NUL-free entry-name cap symmetrically during PackUVE and ScanBundleEntriesUVE, rejecting malformed framing before untrusted entry ranges are visited or copied. | Native AssetBundleUVE pack/unpack/lookup regressions, full serial CTest, managed/editor smoke, and caller-owned bundle filesystem consumers. | The bundle owns only bounded copied entry metadata and raw payload bytes; it does not own asset decoding, import registration, filesystem sandbox policy beyond safe relative names, streaming, compression, or resource lifetime. |
| PHYSICS-STEP-FINITE-TIME-GRAVITY-V1 | engine/physics/src/physics_system_uve.cpp | `PhysicsSystemUVE::StepUVE` | Increment 320 rejects non-finite injected gravity and non-finite or negative fixed delta before iterating bodies, preserving rigid-body velocity, angular state, and transforms on invalid step inputs while leaving valid integration unchanged. | Native PhysicsSystemUVE regressions, full serial CTest, managed/editor smoke, and caller-owned fixed-step simulation consumers. | The system owns only injected gravity and per-step orchestration; it does not own clocks, scheduling, broad backend simulation, networking, renderer state, or ECS lifetime. |
| SCENE-NAME-COMPONENT-CAP-V1 | engine/scene/src/scene_serializer_uve.cpp | `IsNameComponentValidUVE` | Increment 321 enforces a 128-byte NUL-free authored entity-name cap during scene restore, preserving empty optional names and legacy documents that omit NameComponentUVE while rejecting oversized payloads through the existing failure-atomic rollback path. | Native NameComponent and SceneSerializerUVE regressions, full serial CTest, managed/editor smoke, and caller-owned scene authoring consumers. | The component owns only copied human-readable metadata; it does not own global uniqueness, localization, editor naming policy, filesystem identity, ECS lifetime, or runtime asset lookup. |
| INPUT-SCROLL-ACCUMULATOR-VALIDATION-V1 | engine/input/src/input_system_uve.cpp | `SetMouseScrollDeltaUVE` | Increment 324 rejects non-finite scroll deltas and ignores finite additions that would make the cross-thread accumulator non-finite, preserving the last valid accumulated value until `UpdateUVE` drains it; valid finite deltas retain existing multi-report accumulation semantics. | Native InputSystemUVE scroll regressions, full serial CTest, managed/editor smoke, and the WindowManagerUVE desktop injection bridge. | The boundary owns only copied finite scroll accumulation and frame-drain safety; it does not own wheel polling, platform callbacks, gesture interpretation, mobile lifecycle, device handles, or input action persistence. |
| ASSET-TEXT-SOURCE-CAP-V1 | engine/asset/src/asset_importer_uve.cpp | `TextFileImportUVE` | Increment 328 adds a 16 MiB built-in text-source cap enforced before reading and after the read, rejecting unsized/oversized or source-growth cases before normalization and destination publication while preserving NUL rejection and line-ending settings. | Native AssetImporterUVE text-cap, normalization, classification, and generic publication regressions, full serial CTest, managed/editor smoke, and caller-owned text import consumers. | The importer owns only bounded plain-text source reading and normalization; it does not own arbitrary document schemas, asset parsing, filesystem watching, background workers, or asset database policy beyond existing registration. |
| PLATFORM-FULLSCREEN-STATE-V1 | engine/window/include/uve/window/i_window_manager_uve.h | `SetFullscreenUVE` | Increment 330 makes fullscreen/windowed state publication backend-confirmed: `WindowManagerUVE` commits its cached mode only after GLFW reports the requested primary-monitor or windowed transition, preserving the previous cached state when confirmation fails. | Native WindowManagerUVE fullscreen round-trip regressions with direct GLFW monitor assertions, full serial CTest, managed/editor smoke, and caller-owned platform consumers. | The contract owns only requested-mode publication and prior-state preservation; it does not own display-mode enumeration/application policy, monitor hot-plug state, mobile lifecycle, safe areas, power management, or backend selection. |
| ASSET-DATATABLE-SOURCE-CAP-V1 | engine/asset/include/uve/asset/data_table_importer_uve.h | `RegisterDataTableImportersUVE` | Increment 331 preserves schema-driven CSV/TSV/JSON DataTable import while enforcing the existing 4 MiB document cap before source materialization and again after the read, rejecting oversized or source-growth documents before typed parsing and `.uvetable` publication. | Native DataTable importer, typed parser, destination-publication, and over-cap regressions, full serial CTest, managed/editor smoke, and caller-owned table import consumers. | The registration owns only explicit schema-driven source import and bounded document handoff; it does not infer schemas, own filesystem watching, background workers, compression, or database policy beyond existing destination registration. |
| SCENE-CAMERA-FOV-LOWER-BOUND-V1 | engine/scene/include/uve/scene/components/camera_component_uve.h | `IsCameraComponentValidUVE` | Increment 332 requires a finite authored camera FOV of at least 0.1 degrees before scene persistence or render projection, preventing `tan(FOV/2)` underflow and non-finite focal lengths while preserving the open upper bound and positive ordered clip planes. | Native CameraComponentUVE and CameraSystemUVE projection/frustum regressions, scene serializer restore, full serial CTest, managed/editor smoke, and caller-owned camera consumers. | The validator owns only projection-safe authored camera values; it does not own projection matrices, render targets, advanced camera variants, display aspect acquisition, or authoring repair UX. |
