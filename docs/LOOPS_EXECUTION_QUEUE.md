# UNIVEX Engine — Loops Execution Queue

> This is the execution queue for the autonomous loops workflow. It preserves the existing roadmaps and the supplied DOCX-derived Motion Query inventory as authorities. The queue is dependency-ordered; it is not permission to generate speculative source files in bulk.

## Operating contract

Each loop selects one bounded, reviewable capability. The implementation must extend the existing engine authority, preserve ownership and thread boundaries, add focused tests, run the full validation matrix, update the domain roadmaps, create a signed commit, push the working branch, wait for required CI, and merge only when the review state is green.

| Review state | Required action |
|---|---|
| **Red** | Stop. Treat the issue as a blocker, find the root cause, fix it, and repeat review and validation. Do not merge. |
| **Orange** | Stop before merge. Re-review architecture, ownership, failure paths, and test evidence; add the missing proof or reduce scope. |
| **Green** | Required checks and review are clear. Push and merge autonomously, then continue to the next queue item. |

## Execution order

| Phase | Queue | Entry condition | Completion gate |
|---:|---|---|---|
| 1 | **Viewport presentation** | Legacy Rendering Increment 63 was already implemented and verified. | Real renderer presentation, shader parity, native/managed validation, and OpenGL/Xvfb smoke. **Completed.** |
| 2 | **Selection and viewport visual feedback** | Uses the renderer-owned editor visual pass from Phase 1. | Bounded selection rectangle, gizmo-axis, and camera-orientation facts reach the GPU pass through copied value-only state. **Completed.** |
| 3 | **Material and mesh asset workflow** | Inspector GUID binding, deterministic typed UVE envelope reimport, copied AssetManager failure diagnostics, copied Renderer3D invalid/pending/failed/fallback counts, deterministic raw model/texture/material/audio source classification with parser-required diagnostics and editor `rawAudio` labeling, selected-entry Content Browser import/reimport capability plus explicit native queue execution, and the bounded Text Import Parser v1 are closed. | Text parsing is deterministic, NUL-safe, cache-versioned, and covered by native/managed tests. Remaining FBX model and PNG/JPEG pixel conversion backends, OBJ/glTF index/mesh/material conversion and glTF buffer/image resolution, MTL texture/shader/material conversion, audio sample decoding/derived `.uveaudio` conversion, streaming preparation, parser-specific previews, and external-library/licensing decisions remain an explicit audit boundary before implementation; bounded PCM WAV metadata, PNG IHDR facts, JPEG frame metadata, OBJ source statistics, glTF/GLB structure, and MTL source statistics are now proven by three native regressions each, and Increment 216 adds a bounded RGBA8 decoded-pixel budget predicate with overflow-safe width×height×4 rejection and a 64 MiB default cap; Increment 217 adds `ResolveObjIndexUVE` for bounded positive 1-based and negative-relative face-index resolution with failure-atomic output and no mesh allocation ownership; Increment 218 adds `ValidateGltfAccessorSpanUVE` for overflow-safe bounded accessor byte-span validation with stride and element-count caps without JSON/buffer decoding ownership; Increment 219 adds `ValidateMtlTextureReferenceUVE` for bounded relative, NUL-free, traversal-safe texture-reference preflight without filesystem access; Increment 220 adds `ValidateJpegRgba8PixelBudgetUVE` for overflow-safe normalized RGBA8 decoded-pixel budgeting with a 64 MiB default cap without entropy-decoder or allocation ownership; Increment 221 adds `ValidateWavPcm16SampleWindowUVE` for bounded caller-owned PCM16 streaming/chunk windows with positive-size, end-bound, and cap validation without stream/device lifecycle ownership; Increment 222 adds `ApplyTouchOrientationUVE` with finite cardinal 0/90/180/270-degree normalized-coordinate mapping and failure-atomic invalid-input handling without platform polling or lifecycle ownership. |
| 4 | **Core asset-type importers and data table pipeline** | Stable virtual paths, asset identity, derived-data boundaries, explicit parser/licensing choices, and generation-aware dependency invalidation planning; typed UVE envelope copy-register contracts and the bounded schema-driven Data Table CSV/TSV/JSON pipeline are now completed foundations. | The dependency graph can produce a deterministic bounded reverse-dependent closure, and the typed DataTableUVE importer/asset-store/loader/EngineCore/editor-preview path is covered by focused tests. Raw model, material, animation, and shader conversion backends, OBJ/glTF index and mesh/material conversion beyond validated source statistics/structure, MTL texture/shader/material conversion, glTF buffer/image resolution, JPEG/PNG pixel decoding/derived `.uvetex` conversion beyond validated frame/IHDR facts, audio sample/metadata conversion beyond validated PCM WAV facts, parser-specific previews, and external-library/licensing decisions remain independent partial work; Increment 216’s RGBA8 budget gate is a preflight safety contract only and does not decode or allocate pixels; Increment 217’s OBJ resolver is an index handoff only and does not triangulate faces, resolve complete face tokens, load MTL files, or convert mesh data; Increment 218’s glTF span validator is a bounds preflight only and does not parse accessors, read buffers, decode components, resolve images, or convert meshes; Increment 219’s MTL validator is a token-safety policy only and does not resolve paths, load textures, compile shaders, or convert materials; Increment 220’s JPEG validator is a decode preflight only and does not parse entropy data, allocate pixels, select a codec, or convert textures; Increment 221’s WAV window validator is chunk preflight only and does not refill, seek, schedule, decode partial buffers, own voices, or select an audio backend; Increment 222’s orientation policy is value-only and does not acquire device orientation, poll touch hardware, own mobile lifecycle, or mutate snapshots. |
| 5 | **Scene components and prefab maturity** | Runtime scene ownership and serialization are authoritative; camera, light, collider, rigid-body, audio-source, mesh, Script, Particle, and PrefabInstance validation plus bounded deterministic override persistence are now closed foundations for their authored data. | Caller-owned failure-atomic prefab Apply/Revert is proven with explicit baseline semantics, DetectPrefabOverrideConflictsUVE now provides bounded read-only baseline preflight with copied conflict evidence and hard-cap truncation, validated ScriptComponent ownership now reconciles empty-path detach and graph-to-runtime attach without filesystem ownership, ParticleRuntimeUVE now provides bounded CPU emission/update state with finite preflight, deterministic integration, and lifetime culling, and the existing Increment 18 AudioSourceSystemUVE/AudioSystemUVE/EngineCore listener lifecycle is now documented as closed; asset loading is now supplemented by failure-atomic typed replacement reload with last-known-good retention and copied diagnostics, GPU particle simulation/renderer depth, mixer/effects/routing/clip assets/streaming/backend selection, baseline-checked source-prefab mutation through caller-owned targets is now failure-atomic with post-success override clearing; automatic retry/fallback policy, merge/revision tracking, shape/filter breadth, constraints, character depth, editor commands, and conflict-resolution UX require independent bounded increments. |
| 6 | **Tooling partial closure** | Audit stale or partial implementations before adding code. | ScriptAssetLoaderUVE now provides bounded VFS-backed ScriptComponent path-to-graph-schema loading with copied diagnostics and deterministic repeated reads; Increment 167 closes bytecode reload/last-known-good publication, fallback, generation, and compatible-state preservation; the plugin registry now has copied ABI negotiation and scope-guarded manifest removal proof; DeveloperConsoleAccessUVE now provides native/managed Denied/ReadOnly/Full gates and monotonic Shipping denial; compile/attach orchestration, asset-manager integration, OS dynamic loading/handle ownership, console identity/session/role security, audit persistence, and documentation delivery still require independent implementation and proof or an explicit deferred disposition. |
| 7 | **Simulation, input, audio, and save depth** | Core services and platform targets are explicit; Increment 203 adds bounded value-only `ValidateWindowDescUVE` proof for positive desktop dimensions, non-empty title, and valid OpenGL major version without backend or lifecycle ownership; Increment 204 adds a bounded value-only reliable sequence window with wrap-safe ordering, duplicate/too-old classification, 32-entry history, and cumulative/forward selective ACK proof without socket or replication ownership. | Deterministic rigid-body integration, AABB collision resolution, raycast layer queries, symmetric collider layer/mask filtering, AreaComponentUVE persistence, bounded read-only area-to-collider overlap facts, a generation-safe copied enter/exit lifecycle tracker with truncation-safe baseline retention, EngineCore typed queued Entered/Exited event-bus delivery, validated Box/Sphere/Capsule collider descriptors mapped through a shared conservative AABB cache, exact value-only Sphere↔Sphere, Capsule↔Sphere, Capsule↔Capsule, Sphere↔Box, rotated local-Y Capsule↔Box, Sphere↔rotated Box, Capsule↔rotated Box, and oriented Box↔Box SAT penetration integrated after broad-phase candidate filtering with deterministic axis/depth, strict touching rejection, coincident-center +X fallback, normalized world-rotation centerline construction, tight rotated-capsule and rotated-box AABB extents, box-local exact sphere/capsule evaluation with world-axis restoration, deterministic 15-axis OBB SAT with strict touching rejection, capsule-centerline closest-point and closest-segment-pair evaluation, segment-to-AABB closest-distance evaluation, and diagonal false-positive rejection, pure deterministic swept-AABB `SweepAabbUVE` time-of-impact math, opt-in CharacterControllerUVE TOI-assisted tunneling prevention with normal projection and caller-selected grounded/support-normal classification, a stateless per-query median-split BVH broad phase preserving legacy pair ordering, bounded read-only conservative SphereCastSystemUVE, axis-aligned BoxCastUVE, and local-Y CapsuleCastUVE over expanded collider world-AABBs with finite shape validation, layer/ignore filters, deterministic closest hits, moving-center/normal/material facts, and focused native regressions, opt-in CharacterControllerUVE `maximumStepHeight` lift/clearance/drop traversal with failure-atomic rollback and copied `stepUpUsed` evidence, plus a copied refit-only DynamicAabbBvhUVE policy for moved proxies with full-generation stale rejection, an optional bounded generation-safe distance/hinge positional solver integrated after collision resolution, a bounded value-only diagonal box inverse-inertia and torque/impulse helper with finite fail-closed outputs, a bounded value-only hinge motor/limit policy evaluator with capped target-speed correction, deterministic lower/upper limit correction, disabled no-op behavior, failure-atomic finite-input rejection, and five focused native tests, opt-in CharacterControllerUVE dynamic-body push that mutates only validated non-kinematic positive-mass target velocity along the mover-to-target normal with bounded speed and clamp/count diagnostics, persisted RigidBodyComponentUVE angular velocity/torque/diagonal inverse-inertia state with backward-compatible zero defaults, and PhysicsSystemUVE semi-implicit torque plus normalized local-quaternion advancement, bounded kinematic substep movement/contact projection, GamepadInputSystemUVE bounded copied axis/button snapshots, AudioSystemUVE bounded mixer-group routing/multiplier diagnostics plus an optional borrowed clip identity resolver with failure-atomic rejection and accepted-path copying, and SaveGameSystemUVE current-schema dispatch now composed with a bounded SavePayloadMigrationRegistryUVE supporting deterministic shortest-path multi-hop transforms, applied-step diagnostics, and proven registered v0-to-v1/multi-hop load paths; Increment 205 adds save-only bounded RLE compression with raw backward-compatible fallback, failure-atomic decompression, and a 64 MiB expanded-payload cap used by save/load/metadata paths; Increment 206 adds a bounded backend-neutral PCM gain effect over copied normalized float samples with finite gain validation, hard output clamping, a 1,048,576-sample cap, and failure-atomic publication; Increment 207 adds `NormalizeTouchCoordinateUVE`, a bounded value-only safe-area-relative touch coordinate policy with finite validation, normalized clamping, and failure-atomic invalid-input handling; Increment 208 adds `EvaluateGyroscopicTorqueUVE`, a bounded value-only diagonal-inertia gyroscopic torque evaluator with finite validation and caller-applied ownership; Increment 209 adds `EvaluatePrefabRevisionUVE`, a bounded value-only Current/Stale/Invalid comparison for nonzero prefab source and instance revisions without asset, merge, or conflict-resolution ownership; Increment 210 adds bounded RIFF/WAVE PCM16 sample decoding into copied normalized float samples with malformed-input rejection, atomic output publication, and a 1,048,576-sample cap; Increment 211 adds `ValidateSkeletonRetargetMapUVE`, a bounded copied one-to-one source/target joint-map validator against shared skeleton topology without pose conversion or runtime playback ownership; Increment 212 adds `IsValidParticleDrawCommandUVE`, a bounded finite copied particle draw-command gate for render-bridge handoff without GPU allocation or submission ownership; Increment 213 adds `EvaluateReliableRetransmitPolicyUVE`, a bounded caller-owned Waiting/Due/Exhausted/Invalid retry decision without clock, timer, socket, or packet ownership; Increment 214 adds `EvaluateSaveRevisionUVE`, a bounded nonzero base/local/remote Unchanged/LocalAhead/RemoteAhead/Conflict/Invalid classifier without cloud, merge, encryption, or rewrite-back ownership; Increment 215 adds `ValidateMonitorSnapshotUVE`, a bounded copied monitor snapshot gate for bounded names, positive dimensions, and at most one primary without monitor handles, hot-plug, or display-mode ownership; Increment 216 adds `ValidatePngRgba8PixelBudgetUVE`, a bounded RGBA8-only decoded-pixel budget gate with overflow-safe width×height×4 checking and a 64 MiB default cap without decoder or allocation ownership; InputSystemUVE now provides optional borrowed gamepad injection with per-input-frame copied current/previous snapshot history, deterministic button action trigger/held/release edges, scaled/clamped axis binding composition, null-safe fallback, and failure-atomic bounded action remapping with finite/index validation; MobileInputSystemUVE now provides a bounded backend-neutral ten-slot touch snapshot with stable-identifier deltas, finite positions, clamped pressure, and finite gyroscope rotation-rate values; MobileGestureRecognizerUVE now provides deterministic frame-driven tap/swipe classification with stable touch tracking, finite thresholds, dominant-axis direction, duplicate-frame suppression, and bounded reports; exact oriented/rotated-box/rotated-capsule casts and sphere/capsule mixed pairs beyond current narrow phases, exact step geometry, gyroscopic coupling, full angular constraint/motor/limit application (the bounded value-only hinge policy is proven but remains caller-applied), backend-wide dynamic-body response and CCD/TOI integration, BVH reuse/update policy, persistent/editor-authored constraints, constraint islands, real input backend polling, Android/iOS lifecycle and coordinate mapping, multi-touch chords, platform gesture frameworks, platform input policy, mixer effects, streaming, concrete audio backends, save migration rewrite policy, compression/encryption/cloud hooks, and gameplay-domain save state remain independently tested and profiled work; AudioSource entity ownership, active-camera listener updates, play-on-awake, and listener-relative attenuation already have native runtime/test proof. |
| 8 | **Animation runtime convergence** | Shared pose, skeleton, time, and evaluation contracts are stable. | SkeletonDefinitionUVE, PoseBufferUVE, and AnimationEvaluationContextUVE now provide bounded copied joint topology, skeleton identity/cardinality, finite normalized local poses, and deterministic evaluation time consumed by MotionQueryUVE and ControlRigUVE validation/evaluation; ControlRigUVE now provides deterministic bounded pose blending with finite weight clamping, normalized rotations, weighted two-bone constraint evaluation, an existing AimLookAt solve, and Increment 285’s copied SpringPosition evaluation with bounded animation-delta response, stiffness/damping validation, influence clamping, and explicit no-history ownership; bounded animation clip metadata validation now proves finite duration/sample-rate/track-count and non-zero skeleton identity handoff; animation assets/player/tree/resource loading, full playback/blending/events, Control Rig/IK depth, retargeting, procedural animation, GPU skinning, and broader editor authoring remain partial; bounded native Motion Query database authoring now includes revision-safe copied one-entry clipboard commands with identity-remapped paste, bounded 32-entry snapshot-based Undo/Redo, a copied deterministic 14-command metadata catalog for bridge/managed discovery, and bounded editor utilities for display-name validity, schema-ID normalization, and candidate identifier preflight, plus a deterministic validated default Motion Query database contract factory for editor/importer starting points, while full reflection and Visual Scripting animation integration remain independent bounded increments. |
| 9 | **Gameplay and content systems** | Runtime/editor foundations above are complete enough for user-facing content. | Nodes/components, character interaction, particles/VFX, splines, procedural generation, data-driven gameplay, Sequencer, decals, impostors, and lightmap workflows have real runtime/editor proof. Visual Scripting now has deterministic native palette descriptors, managed filtering, right-click search popup, keyboard/click selection, cursor-position insertion, a 130-node catalog spanning flow.sequence, flow.branch, Float math including fourteen scalar utility nodes, Vector2 math with Make/Add/Subtract/Multiply/Length/Normalize/Dot/Distance/Direction/Lerp, Vector3 math with Make/Add/Subtract/Multiply/Dot/Cross/Length/Normalize/Distance/Direction/Lerp, Boolean logic including six finite Number-to-Boolean comparison nodes, Has/Get Component entity queries, engine.log, and engine.get_time, explicit execution-pin roles with deterministic one-upstream/one-downstream cardinality validation, bounded typed VM values including Boolean storage and copied Entity/Component query values, explicit dependency-aware VM dispatch for all supported data-node IDs, copied entity-component facts, per-entity typed runtime tick execution, a scene-owned ScriptEntityQueryAdapterUVE that maps caller-registered native component types to binding-ordered copied presence facts for alive entities, and a query-aware ScriptRuntimeUVE tick path that refreshes those facts immediately before each enabled instance executes; Increment 274 added bounded copied ScriptVmTraceEventUVE records for node execution, typed value transfer, query-fact refresh, completion, and failure, with deterministic refresh-first ordering and truncation evidence; Increment 275 now copies debugger trace history through ScriptSourceMappingUVE, EditorBridgeUVE, and the managed Avalonia snapshot/status path alongside source-mapped breakpoints; Increment 276 now adds flow.sequence and flow.branch authoring/presentation semantics, deterministic execution-link cardinality diagnostics, and an explicit compiler deferral until ConditionalJump runtime support; Increment 277 now establishes a version-2 ConditionalJump bytecode contract with bounded true/false targets, strict decoder validation, and legacy v1 data-only compatibility; Increment 278 now evaluates copied Boolean conditions through the VM, selects bounded true/false targets with instruction-budget loop safety, and exposes an explicit copied-context debugger stepping seam; Increment 279 now lowers one validated flow.sequence node into v3 SequenceDispatch with ordered direct non-flow targets, missing-output continuation, VM budget safety, and debugger stepping proof; Increment 280 now lowers one validated flow.branch node into ConditionalJump with caller-bound Boolean Condition, bounded True/False direct non-flow targets, missing-output continuation, and explicit data-condition staging and nested-flow deferral; Increment 281 now stages one direct output from an approved built-in Boolean producer into Condition through producer-before-branch ordering and an immediate TransferValue, while custom producers and producer data dependencies remain explicitly deferred; Increment 282 now orders at most one approved built-in Boolean dependency before that producer, emits both typed transfers, and rejects cycles and deeper chains without partial IR; Increment 283 now adds an explicit `engine.log` catalog/compiler/VM/runtime seam that accepts one copied Number through a caller-owned callback, emits trace evidence, and fails closed when unbound or rejected; Increment 284 now adds an explicit `engine.get_time` catalog/compiler/VM/runtime seam that writes one finite copied Number through a caller-owned callback and fails closed on unbound, rejected, non-finite, or capacity-exhausted output; Increment 286 now stages one direct `engine.get_time.Value` output before a `math.float.*` A/B consumer through an immediate TransferValue and proves runtime addition; Increment 287 now stages one direct built-in Vector3 `Vector`/`Result` output before one `math.vector3.*` A/B/Vector consumer, proves copied Vector3 execution, and rejects composed/deeper vector dependencies; Increment 288 now stages finite `math.vector3.dot.Result` and `math.vector3.length.Length` outputs before one `math.float.*` A/B consumer, proves dot-to-Float execution, and rejects composed/deeper Number chains; Increment 289 now stages one direct `math.float.*.Result` output before a downstream `math.float.*` A/B consumer, proves multiply-to-add Float chain execution, and rejects a second staged consumer; Increment 290 now stages one direct `logic.boolean.*.Result` output before another Boolean consumer input, proves not-to-and chain execution, and rejects a second staged consumer; Increment 291 now stages one direct finite Number producer (`engine.get_time.Value` or `math.float.*.Result`) before `math.vector3.multiply.Scale`, proves Float-driven Vector3 scaling, and rejects a second Scale consumer; Increment 292 now enforces at most one non-execution source per data input pin, preserves distinct-input fan-in, and keeps execution cardinality and typed staging behavior unchanged; Increment 293 now extends Control Rig AimLookAt with an optional validated pole control, proves deterministic pole-up evaluation, and falls back to world-up for degenerate pole directions without retaining history; Increment 294 now appends finite 0–180° AimLookAt angle limits with backward-compatible defaults, proves deterministic 30° clamping, and rejects invalid ranges without retaining history; Increment 295 now stages one copied `query.entity.has_component.Result` output before a downstream Boolean consumer, proves query-fact-driven Boolean execution, and rejects a second staged consumer; Increment 296 now proves the same copied query result can stage before `flow.branch.Condition`, preserves true/false branch targets, and retains explicit one-hop/deeper-condition and arbitrary ECS scheduling deferrals; Increment 297 now stages one copied `query.entity.get_component.Result` token before `query.entity.has_component.Component`, proves copied get-to-has query execution, and rejects a second Component consumer and composed/deeper query dependencies; Increment 298 now carries compiler-marked staging transfers through v4 IR/bytecode and emits a dedicated copied `StagedValueTransferred` trace event in both the VM and debugger, while v1–v3 bytecode remains decodable with ordinary transfer semantics; Increment 299 now bounds `ScriptGraphUVE::ValidateUVE` to 64 nodes, accepts the exact boundary, and returns one deterministic graph-level `NodeCountExceeded` diagnostic before node/link traversal when exceeded; Increment 300 now bounds IR-to-bytecode lowering at 256 instructions, accepts the exact boundary, and emits one deterministic `InstructionLimitExceeded` diagnostic before lowering when exceeded; the serialized bytecode cap remains an independent 4096-instruction boundary; Increment 301 now bounds `ScriptGraphUVE::ValidateUVE` to 256 links after the 64-node guard, accepts the exact boundary with valid typed links, and returns one deterministic graph-level `LinkCountExceeded` diagnostic before traversal when exceeded; Increment 302 now extends the existing bounded TwoBoneIK solve with deterministic finite root and mid rotations that align each control's local +X bone axis to the solved root-to-mid and mid-to-end directions, blended by the existing clamped weight while preserving position, pole, reachability, and no-history ownership; Increment 303 now closes the bounded live-debug persistence contract with schema-versioned strict JSON envelopes, copied filter/truncation annotations, 512-event and 1 MiB payload limits, monotonic sequence/frame validation, runtime identity omission, deterministic schema/field/parse diagnostics, and offline session import/export through the existing debugger authority; Increment 304 now adds deterministic bounded copied analysis over live-debug traces with the same sequence/frame/cost/event invariants, event count, double-precision total cost, maximum evaluated candidates, lexicographically ordered kind counts, and a fail-closed 512-event cap; Increment 305 now adds six finite Number-to-Boolean comparison nodes (`Equal`, `Not Equal`, `Greater`, `Less`, `Greater Equal`, and `Less Equal`), stages up to two direct Number producers onto one comparison A/B pair in deterministic order, emits copied Boolean results, and rejects composed dependencies or multiple comparison consumers; Increment 306 now adds six direct finite Number utility nodes (`Modulo`, `Abs`, `Min`, `Max`, `Clamp`, and `Power`) with copied Number results, zero-near modulo rejection, reversed-range Clamp rejection, and fail-closed non-finite input/result handling; Increment 307 now proves one finite Number comparison Result stages into `flow.branch.Condition`, preserves producer-before-transfer-before-ConditionalJump ordering, and selects the deterministic true or false continuation; Increment 308 now adds a complete typed local-variable runtime subsystem with nine Make/Get/Set Number, Boolean, and Vector3 nodes, per-instance 256-slot copied storage, finite/default validation, type-safe updates, compatible reload persistence, editor snapshot counts, and staged Get outputs; Increment 309 now adds a complete bounded Vector2 function-library family with Make, Add, Subtract, Multiply, Length, and Normalize nodes, a value-only ScriptVector2 VM type, finite copied results, hypot-based length, deterministic zero-length rejection, and one-hop Vector2/Number staging; Increment 311 now adds version-5 FlowControlDispatch for Return, Do Once, Gate, and Switch, bounded persistent latch/Gate state, deterministic Default routing, a 53-node catalog, and compiler/bytecode/VM/editor proof; Increment 312 now adds explicit Number↔Boolean and Vector2↔Vector3 conversion nodes, finite conversion semantics, Vector2→Vector3 Z default metadata, bounded conversion staging, and exact-pin compiler/VM/editor proof; Increment 315 now adds eight bounded scalar Float nodes with finite fail-closed execution and deterministic Seed-based random outputs; Increment 316 now adds seven bounded Vector2/Vector3 nodes with finite copied kernels, zero-length direction rejection, one-hop compiler staging, and catalog/VM/editor proof; Increment 324 now adds the eleven-node Transform family with normalized TRS value semantics, Matrix4x4-backed point evaluation, typed compiler/VM/runtime integration, and editor palette proof; strings, arrays/maps/sets/struct reflection, persistence, global/shared scope, and arbitrary scheduling remain explicitly deferred; composed/deeper query-Boolean chains and arbitrary ECS scheduling remain explicitly deferred; composed/deeper Scale dependencies and arbitrary graph scheduling remain explicitly deferred; composed/deeper Boolean chains and arbitrary graph scheduling remain explicitly deferred; composed/deeper Float chains and arbitrary graph scheduling remain explicitly deferred; composed/deeper Number/vector dependencies and arbitrary graph scheduling remain explicitly deferred; hidden clock access, string payloads, mutation, ECS access, and arbitrary service calls remain deferred; broader runtime/editor node coverage, debugger controls beyond this trace/step foundation, general graph scheduling, and engine-call breadth remain partial. |
| 10 | **Renderer expansion** | Material/mesh workflow and profiling budgets are stable. | ParticleRuntimeUVE now exposes a bounded copied particle render bridge into RenderQueueUVE; Renderer3DUVE has an explicit borrowed per-frame input seam with copied extraction/truncation diagnostics, a GPU-independent particle draw-command DTO, and a bounded built-in OpenGL particle submission through a persistent vertex buffer and source-alpha pipeline; camera-facing billboarding, particle textures/assets, GPU simulation/compute, camera culling/occlusion, dynamic resolution, platform backends, and mobile visual quality remain partial and require measured validation. |
| 11 | **DOCX Motion Query inventory execution** | Legacy partial queue and shared animation foundations are closed or have explicit approved dispositions. | Every one of the 198 source filenames is accounted for in `MOTION_QUERY_INVENTORY_ROADMAP.md`, adapted to UVE-native naming and ownership, and implemented only when its runtime/editor/diagnostic/test dependencies are ready. |
| 12 | **Platform, release, sample, and documentation** | Installable runtime, asset cooking, project CLI, and stable editor workflows exist. | Windows/Linux/mobile targets, build configurations, cooking, packaging, release automation, sample project, API/manual/plugin/build guides. |
| 13 | **Ecosystem** | Stable release artifacts, project format, security, and operational ownership exist. | Hub, project management, engine integration, account/privacy, public site, cloud collaboration, marketplace safety, beta, and launch gates. |

## Legacy partial audit rules

A `PARTIAL` row may be promoted to `COMPLETED` only after inspecting the actual repository. If the implementation already exists, the loop should close the stale status with tests and documentation rather than duplicate code. If only part exists, the increment must identify the missing authority and implement only that boundary. If the capability depends on unavailable platform or service infrastructure, it remains `PARTIAL` with a written disposition instead of receiving a false completion claim.

## Source authorities

The detailed domain roadmaps remain authoritative for their own capabilities: [`CORE_RUNTIME_ROADMAP.md`](CORE_RUNTIME_ROADMAP.md), [`RENDERING_ROADMAP.md`](RENDERING_ROADMAP.md), [`EDITOR_ROADMAP.md`](EDITOR_ROADMAP.md), [`TOOLING_ROADMAP.md`](TOOLING_ROADMAP.md), [`GAMEPLAY_CONTENT_ROADMAP.md`](GAMEPLAY_CONTENT_ROADMAP.md), [`PLATFORM_RELEASE_ROADMAP.md`](PLATFORM_RELEASE_ROADMAP.md), and [`ECOSYSTEM_ROADMAP.md`](ECOSYSTEM_ROADMAP.md). The DOCX-derived Motion Query filename and target-path authority is [`MOTION_QUERY_INVENTORY_ROADMAP.md`](MOTION_QUERY_INVENTORY_ROADMAP.md). The master AAA intent is [`AAA_CORE_ROADMAP.md`](AAA_CORE_ROADMAP.md).


## Batched subsystem sequence after Increment 307

The remaining Visual Scripting and Motion Query work is intentionally grouped by feature or subsystem. A batch may span many implementation and test files when they share one ownership boundary; unrelated systems remain separate.

| Batch | Coherent scope | Completion boundary |
|---:|---|---|
| 1 | **Visual Scripting Variable Runtime v1** | Typed local Number/Boolean/Vector3 storage with explicit graph-instance scope, bounded slot capacity, default initialization, Get/Set/Make node registration, compiler/VM/runtime integration, editor read-only discovery, reload compatibility, and native/managed/runtime tests. Strings, arrays/maps/sets/struct reflection, persistence, and arbitrary shared/global scope remain deferred. |
| 2 | **Visual Scripting Function Library v1** | Group related remaining pure math/vector/rotation/transform functions into coherent families with shared numeric policy, registration, execution, typed pins, editor discovery, and integrated tests rather than one node per increment. Increment 315 completes eight bounded scalar Float nodes—Lerp, Remap, Sin, Cos, Tan, Sqrt, Random, and Random Range—with finite fail-closed semantics and deterministic Seed-based random outputs. Increment 316 completes seven bounded Vector2/Vector3 nodes—Vector2 Dot, Distance, Direction, and Lerp plus Vector3 Distance, Direction, and Lerp—with finite copied kernels, zero-length direction rejection, one-hop compiler staging, and catalog/VM/editor proof; rotation, transform, and additional families remain separate bounded work. |
| 3 | **Visual Scripting Type System and Conversion v1** | Bounded typed conversion and default-value contracts across the supported VM alternatives, diagnostic ownership, pin compatibility, serialization, and editor presentation. |
| 4 | **Visual Scripting Execution and Runtime Integration v1** | Grouped flow-control and runtime lifecycle foundations such as Do Once/Gate/Switch/Return plus bounded execution state, only where their shared scheduling and ownership contracts are complete. |
| 5 | **Motion Query Foundation and Configuration** | Close a coherent group of plugin manifest/build, schema/version, asset-ingestion, derived-data, and compatibility contracts using the existing Runtime authorities and inventory dispositions. Increment 313 completes the plugin JSON/native manifest and organized-layout parity seam with bounded semantic version validation; schema/version, asset-ingestion, derived-data, and remaining compatibility dispositions continue as separate bounded work. |
| 6 | **Motion Query Runtime Search and Evaluation** | Group feature channels, sampling, history, trajectory, search, interaction, transition, and animation-node integration into dependency-complete runtime batches with fixtures and performance evidence. Increment 314 completes the shared animation-node settings-policy validator across search budgets, matching weights, continuity, and transition bounds; deeper runtime evaluation changes remain separate bounded work. |
| 7 | **Motion Query Editor Authoring and Debugging** | Group database editor, asset browser/tree, viewport/view-model, debugger, trace provider/analyzer, persistence, replay, and managed bridge presentation by shared editor/debug ownership. Increment 317 adds the first managed Avalonia authoring surface over copied native database rows: select/remove, display-name, schema-ID, maximum-candidate, and validation commands use the existing revision-guarded bridge; registration, candidate payload construction, asset browser/tree, viewport/view-model breadth, and full reflection remain separate bounded work. Increment 318 adds copied candidate count, maximum limit, schema ID, validity, dirty-state facts, and the native 14-command metadata catalog to the managed panel; typed registration and full candidate payload authoring remain deferred until a bounded wire schema exists. Increment 319 adds a revision-guarded native Copy/Paste database bridge with bounded resource identity, display name, database ID, and context generation remapping; feature vectors, trajectories, poses, filesystem asset loading, and registration payloads remain separate partial work. Increment 320 completes the managed history/clipboard presentation workflow: copied native `clipboardAvailable`, `canUndo`, and `canRedo` facts drive bounded Undo, Redo, Refresh, Copy, and Paste controls through revision-guarded commands. Increment 321 closes the typed registration and candidate-feature wire schema with bounded context/schema/settings/events, vectors, trajectories, skeleton/pose data, evaluation time, candidate identity, native parser validation, nested authoring revision guards, real registration/add/remove/undo probe coverage, and managed Avalonia controls. Increment 322 adds case-insensitive `.uvemotionquery` primary classification and copied native/bridge/managed Motion Query Content Browser focus over the current folder, with independent Registered correlation and no-I/O ownership; asset loading, full reflection, viewport, and broader view-model breadth remain partial. Increment 323 completes the Visual Scripting Rotation/Quaternion family: nine typed descriptors, appended Rotation graph/VM value semantics, checked Euler/LookAt/Slerp/axis-angle math, compiler staging, VM dispatch, and native regression coverage; Transform, variable-collection, and broader service families remain partial. Increment 324 completes the Visual Scripting Transform family: eleven typed descriptors, appended Transform graph/VM value semantics, normalized TRS and Matrix4x4-backed point math, compiler staging, VM dispatch, runtime finite checks, editor palette proof, and native regression coverage; Increment 325 completes the remaining Flow loop family: Event, Loop, For Loop, While Loop, and Delay descriptors, v5 FlowControlDispatch lowering, capped loop iteration state, For Loop Index output, persistent frame-gated Delay behavior, context-free fail-closed semantics, and native catalog/compiler/VM/editor proof; Increment 326 now completes twelve typed Variable collection descriptors for Array, Map, Set, and Struct, value-only finite collection validation, bounded local storage, exact Get→Set staging, and native/editor proof; Increment 327 now completes six typed Entity lifecycle nodes for Spawn, Destroy, Find, Get Entity, Add Component, and Remove Component through caller-owned copied callbacks with fail-closed VM dispatch and native/editor proof; Increment 328 now completes eight Input nodes for keyboard, mouse, gamepad, axis, and action queries plus seven Camera query/control nodes through caller-owned copied callbacks, finite token/value validation, bounded compiler staging, and native/editor proof; Increment 329 now completes the 150-node catalog with ten Animation control/state nodes and ten Motion Query build/search/control nodes through caller-owned copied callbacks, bounded Entity-to-Actor staging, native/editor/compiler proof, and finite fail-closed VM execution; Increment 330 now completes the 161-node catalog with eleven Physics query/force nodes through copied Vector3/Entity/Number/Boolean values, bounded casts/overlap/counts, caller-owned physics callbacks, bounded Entity-to-Body/Ignore staging, native/editor/compiler proof, and finite fail-closed VM execution; persistence, arbitrary reflection, playback/session/database ownership, physics backend ownership, and broader service families remain partial. |

## Next autonomous batch after Increment 330

The following ten increments are the current dependency-ordered plan. Each item is a logical-scope target, not a promise to split implementation by file count; the exact boundary may be refined after repository inspection, but no item may be reduced to an isolated helper or speculative node.

| Increment | Coherent target | Planned completion boundary |
|---:|---|---|
| 322 | **Motion Query Content Browser asset focus** | **Complete.** Added `.uvemotionquery` primary classification, native Motion Query focus filtering, copied bridge/managed focus labels, deterministic content-browser tests, and roadmap/contract evidence without loading assets or mutating the filesystem. |
| 323 | **Visual Scripting Rotation and Quaternion family** | **Complete.** Added Make/Break Rotation, Degrees, Radians, Euler, Quaternion, Look At, Slerp, and Rotate as one typed catalog/compiler/VM/math family with finite normalization, deterministic failure semantics, compiled VM proof, and native regression coverage. |
| 324 | **Visual Scripting Transform family** | **Complete.** Added eleven typed Make/Break, Get/Set Position/Rotation/Scale, Translate, Rotate, and Transform Point nodes with normalized TRS value ownership, exact pins, compiler staging, VM/runtime integration, display-order collision resolution, editor palette proof, and native regression coverage. |
| 325 | **Visual Scripting remaining Flow loop family** | **Complete.** Implemented Event, Loop, For Loop, While Loop, and Delay with exact typed descriptors, v5 FlowControlDispatch lowering, capped iteration state, For Loop Index output, persistent frame-gated Delay, context-free fail-closed behavior, debugger stepping coverage, native regression coverage, and updated catalog/bridge/roadmap contracts. |
| 326 | **Visual Scripting variable collections** | **Complete.** Implemented twelve typed Array/Map/Set/Struct Make/Get/Set descriptors, appended graph/VM value types, finite 64-entry collection validation, bounded copied local storage, exact one-hop Get→Set staging, native catalog/compiler/VM/runtime coverage, and editor palette proof; persistence and arbitrary reflection remain deferred. |
| 327 | **Visual Scripting entity/component family** | **Complete.** Added six typed Entity lifecycle nodes—Spawn, Destroy, Find, Get Entity, Add Component, and Remove Component—with caller-owned copied callbacks, fail-closed VM dispatch in control-flow and dependency-scheduler paths, catalog/bridge coverage, and native regression tests; existing Has/Get Component remain the read-only copied-fact query family. |
| 328 | **Visual Scripting Input and Camera families** | **Complete.** Added eight keyboard/mouse/gamepad/action query nodes and seven camera query/control nodes through caller-owned copied callbacks, finite token/value validation, bounded Input Boolean/Number/Vector2 and Camera Entity compiler staging, catalog/bridge coverage, and native regression tests. |
| 329 | **Visual Scripting Animation and Motion Query bridge family** | **Complete.** Added ten Animation control/state nodes and ten Motion Query build/search/control nodes through caller-owned copied callbacks aligned with existing core/plugin value authorities, finite token/vector/pose/weight validation, bounded Entity staging into Actor pins, catalog/bridge/compiler coverage, and native direct/scheduler/fail-closed regression tests; playback/session/database ownership remains outside the VM bridge. |
| 330 | **Visual Scripting Physics query and force family** | **Complete.** Added eleven Raycast, Sphere/Box/Capsule Cast, Overlap, Apply Force/Impulse, velocity, gravity, and collision-state nodes with copied value-only callback contracts, bounded query/mutation validation, one-hop Entity staging into Body/Ignore consumers, catalog/bridge/compiler coverage, and native direct/scheduler/fail-closed regression tests; current native cast backends and physics ownership remain caller-owned. |
| 331 | **Visual Scripting Audio, UI, and Debug family** | **Deferred while verified runtime/editor partials are being closed.** Revisit only after the partial queue—including plugin lifecycle, console security, runtime/content depth, animation convergence, and renderer expansion—has been audited and completed or explicitly dispositioned. |

No new increment should be created merely because one file, class, node, or helper is complete. Each batch must remain logically coherent, integrated, registered, tested, documented, and ready for one meaningful CI cycle.
