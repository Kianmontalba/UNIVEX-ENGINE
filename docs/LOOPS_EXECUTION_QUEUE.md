# UNIVEX Engine — Loops Execution Queue

> This is the execution queue for the autonomous loops workflow. It preserves the existing roadmaps and the supplied DOCX-derived Motion Query inventory as authorities. The queue is dependency-ordered; it is not permission to generate speculative source files in bulk.

## Operating contract

Each loop selects one bounded, reviewable capability. The implementation must extend the existing engine authority, preserve ownership and thread boundaries, add focused tests, run the full validation matrix, update the domain roadmaps, create a signed commit, push the working branch, wait for required CI, and merge only when the review state is green.

| Review state | Required action |
|---|---|
| **Red** | Stop. Treat the issue as a blocker, find the root cause, fix it, and repeat review and validation. Do not merge. |
| **Orange** | Stop before merge. Re-review architecture, ownership, failure paths, and test evidence; add the missing proof or reduce scope. |
| **Green** | Required checks and review are clear. Push and merge autonomously, then continue to the next queue item. |

## Execution order

| Phase | Queue | Entry condition | Completion gate |
|---:|---|---|---|
| 1 | **Viewport presentation** | Legacy Rendering Increment 63 was already implemented and verified. | Real renderer presentation, shader parity, native/managed validation, and OpenGL/Xvfb smoke. **Completed.** |
| 2 | **Selection and viewport visual feedback** | Uses the renderer-owned editor visual pass from Phase 1. | Bounded selection rectangle, gizmo-axis, and camera-orientation facts reach the GPU pass through copied value-only state. **Completed.** |
| 3 | **Material and mesh asset workflow** | Inspector GUID binding, deterministic typed UVE envelope reimport, copied AssetManager failure diagnostics, copied Renderer3D invalid/pending/failed/fallback counts, deterministic raw model/texture/material source classification, selected-entry Content Browser import/reimport capability plus explicit native queue execution, and the bounded Text Import Parser v1 are closed. | Text parsing is deterministic, NUL-safe, cache-versioned, and covered by native/managed tests. Remaining FBX/OBJ/glTF model and PNG/JPEG texture conversion backends, parser-specific previews, and external-library/licensing decisions remain an explicit audit boundary before implementation. |
| 4 | **Core asset-type importers and data table pipeline** | Stable virtual paths, asset identity, derived-data boundaries, explicit parser/licensing choices, and generation-aware dependency invalidation planning; typed UVE envelope copy-register contracts are now a completed foundation. | The dependency graph can produce a deterministic bounded reverse-dependent closure. Raw model, texture, audio, material, animation, shader, and typed CSV/JSON/TSV import contracts are selected and implemented only where fixtures and validation exist. |
| 5 | **Scene components and prefab maturity** | Runtime scene ownership and serialization are authoritative; camera, light, collider, rigid-body, audio-source, mesh, Script, Particle, and PrefabInstance validation plus bounded deterministic override persistence are now closed foundations for their authored data. | Caller-owned failure-atomic prefab Apply/Revert is proven with explicit baseline semantics, DetectPrefabOverrideConflictsUVE now provides bounded read-only baseline preflight with copied conflict evidence and hard-cap truncation, validated ScriptComponent ownership now reconciles empty-path detach and graph-to-runtime attach without filesystem ownership, ParticleRuntimeUVE now provides bounded CPU emission/update state with finite preflight, deterministic integration, and lifetime culling, and the existing Increment 18 AudioSourceSystemUVE/AudioSystemUVE/EngineCore listener lifecycle is now documented as closed; asset loading, replacement reload, GPU particle simulation/renderer depth, mixer/effects/routing/clip assets/streaming/backend selection, source-prefab mutation, automatic merge/revision tracking, shape/filter breadth, constraints, character depth, editor commands, and conflict-resolution UX require independent bounded increments. |
| 6 | **Tooling partial closure** | Audit stale or partial implementations before adding code. | ScriptAssetLoaderUVE now provides bounded VFS-backed ScriptComponent path-to-graph-schema loading with copied diagnostics and deterministic repeated reads; bytecode reload/last-known-good state, compile/attach orchestration, asset-manager integration, plugin capability/lifecycle, developer console security, and documentation delivery each still require implementation and proof or an explicit deferred disposition. |
| 7 | **Simulation, input, audio, and save depth** | Core services and platform targets are explicit. | Deterministic rigid-body integration, AABB collision resolution, raycast layer queries, symmetric collider layer/mask filtering, AreaComponentUVE persistence, bounded read-only area-to-collider overlap facts, a generation-safe copied enter/exit lifecycle tracker with truncation-safe baseline retention, EngineCore typed queued Entered/Exited event-bus delivery, validated Box/Sphere/Capsule collider descriptors mapped through a shared conservative AABB cache, pure deterministic swept-AABB `SweepAabbUVE` time-of-impact math, opt-in CharacterControllerUVE TOI-assisted tunneling prevention with normal projection, a stateless per-query median-split BVH broad phase preserving legacy pair ordering plus a copied refit-only DynamicAabbBvhUVE policy for moved proxies with full-generation stale rejection, an optional bounded generation-safe distance/hinge positional solver integrated after collision resolution, bounded kinematic substep movement/contact projection, GamepadInputSystemUVE bounded copied axis/button snapshots, AudioSystemUVE bounded mixer-group routing/multiplier diagnostics, and SaveGameSystemUVE current-schema dispatch with unsupported-version diagnostics are independently tested; exact sphere/capsule narrow phases, oriented bounds, shape casts, ground/step solver behavior, angular constraints/motors/limits, backend-wide CCD/TOI integration, BVH reuse/update policy, persistent/editor-authored constraints, constraint islands, dynamic-body push, real input backend polling, action-layer gamepad bindings, touch/gyro/remapping, mixer effects, clip asset resolution, streaming, concrete audio backends, explicit vN-to-vN+1 save transforms, migration rewrite policy, compression/encryption/cloud hooks, and gameplay-domain save state remain independently tested and profiled work; AudioSource entity ownership, active-camera listener updates, play-on-awake, and listener-relative attenuation already have native runtime/test proof. |
| 8 | **Animation runtime convergence** | Shared pose, skeleton, time, and evaluation contracts are stable. | SkeletonDefinitionUVE, PoseBufferUVE, and AnimationEvaluationContextUVE now provide bounded copied joint topology, skeleton identity/cardinality, finite normalized local poses, and deterministic evaluation time consumed by MotionQueryUVE and ControlRigUVE validation/evaluation; ControlRigUVE now provides deterministic bounded pose blending with finite weight clamping, normalized rotations, weighted two-bone constraint evaluation, an existing AimLookAt solve, and Increment 285’s copied SpringPosition evaluation with bounded animation-delta response, stiffness/damping validation, influence clamping, and explicit no-history ownership; animation assets/player/tree/resource loading, full playback/blending/events, Control Rig/IK depth, retargeting, procedural animation, GPU skinning, and broader editor authoring remain partial; bounded native Motion Query database authoring now includes revision-safe copied one-entry clipboard commands with identity-remapped paste, bounded 32-entry snapshot-based Undo/Redo, a copied deterministic 14-command metadata catalog for bridge/managed discovery, and bounded editor utilities for display-name validity, schema-ID normalization, and candidate identifier preflight, plus a deterministic validated default Motion Query database contract factory for editor/importer starting points, while full reflection and Visual Scripting animation integration remain independent bounded increments. |
| 9 | **Gameplay and content systems** | Runtime/editor foundations above are complete enough for user-facing content. | Nodes/components, character interaction, particles/VFX, splines, procedural generation, data-driven gameplay, Sequencer, decals, impostors, and lightmap workflows have real runtime/editor proof. Visual Scripting now has deterministic native palette descriptors, managed filtering, right-click search popup, keyboard/click selection, cursor-position insertion, a 22-node catalog spanning flow.sequence, flow.branch, Float math, Vector3 math, Boolean logic, Has/Get Component entity queries, engine.log, and engine.get_time, explicit execution-pin roles with deterministic one-upstream/one-downstream cardinality validation, bounded typed VM values including Boolean storage and copied Entity/Component query values, explicit dependency-aware VM dispatch for all supported data-node IDs, copied entity-component facts, per-entity typed runtime tick execution, a scene-owned ScriptEntityQueryAdapterUVE that maps caller-registered native component types to binding-ordered copied presence facts for alive entities, and a query-aware ScriptRuntimeUVE tick path that refreshes those facts immediately before each enabled instance executes; Increment 274 added bounded copied ScriptVmTraceEventUVE records for node execution, typed value transfer, query-fact refresh, completion, and failure, with deterministic refresh-first ordering and truncation evidence; Increment 275 now copies debugger trace history through ScriptSourceMappingUVE, EditorBridgeUVE, and the managed Avalonia snapshot/status path alongside source-mapped breakpoints; Increment 276 now adds flow.sequence and flow.branch authoring/presentation semantics, deterministic execution-link cardinality diagnostics, and an explicit compiler deferral until ConditionalJump runtime support; Increment 277 now establishes a version-2 ConditionalJump bytecode contract with bounded true/false targets, strict decoder validation, and legacy v1 data-only compatibility; Increment 278 now evaluates copied Boolean conditions through the VM, selects bounded true/false targets with instruction-budget loop safety, and exposes an explicit copied-context debugger stepping seam; Increment 279 now lowers one validated flow.sequence node into v3 SequenceDispatch with ordered direct non-flow targets, missing-output continuation, VM budget safety, and debugger stepping proof; Increment 280 now lowers one validated flow.branch node into ConditionalJump with caller-bound Boolean Condition, bounded True/False direct non-flow targets, missing-output continuation, and explicit data-condition staging and nested-flow deferral; Increment 281 now stages one direct output from an approved built-in Boolean producer into Condition through producer-before-branch ordering and an immediate TransferValue, while custom producers and producer data dependencies remain explicitly deferred; Increment 282 now orders at most one approved built-in Boolean dependency before that producer, emits both typed transfers, and rejects cycles and deeper chains without partial IR; Increment 283 now adds an explicit `engine.log` catalog/compiler/VM/runtime seam that accepts one copied Number through a caller-owned callback, emits trace evidence, and fails closed when unbound or rejected; Increment 284 now adds an explicit `engine.get_time` catalog/compiler/VM/runtime seam that writes one finite copied Number through a caller-owned callback and fails closed on unbound, rejected, non-finite, or capacity-exhausted output; Increment 286 now stages one direct `engine.get_time.Value` output before a `math.float.*` A/B consumer through an immediate TransferValue and proves runtime addition; Increment 287 now stages one direct built-in Vector3 `Vector`/`Result` output before one `math.vector3.*` A/B/Vector consumer, proves copied Vector3 execution, and rejects composed/deeper vector dependencies; Increment 288 now stages finite `math.vector3.dot.Result` and `math.vector3.length.Length` outputs before one `math.float.*` A/B consumer, proves dot-to-Float execution, and rejects composed/deeper Number chains; Increment 289 now stages one direct `math.float.*.Result` output before a downstream `math.float.*` A/B consumer, proves multiply-to-add Float chain execution, and rejects a second staged consumer; Increment 290 now stages one direct `logic.boolean.*.Result` output before another Boolean consumer input, proves not-to-and chain execution, and rejects a second staged consumer; Increment 291 now stages one direct finite Number producer (`engine.get_time.Value` or `math.float.*.Result`) before `math.vector3.multiply.Scale`, proves Float-driven Vector3 scaling, and rejects a second Scale consumer; Increment 292 now enforces at most one non-execution source per data input pin, preserves distinct-input fan-in, and keeps execution cardinality and typed staging behavior unchanged; Increment 293 now extends Control Rig AimLookAt with an optional validated pole control, proves deterministic pole-up evaluation, and falls back to world-up for degenerate pole directions without retaining history; Increment 294 now appends finite 0–180° AimLookAt angle limits with backward-compatible defaults, proves deterministic 30° clamping, and rejects invalid ranges without retaining history; Increment 295 now stages one copied `query.entity.has_component.Result` output before a downstream Boolean consumer, proves query-fact-driven Boolean execution, and rejects a second staged consumer; Increment 296 now proves the same copied query result can stage before `flow.branch.Condition`, preserves true/false branch targets, and retains explicit one-hop/deeper-condition and arbitrary ECS scheduling deferrals; Increment 297 now stages one copied `query.entity.get_component.Result` token before `query.entity.has_component.Component`, proves copied get-to-has query execution, and rejects a second Component consumer and composed/deeper query dependencies; Increment 298 now carries compiler-marked staging transfers through v4 IR/bytecode and emits a dedicated copied `StagedValueTransferred` trace event in both the VM and debugger, while v1–v3 bytecode remains decodable with ordinary transfer semantics; Increment 299 now bounds `ScriptGraphUVE::ValidateUVE` to 64 nodes, accepts the exact boundary, and returns one deterministic graph-level `NodeCountExceeded` diagnostic before node/link traversal when exceeded; Increment 300 now bounds IR-to-bytecode lowering at 256 instructions, accepts the exact boundary, and emits one deterministic `InstructionLimitExceeded` diagnostic before lowering when exceeded; the serialized bytecode cap remains an independent 4096-instruction boundary; Increment 301 now bounds `ScriptGraphUVE::ValidateUVE` to 256 links after the 64-node guard, accepts the exact boundary with valid typed links, and returns one deterministic graph-level `LinkCountExceeded` diagnostic before traversal when exceeded; Increment 302 now extends the existing bounded TwoBoneIK solve with deterministic finite root and mid rotations that align each control's local +X bone axis to the solved root-to-mid and mid-to-end directions, blended by the existing clamped weight while preserving position, pole, reachability, and no-history ownership; Increment 303 now closes the bounded live-debug persistence contract with schema-versioned strict JSON envelopes, copied filter/truncation annotations, 512-event and 1 MiB payload limits, monotonic sequence/frame validation, runtime identity omission, deterministic schema/field/parse diagnostics, and offline session import/export through the existing debugger authority; composed/deeper query-Boolean chains and arbitrary ECS scheduling remain explicitly deferred; composed/deeper Scale dependencies and arbitrary graph scheduling remain explicitly deferred; composed/deeper Boolean chains and arbitrary graph scheduling remain explicitly deferred; composed/deeper Float chains and arbitrary graph scheduling remain explicitly deferred; composed/deeper Number/vector dependencies and arbitrary graph scheduling remain explicitly deferred; hidden clock access, string payloads, mutation, ECS access, and arbitrary service calls remain deferred; broader runtime/editor node coverage, debugger controls beyond this trace/step foundation, general graph scheduling, and engine-call breadth remain partial. |
| 10 | **Renderer expansion** | Material/mesh workflow and profiling budgets are stable. | ParticleRuntimeUVE now exposes a bounded copied particle render bridge into RenderQueueUVE; Renderer3DUVE has an explicit borrowed per-frame input seam with copied extraction/truncation diagnostics, a GPU-independent particle draw-command DTO, and a bounded built-in OpenGL particle submission through a persistent vertex buffer and source-alpha pipeline; camera-facing billboarding, particle textures/assets, GPU simulation/compute, camera culling/occlusion, dynamic resolution, platform backends, and mobile visual quality remain partial and require measured validation. |
| 11 | **DOCX Motion Query inventory execution** | Legacy partial queue and shared animation foundations are closed or have explicit approved dispositions. | Every one of the 198 source filenames is accounted for in `MOTION_QUERY_INVENTORY_ROADMAP.md`, adapted to UVE-native naming and ownership, and implemented only when its runtime/editor/diagnostic/test dependencies are ready. |
| 12 | **Platform, release, sample, and documentation** | Installable runtime, asset cooking, project CLI, and stable editor workflows exist. | Windows/Linux/mobile targets, build configurations, cooking, packaging, release automation, sample project, API/manual/plugin/build guides. |
| 13 | **Ecosystem** | Stable release artifacts, project format, security, and operational ownership exist. | Hub, project management, engine integration, account/privacy, public site, cloud collaboration, marketplace safety, beta, and launch gates. |

## Legacy partial audit rules

A `PARTIAL` row may be promoted to `COMPLETED` only after inspecting the actual repository. If the implementation already exists, the loop should close the stale status with tests and documentation rather than duplicate code. If only part exists, the increment must identify the missing authority and implement only that boundary. If the capability depends on unavailable platform or service infrastructure, it remains `PARTIAL` with a written disposition instead of receiving a false completion claim.

## Source authorities

The detailed domain roadmaps remain authoritative for their own capabilities: [`CORE_RUNTIME_ROADMAP.md`](CORE_RUNTIME_ROADMAP.md), [`RENDERING_ROADMAP.md`](RENDERING_ROADMAP.md), [`EDITOR_ROADMAP.md`](EDITOR_ROADMAP.md), [`TOOLING_ROADMAP.md`](TOOLING_ROADMAP.md), [`GAMEPLAY_CONTENT_ROADMAP.md`](GAMEPLAY_CONTENT_ROADMAP.md), [`PLATFORM_RELEASE_ROADMAP.md`](PLATFORM_RELEASE_ROADMAP.md), and [`ECOSYSTEM_ROADMAP.md`](ECOSYSTEM_ROADMAP.md). The DOCX-derived Motion Query filename and target-path authority is [`MOTION_QUERY_INVENTORY_ROADMAP.md`](MOTION_QUERY_INVENTORY_ROADMAP.md). The master AAA intent is [`AAA_CORE_ROADMAP.md`](AAA_CORE_ROADMAP.md).
