<div align="center">

<h1><strong>UNIVEX ENGINE — DEVELOPER TOOLING ROADMAP</strong></h1>

<strong>Visual scripting, plugins, diagnostics, content-data tools, console, CI, and engineering documentation</strong>

</div>

> Tooling exists to make verified engine systems easier to author and diagnose. It must not invent parallel runtime ownership or present controls that cannot affect real engine state.

| Status | Completed foundation | Delivered capability | Ongoing boundary |
|---|---|---|---|
| **COMPLETED** | **Increment 4** | Configuration and command-line foundation. | Future tools reuse versioned settings and explicit command-line contracts. |
| **COMPLETED** | **Increment 7** | Generic asset import/hot-reload/bundle foundation. | Specific import formats and automatic behavior remain partial. |
| **COMPLETED** | **Increment 21** | Shader authoring and hot-reload tooling foundation. | Shader diagnostics stay renderer-owned, not a generic editor-console substitute. |
| **COMPLETED** | **Increment 37** | GCC CI verification and X11/GLFW build prerequisites. | CI remains the build/test evidence baseline. |
| **COMPLETED** | **Increments 59–61** | Project content index, deterministic import jobs/cache, and explicit change-review journal. | FileSystem/Import views remain safe, read-only monitoring surfaces until deliberate workflow increments. |
| **COMPLETED** | **Increment 62** | Deterministic Inspector drawer registry. | Drawers route authored writes through validated editor commands; this is not reflection or a plugin ABI. |

<div align="center">

<h2><strong>PARTIAL — ACTIVE TOOLING SEQUENCE</strong></h2>

</div>

| Status | Milestone | Intended capability | Required proof before completion |
|---|---|---|---|
| **PARTIAL — planned** | **Increment 64 — Editor Tool Sessions v1** | Reusable begin/preview/commit/cancel tool interaction lifecycle for existing transform gizmos. | Behavior parity tests for gizmo interaction, snapping, history, cancellation, and authoring restrictions. |
| **PARTIAL — planned** | **Increment 68 — Project Health & Headless Automation v1** | `uve_project_check` validates registry entries, envelopes, primitive payloads, and broken references without a window. | Deterministic human/machine output, CI fixtures, and recovery-oriented diagnostics. |
| **COMPLETED** | **Increment 75 — Visual scripting graph core** | Native C++ graph model, stable node registry, typed pins/links, structural mutation guards, and deterministic validation diagnostics. | Compiler IR, versioned bytecode, VM, bindings, debugger, script state, hot reload, and managed presentation remain separate partial milestones; no C# runtime or managed bridge is implied. |
| **COMPLETED** | **Increment 86 — Visual Scripting Editor Canvas v1** | Native bounded canvas session with node layout, ordered selection, pan/zoom view state, typed links, deterministic palette/snapshot DTOs, named bridge commands, and exact graph/layout/selection Undo/Redo. Pan/zoom is explicitly non-undoable session state. | Source mapping, watch evaluation, breakpoint UI, and durable layout persistence remain separate partial work; C# receives copied DTOs only. Focused native canvas/bridge/stdio tests and managed parser tests are required. |
| **COMPLETED** | **Increment 87 — Managed Visual Scripting Canvas Presentation v1** | Avalonia presentation control for copied nodes, links, typed-pin indicators, selection, pan/zoom, diagnostics, and named command emission through the Increment 86 bridge. | The control remains presentation-only; geometry/hit-testing/command tests, managed build/tests, Xvfb shell smoke, and full native/managed CI passed before merge. |
| **COMPLETED** | **Increment 88 — Developer Console and Diagnostics v1** | Bounded native command registry, help/clear/cvar built-ins, explicit CVAR facts, bounded output/history snapshots, named bridge requests, stdio serialization, and managed lower-dock Console presentation. | No arbitrary command scripting or filesystem/process/network/ECS/renderer/plugin mutation. Focused native/bridge/stdio/managed tests and full CI validation passed before merge. |
| **COMPLETED** | **Increment 89 — Developer Console Policy and Discovery v1** | Explicit development/shipping availability policy, severity-filtered output, deterministic prefix completion, bounded history navigation, new named bridge requests, additive JSON DTO fields, and managed discovery controls. | Shipping builds reject discovery mutations; completion/history remain native-owned and bounded. No remote console, arbitrary scripting, process execution, filesystem mutation, or cheat surface is introduced. Focused native/bridge/stdio/managed tests and full CI validation passed before merge. |
| **COMPLETED** | **Increment 90 — Typed Data Table Core v1** | Bounded native schemas and rows, Boolean/Integer/Number/String values, copied snapshots, deterministic diagnostics, and failure-atomic CSV ingestion. | The caller supplies the schema; JSON/TSV/XLSX adapters, automatic inference, asset references, hot reload, export, editor bridge, managed UI, runtime bindings, and visual-scripting nodes remain deferred. Focused tests, full GCC/Clang/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 91 — Data Table TSV/JSON Import v1** | Deterministic tab-delimited import and schema-checked JSON array-of-objects import with direct JSON type validation, bounded diagnostics, and failure-atomic row replacement. | Object member order is ignored but schema order controls values. Automatic inference, schema migration, XLSX, filesystem/asset-database ownership, references, hot reload, export, editor bridge, managed UI, runtime bindings, and visual-scripting nodes remain deferred. Focused tests, full GCC/Clang/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 92 — Data Table Asset Envelope and Catalog v1** | Versioned deterministic JSON envelope for validated native tables plus copied lexicographically sorted catalog descriptors. | Deserialization validates into a temporary table before replacement; the catalog owns descriptors only. Filesystem scanning, asset-database registration, content hashing, hot reload, editor bridge, managed UI, runtime binding, references, and visual-scripting nodes remain deferred. Focused tests, full GCC/Clang/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 93 — Read-Only Data Table Catalog Bridge v1** | Bounded native catalog DTOs, additive stdio serialization, revision synchronization, backward-compatible managed parsing, and a presentation-only Avalonia Data Tables tab. | No catalog mutation request, filesystem access, asset-database ownership, editor-side table editing, runtime binding, references, hot reload, or visual-scripting node is introduced. Focused native/stdio/managed tests, full GCC/Clang/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 94 — Read-Only Data Table Detail Preview Bridge v1** | Bounded typed column descriptors, native-formatted cell display values, additive stdio preview DTO, backward-compatible managed parsing, and presentation-only column/row preview. | C++ retains schema and row authority. No table mutation, filesystem access, asset-database registration, reference binding, hot reload, runtime binding, or visual-scripting node is introduced. Focused native/stdio/managed tests, full GCC/Clang/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 95 — Native Data Table Session Registry v1** | Bounded RAII-owned validated tables, deterministic duplicate/capacity rejection, generation-counted register/remove/clear lifecycle, copied snapshot queries, and sorted catalog facts. | No filesystem scanning, asset-database registration, automatic persistence, hot reload, managed mutation, runtime binding, or visual-scripting node is introduced. Focused native registry tests, full GCC/Clang/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 96 — Registry-Backed Data Table Bridge v1** | Non-owning native registry injection into the editor bridge, automatic registry catalog synchronization, and validated selected-table preview lookup through copied DTOs. | Registry remains native-owned and main-thread scoped. No table mutation, filesystem access, persistence, runtime binding, or managed pointer crosses the bridge boundary. Focused native integration tests, full cross-compiler/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 97 — Read-Only Data Table Preview Selection Protocol v1** | Named revision-checked native selection request, stdio JSON parsing, managed command serialization, and Avalonia catalog selection for copied native preview facts. | Selection changes presentation state only. Unknown names, stale revisions, registry-free bridge sessions, and malformed payloads reject without table mutation or pointer transfer. Focused native/stdio/managed tests, full cross-compiler/CI validation, and managed smoke/probe checks passed before merge. |
| **COMPLETED** | **Increment 98 — Data Table `.uve*` Asset Store v1** | `AssetKindUVE::DataTable`, envelope-backed save/load helpers, deterministic serializer reuse, asset-kind validation, and failure-atomic destination replacement. | One validated table per file; no directory scanning, asset-database registration, hot reload, schema inference, registry mutation, or runtime binding. Focused asset-store tests, full GCC/Clang/CI validation, and managed regression/smoke checks passed before merge. |
| **COMPLETED** | **Increment 99 — Validated Data Table Asset-Database Registration v1** | Explicit helper validates one `.uvetable` envelope before registering its lexical path and returning an existing or generated GUID. | Invalid extensions, missing files, wrong envelope kinds, and malformed payloads reject before database mutation. No directory scan, import queue, hot reload, runtime binding, or managed pointer. Focused asset/database tests, full GCC/Clang/CI validation, and managed regression/smoke checks passed before merge. |
| **COMPLETED** | **Increment 100 — Typed Data Table Asset-Manager Loader v1** | Explicitly registers the existing envelope-backed `DataTableUVE` loader and enables asynchronous ref-counted typed handles through `AssetManagerUVE`. | Registration is synchronous; load ownership, worker execution, reference counting, and failure state remain generic `AssetManagerUVE` responsibilities. No hot reload, registry mutation, bridge update, or managed pointer. Focused loader tests, full GCC/Clang/CI validation, and managed regression/smoke checks passed before merge. |
| **COMPLETED** | **Increment 101 — Schema-Driven Data Table Source Importers v1** | Explicit `.csv`, `.tsv`, and `.json` importer registration, caller-supplied typed schema settings, bounded source reads, and `.uvetable` destination generation. | No schema inference, directory scan, import queue, hot reload, registry mutation, runtime binding, or managed pointer. Focused importer tests, full GCC/Clang/CI validation, managed regression/smoke checks, and a remediated HotReload timing test passed before merge. |
| **COMPLETED** | **Increment 102 — Deterministic Data Table Import-Cache Identity v1** | Settings-version fingerprint includes table name and ordered typed schema, allowing the generic derived-artifact cache to force reimport when semantic import settings change. | Cache remains metadata-only and generic. No cache schema migration, filesystem watcher, background work, schema inference, hot reload, registry mutation, runtime binding, or managed pointer. Focused cache identity tests, full GCC/Clang/CI validation, and managed regression/smoke checks passed before merge. |
| **COMPLETED** | **Increment 103 — Data Table Pipeline Bootstrap v1** | One explicit helper composes `.csv`/`.tsv`/`.json` source importer registration with typed `DataTableUVE` asset-manager loader registration. | The helper owns no service or asset state and introduces no new cache, queue, registry, editor bridge, runtime, or managed authority. Focused pipeline tests, full GCC/Clang/CI validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 104 — EngineCore Data Table Pipeline Integration v1** | EngineCore registers the Data Table importer and typed loader after constructing the generic asset services, exposing the complete pipeline through `GetServicesUVE()`. | Headless EngineCore integration proved CSV import and asynchronous typed load; the handle lifetime boundary was validated with AddressSanitizer. Full GCC/Clang/CI native suites and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 105 — Visual Script Canvas Layout Persistence v1** | Bounded deterministic JSON layout encoding/decoding, complete live-node validation, and native `ApplyLayoutUVE()` undo support. | The codec persists copied layout/view facts only; graph, runtime, selection, filesystem, and managed ownership remain outside the boundary, while pan/zoom remains non-undoable session state. Focused persistence tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 106 — Read-Only Visual Script Debugger Bridge Snapshot v1** | Native debugger facts are copied into the editor bridge/stdio snapshot and parsed as bounded managed DTOs. | The bridge is non-owning and read-only; no runtime mutation, raw native pointer, OpenGL resource, or managed ownership crosses the boundary. Focused native stdio and managed parser tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 107 — Managed Visual Script Debugger Status Presentation v1** | The Scripting panel renders bounded debugger state, instruction/source facts, pause reason, and breakpoint count from the copied DTO. | The managed UI adds presentation only; it does not execute commands, own debugger state, or bypass the native bridge. Full GCC/Clang/CI native validation and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 108 — Managed Visual Script Breakpoint Presentation v1** | A bounded, non-interactive Scripting-panel list renders copied breakpoint node IDs from the debugger DTO. | The list is presentation-only and hit-test disabled; breakpoint creation, removal, and debugger execution remain native concerns. Full GCC/Clang/CI native validation and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 109 — Managed Visual Script Debugger Source Mapping Presentation v1** | The debugger source node ID is mapped to a copied canvas display label with a bounded fallback when the node is not in the visible snapshot. | Mapping uses only copied DTO values and cannot mutate the graph, debugger, or runtime. Full GCC/Clang/CI native validation and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 110 — ScriptRuntime Per-Entity Bytecode Reload Safety v1** | Validated program replacement, generation tracking, last-known-good retention on failure, and structured bytecode diagnostics. | Reload is transactional per live entity; invalid candidates never replace the active program, missing instances are reported explicitly, and the runtime remains the sole execution authority. Focused runtime tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 111 — Native Plugin Manifest Capability Validation v1** | Deterministic structured diagnostics validate bounded plugin identity, display name, engine protocol, capability count, capability identifiers, and duplicate capabilities before registry mutation. | The validator strengthens the existing static plugin seam only; it adds no dynamic loading, filesystem manifest authority, ABI negotiation, arbitrary callbacks, or ownership bypass. Focused plugin validator tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 112 — Native Plugin Capability Policy v1** | An explicit bounded allowlist validates requested manifest capabilities before registry mutation, with deterministic rejection diagnostics for policy overflow, duplicate policy entries, invalid policy IDs, and disallowed capabilities. | The policy is static value data; it adds no dynamic loading, filesystem manifest authority, ABI negotiation, arbitrary callbacks, or ownership bypass. Focused policy tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 113 — Policy-Aware Native Plugin Registry Registration v1** | `NativePluginRegistryUVE` accepts an explicit capability policy overload and rejects disallowed manifests before registry mutation, while retaining the default allow-all compatibility path. | Registry mutation, duplicate IDs, capacity, scopes, generations, and lifecycle remain native registry authority. Focused policy-aware registry tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 114 — Structured Developer Console Command Registration v1** | `DeveloperConsoleUVE` exposes deterministic registration result codes and bounded diagnostic messages for invalid identifiers, invalid help, missing handlers, capacity exhaustion, and duplicate identifiers while preserving the existing bool API. | Command callbacks remain native-owned and execution remains disabled by the Shipping build policy; no raw runtime ownership, arbitrary script execution, or security bypass crosses the console boundary. Focused registration diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 115 — Structured Developer Console CVAR Registration v1** | `DeveloperConsoleUVE` exposes deterministic registration result codes and bounded diagnostic messages for invalid names, invalid values, capacity exhaustion, and duplicate names while preserving the existing bool API. | CVAR values remain bounded native data; read-only enforcement, mutation, history, completion, and Shipping exclusion remain existing console authority. Focused registration diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 116 — Structured Developer Console CVAR Mutation Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic mutation result codes for unavailable Shipping policy, unknown names, read-only CVARs, invalid values, unchanged values, and applied values while preserving the existing bool setter. | CVAR mutation remains native-owned and bounded; the detailed result is observational and does not add history bypass, arbitrary execution, managed ownership, or runtime pointer exposure. Focused mutation diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 117 — Structured Developer Console Command Execution Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic execution result codes for unavailable Shipping policy, invalid input, unknown commands, and executed commands while preserving the existing bool API and history/output side effects. | Command execution remains native-owned and bounded; detailed diagnostics do not add arbitrary dispatch, history bypass, managed ownership, or runtime pointer exposure. Focused execution diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 118 — Structured Developer Console Severity-Filter Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic filter result codes for unavailable Shipping policy, invalid filters, unchanged filters, and applied filters while preserving the existing bool setter and snapshot filtering behavior. | Severity filtering remains native-owned presentation state; detailed diagnostics do not add log ownership bypass, arbitrary execution, managed ownership, or runtime pointer exposure. Focused filter diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 119 — Structured Developer Console Completion-Prefix Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic prefix result codes for unavailable Shipping policy, invalid prefixes, unchanged prefixes, and applied prefixes while preserving the existing bool setter and completion snapshot bounds. | Completion filtering remains native-owned presentation state; detailed diagnostics do not add completion ownership bypass, arbitrary execution, managed ownership, or runtime pointer exposure. Focused prefix diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 120 — Structured Developer Console History-Navigation Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic history result codes for unavailable Shipping policy, invalid deltas, empty history, boundary requests, and applied cursor moves while preserving the existing bool API and cursor snapshot semantics. | History remains bounded native state; detailed diagnostics do not add history mutation bypass, arbitrary execution, managed ownership, or runtime pointer exposure. Focused navigation diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 121 — Structured Developer Console Clear Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic clear result codes for unavailable Shipping policy, already-clear output, and applied output clearing while preserving the existing bool API and truncation-reset semantics. | Console output remains bounded native state; detailed diagnostics do not add output ownership bypass, arbitrary execution, managed ownership, or runtime pointer exposure. Focused clear diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 122 — Structured Developer Console Build-Policy Diagnostics v1** | `DeveloperConsoleUVE` exposes deterministic policy result codes for unchanged and applied Development/Shipping transitions while preserving the existing bool API and completion/history reset semantics. | Build policy remains native availability authority; detailed diagnostics do not add policy bypass, arbitrary execution, managed ownership, or runtime pointer exposure. Focused policy diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 123 — ScriptRuntime Compatible-State Preservation v1** | `ScriptRuntimeUVE` stores bounded native per-instance state, preserves that state across validated same-version bytecode reloads, and reports `compatibleStatePreserved` while retaining last-known-good programs on invalid candidates. | State is value-only and bounded; no raw ECS pointers, managed ownership, arbitrary migration callbacks, or runtime ownership bypass cross the scripting boundary. Focused state/reload tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 124 — Structured ScriptRuntime State-Update Diagnostics v1** | `ScriptRuntimeUVE` exposes deterministic state-update result codes for applied, unchanged, no-active-instance, and capacity-exceeded updates while preserving the existing bool setter. | State remains native-owned, value-only, and bounded; detailed diagnostics do not add arbitrary migration callbacks, managed ownership, or runtime pointer exposure. Focused update diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 125 — Structured ScriptRuntime Enabled-State Diagnostics v1** | `ScriptRuntimeUVE` exposes deterministic enabled-state result codes for applied, unchanged, and no-active-instance transitions while preserving the existing bool setter and disabled-instance tick filtering. | Enabled state remains native runtime control; detailed diagnostics do not add arbitrary execution, managed ownership, or runtime pointer exposure. Focused enabled-state diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 126 — Structured ScriptRuntime Attachment Diagnostics v1** | `ScriptRuntimeUVE` exposes deterministic attachment result codes for invalid entities, invalid programs with bounded diagnostics, exhausted capacity, duplicate instances, and accepted attachments while preserving the existing bool API. | Instance attachment remains native-owned and generation-safe; detailed diagnostics do not add runtime pointer exposure, managed ownership, or bypass bytecode validation. Focused attachment diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 127 — Structured ScriptRuntime Detachment Diagnostics v1** | `ScriptRuntimeUVE` exposes deterministic detach result codes for no matching active instance and applied exact-handle detachments while preserving the existing bool API. | Instance detachment remains native-owned and generation-safe; detailed diagnostics do not add runtime pointer exposure, managed ownership, or lifecycle bypass. Focused detach diagnostics, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 128 — ScriptRuntime Read-Only Instance Snapshot v1** | `ScriptRuntimeUVE` exposes deterministic copied snapshots of entity, generation, bytecode version, instruction count, state-value count, and enabled status without raw pointers or mutation shortcuts. | Snapshot data is value-only and sorted by generational entity identity; focused snapshot tests, full GCC/Clang/CI native validation, and managed regression checks passed before merge. |
| **COMPLETED** | **Increment 129 — ScriptRuntime Bridge Snapshot Presentation v1** | The editor bridge and framed stdio protocol expose bounded copied ScriptRuntime instance metadata, while the managed host parses valid entries and safely falls back for older frames. | The bridge is optional, non-owning, read-only, and revision-tracked; no ECS pointer, runtime ownership, mutation authority, or OpenGL resource crosses into C#. Focused native stdio/managed parser tests, full cross-compiler validation, and Green CI passed before merge. |
| **COMPLETED** | **Increment 130 — Managed ScriptRuntime Panel Presentation v1** | The managed Scripting panel renders copied ScriptRuntime instance count and bounded rows for generational identity, program generation/version, instruction/state counts, and enabled state. | The panel is presentation-only and consumes immutable bridge DTOs; it does not add runtime requests, ECS access, filesystem work, process ownership, or OpenGL handling. Managed UI tests, full cross-compiler/native validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 131 — ScriptRuntime Read-Only Bridge Request v1** | The native bridge advertises `ReadScriptRuntime`, the framed stdio server accepts `readScriptRuntime`, and the managed client can issue the named request to receive the bounded copied snapshot. | The request is strictly observational, ignores stale mutation revisions, and adds no runtime control, ECS access, filesystem work, process ownership, or OpenGL handling. Focused native/stdio/managed request tests, full cross-compiler validation, and Green CI passed before completion. |
| **COMPLETED** | **Increment 132 — Managed ScriptRuntime Snapshot Refresh Workflow v1** | The managed Scripting panel offers a capability-gated Refresh runtime control that issues `readScriptRuntime` and refreshes the copied ScriptRuntime rows. | The control is disabled when disconnected or when the native capability is absent; it adds no runtime mutation, ECS access, filesystem work, process ownership, or OpenGL handling. Managed UI/command tests, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 133 — ScriptRuntime Bridge Availability Reason v1** | Native ScriptRuntime snapshots include an authoritative bounded reason for unavailable attachment or available copied read-only state, and the managed panel presents that reason without inferring runtime state from counts. | The reason is additive and backward-compatible for older frames; no runtime pointer, mutation authority, ECS access, filesystem work, process ownership, or OpenGL resource crosses into C#. Focused native/stdio/managed parser tests, full cross-compiler validation, and Green CI passed before completion. |
| **COMPLETED** | **Increment 134 — Managed ScriptRuntime Enabled-State Summary v1** | The managed Scripting panel summarizes visible copied instances as enabled or disabled while preserving native total and truncation state. | Counts are derived only from bounded bridge entries and are labeled as visible when truncated; no omitted runtime state is inferred and no mutation, ECS access, filesystem work, process ownership, or OpenGL handling crosses into C#. Managed presentation tests, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 135 — Managed ScriptRuntime Instance Detail Selection v1** | The Scripting panel allows selecting a bounded copied ScriptRuntime row and presents its generational identity, program metadata, execution counts, and enabled state without issuing a native command. | Selection is local managed presentation over immutable bridge entries; it adds no runtime mutation, ECS access, filesystem work, process ownership, or OpenGL handling. Managed UI tests, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 136 — Managed ScriptRuntime Empty-State Presentation v1** | The Scripting panel explicitly distinguishes unavailable runtime attachment from an available runtime with no active copied instances, while preserving native truncation semantics. | Empty-state messaging is derived from the authoritative reason and bounded entry count; it does not infer omitted instances or add runtime mutation, ECS access, filesystem work, process ownership, or OpenGL handling. Managed presentation tests, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 137 — Managed ScriptRuntime Local Filter v1** | The Scripting panel filters bounded copied ScriptRuntime rows locally by their displayed generational identity and execution metadata while preserving native ordering. | Filtering is managed-only and cannot request, mutate, or infer native runtime state; empty filtered results are explicitly labeled as local no-match state. Managed filter tests, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 138 — Managed ScriptRuntime Filter Result Summary v1** | The Scripting panel reports how many bounded copied rows match the local filter while preserving native total counts and truncation state. | Match counts are derived only from copied DTO entries and are clearly separated from native totals; no runtime mutation, ECS access, filesystem work, process ownership, or OpenGL handling crosses into C#. Managed summary tests, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 139 — Managed ScriptRuntime Filter Reset Control v1** | The Scripting panel provides a local Clear control that restores the latest copied rows without issuing a native request or changing selection semantics. | The reset changes only managed presentation state and preserves native totals, ordering, truncation, availability, and ownership boundaries. Managed UI validation, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 140 — Managed ScriptRuntime Bridge Revision Presentation v1** | The Scripting panel shows the bridge revision associated with the currently presented copied ScriptRuntime snapshot for debugging and refresh traceability. | Revision text is copied from the existing bridge snapshot envelope and adds no runtime mutation, ECS access, filesystem work, process ownership, or OpenGL handling. Managed presentation validation, full native/managed validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 141 — ScriptRuntime Structured Tick Diagnostics v1** | `ScriptRuntimeUVE` exposes a detailed tick batch result with enabled-instance, completed, budget-exceeded, invalid-instruction, and diagnostic counts while retaining the legacy vector-returning `TickUVE` wrapper. | Diagnostics are derived from read-only VM execution results and preserve deterministic entity order; no runtime state mutation or bridge ownership is introduced. Focused scripting tests, full cross-compiler validation, and Green CI passed before completion. |
| **COMPLETED** | **Increment 142 — ScriptRuntime Tick Diagnostics Presentation v1** | The native bridge advertises and serves an explicit `readScriptRuntimeTickDiagnostics` request, executes `TickDetailedUVE()` only on the native request path, copies bounded counters into the additive `scriptRuntimeTickSummary` snapshot field, and the managed Scripting panel presents them through a capability-gated Tick diagnostics control. | The managed host never executes VM code or receives runtime pointers; snapshot rendering consumes only immutable value DTOs. Focused native direct-dispatch/stdio tests, managed parser/capability tests, full cross-compiler validation, headless shell smoke, and Green CI passed before completion. |
| **COMPLETED** | **Increment 143 — ScriptRuntime Tick Summary History v1** | The native bridge retains a bounded FIFO of recent explicit diagnostic tick summaries with monotonic sequence IDs, and the managed Scripting panel presents copied history and explicit truncation state. | The history is capped at eight value-only entries, older entries are explicitly marked truncated, and C# remains a presentation-only consumer. Focused native FIFO/managed parser tests, GCC/Clang/CI-GCC 858/858 validation, managed 55/55 validation, and Green CI passed before completion; the known CI-GCC HotReload test was rerun individually and then passed in the complete matrix rerun. |
| **COMPLETED** | **Increment 144 — Visual Script Node Descriptor v2** | Extend the native node registry with category, icon identity, stable display order, pin role, default-value metadata, compact labels, and presentation flags for the user-supplied Node-only Liquid Glass design. | C++ remains authoritative for descriptors and typed pins; C# receives bounded immutable DTOs; no second managed registry is introduced. Focused native descriptor/canvas tests, managed bridge parser tests, GCC/Clang/CI-GCC 860/860 validation, managed 55/55 validation, and Green CI passed before completion. |
| **COMPLETED** | **Increment 145 — Visual Script Graph Edit Command Contract v1** | Native add/remove node, add/remove link, move node, and selection commands expose explicit history, dirty-state, stale-revision, and Play-mode semantics through the existing value-only bridge command path. | Graph/layout edits mark the native canvas dirty and expose bounded `canUndo`/`canRedo` facts; selection history remains presentation-only, stale revisions are rejected before mutation, and all Visual Script mutations remain Edit-mode-only. Focused native command tests, managed parser/control tests, GCC/Clang/CI-GCC 860/860 validation, managed 55/55 validation, and Green CI passed before completion. |
| **COMPLETED** | **Increment 146 — Visual Script Link Validation and Diagnostic DTO v1** | Deterministic direction, type, duplicate, self-link, endpoint, and node-type diagnostics now expose bounded primary and optional related pin endpoints through the canonical native validator and bridge snapshot. | C++ remains authoritative for validation and diagnostic ordering; C# receives nullable value-only related endpoint context with older-frame fallback. Focused native validator/managed parser tests, GCC/Clang/CI-GCC 860/860 validation, managed 55/55 validation, and Green CI passed before completion. |
| **COMPLETED** | **Increment 147 — Visual Script Graph Serialization Schema v1** | Versioned nodes, links, layout positions, metadata, validation, migrations, deterministic ordering, and malformed-input rejection. | Native ordered JSON schema encode/decode, bounded arrays/text, strict unknown-field rejection, future-version migration gate, atomic native canvas apply with Undo/Redo, stdio `serializeGraph`/`deserializeGraph`, and managed immutable DTO parser. Focused native/managed tests and full cross-compiler/managed validation passed before completion. |
| **COMPLETED** | **Increment 148 — Liquid Glass Node Canvas Presentation v1** | Avalonia node cards, category-tinted headers, typed pins, compact rows, soft grid, Bézier-style links, selection treatment, and bounded fallback styling. | The managed canvas renders only immutable native DTOs, derives stable category/pin colors with a neutral fallback, and keeps native graph/history/renderer ownership outside C#. Focused style/geometry tests, managed validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 149 — Visual Script Node Palette and Search v1** | Native-advertised categories/descriptors with managed local search and insertion command. | The native canvas snapshot copies bounded descriptor metadata and typed pins while retaining the legacy type-ID list; managed search is deterministic and local; insertion emits a named type-only command whose ID allocation and graph/history validation remain native. Focused native/managed tests, GCC/Clang/CI-GCC 865/865 validation, managed 61/61 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 150 — Visual Script Interactive Link Authoring v1** | Managed drag-to-connect preview, compatible-target emphasis, native acceptance, and rejection diagnostics. | The managed canvas previews only copied DTO links, emphasizes compatible input targets using conservative native-compatible type matching, emits `addVisualScriptLink` only after local direction/type checks, and leaves final acceptance/diagnostics/history to native C++. Focused managed interaction tests, GCC/Clang/CI-GCC 865/865 validation, managed 63/63 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 151 — Visual Script Node Property and Default Value Presentation v1** | Selected-node details and validated native default-value commands. | Native owns bounded Boolean/Number default validation, overrides, dirty state, and Undo/Redo; C# renders copied pins and emits only named value-only commands. Focused native/managed tests, GCC/Clang/CI-GCC 865/865 validation, managed 64/64 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 152 — Visual Script Compile Diagnostics Presentation v1** | Bounded severity, stable code, node/pin location, source context, canvas markers, and readable diagnostics surface. | Native graph validation/IR compilation remains the source of truth with append-only diagnostic codes, bounded Error severity/source context, and immutable bridge DTOs; C# renders severity-colored node/pin markers and readable copied context only. Focused native/managed tests, GCC/Clang/CI-GCC 867/867 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 153 — Visual Script Graph Runtime Binding v1** | Bind validated graph resources to the existing native bytecode/compiler/runtime seam. | Native performs transactional graph validation, IR compilation, bytecode lowering, and generational ScriptRuntime attach; compile/lowering failures cannot mutate runtime state, duplicate entities are rejected, and no managed VM execution or ownership shortcut exists. Focused native binding tests, GCC/Clang/CI-GCC 871/871 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 154 — Reflection and Type Metadata Foundation v1** | Shared native metadata for properties, methods, resources, nodes, serialization, Visual Scripting, and Inspector generation. | Native `TypeMetadataRegistryUVE` provides bounded type identity, versioned metadata entries, property/method descriptors, resource/node/Inspector categories, deterministic copied snapshots, duplicate-safe registration, and generation tracking without managed ownership. Focused native metadata tests, GCC/Clang/CI-GCC 874/874 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 155 — Resource Handles and Dependency Graph Foundation v1** | Generation-safe handles, dependencies, stale-reference diagnostics, and bounded editor inspection. | Native `ResourceDependencyGraphUVE` uses existing AssetGuid identity plus generation-safe handles, rejects stale/unknown/duplicate/cyclic references, blocks removal while dependents exist, and exposes deterministic copied inspection snapshots without transferring resource lifetime ownership. Focused native dependency tests, GCC/Clang/CI-GCC 877/877 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 156 — Unified Time and Transform/Pose Contract v1** | Game/real/fixed/animation/pause domains and shared transform/pose representation. | Native `UnifiedTimeContractUVE` advances copied real/game/fixed/animation domains with pause semantics, bounded deltas, and fixed-step caps; `TransformPoseUVE` reuses Vector3/Quaternion math with finite-value and normalized-rotation validation for future AnimationTree, Control Rig, retargeting, motion matching, and runtime animation. Focused native tests, GCC/Clang/CI-GCC 882/882 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 157 — Animation Clip Runtime Foundation v1** | Clip metadata, sampled pose, curves/events, deterministic evaluation, and resource validation. | Native `AnimationClipUVE` validates bounded identifiers, duration, sorted normalized pose samples, and sorted events; sampling linearly interpolates shared poses with loop normalization, while event collection is deterministic across loop boundaries. Focused native tests, GCC/Clang/CI-GCC 886/886 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 158 — AnimationTree Runtime Graph v1** | Clip/player, blend, parameter, state, transition, one-shot, time-scale, sync, subtree, pose-cache, and output-pose nodes. | Native `AnimationTreeUVE` validates bounded acyclic node graphs and validated clip references, then evaluates deterministic clip playback, blending, parameter transitions, time scaling, pass-through node families, pose caching, and output selection into copied shared poses. Focused native tests, GCC/Clang/CI-GCC 890/890 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 159 — Animation State Machine and Blend Debugging v1** | Transition policy, priorities, interruption, exit-time, layered/masked blending, sync groups, and copied debug state. | Native `AnimationStateMachineUVE` validates bounded states/transitions and clip references; the evaluator applies exit-time gating, deterministic priority selection, bounded crossfade progression, interruption metadata, sync/layer debug metadata, and copied debug snapshots. Focused native tests, GCC/Clang/CI-GCC 894/894 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 160 — Control Rig and IK Foundation v1** | Native controls, spaces, constraints, two-bone IK, FABRIK where justified, pole vectors, aim/look-at, and shared-pose evaluation. | Native `ControlRigUVE` validates caller-owned controls, parent references, local/world space metadata, and bounded constraints; deterministic two-bone IK clamps unreachable targets, uses pole-vector bend planes, supports weighted application, and aim/look-at emits normalized rotations. Evaluation returns copied controls only. Focused native tests, GCC/Clang/CI-GCC 899/899 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **COMPLETED** | **Increment 161 — Animation Retargeting Contract v1** | Mapping profiles, reference-pose adaptation, orientation correction, missing-root handling, IK controls, and validation. | Native `AnimationRetargetingProfileUVE` enforces bounded one-to-one mappings, reference-pose validation, normalized orientation corrections, translation scaling, explicit Reject/Ignore missing-root policy, and optional weighted IK controls; retargeting returns copied target poses and never owns skeleton/runtime resources. Focused native tests, GCC/Clang/CI-GCC 903/903 validation, managed 65/65 validation, and Avalonia Xvfb startup smoke passed before completion. |
| **PARTIAL** | **Increment 162 — Motion Query/Motion Matching Audit and Foundation v1** | Audit the supplied inventory, map files to UniVex subsystems, remove obsolete assumptions, and implement only verified shared pose/query foundations. | Supplied inventory is an audit checklist, not code to copy blindly. |
| **PARTIAL** | **Increment 163 — Frame Scheduler and Task Graph Foundation v1** | Dependency-aware jobs and safe synchronization for animation, ECS, render preparation, physics, streaming, audio, and assets. | Scheduler adoption follows stable pose/resource contracts and includes deterministic tests. |
| **PARTIAL** | **Increment 164 — Profiler and Diagnostic Capture Foundation v1** | CPU/task/memory/animation/ECS/VM/render instrumentation, capture sessions, breadcrumbs, and diagnostic bundles. | Production debugging remains observable without leaking ownership to managed code. |
| **PARTIAL** | **Increment 165 — Automated Visual Script and Animation Regression v1** | Headless serialization, graph editing, compiler, animation evaluation, bridge DTO, UI, deterministic replay, and bounded stress testing. | Cross-compiler, managed, headless, and long-running regression evidence is required. |
| **PARTIAL** | Visual scripting editor | Source mapping, breakpoint presentation, watches, and richer node authoring. | Increment 86 provides the tested native canvas boundary; Increment 109 owns only copied source-label presentation and must not become a runtime mutation shortcut. |
| **PARTIAL** | Bytecode program reload | Validated program replacement, compatible state preservation, last-known-good fallback, and diagnostics. | Versioned bytecode/schema compatibility tests. |
| **PARTIAL** | Plugin architecture | **Increment 85 completed the static native seam:** versioned manifests, bounded capabilities, generation-checked registration scopes, and explicit close lifecycle. Dynamic loading, ABI negotiation, filesystem manifests, safe unload of plugin-owned registrations, and security policy remain partial. | A separately designed ABI/lifecycle/security proposal is still required; plugins do not become a shortcut around engine ownership. Focused Increment 85 registry suite passed 3/3. |
| **PARTIAL** | Data tables | Typed data assets, CSV/JSON/TSV import, validation, and selected editor workflow. | Deliberate parser/library choices, stable asset pipeline, and test fixtures. |
| **PARTIAL** | Developer console | Runtime commands, CVARs, history, completion, log filtering, and shipping exclusion. | Explicit command registration/security model; no cheat/debug surface silently ships. |
| **PARTIAL** | Documentation delivery | API reference, user manual, visual-script reference, plugin guide, and build/deployment guide. | Generated/manual docs must match implemented behavior, not aspirational spec text. |

<div align="center">

<h2><strong>TOOLING DESIGN RULES</strong></h2>

</div>

| Rule | Requirement |
|---|---|
| **Command routing** | Editor tools mutate documents only through validated command paths with dirty-state/history/Play-mode semantics. |
| **No automatic destructive work** | Change detection, index refresh, import, and source mutation stay explicit until their safety model is implemented. |
| **Plugin discipline** | Runtime extension must be versioned and scoped; a generic callback registry is not a secure plugin system. |
| **Headless validation** | Project/build diagnostics must execute without a graphics window for CI usefulness. |
| **Native scripting direction** | Visual scripting remains native C++ graph/compiler/bytecode/VM work; C# is not introduced as a replacement system without a separately approved interoperability architecture. |
