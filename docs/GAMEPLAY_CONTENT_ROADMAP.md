<div align="center">

<h1><strong>UNIVEX ENGINE — GAMEPLAY & CONTENT ROADMAP</strong></h1>

<strong>User-facing nodes, simulation content, animation, procedural tools, and cinematic/gameplay systems</strong>

</div>

> This roadmap describes content-facing systems that must grow from the existing C++ scene, physics, audio, input, save, renderer, and editor foundations. User-facing node names remain clean and do not carry the `UVE` suffix; internal engine APIs do.

| Status | Completed foundation | Delivered capability | Ongoing boundary |
|---|---|---|---|
| **COMPLETED** | **Increments 5–6** | ECS scene graph, transforms, serializable scenes, and prefabs. | These are the base for all later gameplay/component work. |
| **COMPLETED** | **Increments 15–16** | Collision, simulation foundation, raycasts, and physics materials. | Character/rigid-body/area feature depth remains partial. |
| **COMPLETED** | **Increments 17–19** | Input, audio, and save/checkpoint foundations. | GamepadInputSystemUVE bounded snapshots, AudioSystemUVE mixer-group routing/multiplier diagnostics, and SaveGameSystemUVE current-schema dispatch with unsupported-version diagnostics are now proven foundations; full gameplay integration, effects, cloud/save-domain modeling, and content policy remain partial. |
| **COMPLETED** | **Increments 38–58** | Real Scene Editor authoring: selection, transforms, hierarchy, history, Play sandbox, and built-in primitives. | Editor primitives are not a complete gameplay-node catalog. |

<div align="center">

<h2><strong>PARTIAL — USER-FACING NODE AND COMPONENT SET</strong></h2>

</div>

| Status | System | Intended capability | Completion proof / boundary |
|---|---|---|---|
| **PARTIAL** | Core 3D nodes | `Node3D`, mesh/material references, camera variants, directional/point/spot lights, collision shapes, rigid/static/character bodies, areas, audio sources, and script attachments. | Camera, light, collider, rigid-body, and audio-source validity plus invalid-scene rollback are proven; collider-vs-collider layer/mask filtering now requires symmetric acceptance before deterministic AABB pairs are emitted; AreaComponentUVE persistence, a bounded read-only area-to-collider overlap query, a generation-safe copied enter/exit lifecycle tracker, and EngineCore typed queued Entered/Exited event publication are also proven; MeshComponent reference coherence, Script path validation, and Particle budget validation with rollback are also proven; validated ScriptComponent ownership now reconciles empty-path detach and graph-to-runtime attach without filesystem ownership; asset loading, replacement reload, camera/light/collider/rigid-body/audio authoring commands, script execution depth, particle runtime, exact sphere/capsule interactions, expanded shape authoring, constraints, character depth, mixer behavior, raw mesh import, and each remaining node/component still require real runtime behavior and tests; no empty node shells. |
| **PARTIAL** | Core 2D/UI nodes | `Node2D`, sprite, label, button, panel, and progress UI elements. | Requires an actual runtime UI/render/input architecture; editor UI does not substitute for game UI. |
| **PARTIAL** | Character and interaction | Kinematic character controller, triggers/areas, component querying, spawning/destruction, and gameplay state conventions. | AreaComponentUVE serialization, bounded read-only area-to-collider overlap facts, a generation-safe copied Entered/Exited tracker with truncation-safe baseline retention, EngineCore queued Entered/Exited event delivery, CharacterControllerUVE bounded kinematic substep movement with wall blocking, tangential sliding, and symmetric layer/mask filtering, deterministic BVH-backed collider candidate generation with legacy pair ordering and a copied refit-only DynamicAabbBvhUVE policy for moved proxies with full-generation stale rejection, and optional bounded distance/hinge positional constraints are now available; ground/step behavior, component querying, spawning/destruction, gameplay state conventions, angular joint behavior/motors/limits, dynamic-body push policy, full CCD/TOI integration, locomotion/animation integration, and a representative sample scene still require defined behavior under physics/input tests; CharacterControllerUVE also exposes an opt-in bounded swept-AABB TOI path for tunneling prevention. |
| **PARTIAL** | Animation | Animation assets, playback, blending, events, state graphs, and later IK. | Shared SkeletonDefinitionUVE/PoseBufferUVE/AnimationEvaluationContextUVE contracts now converge Motion Query and Control Rig validation/evaluation with copied bounded identity/time data; ControlRigUVE now provides deterministic bounded pose blending with finite weight clamping, normalized rotations, weighted two-bone constraint evaluation, an existing AimLookAt solve, and Increment 285’s copied SpringPosition evaluation with bounded animation-delta response, stiffness/damping validation, influence clamping, and explicit no-history ownership; native Motion Query database authoring now includes revision-safe copied one-entry clipboard commands with identity-remapped paste, bounded 32-entry snapshot-based Undo/Redo, a copied deterministic 14-command metadata catalog for bridge/managed discovery, and bounded editor utilities for display-name validity, schema-ID normalization, and candidate identifier preflight, plus a deterministic validated default Motion Query database contract factory for editor/importer starting points, while asset format/import/runtime binding, full playback/blending/events, retargeting, procedural animation, IK depth, and broader Animation Editor authoring remain partial. |
| **PARTIAL** | Particles/VFX | GPU particle emitter, render integration, authoring controls, and lifecycle/culling policy. | Particle emitter budget validation and rollback are proven; ParticleRuntimeUVE provides bounded CPU emission/update state with finite inputs, deterministic sequence IDs, semi-implicit integration, and lifetime culling; ParticleRenderBridgeUVE copies enabled state into RenderQueueUVE, Renderer3DUVE consumes it through a borrowed per-frame input seam with copied diagnostics, ParticleDrawRecorderUVE records bounded GPU-independent command DTOs, and the built-in OpenGL path submits bounded non-instanced world-axis quads with lifetime alpha; camera-facing billboarding, particle textures/assets, GPU simulation/compute, occlusion/culling depth, authoring controls, and measured renderer/compute design remain partial. |
| **PARTIAL** | Audio gameplay | Source/listener binding, music/SFX routing, attenuation, mixer groups, and effects. | AudioSource value validation, spatial distance semantics, invalid-scene rollback, entity-to-voice ownership, play-on-awake, active-camera listener updates, listener-relative attenuation, and AudioSystemUVE bounded named mixer groups with Master default, explicit source routing, volume/pitch multipliers, and copied diagnostics are proven; music/SFX content policy, effects, clip asset resolution, streaming, and concrete platform backend selection remain partial. |

<div align="center">

<h2><strong>PARTIAL — AUTHORING AND ADVANCED CONTENT SYSTEMS</strong></h2>

</div>

| Status | System | Intended capability | Entry condition |
|---|---|---|---|
| **PARTIAL** | Spline system | Control points, curve evaluation, mesh generation, followers, visual handles, loops, tangents, and events. | Stable viewport presentation, tool-session lifecycle, and mesh generation contract. |
| **PARTIAL** | Procedural generation | Noise, procedural meshes, terrain foundations, dungeons, biome/erosion work, and deterministic seed policy. | Measured content/renderer data paths; no monolithic world generator. |
| **PARTIAL** | Data-driven gameplay | Data tables, typed rows, validation, references, and selected visual-script bindings. | Tooling/import contracts and stable component/node IDs. |
| **PARTIAL** | Cinematic sequencer | Sequence asset, tracks, keyframes, curves, scrubbing, events, camera/audio/transform binding, and later export. | Animation, audio, camera, asset, and viewport contracts are stable. |
| **PARTIAL** | Decals / billboards / impostors | Gameplay-visible projected marks and scalable environment representation. | Material/mesh asset workflow, LOD policy, and renderer performance budget. |
| **PARTIAL** | Lightmap baking | Static-scene lighting asset generation for constrained/mobile targets. | Mesh import, UV policy, material workflow, cooking/build pipeline, and bake algorithm choice. |

<div align="center">

<h2><strong>GAMEPLAY CONTENT GATE</strong></h2>

</div>

| Requirement | Meaning |
|---|---|
| **No fake components** | A component appears in menus/Inspector only after it has real runtime, persistence, and test behavior. |
| **One scene model** | Gameplay, editor, serializer, visual scripting, and physics operate on the same scene/entity ownership model. The Visual Scripting canvas now has copied native palette descriptors, managed search filtering, right-click node search, keyboard/click selection, cursor-position insertion, a 72-node production catalog covering flow.sequence, flow.branch, Float math including fourteen scalar utility nodes, Vector2 math with Make/Add/Subtract/Multiply/Length/Normalize/Dot/Distance/Direction/Lerp, Vector3 math with Make/Add/Subtract/Multiply/Dot/Cross/Length/Normalize/Distance/Direction/Lerp, Boolean logic including six finite Number-to-Boolean comparison nodes, Has/Get Component entity queries, engine.log, and engine.get_time, explicit execution-pin roles with deterministic one-upstream/one-downstream cardinality validation, pure typed Vector3 math and copied Entity/Component query values, bounded typed VM values including Boolean storage, explicit dependency-aware VM dispatch for all supported data-node IDs, copied entity-component facts, per-entity typed runtime tick execution, a scene-owned ScriptEntityQueryAdapterUVE that maps caller-registered native component types to binding-ordered copied presence facts for alive entities, and a query-aware ScriptRuntimeUVE tick path that refreshes those facts immediately before each enabled instance executes; Increment 274 added bounded copied ScriptVmTraceEventUVE records for node execution, typed value transfer, query-fact refresh, completion, and failure, with deterministic refresh-first ordering and truncation evidence; Increment 275 now copies debugger trace history through ScriptSourceMappingUVE, EditorBridgeUVE, and the managed Avalonia snapshot/status path alongside source-mapped breakpoints; Increment 276 now adds flow.sequence and flow.branch authoring/presentation semantics, deterministic execution-link cardinality diagnostics, and an explicit compiler deferral until ConditionalJump runtime support; Increment 277 now establishes a version-2 ConditionalJump bytecode contract with bounded true/false targets, strict decoder validation, and legacy v1 data-only compatibility; Increment 278 now evaluates copied Boolean conditions through the VM, selects bounded true/false targets with instruction-budget loop safety, and exposes an explicit copied-context debugger stepping seam; Increment 279 now lowers one validated flow.sequence node into v3 SequenceDispatch with ordered direct non-flow targets, missing-output continuation, VM budget safety, and debugger stepping proof; Increment 280 now lowers one validated flow.branch node into ConditionalJump with caller-bound Boolean Condition, bounded True/False direct non-flow targets, missing-output continuation, and explicit data-condition staging and nested-flow deferral; Increment 281 now stages one direct output from an approved built-in Boolean producer into Condition through producer-before-branch ordering and an immediate TransferValue, while custom producers and producer data dependencies remain explicitly deferred; Increment 282 now orders at most one approved built-in Boolean dependency before that producer, emits both typed transfers, and rejects cycles and deeper chains without partial IR; Increment 283 now adds an explicit `engine.log` catalog/compiler/VM/runtime seam that accepts one copied Number through a caller-owned callback, emits trace evidence, and fails closed when unbound or rejected; Increment 284 now adds an explicit `engine.get_time` catalog/compiler/VM/runtime seam that writes one finite copied Number through a caller-owned callback and fails closed on unbound, rejected, non-finite, or capacity-exhausted output; Increment 286 now stages one direct `engine.get_time.Value` output before a `math.float.*` A/B consumer through an immediate TransferValue and proves runtime addition; Increment 287 now stages one direct built-in Vector3 `Vector`/`Result` output before one `math.vector3.*` A/B/Vector consumer, proves copied Vector3 execution, and rejects composed/deeper vector dependencies; Increment 288 now stages finite `math.vector3.dot.Result` and `math.vector3.length.Length` outputs before one `math.float.*` A/B consumer, proves dot-to-Float execution, and rejects composed/deeper Number chains; Increment 289 now stages one direct `math.float.*.Result` output before a downstream `math.float.*` A/B consumer, proves multiply-to-add Float chain execution, and rejects a second staged consumer; Increment 290 now stages one direct `logic.boolean.*.Result` output before another Boolean consumer input, proves not-to-and chain execution, and rejects a second staged consumer; Increment 291 now stages one direct finite Number producer (`engine.get_time.Value` or `math.float.*.Result`) before `math.vector3.multiply.Scale`, proves Float-driven Vector3 scaling, and rejects a second Scale consumer; Increment 292 now enforces at most one non-execution source per data input pin, preserves distinct-input fan-in, and keeps execution cardinality and typed staging behavior unchanged; Increment 293 now extends Control Rig AimLookAt with an optional validated pole control, proves deterministic pole-up evaluation, and falls back to world-up for degenerate pole directions without retaining history; Increment 294 now appends finite 0–180° AimLookAt angle limits with backward-compatible defaults, proves deterministic 30° clamping, and rejects invalid ranges without retaining history; Increment 295 now stages one copied `query.entity.has_component.Result` output before a downstream Boolean consumer, proves query-fact-driven Boolean execution, and rejects a second staged consumer; Increment 296 now proves the same copied query result can stage before `flow.branch.Condition`, preserves true/false branch targets, and retains explicit one-hop/deeper-condition and arbitrary ECS scheduling deferrals; Increment 297 now stages one copied `query.entity.get_component.Result` token before `query.entity.has_component.Component`, proves copied get-to-has query execution, and rejects a second Component consumer and composed/deeper query dependencies; Increment 298 now carries compiler-marked staging transfers through v4 IR/bytecode and emits a dedicated copied `StagedValueTransferred` trace event in both the VM and debugger, while v1–v3 bytecode remains decodable with ordinary transfer semantics; Increment 299 now bounds `ScriptGraphUVE::ValidateUVE` to 64 nodes, accepts the exact boundary, and returns one deterministic graph-level `NodeCountExceeded` diagnostic before node/link traversal when exceeded; Increment 300 now bounds IR-to-bytecode lowering at 256 instructions, accepts the exact boundary, and emits one deterministic `InstructionLimitExceeded` diagnostic before lowering when exceeded; the serialized bytecode cap remains an independent 4096-instruction boundary; Increment 301 now bounds `ScriptGraphUVE::ValidateUVE` to 256 links after the 64-node guard, accepts the exact boundary with valid typed links, and returns one deterministic graph-level `LinkCountExceeded` diagnostic before traversal when exceeded; Increment 302 now extends the existing bounded TwoBoneIK solve with deterministic finite root and mid rotations that align each control's local +X bone axis to the solved root-to-mid and mid-to-end directions, blended by the existing clamped weight while preserving position, pole, reachability, and no-history ownership; Increment 303 now closes the bounded live-debug persistence contract with schema-versioned strict JSON envelopes, copied filter/truncation annotations, 512-event and 1 MiB payload limits, monotonic sequence/frame validation, runtime identity omission, deterministic schema/field/parse diagnostics, and offline session import/export through the existing debugger authority; Increment 304 now adds deterministic bounded copied analysis over live-debug traces with the same sequence/frame/cost/event invariants, event count, double-precision total cost, maximum evaluated candidates, lexicographically ordered kind counts, and a fail-closed 512-event cap; Increment 305 now adds six finite Number-to-Boolean comparison nodes (`Equal`, `Not Equal`, `Greater`, `Less`, `Greater Equal`, and `Less Equal`), stages up to two direct Number producers onto one comparison A/B pair in deterministic order, emits copied Boolean results, and rejects composed dependencies or multiple comparison consumers; Increment 306 now adds six direct finite Number utility nodes (`Modulo`, `Abs`, `Min`, `Max`, `Clamp`, and `Power`) with copied Number results, zero-near modulo rejection, reversed-range Clamp rejection, and fail-closed non-finite input/result handling; Increment 307 now proves one finite Number comparison Result stages into `flow.branch.Condition`, preserves producer-before-transfer-before-ConditionalJump ordering, and selects the deterministic true or false continuation; Increment 308 now adds a complete typed local-variable runtime subsystem with nine Make/Get/Set Number, Boolean, and Vector3 nodes, per-instance 256-slot copied storage, finite/default validation, type-safe updates, compatible reload persistence, editor snapshot counts, and staged Get outputs; Increment 309 now adds a complete bounded Vector2 function-library family with Make, Add, Subtract, Multiply, Length, and Normalize nodes, a value-only ScriptVector2 VM type, finite copied results, hypot-based length, deterministic zero-length rejection, and one-hop Vector2/Number staging; Increment 311 now adds version-5 FlowControlDispatch for Return, Do Once, Gate, and Switch, bounded persistent latch/Gate state, deterministic Default routing, a 53-node catalog, and compiler/bytecode/VM/editor proof; Increment 312 now adds explicit Number↔Boolean and Vector2↔Vector3 conversion nodes, finite conversion semantics, Vector2→Vector3 Z default metadata, bounded conversion staging, and exact-pin compiler/VM/editor proof; Increment 315 now adds eight bounded scalar Float nodes—Lerp, Remap, Sin, Cos, Tan, Sqrt, Random, and Random Range—with finite fail-closed execution, deterministic Seed-based random outputs, catalog/compiler/VM/editor integration, and native proof; Increment 316 now adds seven bounded Vector2/Vector3 math nodes—Vector2 Dot, Distance, Direction, and Lerp plus Vector3 Distance, Direction, and Lerp—with finite copied kernels, zero-length direction rejection, compiler one-hop staging, catalog/VM/editor integration, and native proof; Increment 317 now adds a bounded managed Motion Query authoring surface over copied native database rows with revision-guarded select/remove, display-name, schema-ID, maximum-candidate, and validation commands; registration, candidate construction, full reflection, asset browser/tree, viewport, and view-model breadth remain partial; strings, arrays/maps/sets/struct reflection, persistence, global/shared scope, and arbitrary scheduling remain explicitly deferred; composed/deeper query-Boolean chains and arbitrary ECS scheduling remain explicitly deferred; composed/deeper Scale dependencies and arbitrary graph scheduling remain explicitly deferred; composed/deeper Boolean chains and arbitrary graph scheduling remain explicitly deferred; composed/deeper Float chains and arbitrary graph scheduling remain explicitly deferred; composed/deeper Number/vector dependencies and arbitrary graph scheduling remain explicitly deferred; hidden clock access, string payloads, mutation, ECS access, and arbitrary service calls remain deferred; broader runtime/editor coverage, debugger controls beyond this trace/step foundation, general graph scheduling, and engine-call breadth remain partial. |
| **Deterministic authoring** | Commands, simulation state, saves, and procedural seeds must have explicit replay/validation behavior where appropriate. |
| **Sample project last** | A full sample game becomes a completion proof after the required runtime/content systems exist; it is not a substitute for their implementation. |
